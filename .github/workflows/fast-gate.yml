# crew#584 CP-C, founder playbook 2026-08-29 Phase 2: "Fail Fast, Pass Slow". One job, under
# 60 s: shell lint, hardcode scan, pipeverdict scan, ruff (IDP_CI_FAST=1 bin/idp-ci). ci.yml,
# build-multiarch.yml and portability-drill.yml each call it as their first job and every heavy
# job `needs:` it, so a lint red never boots k3s or builds four images. Measured before: a lint
# error surfaced after 5 min (offline-gate) while 11 min of drills ran for nothing (idp#659).
name: fast-gate
on:
  workflow_call:
jobs:
  fast-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { fetch-depth: 0 }   # python-strict grades the range base...HEAD
      # The estate Python standard (crew#620 CP4, founder 2026-08-29 "platform wide"): the SAME file
      # every Mac runs as a commit hook, so CI and the hook can never disagree (one copy, LAW 43).
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { repository: chidionyema/estate, path: .estate-hooks }
      # Founder mandate 2026-09-03 (the CI purge): a commit authored by a bot or an
      # estate agent skips this gate whole — the job still reports green so every
      # `needs: fast-gate` job downstream runs.
      - name: bot exemption
        id: actor
        env:
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          ACTOR: ${{ github.actor }}
        run: |
          case "${PR_AUTHOR:-$ACTOR}" in
            *'[bot]'|estate-agent*) echo "bot=1" >> "$GITHUB_OUTPUT"; echo "bot/agent author (${PR_AUTHOR:-$ACTOR}): fast-gate skipped";;
            *) echo "bot=0" >> "$GITHUB_OUTPUT";;
          esac
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        if: steps.actor.outputs.bot != '1'
        with: { python-version: "3.12", cache: pip, cache-dependency-path: sovereign/requirements*.txt }
      - if: steps.actor.outputs.bot != '1'
        run: pip install -q ruff==0.15.18 pyyaml   # pyyaml: the hardcode and pipeverdict rungs; ruff: the pin ci.yml's bdd job uses; shellcheck ships on ubuntu-latest
      - if: steps.actor.outputs.bot != '1'
        run: IDP_CI_FAST=1 bin/idp-ci
      - name: python-strict (estate standard, one copy)
        if: steps.actor.outputs.bot != '1'
        run: |
          base=$(git merge-base origin/main HEAD 2>/dev/null || git rev-parse HEAD~1)
          PYTHON_STRICT_RANGE="${base}...HEAD" .estate-hooks/guards/hooks/python-strict-default
