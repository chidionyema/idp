extends: substitution
message: "dev speak: say '%s' instead of '%s'"
link: https://github.com/chidionyema/crew/issues/612
level: error
ignorecase: true
swap:
  prover: the checker
  provers: the checkers
  nonce: request code
  HMAC-signed: signed
  HMAC: signed with a shared key
  OIDC: single sign-on
  SAML: single sign-on
  SSO: single sign-on
  OTLP: telemetry
  idempotent: safe to run again
  reconcile: apply the declared state
  reconciled: applied
  kustomization: the folder of manifests
  HelmRelease: the chart install
  ExternalSecret: the vault-fed secret
  ClusterSecretStore: the vault connection
  CronJob: the scheduled job
  statefulset: the workload
  envsubst: value substitution
  ratchet: the never-worse rule
  DoD: definition of done
  opmodel: the operating-model gate
  conftest: the policy check
  kyverno: the admission policy
  oauth2-proxy: the login proxy
  ForwardAuth: the login check
  HTTPRoute: the route
  namespace: the area of the cluster
  sha256: fingerprint
  PR: pull request
  PRs: pull requests
  repo: repository
  repos: repositories
  k8s: Kubernetes
  ns: the area of the cluster
  cwd: the working folder
  env var: setting
  stdout: the output
