# Spec updates owed by the founder requirements sweep (2026-08-25)

Source: `founder-requirements-2026-08-24.md` in this directory, 194 rows from the eight crew-agent
transcripts of 2026-08-24 15:42Z to 2026-08-25 03:05Z; 185 OPEN. Every OPEN row must land in an
executable spec (R29): a `*.feature` scenario, a test, or a standards row. This list says which file
carries each group and what is missing from it today. A row not listed here is process guidance already
carried by `~/AGENTS.md` (laws 1-47) and needs no spec change; it is enforced by the hooks named there.

| # | Requirement group (rows) | Spec that must carry it | Update owed | Tracked |
|---|---|---|---|---|
| S1 | Cluster stable, self-healing, staging = prod, chaos-tested, both arches (k8s 1-4, 15-17, 20, 25-27) | `features/cloud-agnostic/*.feature`, `docs/STANDARDS.md` cluster row | Scenarios: default-deny NetworkPolicy and ResourceQuota per namespace; staging and prod overlays render identical except image tag; every image tag has amd64+arm64 manifests; a pod kill recovers inside the SLO | crew#78 |
| S2 | Fly removed, no revival, portability drilled (k8s 5, 19; R1-no-fly-revival) | `features/cloud-agnostic/`, `policy/` (cloud-agnostic-gate) | Scenario: a manifest naming a vendor outside `platform/oci`, `secret-store`, `clusters/*` fails the gate; drill scenario for crew#38 | crew#38, crew#78 |
| S3 | Port ledger is absolute law (k8s 11, 18; machine-load 7, 12) | `features/gates/estate_gates.feature`, `catalog/ports.md` | Scenario: a listener not in the ledger fails `bin/ports-check`, foreign processes included; Traefik registered as owner of 80/443 before any bind | crew#236 |
| S4 | Environment parity, no OS/DNS workarounds in shared code (k8s 12-13; platform 34, 36) | `features/gates/` (env-parity, owned by another session: `bin/env-parity`) | Scenario: a Dockerfile, compose file or manifest carrying a host-specific flag fails; local fixes live only in `~/.colima` / host config | crew#236 |
| S5 | Measured cluster cost, managed vs self-provisioned, sized for growth (k8s 21-23) | `docs/specs/fortress-stack.md` cost section; `features/fortress/` | A cost table with the command that produced each number; scenario: the cost page is regenerated, not typed | crew#200 |
| S6 | Machine load: nothing new starts while load is undiagnosed; Netdata; what launches Chrome helpers; password prompts (machine-load 3-6, 9, 11) | `features/gates/estate_gates.feature`, `launchd/*.plist.tmpl` | Scenario: a launchd job added without a plist-gate pass is refused; a load probe row in `crew/STATE.md`; the Chrome-helper launcher named | crew#255 |
| S7 | Provider-agnostic framework, no Claude-specific artifacts, provider-swap risks (platform 10-19, 46) | `features/self-aware-platform/`, `docs/STANDARDS.md` model-routing row (LiteLLM) | Scenario: a repo with a `CLAUDE.md` or `.claude/` that is not a symlink to `AGENTS.md` fails the gate; LiteLLM route with frontier fallback and budget cap proven by a receipt | crew#235 |
| S8 | LiteLLM spend limits, $5/day cap, Langfuse telemetry, agentgateway with CEL policy and OTel (platform 25-28, 31, 47) | `docs/STANDARDS.md` traces row; `features/self-aware-platform/` | Scenario: a call over the daily cap is refused at the proxy; a trace lands in the estate collector; a tool call without a policy match is refused by agentgateway | crew#258, crew#87 |
| S9 | Backstage local now, ArgoCD later; real UI, GitHub Projects usable; Dagster descriptions (platform 38, 41-45) | `features/estate-rebuild/`, `catalog/` | Scenario: every scheduled job has a non-empty description in Dagster; every catalog entity renders in the portal; the Projects board has views, not a 178-item list | crew#236 row 2 |
| S10 | Fallback/backdoor switchable in 10 s on every platform layer (platform 4) | `docs/STANDARDS.md` (new column: fallback and its switch) | Every row names its fallback and the one command that flips it; scenario per row | crew#235 |
| S11 | Secrets: one sanctioned way, SOPS + External Secrets Operator approved, MINIMAX key managed (secrets 1-6) | `features/fortress/`, `docs/STANDARDS.md` secrets row; `estate-secrets` | Scenario: a plaintext secret in any manifest fails; a key rotates without an app change; the security gate runs in every active repo (done 2026-08-25, crew#236 row 1) | crew#240, crew#227 |
| S12 | Data pipeline: metadata gaps, 77 record locations, 6 scheduling roots, buyer-grade data science (other 1-3, 5-6) | new `features/data-platform/`; `docs/specs/` | A generated inventory of every record location and every scheduling root, and the scenario that the count goes down; no hand-rolled ETL | crew#200 |
| S13 | KINI presence model, receipts, undo, vision-to-repo, shadow-founder, trust boundary (kini-spec pointer row; crew#200 gap table: 2 MET, 11 PARTIAL, 29 ABSENT of 42) | `docs/specs/kini-master-spec.md` §2-§5; `features/sovereign-bus/cp23-cp35` | The 29 ABSENT rows in the crew#200 gap table each need a failing scenario before code; cp23, cp24, cp26, cp29, cp32 are prose today and need steps | crew#227, crew#213 |
| S14 | Process rows that are not yet a guard: red-zone broadcast on profanity, listen-in on all sessions, paid-infra refusal (R14), document policy, spend guard (process 18, 21, 34-35, 47, 75, 89-91) | `claude-guards/policy/*.rego`, `~/AGENTS.md` | One rego rule and one both-ways test per row; R14 is a ruling in `rulings.json` and needs the refusing hook named | claude-guards |
| S15 | Definition of done: nothing is done until the founder can see and use it (process 3, 17, 61, 81) | `docs/policy/definition-of-done.md` (exists, v2.1); `dod-guard.py` | Already carried; no update owed | — |

Rows in the table not in a group above are one-off asks that were completed or superseded in the same
session (k8s 6-10, 14; platform 8, 30, 32; process 5, 12, 40, 49, 62, 69, 92). They stay in the table as
the record and get no spec.
