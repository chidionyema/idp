# Laws and guards

Generated by `bin/idp-laws-guards-report` from the laws file, the guards tree at `51a9ca3` and the harness settings file. Do not edit by hand; run the script and commit. The settings file names, per hook event and in the order below, every command the harness runs; `hook-run.py` runs each guard and writes its ledger row, and `opa-hook.py` is one of the guards, not a door in front of them.

## The 0 laws

| # | Law | Fires |
|---|---|---|

When two laws want different things, the lower number wins. Full prose: `~/AGENTS-FULL.md`.

## SessionStart: 4 guards, in this order

| # | Guard | What it does | Laws it cites | Tools |
|---|---|---|---|---|
| 1 | `sync-guard.py` | The Mac runs main. At every SessionStart the live guards checkout fast-forwards to origin/main, | 11 | every tool |
| 2 | `laws-link-guard.py` | Keep every provider's rules file pointing at ~/AGENTS.md | 24 | every tool |
| 3 | `peer-loop-fence.py` | Refuse a peer message that re-raises something already on the estate board | 12 | every tool |
| 4 | `canonical-root-guard.py` | Tell a session, at its start, when it is working outside the canonical root | — | every tool |

## UserPromptSubmit: 2 guards, in this order

| # | Guard | What it does | Laws it cites | Tools |
|---|---|---|---|---|
| 1 | `directive-capture.py` | Capture every founder message to a durable, queryable log, in every project, forever | — | every tool |
| 2 | `founder-doc-capture.py` | A founder-pasted document becomes a file in git the moment it arrives. No session searches for it | 24, 45 | every tool |

## PreToolUse: 12 guards, in this order

| # | Guard | What it does | Laws it cites | Tools |
|---|---|---|---|---|
| 1 | `scope-guard.py` | PreToolUse guard: keep the global rules file free of project-specific content | 0, 1 | Write, Edit |
| 2 | `config-syntax-guard.py` | PreToolUse guard: refuse a write that would leave a config file unparseable | 3, 38, 45 | Write, Edit |
| 3 | `dupe-work-fence.py` | Refuse a pull request that duplicates another session's claim | — | Bash |
| 4 | `pr-cap-guard.py` | PreToolUse adapter for policy/pr_cap.rego (crew#504 CP5, crew#66): `gh pr create` is refused | — | Bash |
| 5 | `rule-guard.py` | PreToolUse guard: turn written rules into refusals | 0, 38, 46 | Bash |
| 6 | `ticket-gate.py` | Every session carries a GitHub issue, or it does not get to change anything | 32, 33 | Bash |
| 7 | `credential-guard.py` | Refuse a credential value in a reply or in a GitHub comment body (crew#407, LAW 21) | 21 | Bash |
| 8 | `merge-divergence-hook.py` | PreToolUse(Bash) hook: refuse a `git merge <target>` when the target has | 38 | Bash |
| 9 | `read-shunt.py` | read-shunt.py -- route a bulk file read to a cheap worker model; the frontier model gets a digest | 28, 38, 43, 46 | Bash |
| 10 | `ticket-gate.py` | Every session carries a GitHub issue, or it does not get to change anything | 32, 33 | Edit, Write, MultiEdit, NotebookEdit |
| 11 | `opa-hook.py` | Ask OPA about a hook payload. Decides nothing itself | — | Artifact |
| 12 | `read-shunt.py` | read-shunt.py -- route a bulk file read to a cheap worker model; the frontier model gets a digest | 28, 38, 43, 46 | Read |

## PostToolUse: 2 guards, in this order

| # | Guard | What it does | Laws it cites | Tools |
|---|---|---|---|---|
| 1 | `research-capture.py` | PostToolUse hook: capture a research pass without being asked (crew#72) | 35 | WebSearch, WebFetch |
| 2 | `slow_commands.py` | Tell a session when it is about to block on a command that has always been slow | 38 | Bash |

## Stop: 11 guards, in this order

| # | Guard | What it does | Laws it cites | Tools |
|---|---|---|---|---|
| 1 | `opa-hook.py` | Ask OPA about a hook payload. Decides nothing itself | — | every tool |
| 2 | `secret-scrub.py` | Keep live credentials out of the files agents and shells write | 21, 28 | every tool |
| 3 | `laws-link-guard.py` | Keep every provider's rules file pointing at ~/AGENTS.md | 24 | every tool |
| 4 | `dod-guard.py` | Refuse a reply that claims done without the Definition of Done evidence | 31, 45 | every tool |
| 5 | `prompt-ledger.py` | Every founder prompt, captured once and closed with proof | — | every tool |
| 6 | `close-guard.py` | Refuse a reply that does not close, and a DONE: that carries no receipt | 9 | every tool |
| 7 | `founder-deliver.py` | Nothing for the founder goes into the void: a DONE: reply that links a deliverable is sent to | 38 | every tool |
| 8 | `blocker-guard.py` | Stop hook (LAW 47 / R30). A reply that says FOUNDER ACTION: or STAGED: must have reached the | 47 | every tool |
| 9 | `credential-guard.py` | Refuse a credential value in a reply or in a GitHub comment body (crew#407, LAW 21) | 21 | every tool |
| 10 | `session-recorder.py` | Write a recovery file after every turn, so a dead session loses nothing | 6, 16 | every tool |
| 11 | `session_emit.py` | Emit one OTLP log record per turn, so an agent session is a workload the estate can see | 21, 43, 46, 50, 52 | every tool |

## How a guard answers

A guard prints nothing and exits 0 to pass. It refuses with exit 2 and the reason on stderr, or with `{"decision": "block", "reason": ...}` on stdout. A guard that crashes or exceeds its budget is `NoVerdict`, and the door answers no (fail-closed). Every run is a row in the hook ledger (`hook-run.py`), which is where a claim that a guard ran is checked.

Total: 31 guard rows across 5 events.
