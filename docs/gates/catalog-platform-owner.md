# Gate: catalog-owner

*`catalog-platform-owner` · incident 2026-09-15, via-negativa merged with no catalog owner or description until hand-fixed · planes: ci, session*

Every platform layer's Backstage Component and API carries a real owner group in bin/catalog-platform's own LAYERS dict, checked at generation time -- not a single hardcoded literal emitted regardless of what LAYERS says, and not left to a pre-commit hook a --no-verify commit, a clone with hooks not installed, or a GitHub-web merge can all skip.

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/catalog-platform --check-fixture
```

## Refused

Fixture: `tests/fixtures/catalog-platform-owner/bad.py`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL    catalog-platform  no owner for ['dns']
```

## Permitted

Fixture: `tests/fixtures/catalog-platform-owner/good.py`

Exit code `0` (the registry expects zero). ✓

```
ok      catalog-platform  every layer in tests/fixtures/catalog-platform-owner/good.py carries an owner
```

## The registry's own lines

- when it passes: every layer in the fixture carries a real owner group
- when it refuses: a layer has no owner, or the guard is not discriminating

Origin: bin/catalog-platform's LAYERS dict had no owner field at all: every Component and API owner in the generated catalog-info.yaml was the one hardcoded literal group:default/platform, and the only gate that ever ran catalog-platform lived in a pre-commit hook that graded description-completeness only, never ownership. Fixed by giving LAYERS a real 4th field and a fail-closed guard in render() (missing_owners()); this rule proves that guard both ways without touching the real cluster/ tree.
