# Gate: opmodel

*`operating-model-policy` · LAW 51, ZCP · planes: ci*

A pull request carries its identity and grant together, no console step, a canary label on a paid-capacity change, and no estate-zone literal in the lines it adds

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
conftest test --parser json -p policy
```

## Refused

Fixture: `policy/fixtures/opmodel-half-provisioned.json`

Exit code `1` (the registry expects non-zero). ✓

```
[31mFAIL[0m - policy/fixtures/opmodel-half-provisioned.json - main - rule=provisioning_complete | oci_identity_domains_app is created without a role, policy or grant in the same PR | fix: add the oci_identity_domains_grant / oci_identity_policy / group membership that gives it its scope, in this PR
[31m31 tests, 30 passed, 0 warnings, 1 failure, 0 exceptions[0m
```

## Permitted

Fixture: `policy/fixtures/opmodel-ok.json`

Exit code `0` (the registry expects zero). ✓

```
[32m31 tests, 31 passed, 0 warnings, 0 failures, 0 exceptions[0m
```

## The registry's own lines

- when it passes: the operating-model policy allows a complete pull request and refuses each incomplete shape
- when it refuses: the operating-model policy did not behave as specified

Origin: crew#286, crew#287, crew#584
