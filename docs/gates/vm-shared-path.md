# Gate: vmmount

*`vm-shared-path` · R19 · planes: ci*

A VM mount source outside the shared tree is refused; one inside it is permitted

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
bin/vm-shared-path
```

## Refused

Fixture: `/private/tmp`

Exit code `2` (the registry expects non-zero). ✓

```
FAIL  /private/tmp resolves to /private/tmp, outside the VM-shared tree (/Users/chidionyema); the container would see an empty directory
```

## Permitted

Fixture: `$HOME`

Exit code `0` (the registry expects zero). ✓

```
(no output)
```

## The registry's own lines

- when it passes: vm-shared-path permits a path under the shared root and refuses one outside it
- when it refuses: bin/vm-shared-path selftest
