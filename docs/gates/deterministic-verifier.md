# Gate: verifier

*`deterministic-verifier` · founder 2026-09-13 "the fact u are able toi lie eans he his rules isnt operation al and the firs thig we need to addres" / "thats why i dont belive clains without hard prrov 3 different w3ys" · planes: ci*

A claim of completed work is not the agent's to make: the agent may only propose a patch into an ephemeral ledger, three graders the proposer does not control run against the exact bytes -- structural (the bytes compile), symbolic (Z3 over the guard the patch itself declares, which refuses to discharge "tests pass"), and execution (the SUPPLIED TEST SUITE runs in a throwaway tree and the pytest exit code is the verdict, so a proposal carrying no test is refused) -- and an attestation is minted only when all three pass, bound to the SHA-256 of the artifact actually verified. Take the claim away from the claimant

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-verifier-oath
```

## Refused

Fixture: `tests/fixtures/verifier-oath/no-seal`

Exit code `1` (the registry expects non-zero). ✓

```
ok   no-seal: refused as required -- a stage refused ({'structural': 'pass', 'sql': 'pass', 'symbolic': 'pass', 'execution': 'refuse'}) and no seal was minted
ok   verifier-oath: 1 fixture(s); a claim leaves only when three graders agree and the seal is bound to the graded bytes
REFUSED verifier-oath: the named proposal carries no seal; a claim may not leave on it
```

## Permitted

Fixture: `tests/fixtures/verifier-oath/sealed`

Exit code `0` (the registry expects zero). ✓

```
ok   sealed: sealed as required -- three stages passed (VERIFIED); the seal verifies for sha256:d445b2017c1c... and for no other subject
ok   verifier-oath: 1 fixture(s); a claim leaves only when three graders agree and the seal is bound to the graded bytes
```

## The registry's own lines

- when it passes: a patch is admitted only through the three-stage gauntlet, the ledger is the only place an agent can write, and the seal binds the attestation to the bytes that were actually graded (measured 2026-09-13, 9 passed, PYTEST_RC=0)
- when it refuses: a claim of completed work reaches the estate on the word of the agent that made it, with no structural, symbolic or execution grader in front of it, or an attestation whose subject is not the SHA-256 of the graded artifact

Origin: incident 2026-09-13, an agent's first-person claim of done accepted with no third party behind it; see docs/tickets/2026-09-13-deterministic-verifier.md
