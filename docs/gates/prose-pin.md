# Gate: prose

*`prose-pin` · R76 · planes: ci, session*

A test grades behavior or parsed structure, never prose: no test function may only assert sentences or string membership in file text

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/test-prose-gate
```

## Refused

Fixture: `tests/fixtures/prose-pin/bad.py`

Exit code `1` (the registry expects non-zero). ✓

```
tests/fixtures/prose-pin/bad.py: test_the_comment_still_says_the_magic_sentence only asserts prose/string membership in file text (R76)
```

## Permitted

Fixture: `tests/fixtures/prose-pin/good.py`

Exit code `0` (the registry expects zero). ✓

```
(no output)
```

## The registry's own lines

- when it passes: no test function only asserts prose/string membership in file text (R76)
- when it refuses: a test function only asserts prose/string membership in file text (R76)

Origin: founder 2026-09-03
