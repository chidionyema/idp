# Gate: codeloc

*`defs-load-by-path` · LAW 45 · planes: ci*

A code location loads the way workspace.yaml loads it: by file path, not as a package

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
bin/idp-defs-validate
```

## Refused

Fixture: `tests/fixtures/definitions/relative-import.py`

Exit code `1` (the registry expects non-zero). ✓

```
/usr/local/opt/python@3.14/bin/python3.14: No module named dagster
```

## Permitted

Fixture: `tests/fixtures/definitions/loads-by-path.py`

Exit code `1` (the registry expects zero). ✗

```
/usr/local/opt/python@3.14/bin/python3.14: No module named dagster
```

## The registry's own lines

- when it passes: the code location loads by file path, as workspace.yaml loads it; a relative import is refused
- when it refuses: the code location does not load by file path -- the dashboard will show PythonError
