# Gate: plist

*`plist-renders` · LAW 45 · planes: ci*

Every launchd template renders to a plist that parses and names its job

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/plist-gate
```

## Refused

Fixture: `tests/fixtures/plist/bad.plist.tmpl`

Exit code `1` (the registry expects non-zero). ✓

```
Traceback (most recent call last):
  File "<checkout>/bin/plist-gate", line 70, in <module>
    if name__ == "__main__":
NameError: name 'name__' is not defined
```

## Permitted

Fixture: `tests/fixtures/plist/abandoned-children.plist.tmpl`

Exit code `1` (the registry expects zero). ✗

```
Traceback (most recent call last):
  File "<checkout>/bin/plist-gate", line 70, in <module>
    if name__ == "__main__":
NameError: name 'name__' is not defined
```

## The registry's own lines

- when it passes: every launchd template renders to a plist that parses and names its job; gate proved both ways
- when it refuses: a launchd template does not render to a valid plist, or the gate does not discriminate

Origin: idp#49
