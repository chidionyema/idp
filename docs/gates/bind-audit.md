# Gate: binds

*`bind-audit` · R20 · planes: ci*

Only the gateway binds a non-loopback address; everything else is 127.0.0.1 or nothing

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
bin/bind-audit
```

## Refused

Fixture: `--table tests/fixtures/listeners.bad.txt`

Exit code `1` (the registry expects non-zero). ✓

```
== non-loopback listeners on tests/fixtures/listeners.bad.txt ==
OK       rapportd     *:49248                macOS Continuity/Handoff. Apple-owned, not ours to move; turning it off costs the founder Handoff between his own devices.
UNKNOWN  ssh          *:53145                not on the allow-list. Bind it to 127.0.0.1, or add a row to bin/bind-audit saying why it may face the network.
UNKNOWN  node         *:7107                 not on the allow-list. Bind it to 127.0.0.1, or add a row to bin/bind-audit saying why it may face the network.
FAIL: 2 of 3 non-loopback listeners are not allow-listed.
```

## Permitted

Fixture: `--table tests/fixtures/listeners.good.txt`

Exit code `0` (the registry expects zero). ✓

```
== non-loopback listeners on tests/fixtures/listeners.good.txt ==
OK       rapportd     *:49248                macOS Continuity/Handoff. Apple-owned, not ours to move; turning it off costs the founder Handoff between his own devices.
OK       ControlCe    *:7000                 macOS AirPlay Receiver. Apple-owned. Disabled under System Settings > General > AirDrop & Handoff if it is ever unwanted.
OK       ControlCe    *:5000                 macOS AirPlay Receiver, second port. Same owner as 7000.
OK       limactl      *:53                   colima's lima hostagent DNS resolver for the Docker VM. Removing it breaks name resolution inside every container on this machine.
OK       ssh          *:8080                 prospector-edge, the gateway. R20 names the gateway as the one process allowed a non-loopback address. Published through colima's ssh mux, which is why the COMMAND column says ssh and not docker.
PASS: all 5 non-loopback listeners are allow-listed.
```

## The registry's own lines

- when it passes: bind-audit refuses an unknown listener and permits an allow-listed one; selftest green
- when it refuses: bind-audit does not discriminate on the recorded listener tables
