# Gate: ottodoorkey

*`otto-door-key` · THE EMPIRICAL PROOF RULE · planes: ci*

Otto's door presents a key the lifeboat it talks to can validate: when LITELLM_BASE_URL is the loopback otto-brain sidecar, the door derives the same master key from the same Secret the sidecar does. A sidecar with no database cannot look up a virtual key, so it refuses every turn with 400 "No connected db." before any of the three homes is tried

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-otto-door-key-agrees
```

## Refused

Fixture: `tests/fixtures/otto-door-key/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  otto-brain derives its master key with sha256sum, but the door presents
      LITELLM_API_KEY unhashed. The sidecar has no database, so it cannot look up a
      virtual key: it answers 400 'No connected db.' to every turn, before any
      of the three homes is tried. Derive the same key in the door's entrypoint.
```

## Permitted

Fixture: `tests/fixtures/otto-door-key/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
ok    door and lifeboat derive the same master key from Secret/otto-gateway-router
```

## The registry's own lines

- when it passes: the door and the lifeboat derive one master key from one Secret; proved both ways
- when it refuses: the door presents a key the lifeboat cannot validate, so every real turn dies at the lifeboat's door while probes that go around it stay green

Origin: incident 2026-09-10, the three homes carried no real turn for a day while every probe stayed green
