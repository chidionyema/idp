# Gate: priority-class-exists

*`priority-class-exists` · incident 2026-09-08, nodesoftware-operator · planes: ci, session*

Every priorityClassName a workload names is a PriorityClass this tree declares or one of the two Kubernetes ships; a class that exists nowhere is refused at admission, not at apply

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-priority-class-exists
```

## Refused

Fixture: `tests/fixtures/priority-class-exists/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL: a workload names a PriorityClass that does not exist
  platform/app/deployment.yaml:17 names 'idp-infrastructure'
  declared here: platform-batch
  Kubernetes ships: system-cluster-critical, system-node-critical
```

## Permitted

Fixture: `tests/fixtures/priority-class-exists/good`

Exit code `0` (the registry expects zero). ✓

```
PASS: 1 priorityClassName references, all resolved (1 declared here, 2 built in)
```

## The registry's own lines

- when it passes: every priorityClassName in the tree resolves to a class that exists
- when it refuses: a workload names a PriorityClass that neither this tree declares nor Kubernetes ships; admission refuses every pod it asks for and the Deployment reports FailedCreate

Origin: platform/nodesoftware-operator/deployment.yaml named priorityClassName idp-infrastructure. No such class is declared in this tree and none exists in the cluster, so the apiserver refused every pod the ReplicaSet asked for -- `pods "nodesoftware-operator-6d87fb9cbd-" is forbidden: no PriorityClass with name idp-infrastructure was found`. The Deployment sat at ReplicaFailure/FailedCreate and its Flux row went HealthCheckFailed. The name survived review because the line carried a comment pointing at the directory where the real classes live, which makes a wrong name look checked, and because the failure names a scheduling concept, so it reads like capacity rather than a typo.
