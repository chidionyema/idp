# Gate: gvisor

*`gvisor-cell` · zero-trust-boundary.md step 5 · planes: ci, cluster*

A pod in the gVisor sandbox cell must name runtimeClassName gvisor and must not be privileged, host-networked or mount a hostPath

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/gvisor-cell-gate
```

## Refused

Fixture: `tests/fixtures/gvisor-cell/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
tests/fixtures/gvisor-cell/bad.yaml: Pod/untrusted-model-code in otto-gvisor-sandbox: a cell Pod must set spec.runtimeClassName=gvisor
gvisor-cell-gate: 1 cell-namespace violation(s) refused of 1 pod(s) in otto-gvisor-sandbox
```

## Permitted

Fixture: `tests/fixtures/gvisor-cell/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
gvisor-cell-gate: 1 pod(s) declared in otto-gvisor-sandbox, every one sandboxed
```

## The registry's own lines

- when it passes: a cell Pod without the runtime class (or privileged, host-networked, hostPath) is refused; the conforming cell Pod passes
- when it refuses: bin/gvisor-cell-gate cannot tell a gVisor cell boundary break from a conforming Pod (ZT step 5)

Origin: crew#892 CP4
