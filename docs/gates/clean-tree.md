# Gate: clean-tree

*`clean-tree` · LAW 2, docs/policy/no-agent-works-in-the-main-checkout.md · planes: ci, session*

A gate's answer is only true for the tree it ran in: grading from the shared checkout while it carries uncommitted work, or from any checkout mid-merge, is refused -- three separate estate defects were reported 2026-09-12 that existed only in another session's half-written tree

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-clean-tree
```

## Refused

Fixture: `--state tests/fixtures/clean-tree/bad.json`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  clean-tree  /shared/idp is the SHARED checkout on branch main and carries 1 uncommitted change(s):  M bin/x
      Every session on this machine works in this directory, so a gate run here reads another session's half-finished work as estate truth (measured 2026-09-12, three times). Use a worktree cut from origin/main:
          git worktree add /tmp/wt-verify origin/main
```

## Permitted

Fixture: `--state tests/fixtures/clean-tree/good.json`

Exit code `0` (the registry expects zero). ✓

```
ok    clean-tree  /tmp/wt-verify on main, 1 uncommitted change(s)
```

## The registry's own lines

- when it passes: the shared checkout mid-merge is refused, and an ordinary dirty worktree is not
- when it refuses: bin/idp-clean-tree cannot tell a shared checkout or a mid-merge tree from a working one

Origin: measured 2026-09-12, defs-load-by-path BLIND and sleep-ban 14 violations, neither real on main
