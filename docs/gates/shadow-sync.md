# Gate: shadow

*`shadow-sync` · W2.1 · planes: ci*

A Secret cloned into the shadow dimension is a same-shape shell or it is refused; a live value must never survive re-shaping -- a non-shell Secret in a shadow state-set is a fail (secrets are never copied into a vcluster, only keyed dummies so a Deployment mounts)

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-shadow-sync
```

## Refused

Fixture: `--check tests/fixtures/shadow-sync/verbatim`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL    shadow  leaked.yaml: non-shell Secret (leaked)
```

## Permitted

Fixture: `--check tests/fixtures/shadow-sync/shelled`

Exit code `0` (the registry expects zero). ✓

```
ok      shadow  1 Secret(s) in tests/fixtures/shadow-sync/shelled are shells, none verbatim
```

## The registry's own lines

- when it passes: a shadow state-set carries only shell secrets, never a live value (W2.1)
- when it refuses: a live secret value could reach the shadow dimension (W2.1)

Origin: docs/policy/agent-infra-safety-spec.md
