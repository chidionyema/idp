# Gate: catalog-type

*`catalog-spec-type` · incident: phone-nav blank page (founder-surface type absent from ontology), /dagster page blank (relative URL, same root cause class) · planes: ci, session*

A catalog entity whose spec.type is not in schema/catalog-ontology.json cannot be queried reliably -- the frontend receives 200 [] indistinguishable from "no results" and "catalog broken", and an agent inventing a type propagates the gap every time it runs. The enum is the contract; the contract is checked before the commit exists.

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-catalog-spec-type-gate
```

## Refused

Fixture: `tests/fixtures/catalog-spec-type/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  tests/fixtures/catalog-spec-type/bad/catalog-info.yaml: Component/rogue-layer: spec.type 'rogue-service-type' is not in the ontology enum.
   Allowed: platform-layer, founder-surface, service, website, model, experimental
1 violation(s). Failing.
```

## Permitted

Fixture: `tests/fixtures/catalog-spec-type/good`

Exit code `0` (the registry expects zero). ✓

```
ok    catalog-spec-type  1 file(s) clean
```

## The registry's own lines

- when it passes: a spec.type not in the ontology is refused; valid types and Template skip pass; the real backstage/ tree is clean
- when it refuses: a catalog entity names a spec.type not in schema/catalog-ontology.json, or the gate is not discriminating

Origin: spec.type is a free-form string in stock Backstage; the catalog API returns 200 [] for any unknown type. A frontend querying founder-surface got an empty list when no entity declared that type, and a relative /dagster URL caused the same blank-page failure in the scheduler surface. schema/catalog-ontology.json closes the enum; this gate refuses any type not in it. Template kind and scaffold ${{ }} tokens are skipped -- they use Backstage scaffold syntax, not real entity values.
