# Gate: sched

*`one-scheduler` · THE HEADLINE (one platform layer, not a stitched one) · planes: ci*

Dagster is the estate's one scheduler; a recurring job declared in launchd, a GitHub Actions cron or a Kubernetes CronJob is named here until it is folded in

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-one-scheduler
```

## Refused

Fixture: `tests/fixtures/one-scheduler/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  sched    the estate's scheduling is not what the ledger says
      NEW OUTSIDE DAGSTER  launchd/hourly.plist.tmpl (launchd job on the laptop) -- declare it as a Dagster schedule in scheduler/, or add it to platform/scheduling/one-scheduler.yaml with the reason
```

## Permitted

Fixture: `tests/fixtures/one-scheduler/good`

Exit code `0` (the registry expects zero). ✓

```
ok    sched    Dagster is the one scheduler; 1 declaration(s) still outside it, every one recorded in platform/scheduling/one-scheduler.yaml
```

## The registry's own lines

- when it passes: {tail}
- when it refuses: a recurring job was declared outside Dagster, or the ledger of the ones still to fold is stale

Origin: founder 2026-09-07
