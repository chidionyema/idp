# Gate: clickhouse-system-log-ttl

*`clickhouse-system-log-ttl` · incident 2026-09-08, signoz-clickhouse CrashLoopBackOff · planes: ci, session*

A ClickHouse system table the operator gives an <engine> carries its retention inside that engine string, never as a sibling <ttl>; ClickHouse exits 36 on boot when both are present

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-clickhouse-system-log-ttl
```

## Refused

Fixture: `tests/fixtures/clickhouse-system-log-ttl/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
tests/fixtures/clickhouse-system-log-ttl/bad.yaml:6: system table `trace_log` has an operator <engine>; put the TTL inside `trace_log/engine`, not beside it -- trace_log/ttl: "event_date + INTERVAL 1 DAY DELETE"
tests/fixtures/clickhouse-system-log-ttl/bad.yaml:7: system table `query_log` has an operator <engine>; put the TTL inside `query_log/engine`, not beside it -- query_log/ttl: "event_date + INTERVAL 3 DAY DELETE"
tests/fixtures/clickhouse-system-log-ttl/bad.yaml:12: system table `metric_log` has an operator <engine>; put the TTL inside `metric_log/engine`, not beside it -- <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
3 sibling-<ttl> retention(s). ClickHouse exits 36 on boot with "If 'engine' is specified for system table, TTL parameters should be specified directly inside 'engine'". Incident 2026-09-08.
```

## Permitted

Fixture: `tests/fixtures/clickhouse-system-log-ttl/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
(no output)
```

## The registry's own lines

- when it passes: every ClickHouse system-log retention is inside its engine string
- when it refuses: a system table the Altinity operator ships an <engine> for also has a sibling <ttl>, which ClickHouse refuses at startup with BAD_ARGUMENTS and exit 36

Origin: One malformed stanza took the estate down: the server exited 36 on every boot, so signoz-clickhouse had no endpoints, so the observability Kustomization hung on its Job/langfuse-clickhouse-database health check, so lastApplied stayed thirty commits back, so langfuse kept the two reserved cores #2429 had already removed in git -- and both nodes went to 91% and 95% CPU requested, evicting flux's own controllers.
