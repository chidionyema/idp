# Gate: law32

*`law32-demo-onboarding` · LAW 32 · planes: ci*

A new bin file ships with docs/tutorials/demo and docs/how-to/onboarding, above the prose floor and in the nav

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/law32-gate
```

## Refused

Fixture: `--added bin/feature-with-no-pages`

**BLIND** — the gate is not on disk: bin/feature-with-no-pages

Nothing was proved here. BLIND is a fail, never a pass.

## Permitted

Fixture: `--added bin/supply-chain`

Exit code `0` (the registry expects zero). ✓

```
ok    law32 1 new bin file(s) carry a demo+onboarding pair; 178 page(s) above the 200-char floor and in the nav
```

## The registry's own lines

- when it passes: every new bin file has its demo and onboarding; every page is above the prose floor and in the nav; gate proved both ways
- when it refuses: a feature shipped without demo/onboarding, or a page is thin or unlisted

Origin: founder 2026-08-25
