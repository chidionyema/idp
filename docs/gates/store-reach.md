# Gate: store-reach

*`store-reach` · incident 2026-09-08, ClusterSecretStore/estate-vault · planes: ci, session*

A secret store that authenticates outside the cluster has a fence that lets it get there: egress_internet, and egress_metadata too when its identity is fetched at run time

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-store-can-reach-its-vault
```

## Refused

Fixture: `tests/fixtures/store-reach/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL platform/secret-store/store.yaml: ClusterSecretStore/example-vault reaches a provider outside the cluster, and flows.external-secrets declares no egress_internet. The store will report 'unable to create client' with reason InvalidProviderConfig, which names no network.
FAIL platform/secret-store/store.yaml: ClusterSecretStore/example-vault authenticates with an identity it fetches at run time, and flows.external-secrets declares no egress_metadata. The pod cannot reach 169.254.169.254, and egress_internet cuts link-local out of the world on purpose, so internet egress alone does not cover it.
```

## Permitted

Fixture: `tests/fixtures/store-reach/good`

Exit code `0` (the registry expects zero). ✓

```
ok: 1 store(s) reaching outside the cluster; external-secrets declares the routes each needs
```

## The registry's own lines

- when it passes: every store reaching outside the cluster has the egress its provider needs
- when it refuses: a SecretStore or ClusterSecretStore names a provider outside the cluster that its namespace's fence gives it no route to; the store reports InvalidProviderConfig and every ExternalSecret behind it stops

Origin: The external-secrets fence allowed egress to the apiserver, kube-dns, the observability namespace and its own pods. ClusterSecretStore/estate-vault authenticates as InstancePrincipal, so it needed 169.254.169.254 and an HTTPS route to OCI, and had neither. It reported `unable to create client`, reason InvalidProviderConfig -- a message that names no network -- and every ExternalSecret in the estate then failed with `ClusterSecretStore "estate-vault" is not ready`. Twenty-nine Flux rows waited behind one.
