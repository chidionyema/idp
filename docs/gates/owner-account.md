# Gate: owners

*`owner-account` · R54 · planes: ci*

Every provider account names a second owner

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
bin/owner-account-gate
```

## Refused

Fixture: `tests/fixtures/owner-accounts/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
single   example        no second owner; recovery is the login (founder-gmail)
FAIL     owner-account-gate: 1 of 1 providers have a single owner; target 0 (crew#227 CP7)
```

## Permitted

Fixture: `tests/fixtures/owner-accounts/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
ok       owner-account-gate: 0 of 1 providers have a single owner; target 0 (crew#227 CP7)
```

## The registry's own lines

- when it passes: owner-account-gate passes a provider with a second owner and refuses one without
- when it refuses: owner-account-gate fixtures
