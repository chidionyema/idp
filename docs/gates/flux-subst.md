# Gate: flux-subst

*`flux-subst` · incident 2026-09-06, otto-gateway · planes: ci, session*

No shell parameter expansion (${NAME:-...} and kin) in a Flux-reconciled manifest; Flux substitution rewrites it before the pod runs it

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-flux-subst-gate
```

## Refused

Fixture: `tests/fixtures/flux-subst/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
tests/fixtures/flux-subst/bad.yaml:12: shell expansion Flux will rewrite: if [ -n "${HERMES_ENV_DIR:-}" ] && [ -d "$HERMES_ENV_DIR" ]; then
tests/fixtures/flux-subst/bad.yaml:29: $${ escape where nothing substitutes: args: ['sh', '-c', 'MAX_USD="$${SPEND_VELOCITY_MAX_DOLLARS:-10.0}"']
```

## Permitted

Fixture: `tests/fixtures/flux-subst/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
(no output)
```

## The registry's own lines

- when it passes: no manifest hands Flux a shell expansion; the gate is proved both ways
- when it refuses: a manifest hands Flux a shell expansion, or the gate is not discriminating
