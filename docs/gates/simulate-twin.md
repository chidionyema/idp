# Gate: simulategrade

*`simulate-twin` · MUM-288, ADR 0006 · planes: ci*

A state-changing estate MCP tool (an @mcp.tool() named execute_* or exec_*) is refused unless the same module registers a simulate twin to propose its change first (MUM-288, ADR 0006)

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-simulate-gate
```

## Refused

Fixture: `tests/fixtures/simulate-grade/bad`

Exit code `1` (the registry expects non-zero). ✓

```
mcp/plugins/estate_solo_exec.py registers execute_change with no simulate_*change twin (none)
FAIL    simulate-grade  1 mutating MCP tool(s) without a simulate twin (MUM-288, ADR 0006)
```

## Permitted

Fixture: `tests/fixtures/simulate-grade/good`

Exit code `0` (the registry expects zero). ✓

```
ok    simulate-grade  every mutating estate MCP tool in plugins ships a simulate twin (MUM-288)
```

## The registry's own lines

- when it passes: every mutating estate MCP tool in mcp/plugins ships a simulate twin; the gate is proved both ways
- when it refuses: a mutating estate MCP tool registers with no simulate twin, and the gate cannot tell the breach from the pair

Origin: MUM-288
