# Gate: ai-act

*`ai-act` · AI Act, kept voluntarily · planes: ci*

Every registered AI system has its Annex IV file, risk entries and declared data (Arts. 9, 10, 11)

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/ai-act-gate
```

## Refused

Fixture: `tests/fixtures/ai-act/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  ai-act risk R-1: review overdue (2020-01-01 < 2026-09-12)
FAIL  ai-act system demo: docs/ai-systems/demo/technical-file.md lacks Annex IV section '## 9.'
```

## Permitted

Fixture: `tests/fixtures/ai-act/good`

Exit code `0` (the registry expects zero). ✓

```
ok    ai-act 1 system(s), 1 risk(s), every Annex IV section, risk field and data source present
```

## The registry's own lines

- when it passes: every registered AI system has its Annex IV file, risk entries and declared data; gate proved both ways
- when it refuses: ai-act-gate does not discriminate, or a registered system has a gap

Origin: crew#202
