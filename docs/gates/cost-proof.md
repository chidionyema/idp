# Gate: cost-proof

*`cost-proof` · LAW 2 · planes: ci*

A pull request that changes a file deciding what the estate SPENDS carries a cost-proof block naming a before, an after and the command that took both -- a number with no way to re-take it is a remembered number, and a change that says caching-enabled without the measured pair is an assertion, not a proof. A change outside the cost paths is never refused.

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-cost-proof
```

## Refused

Fixture: `--pr-body tests/fixtures/cost-proof/bad.md --files platform/llm/config.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  this pull request moves money and carries no usable proof
      cost-touching files: platform/llm/config.yaml
      no ```cost-proof block in the body
        -> a change that moves money must carry the measured pair, taken the same way twice
      a ```cost-proof block needs a before, an after and the command that took both:
      ```cost-proof
      before: 1685 calls, 5,666,503 input tokens, $0.1177 / 15 min
      after:  166 calls, 17,786,972 input tokens, $0.2909 / 15 min
      command: kubectl logs -n llm job/proof
      ```
```

## Permitted

Fixture: `--pr-body tests/fixtures/cost-proof/good.md --files platform/llm/config.yaml`

Exit code `0` (the registry expects zero). ✓

```
ok    cost-proof present and usable (platform/llm/config.yaml)
```

## The registry's own lines

- when it passes: a cost-touching change carries a measured before and after, taken by a named command
- when it refuses: a change that moves money reaches the merge gate with no measured pair, or with one number
