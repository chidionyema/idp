# Gate: notoil

*`no-toil-policy` · LAW 47, R52 · planes: ci*

A doc that tells a person to mint a credential by hand is refused; a FOUNDER ACTION line is not

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
conftest test --parser json -p policy
```

## Refused

Fixture: `policy/fixtures/notoil-doc-manual.json`

Exit code `1` (the registry expects non-zero). ✓

```
[31mFAIL[0m - policy/fixtures/notoil-doc-manual.json - main - Policy Violation: Instructions contain manual human toil steps. Automate the bootstrapping sequence.
[31m31 tests, 30 passed, 0 warnings, 1 failure, 0 exceptions[0m
```

## Permitted

Fixture: `policy/fixtures/notoil-doc-founder-action.json`

Exit code `0` (the registry expects zero). ✓

```
[32m31 tests, 31 passed, 0 warnings, 0 failures, 0 exceptions[0m
```

## The registry's own lines

- when it passes: a doc that hands a person a credential to mint is refused; a FOUNDER ACTION line is allowed
- when it refuses: the no-toil policy did not behave as specified

Origin: crew#66
