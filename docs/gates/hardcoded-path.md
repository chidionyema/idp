# Gate: hardcode

*`hardcoded-path` · LAW 46 · planes: ci, session*

No file names where the checkout, home directory or machine lives

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
bin/idp-hardcode-scan
```

## Refused

Fixture: `tests/fixtures/hardcoded-path.bad.sh`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  hardcode 1 line(s) name a checkout path
      tests/fixtures/hardcoded-path.bad.sh:3: IDP="$HOME/dev/code/idp"
```

## Permitted

Fixture: `tests/fixtures/hardcoded-path.good.sh`

Exit code `0` (the registry expects zero). ✓

```
ok    hardcode 1 file(s) scanned; 0 hardcode a checkout path
```

## The registry's own lines

- when it passes: {tail}
- when it refuses: a file hardcodes a checkout path, or the scan is not discriminating
