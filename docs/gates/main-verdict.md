# Gate: verdict

*`main-verdict` · crew#865 · planes: ci*

A workflow that grades main never cancels main's own run; stale pull-request runs still are

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/main-verdict-gate
```

## Refused

Fixture: `tests/fixtures/main-verdict/bad.yml`

Exit code `1` (the registry expects non-zero). ✓

```
tests/fixtures/main-verdict/bad.yml: cancel-in-progress is true for every ref, so the next commit to main cancels the run grading this one; exempt main with an expression
FAIL  main-verdict  1 workflows, 1 findings
```

## Permitted

Fixture: `tests/fixtures/main-verdict/good.yml`

Exit code `0` (the registry expects zero). ✓

```
ok  main-verdict  1 workflows, 0 findings
```

## The registry's own lines

- when it passes: {tail}
- when it refuses: bin/main-verdict-gate refuses .github/workflows/
