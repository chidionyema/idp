# Gate: describe

*`job-described` · LAW 28 · planes: ci*

Every scheduled job reaches the Dagster UI with a description of what it does

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 -m estate_scheduler.describe --selftest
```

## BLIND: this rule names no case this page can run

A rule with no refusing case or no permitting case cannot be shown discriminating on this page. Nothing here is a pass.

- refusing case: none with arguments
- permitting case: none with arguments

## The registry's own lines

- when it passes: job descriptions derive from the script, and are refused when absent
- when it refuses: estate_scheduler.describe --selftest
