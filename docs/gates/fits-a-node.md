# Gate: placement

*`fits-a-node` · LAW 45 · planes: ci*

A workload that runs must be one the nodes that exist could place again; a pod the scheduler has refused, and a pod that fits on no other node, are both named

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-fits-a-node
```

## Refused

Fixture: `tests/fixtures/fits-a-node/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL fits-a-node 5 placement problem(s)
      the scheduler refused observability/db-0: 0/2 nodes are available: 2 Insufficient memory
      apps/Deployment/web asks 500m/3072Mi and fits no other node (best has 1000m/548Mi free); an eviction would not come back. Fund it or name it in tests/fixtures/fits-a-node/bad/pinned-workloads.yaml
      observability/StatefulSet/cache asks 500m/2048Mi and fits no other node (best has 1500m/1024Mi free); an eviction would not come back. Fund it or name it in tests/fixtures/fits-a-node/bad/pinned-workloads.yaml
      observability/StatefulSet/heavy asks 500m/1500Mi and fits no other node (best has 1500m/1024Mi free); an eviction would not come back. Fund it or name it in tests/fixtures/fits-a-node/bad/pinned-workloads.yaml
      3 workloads fit exactly one node; the recorded budget is 0 and may only fall
```

## Permitted

Fixture: `tests/fixtures/fits-a-node/good`

Exit code `0` (the registry expects zero). ✓

```
ok fits-a-node 3 pods over 2 node(s); every one could be placed again, 1 pinned to a single node within the budget of 1
```

## The registry's own lines

- when it passes: every pod could be placed again on the nodes that exist
- when it refuses: a pod cannot be placed on any node, or a workload fits exactly one node and is not in platform/state/pinned-workloads.yaml

Origin: idp#2378 (a memory request was raised past what any single node could hold; aggregate capacity read fine and the database was Unschedulable)
