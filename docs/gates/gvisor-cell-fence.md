# Gate: gvrfence

*`gvisor-cell-fence` · zero-trust-boundary.md step 6 · planes: ci, cluster*

A gVisor-cell namespace fence is default-deny with the gateway the one allowed route; a fence granting the cell internet egress is a break caught in the pull request

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/gvisor-cell-fence-gate
```

## Refused

Fixture: `tests/fixtures/gvisor-cell-fence/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
gvisor-cell-fence-gate: otto-gvisor-sandbox: a gVisor-cell fence must not allow internet egress (declared egress_internet: 443)
```

## Permitted

Fixture: `tests/fixtures/gvisor-cell-fence/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
(no output)
```

## The registry's own lines

- when it passes: a gVisor-cell fence granting internet egress is refused; the default-deny cell fence passes
- when it refuses: bin/gvisor-cell-fence-gate cannot tell a cell fence that leaks to the internet from the default-deny fence (ZT step 6)

Origin: crew#892 CP4
