# Gate: founder-door

*`founder-door` · LAW 32 · planes: ci, session*

Every workflow the founder can dispatch carries a '# button:' title and a '# founder:' sentence, and the portal template on disk matches the workflow that generated it. A feature with no door is not finished, and a door nobody regenerated opens onto something else.

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-portal-buttons --check
```

## Refused

Fixture: `tests/fixtures/founder-door/bad.yml`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL    portal-buttons: tests/fixtures/founder-door/bad.yml has no `# button: <plain title>` and `# founder: <one plain sentence>` header lines; the portal shows nothing else
```

## Permitted

Fixture: `.github/workflows/catalog-render.yml`

Exit code `0` (the registry expects zero). ✓

```
ok      portal-buttons: .github/workflows/catalog-render.yml carries a button and a founder sentence
```

## The registry's own lines

- when it passes: every dispatchable workflow has a founder-facing button; the gate is proved both ways
- when it refuses: a workflow the founder can dispatch has no button, or the gate is not discriminating
