# Gate: limit

*`limit-raise-only` · W3.1 · planes: ci*

A change to a workload passes the low-risk lane only when its sole difference is a strict increase of a resources.limits.memory/cpu value on one workload; a request change (which the scheduler counts), a lower limit, or any other edit is refused -- YAML ancestry is read from the full parsed old and new files, never guessed from a git hunk

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-limit-raise-only-diff
```

## Refused

Fixture: `--old tests/fixtures/limit-raise/request/old/w.yaml --new tests/fixtures/limit-raise/request/new/w.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL     limit  changes a request (spec.template.spec.containers.[0].resources.requests.memory '64Mi' -> '128Mi'); requests are refused
```

## Permitted

Fixture: `--old tests/fixtures/limit-raise/good/old/w.yaml --new tests/fixtures/limit-raise/good/new/w.yaml`

Exit code `0` (the registry expects zero). ✓

```
ok       limit  raises 1 limit(s): limits.memory 128Mi->256Mi, and nothing else
```

## The registry's own lines

- when it passes: a change that is exactly a limit raise on one workload can use the low-risk lane (W3.1)
- when it refuses: a request raise, lower limit, or wider edit could pass the low-risk lane (W3.1)

Origin: docs/policy/agent-infra-safety-spec.md
