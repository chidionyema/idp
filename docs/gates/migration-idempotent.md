# Gate: migration

*`migration-idempotent` · R22 mechanism 1 · planes: ci*

A migration whose second apply adds a resource is refused

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
bin/migration-gate
```

## BLIND to the permitting direction: this rule names no permitting case this page can run

The permitting half of this rule cannot be shown here. The refusing case this page CAN run is printed below with the gate's own output; it is a real run, not a description. Where the permitting direction is proved, the registry's `proved_elsewhere` field says.

- refusing case: yes
- permitting case: none this page can run

## Refused

Fixture: `tests/fixtures/migration-not-idempotent`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  migration tests/fixtures/migration-not-idempotent: phase 2 second apply changed state: {"dagster_yaml_sha": null, "resources": 1} -> {"dagster_yaml_sha": null, "resources": 2}
```

## The registry's own lines

- when it passes: migration-gate refuses the non-idempotent fixture
- when it refuses: migration-gate accepted a migration whose second apply adds a resource

Origin: crew#186 CP1
