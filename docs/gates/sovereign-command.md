# Gate: command

*`sovereign-command` · spec v1.0 4.2 · planes: ci*

A sovereign agent's command is inside the allowlist or it is denied; an allowlist that defaults to allow is a denylist wearing the wrong name

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
conftest test --parser json -p policy --namespace sovereign.command
```

## Refused

Fixture: `policy/fixtures/command-not-allowlisted.json`

Exit code `1` (the registry expects non-zero). ✓

```
[31mFAIL[0m - policy/fixtures/command-not-allowlisted.json - sovereign.command - command is not in the allowlist: npm publish --access public
[31m3 tests, 2 passed, 0 warnings, 1 failure, 0 exceptions[0m
```

## Permitted

Fixture: `policy/fixtures/command-allowed.json`

Exit code `0` (the registry expects zero). ✓

```
[32m3 tests, 3 passed, 0 warnings, 0 failures, 0 exceptions[0m
```

## The registry's own lines

- when it passes: the command allowlist permits an allowed verb and denies both an unlisted one and a forbidden fragment
- when it refuses: the command allowlist is not discriminating -- an agent could run a verb outside it

Origin: policy/sovereign_command.rego
