# Gate: split

*`split-brain` · decision 0026 · planes: ci*

One cloud resource has exactly one owner: a Crossplane external-name that matches an OpenTofu resource under platform/oci is two controllers undoing each other, forever

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-split-brain
```

## Refused

Fixture: `tests/fixtures/split-brain/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  split    a cloud resource has two owners; each controller will undo the other
      TWO OWNERS  'estate-receipts': Crossplane Bucket in platform/compiled/receipts/bucket.yaml, and OpenTofu in platform/oci/receipts.tf -- delete one side in this same pull request, never both
```

## Permitted

Fixture: `tests/fixtures/split-brain/good`

Exit code `0` (the registry expects zero). ✓

```
ok    split    1 Crossplane resource(s) against 1 OpenTofu identity/identities; no cloud object has two owners
```

## The registry's own lines

- when it passes: no cloud object is claimed by both OpenTofu and Crossplane
- when it refuses: a cloud object is claimed by both OpenTofu and Crossplane; each controller will undo the other at the reconcile interval

Origin: founder 2026-09-07, "i think if crossplane has benefits for us we need to work out how to fit it in"
