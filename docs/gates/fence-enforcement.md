# Gate: fencdrill

*`fence-enforcement` · zero-trust-boundary.md step 1 · planes: ci*

A profile of a denied packet is only ever a wall when the probe proved it could reach an allowed path; a probe that could not run is a fail-closed FAIL, never a pass

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-fence-enforcement
```

## Refused

Fixture: `tests/fixtures/fence-drill/gate-broken-fence.json`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL    fence-enforcement  gate-broken-fence.json  deny_cross_ns: packet to the other fenced namespace was NOT refused (fence does not drop)
```

## Permitted

Fixture: `tests/fixtures/fence-drill/gate-good.json`

Exit code `0` (the registry expects zero). ✓

```
ok      fence-enforcement  gate-good.json  a packet on a denied path is dropped, and the prover reached an allowed one
verdict fence-enforcement PASS
```

## The registry's own lines

- when it passes: fence-enforcement drops a denied packet, fails closed when the probe could not run
- when it refuses: fence-enforcement cannot tell a dropped packet from a leak or a dead probe (ZT step 1)
