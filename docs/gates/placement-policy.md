# Gate: placement

*`placement-policy` · LAW 28 · planes: ci*

No scheduled job on this laptop that runs in the sleep window or is never pinged

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
conftest test --parser json -p policy
```

## Refused

Fixture: `policy/fixtures/placement-misplaced.json`

Exit code `1` (the registry expects non-zero). ✓

```
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.blinded (check 'graphify-sweep') has no readable check record. Healthchecks was unreachable, so this job's state is UNKNOWN -- which is not the same as fine.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.key-escrow (check 'key-escrow') has never pinged. It is not late -- it has never run.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.key-escrow is scheduled in the 5 o'clock hour and this machine is asleep then. It belongs on a host that stays awake.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.key-escrow is survival work (backup, restore drill or key escrow) and it runs only here. If this laptop is what fails, it was never running.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.never-ran (check 'process-audit') has never pinged. It is not late -- it has never run.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.nightly-drills (check 'drills') has never pinged. It is not late -- it has never run.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.nightly-drills is scheduled in the 4 o'clock hour and this machine is asleep then. It belongs on a host that stays awake.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.offsite-backup (check 'offsite-backup') has never pinged. It is not late -- it has never run.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.offsite-backup is scheduled in the 3 o'clock hour and this machine is asleep then. It belongs on a host that stays awake.
[31mFAIL[0m - policy/fixtures/placement-misplaced.json - main - com.example.offsite-backup is survival work (backup, restore drill or key escrow) and it runs only here. If this laptop is what fails, it was never running.
[31m31 tests, 21 passed, 0 warnings, 10 failures, 0 exceptions[0m
```

## Permitted

Fixture: `policy/fixtures/placement-ok.json`

Exit code `0` (the registry expects zero). ✓

```
[32m31 tests, 31 passed, 0 warnings, 0 failures, 0 exceptions[0m
```

## The registry's own lines

- when it passes: the placement policy allows a monitored desk job and refuses sleep-window and never-pinged work
- when it refuses: the placement policy did not behave as specified
