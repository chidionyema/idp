# Gate: twohats

*`tenant-split` · decision 0021 · planes: ci*

One credential is one tenant's; the operator's road never widens the customer's

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-tenant-split
```

## Refused

Fixture: `tests/fixtures/tenant-split/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  bad.yaml: secret 'vault://otto-customer/token' appears under tenants ['customer-zero', 'estate']
FAIL  bad.yaml: secret 'vault://otto-customer/webhook_secret' appears under tenants ['customer-zero', 'estate']
FAIL    tenant-split: 2 finding(s)
```

## Permitted

Fixture: `tests/fixtures/tenant-split/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
PASS    tenant-split: every channel_binding row keeps the two hats apart
```

## The registry's own lines

- when it passes: idp-tenant-split refuses one secret under two tenants; the clean split passes
- when it refuses: idp-tenant-split cannot tell one-secret-one-tenant from a leak (decision 0021)
