# Gate: headroom

*`secret-headroom` · LAW 44, HGC · planes: ci*

A HelmRelease declares history-max, and the cluster's secret ceiling is raised only as a deliberate capacity decision

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
conftest test --parser json -p policy
```

## Refused

Fixture: `policy/fixtures/headroom-unbounded-release.json`

Exit code `1` (the registry expects non-zero). ✓

```
[31mFAIL[0m - policy/fixtures/headroom-unbounded-release.json - main - rule=secret_headroom | a HelmRelease is added with no maxHistory, so every revision it installs becomes a Secret that nothing ever prunes | fix: declare spec.maxHistory: 3 (a rollback needs the previous revision; three covers a rollback of a rollback). Incident 2026-09-18: the cluster reached 2,650 Secrets against OKE's 2,000 limit and refused every create, leaving 30 Flux objects NotReady.
[31m33 tests, 32 passed, 0 warnings, 1 failure, 0 exceptions[0m
```

## Permitted

Fixture: `policy/fixtures/headroom-ok.json`

Exit code `0` (the registry expects zero). ✓

```
[32m33 tests, 33 passed, 0 warnings, 0 failures, 0 exceptions[0m
```

## The registry's own lines

- when it passes: an unbounded HelmRelease and a raised ceiling are both refused; a bounded release passes
- when it refuses: the secret-headroom rule did not behave as specified

Origin: incident 2026-09-18 (2,650 Secrets against OKE's 2,000 limit, 30 Flux objects NotReady)
