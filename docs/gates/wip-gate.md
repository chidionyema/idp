# Gate: wip-gate

*`wip-gate` · founder 2026-09-17 · planes: session*

No new branch is pushed when 3 or more PRs are already failing CI (the WIP cap). Agents that open new PRs while existing ones are red compound the pile-up. At the cap the only valid task is to fix a red PR. BLIND (no GitHub API) does not refuse -- LAW 38.

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-wip-gate
```

## BLIND to the refusing direction: this rule names no refusing case this page can run

The refusing half of this rule cannot be shown here. The permitting case this page CAN run is printed below with the gate's own output; it is a real run, not a description. Where the refusing direction is proved, the registry's `proved_elsewhere` field says.

- refusing case: none this page can run
- permitting case: none this page can run

## The registry's own lines

- when it passes: fewer than 3 red PRs open -- new branch allowed
- when it refuses: 3 or more PRs are failing CI -- fix a red PR before opening a new one

Origin: Same incident class as main-green-gate: the WIP cap is the structural fix. Toyota Stop-the-Line applied to the PR queue. Three is the hard ceiling; below it the agent has room to work in parallel without losing track of what is actually broken.
