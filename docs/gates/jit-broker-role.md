# Gate: jitrole

*`jit-broker-role` · WJ.5 · planes: ci*

The broker mints only what it already holds: its own ClusterRole carries no escalate, no bind, no impersonate, no wildcard and no secrets, and no ClusterRoleBinding names it -- the absence that makes KSV-0050 on that file an accepted risk rather than a defect

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-jit-broker-role
```

## Refused

Fixture: `tests/fixtures/jit-broker-role/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
ClusterRole/jit-broker rule 2 carries the verb 'bind', which lifts Kubernetes' own escalation-prevention rule
ClusterRole/jit-broker rule 2 carries the verb 'escalate', which lifts Kubernetes' own escalation-prevention rule
```

## Permitted

Fixture: `tests/fixtures/jit-broker-role/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
(no output)
```

## The registry's own lines

- when it passes: the broker's ClusterRole carries no escalate, bind, impersonate, wildcard or secrets, and nothing binds it
- when it refuses: the broker's ClusterRole grew a verb that turns a mint into standing access, or the gate is not discriminating

Origin: trivy 2026-09-07
