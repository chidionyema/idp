# Gate: security

*`security-policy` · LAW 44 · planes: ci*

A control on the security policy page without a proof command that exists is a wish

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/security-policy-gate
```

## Refused

Fixture: `tests/fixtures/security-policy/bad.md`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  security-policy tests/fixtures/security-policy/bad.md:20: proof command 'bin/does-not-exist' does not exist
```

## Permitted

Fixture: `tests/fixtures/security-policy/good.md`

Exit code `0` (the registry expects zero). ✓

```
ok    security-policy 1 controls, every in-repo proof exists, 0 outside this repo not checkable here
```

## The registry's own lines

- when it passes: every control on docs/reference/security-policy.md names a proof command that exists; gate proved both ways
- when it refuses: security-policy-gate does not discriminate, or a control has no proof

Origin: crew#198
