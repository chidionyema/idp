# Gate: zone

*`estate-zone` · R46 · planes: ci*

The DNS zone is one value in clusters/*/estate-config.yaml; a platform file naming it as a literal fails, in the tree and in a pull-request diff

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/estate-zone-gate
```

## Refused

Fixture: `--diff tests/fixtures/estate-zone/added-literal.diff`

Exit code `1` (the registry expects non-zero). ✓

```
platform/edge/route.yaml:3: - catalogue.mumchimp.com
FAIL    zone     1 literal zone name(s) added by this diff outside clusters/*/estate-config.yaml; zone(s): mumchimp.com
```

## Permitted

Fixture: `--diff tests/fixtures/estate-zone/added-substituted.diff`

Exit code `0` (the registry expects zero). ✓

```
ok    zone     0 literal zone name(s) added by this diff outside clusters/*/estate-config.yaml; zone(s): mumchimp.com
```

## The registry's own lines

- when it passes: estate-zone-gate refuses a literal zone (tree and diff), permits the substituted form, and the repo is clean
- when it refuses: estate-zone-gate is not discriminating or the platform names the zone outside clusters/*/estate-config.yaml

Origin: crew#269, crew#796
