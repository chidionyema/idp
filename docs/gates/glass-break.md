# Gate: glass

*`glass-break` · WJ.8 · planes: ci*

The broker, its grant catalogue, the RBAC floor and bin/idp-kube never land on a green check alone: no auto-merge, no self-approval, and the founder's own review or nothing

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-glass-break
```

## Refused

Fixture: `tests/fixtures/glass-break/bad.json`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL    glass auto-merge is armed on a pull request that changes platform/jit/grants.yaml. These land when he presses merge, never when a workflow decides the checks are green
FAIL    glass platform/jit/grants.yaml is glass-break and carries no approval from chidionyema. CODEOWNERS asks for it; this refuses to land without it
```

## Permitted

Fixture: `tests/fixtures/glass-break/good.json`

Exit code `0` (the registry expects zero). ✓

```
ok      glass 1 protected path(s), and he approved every one of them
```

## The registry's own lines

- when it passes: the broker and its floor cannot land on a green check alone (WJ.8)
- when it refuses: a change to the broker could be auto-merged or self-approved (WJ.8)

Origin: docs/policy/agent-infra-safety-spec.md
