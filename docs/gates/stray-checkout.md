# Gate: stray

*`stray-checkout` · LAW 45, incident 2026-09-10 · planes: ci*

No untracked, unignored checkout of this repository is nested under its own root: a gate that walks the tree reads the nested copy's own tests/fixtures/*/bad.yaml or Dockerfile and reports it as a defect in the branch under test

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-stray-checkout
```

## Refused

Fixture: `tests/fixtures/stray-checkout/bad/nested-checkout`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  stray-checkout: a nested checkout of this repository is present, so every
      root-level gate is reading a copy of itself. Measured 2026-09-10: eight gates
      returned non-zero from a nested copy's own fixtures and Dockerfiles, and every
      one returns clean once the copy is excluded.
        nested-checkout  (marker: AGENTS.md)
      Move it outside the repository, or name it so `.gitignore` covers it.
```

## Permitted

Fixture: `tests/fixtures/stray-checkout/good/plain-data`

Exit code `0` (the registry expects zero). ✓

```
ok    stray-checkout: no untracked, unignored checkout of this repository is nested under the root; no gate is reading a copy of itself
```

## The registry's own lines

- when it passes: no nested checkout of this repository is untracked and unignored under the root
- when it refuses: measured 2026-09-10: idp-flux-subst-gate, idp-clickhouse-system-log-ttl, idp-crd-then-cr, idp-priority-class-exists, estate-zone-gate, port-gate, multiarch-gate and bin/dockerfiles all returned non-zero from a nested copy's own fixtures, and all returned clean once the copies were excluded. .gitignore's remedy on 2026-08-29 was the naming convention /.wt-*/, and the next checkouts were made as scratchpad/ and idp/, so the pattern missed them. bin/treewalk.py stops a walk entering a copy; this rule refuses the copy's presence.

Origin: incident 2026-09-10, eight gates red from three nested checkouts
