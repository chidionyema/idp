# Gate: graphview

*`estate-graph-view` · LAW 22, LAW 53 · planes: ci*

The /ops graph section reports the estate's three-state rule and never renders a stale domain as serving: UNKNOWN leads the sentence when the graph is older than its window, a BLIND graph names why rather than reading as clean, and the section keeps the tool's own ordering so a dead workload is not buried under warning events

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
node bin/idp-estate-graph-prove
```

## Refused

Fixture: `tests/fixtures/graphview/bad.json`

Exit code `1` (the registry expects non-zero). ✓

```
refused: runtime: read 86400s ago against a 180s window and still reported MEASURED_OK
```

## Permitted

Fixture: `tests/fixtures/graphview/good.json`

Exit code `0` (the registry expects zero). ✓

```
ok: the view would report this graph honestly
```

## The registry's own lines

- when it passes: the founder's graph view reports the three states honestly and never flatters a stale read (2026-09-12)
- when it refuses: the view could render a stale or blind graph as healthy

Origin: founder 2026-09-12 (the view must not flatter the estate)
