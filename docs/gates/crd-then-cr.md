# Gate: crd-then-cr

*`crd-then-cr` · incident 2026-09-08, crossplane-providers · planes: ci, session*

A Crossplane Provider and an instance of a kind its package installs never share a Flux Kustomization path; Flux dry-runs the whole path first, so neither the instance nor the Provider ever lands

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-crd-then-cr
```

## Refused

Fixture: `tests/fixtures/crd-then-cr/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL platform/xp/providers (Flux Kustomization xp-providers) holds ProviderConfig/default of group oci.upbound.io in platform/xp/providers/providerconfig.yaml, and the Provider that installs that group in platform/xp/providers/providers.yaml. Flux dry-runs the whole path before it applies any of it, so neither will ever land -- not the instance, and not the Provider either. Split the instance into its own path and its own Kustomization, dependsOn the Provider's row with wait: true.
```

## Permitted

Fixture: `tests/fixtures/crd-then-cr/good`

Exit code `0` (the registry expects zero). ✓

```
ok: no Flux Kustomization holds both a kind's definition and an instance of it
```

## The registry's own lines

- when it passes: no Flux Kustomization path holds both a Provider and an instance of a kind it installs
- when it refuses: a Kustomization path holds a Crossplane Provider and an object of a group that Provider installs; the path can never converge, and the error names the instance rather than the ordering

Origin: platform/crossplane/providers held the Provider that installs oci.upbound.io and a ProviderConfig of that group. Flux reported `no matches for kind "ProviderConfig" in version "oci.upbound.io/v1beta1"`, which reads as a missing CRD; the truth was that the apply was refused whole, so the Provider that would have installed the kind was never installed at all -- `kubectl get providers.pkg.crossplane.io` returned "No resources found" for as long as the row had existed. It never fails once and heals.
