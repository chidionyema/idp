# Gate: drift-blind

*`drift-reads-blind-not-clean` · LAW 2, incident 2026-09-08 (bin/idp-compile-helm exited 0 while 11 of 33 charts failed to render) · planes: ci*

A tool that could not read the estate exits 2 and prints BLIND, never 0 with an empty result -- "nothing is drifting" and "nothing could be read" must never print the same thing. The rule is proved on a reader whose kubectl is absent and on a clean set of Flux objects, both ways

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-drift-blind
```

## Refused

Fixture: `tests/fixtures/drift-blind/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL  drift-blind  tests/fixtures/drift-blind/bad: exit 0 while the output says BLIND. A reader that could not look reported a clean estate -- this is the 2026-09-08 defect where eleven of thirty-three charts failed to render and the tool exited 0.
FAIL  drift-blind  1 reading(s) cannot tell blind from clean
```

## Permitted

Fixture: `tests/fixtures/drift-blind/good`

Exit code `0` (the registry expects zero). ✓

```
ok    drift-blind  tests/fixtures/drift-blind/good: exit 0, a real reading
ok    drift-blind  1 reading(s) distinguish blind from clean
```

## The registry's own lines

- when it passes: a reader that cannot see the estate says so and refuses to report a clean one
- when it refuses: a blind reading prints the same thing as a clean estate

Origin: spec 2026-09-13-gitops-sync-engine.md
