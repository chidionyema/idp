# Gate: execution-boundary

*`execution-boundary-holds` · founder 2026-09-13 (verbatim) "60 secsos is the nnax" / "it should not even get bera teh" / "a breach of law or gaurd is a security breach" -- after the cap was breached six times; "find where we have done this, operationalise it, make sure every agent session is aware, and remove bash access afterward" · planes: ci, session*

Nothing an agent runs may hold a turn open. The 60-second ceiling is applied by the executor daemon outside the caller's process tree, stated once in the door and imported by the daemon -- a second copy of the number is a second answer, and a daemon that swallows its own door import answers health while unable to run anything

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-execution-boundary
```

## Refused

Fixture: `tests/fixtures/execution-boundary/bad`

Exit code `1` (the registry expects non-zero). ✓

```
FAIL missing: the daemon
FAIL missing: the entry point
FAIL missing: the status tool
FAIL missing: the launchd job
```

## Permitted

Fixture: `tests/fixtures/execution-boundary/good`

Exit code `0` (the registry expects zero). ✓

```
ok   the execution boundary holds at tests/fixtures/execution-boundary/good (ceiling 60s)
```

## The registry's own lines

- when it passes: the executor daemon applies one ceiling, imported from the door, and every half of the boundary is present
- when it refuses: a ceiling is restated in the daemon, or the boundary is half built and nothing says so

Origin: founder 2026-09-13, the execution boundary
