# Gate: grader-exit

*`grader-exit-gate` · LAW 28 · planes: ci*

A grading step whose verdict cannot reach the run is refused: a grader piped into tee/cat with neither pipefail nor -e, a bare grader that is not the last command under a set line without -e, or a grader ending in '|| true' with no '# optional: <reason>' marker. The step is accepted when the same job asserts the captured verdict in a later step

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/idp-grader-exit-gate
```

## Refused

Fixture: `tests/fixtures/grader-exit/bad`

Exit code `1` (the registry expects non-zero). ✓

```
REFUSED  tests/fixtures/grader-exit/bad/grader-no-e.yml: step 'prove langfuse': no -e: a bare grader that is NOT the last command, under a set line without '-e'; measured -- the command after it overwrites $?, so the verdict never reaches the step's status
  Fix: add '-e' to the set line (e.g. set -euo pipefail), or add '# optional: <reason>' above the step if this leg is genuinely allowed to be absent.
REFUSED  tests/fixtures/grader-exit/bad/grader-orphan-true.yml: step 'check placement': orphan || true: grader line ends in '|| true' without a '# optional: <reason>' marker; the grader's exit is discarded
  Fix: add '-e' to the set line (e.g. set -euo pipefail), or add '# optional: <reason>' above the step if this leg is genuinely allowed to be absent.
REFUSED  tests/fixtures/grader-exit/bad/grader-piped.yml: step 'grade the estate': piped verdict: grader piped into tee/cat with neither 'pipefail' nor '-e' on a set line; measured -- 'set -u; false | tee out' exits 0, so the run is green while the grader failed. 'set -o pipefail' or 'set -e...' fixes it.
  Fix: add '-e' to the set line (e.g. set -euo pipefail), or add '# optional: <reason>' above the step if this leg is genuinely allowed to be absent.
3 step(s) refused.
```

## Permitted

Fixture: `tests/fixtures/grader-exit/good`

Exit code `0` (the registry expects zero). ✓

```
ok    1 workflow(s) checked, no refused grading steps.
```

## The registry's own lines

- when it passes: every grading step in the tree can fail its run, or a later step asserts the captured verdict
- when it refuses: measured 2026-09-10: 'set -u; grader | tee out' exits 0 while the grader failed, so a run reports green having graded nothing. 'set -o pipefail' or '-e' fixes it; a bare last command propagates without -e, and a later step that tests the captured rc is an assertion

Origin: incident 2026-09-10, grading steps whose verdict is discarded
