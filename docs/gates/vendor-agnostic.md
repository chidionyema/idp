# Gate: agnostic

*`vendor-agnostic` · LAW 34 · planes: ci*

No hardcoded claude/anthropic/.claude token in the session engine outside the one runner registry CONTRACT.md:41 names

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/vendor-agnostic-gate
```

## BLIND to the refusing direction: this rule names no refusing case this page can run

The refusing half of this rule cannot be shown here. The permitting case this page CAN run is printed below with the gate's own output; it is a real run, not a description. Where the refusing direction is proved, the registry's `proved_elsewhere` field says.

- refusing case: none this page can run
- permitting case: none this page can run

## The registry's own lines

- when it passes: vendor-agnostic-gate refuses a stray vendor name in workflow.py, permits the registry entry and the adapters/ wrapper, and sovereign/engine is clean
- when it refuses: vendor-agnostic-gate is not discriminating or a vendor name has leaked into the engine outside runners.py

Origin: founder, 2026-09-15
