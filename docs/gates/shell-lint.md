# Gate: shell

*`shell-lint` · LAW 45 · planes: *

Every bash script in bin/ passes shellcheck at warning level

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
shellcheck -S warning
```

## Refused

Fixture: `tests/fixtures/shell-lint/bad.sh`

Exit code `1` (the registry expects non-zero). ✓

```
In tests/fixtures/shell-lint/bad.sh line 5:
cd /var/tmp/does-not-exist
^------------------------^ SC2164 (warning): Use 'cd ... || exit' or 'cd ... || return' in case cd fails.
Did you mean:
cd /var/tmp/does-not-exist || exit
For more information:
  https://www.shellcheck.net/wiki/SC2164 -- Use 'cd ... || exit' or 'cd ... |...
```

## Permitted

Fixture: `tests/fixtures/shell-lint/good.sh`

Exit code `0` (the registry expects zero). ✓

```
(no output)
```

## The registry's own lines

- when it passes: every bash script in bin/ is clean at warning level; the unguarded-cd fixture is refused
- when it refuses: a script in bin/ fails shellcheck at warning level

Origin: the rung that graded this was deleted with the fixture-grading rungs in 519d59e8, and the fast gate went on printing the word "shell" in its summary line while nothing ran shellcheck in CI at all -- .pre-commit-config.yaml was the only place left, and a local hook is not a gate
