# Gate: vendor

*`vendor-name` · platform/calico/raw/README.md · planes: ci*

A deck that replaces a vendor operator never reuses the operator's cluster-scoped names; a ClusterRole has no namespace, so moving the ServiceAccounts does not separate them

This page is generated by `bin/idp-gate-demo` from `rules.yaml`. The output below is what the gate itself printed when the generator ran it; it is not a description of what the gate does.

## Run it yourself

```bash
python3 bin/vendor-name-collision-gate
```

## Refused

Fixture: `tests/fixtures/vendor-names/bad.yaml`

Exit code `1` (the registry expects non-zero). ✓

```
tests/fixtures/vendor-names/bad.yaml: ClusterRole/calico-node is a bare vendor name
```

## Permitted

Fixture: `tests/fixtures/vendor-names/good.yaml`

Exit code `0` (the registry expects zero). ✓

```
1 file(s): no bare vendor cluster-scoped name
```

## The registry's own lines

- when it passes: no replacement deck reuses a vendor operator's cluster-scoped names
- when it refuses: a deck reuses a vendor operator's cluster-scoped name, or the gate is not discriminating

Origin: incident 2026-09-06, tigera-operator tombstones
