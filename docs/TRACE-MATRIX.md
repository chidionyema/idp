# Trace matrix

GENERATED by `bin/trace-matrix` from every tracked `*.feature` file and the tests that load them (`scenarios("...")`, the bin/spec-gate rule), taken 2026-09-26T12:47:53+00:00. Unbound rows first. Do not edit by hand.

## The bar

- Features: **50 BOUND**, **2 PENDING**, **57 PROSE**, **67 UNBOUND** of 176
- PROSE is a feature under `docs/prose/`: intent that nothing runs, by the spec-gate rule; it is counted, not failed. UNBOUND is a feature under `features/` with no test, and fails `--check`.
- Scenarios: **182 run by a test** of 583

## Requirement -> scenario -> test

| Status | Feature | Scenarios | Bound by | Added in |
|---|---|---|---|---|
| UNBOUND | `features/battalion/cp1-cost-spend-governance.feature`<br>Cost & spend governance — hard breaker, hardware-signed activation, breakeven-gated ladder, graded costs | COST-01 — the router budget breaker severs traffic at breach, it does not warn<br>COST-02 — provisioning paid compute requires a hardware-rooted signature, not just budget<br>COST-03 — Rung 2 flat-rate rental activates only above the computed breakeven band<br>COST-04 — every hosting tier row carries a receipt grade, or it fails schema | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp10-p0-prerequisites.feature`<br>The three P0 prerequisites gate every other claim in the Battalion | P0-01 — the breaker severs, or every COST-01-dependent claim in the spec is fiction<br>P0-02 — the CFG-01 config document exists, or every axis is a code-level toggle, not a product capability<br>P0-03 — the UX-02 edge-case suite is green, or "reliable as electricity" is marketing | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp2-jit-provisioning.feature`<br>Compute provisioning is point-of-use, honoring each tier's real activation floor | JIT-01 — Apple Silicon and GPU tiers are not treated as interchangeable on activation floor<br>JIT-02 — a cold start beyond the latency budget degrades to the next lane, marked in-trace | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp3-routing-escalation.feature`<br>Routing and escalation — one router, an independently toggleable matrix, verifier-driven escalation, local floor | ROUTE-01 — all model calls traverse the standardized router, direct calls are rogue<br>ROUTE-02 — resource tier, candidate volume, selection method and escalation pattern are independent config axes<br>ROUTE-03 — verifier-driven escalation makes misroute risk zero by construction<br>ROUTE-04 — NL-judgment escalation is signaled by disagreement between two cheap models<br>ROUTE-05 — semantic intent routing is a named gap, present as a toggleable axis, default off<br>ROUTE-06 — the fallback chain terminates at the always-on local model, never at a paid pool | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp4-verification-selection.feature`<br>Verification and selection — the gauntlet, strategy-slotted selection, claim-graph as NL verifier, capped volume | VER-01 — attestation stays bound to the SHA-256 of verified bytes<br>VER-02 — weighted voting or multi-verifier is used where a real verifier exists, plain best-of-N is disallowed there<br>VER-03 — the claim-graph (DoD v3) is reused as the NL verifier for NL output candidates<br>VER-04 — candidate volume is capped for tasks with no real verifier, regardless of config | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp5-offload-grind-darwin-orchestration.feature`<br>Offload, grind, darwin and orchestration — no tokens on deterministic ops, unkillable grind, confirmed darwin loop, Temporal only | OFF-01 — deterministic operations never execute as a model call<br>OFF-02 — the CPU-sandbox execute_python tool excludes offloadable raw data from the prompt, when built<br>GRIND-01 — the overnight grind worker cannot terminate before its test passes or the 4h wall-clock limit<br>DARWIN-01 — the weekly meta-optimizer loop is confirmed against the repo, or recorded as a gap<br>ORCH-01 — orchestration reuses Temporal child workflows, no second framework<br>ORCH-02 — flat-rate tiers are governed by a time budget, metered tiers by a dollar budget<br>ORCH-03 — every routed call carries a config_id, answerable as a Langfuse query | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp6-free-lunch-invariants.feature`<br>Free-lunch invariants — the only leverage claims allowed to call themselves free of downside | FL-01 — speculative decoding never changes the output distribution<br>FL-02 — the exact-match cache keeps its safeguards, or it forfeits the free-lunch claim<br>FL-03 — config_id trace tagging is pure information gain on already-paid infrastructure<br>FL-04 — weighted verifier voting on Z3-scored branches costs the same as unweighted selection<br>Any other free-lunch claim is rejected at intake unless its tradeoff is named | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp7-config-surface.feature`<br>One hot-reloadable config document is the single point of control for every axis | CFG-01 — a config diff toggling any axis takes effect on the next request, no restart<br>CFG-02 — the config schema is generated from the spec's own REQ list, and a mismatch fails CI | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp8-observability-delivery.feature`<br>Observability and delivery — generated catalog entities, the estate's own board, no laptop dependency, destructive-class spend | OBS-01 — the hosting matrix and the CFG-01 surface are generated catalog entities<br>OBS-02 — founder-facing deliverables render on the estate's own board<br>GOV-01 — zero laptop dependency in the production routing path<br>GOV-02 — autonomous money-spending actions inherit the destructive capability class | nothing runs it | e0d9d2fc |
| UNBOUND | `features/battalion/cp9-ux-reliability-bar.feature`<br>UX and reliability — as seamless and reliable as electricity | UX-01 — every tier/model/axis switch is invisible to the requester on the success path<br>UX-02 — every enumerated edge case has a named, tested degradation path<br>UX-03 — CFG-01 rejects any write that would leave zero enabled lanes reachable | nothing runs it | e0d9d2fc |
| UNBOUND | `features/chaos/chaos-target-namespace-labelled.feature`<br>A chaos experiment targets a namespace Chaos Mesh is allowed to inject into | The backstage namespace is labelled for injection | nothing runs it | #190 crew#292 |
| UNBOUND | `features/cognitive-stack/cp0_gpu_capacity_and_decision.feature`<br>The graph-of-record decision is recorded | The Memgraph-vs-estate.db decision is recorded, not assumed | nothing runs it | 141b68f7 |
| UNBOUND | `features/cognitive-stack/cp1_ingestion_topics.feature`<br>Ingestion — GitHub, Slack, CI/CD and incident feeds land in event-bus streams | Four JetStream streams receive real events<br>The row reconciles and its healthcheck names a real object<br>The door shows live stream stats | nothing runs it | 141b68f7 |
| UNBOUND | `features/cognitive-stack/cp2_memgraph_causal_edges.feature`<br>Storage — Memgraph holds every ingested fact as a node with a causal edge | A consumed topic message becomes a node with a causal edge<br>The graph is queryable from its own door<br>The namespace is fenced like every other workload | nothing runs it | 141b68f7 |
| UNBOUND | `features/cognitive-stack/cp3_auto_healer.feature`<br>Auto-Healer — an uncorroborated edge is deleted, never left as a human claim | A manufactured false edge is deleted within one healer cycle<br>A corroborated edge survives<br>The healer's own workload is fenced and health-checked like every other | nothing runs it | 141b68f7 |
| UNBOUND | `features/cognitive-stack/cp4_ray_trigger.feature`<br>Neural Compiler trigger — a major graph node change kicks off a Ray job | A core API schema node change submits a Ray job<br>A minor node change does not trigger training<br>The trigger's own workload is fenced and health-checked | nothing runs it | 141b68f7 |
| UNBOUND | `features/cognitive-stack/cp5_lora_output.feature`<br>Neural Compiler output — Unsloth trains a named Rank-16 LoRA adapter from the graph delta | A triggered job produces a rank-16 adapter named after its trigger<br>The adapter is provider/model agnostic<br>The door shows the new artifact | nothing runs it | 141b68f7 |
| UNBOUND | `features/cognitive-stack/cp6_kv_cache_pinned.feature`<br>Inference Engine setup — the enterprise-constraints prefix is computed once and pinned | Deploy computes and pins the constraints prefix exactly once<br>Prefix cache hit rate is observable from the door<br>The engine stays provider/model agnostic | nothing runs it | 141b68f7 |
| UNBOUND | `features/cognitive-stack/cp7_uuid_pointer_and_lora_mount.feature`<br>Inference Engine execution — UUID cache inheritance and dynamic LoRA mount/unmount | A new agent inherits the pinned cache at zero input-token cost<br>The relevant LoRA mounts, executes one patch, and unmounts<br>The trace is visible from the door | nothing runs it | 141b68f7 |
| UNBOUND | `features/deploy-river/cp2_river_scene.feature`<br>CP2 Deploy River scene | a fresh push ignites a comet at the source gate<br>a green gate flashes the ring and accelerates the comet<br>a red gate cracks the ring and the comet falls into the rejection basin<br>a docked comet shows the receipt (proof, not assertion)<br>the Andon ambient light shifts amber while main is red<br>focusing a gate unfolds its data hologram and focus moves collapses it<br>a comet's road is read-only; the renderer never invents stages<br>zero chrome — no sidebar, no breadcrumbs, no card floating in space<br>presence — the scene knows being-watched from being-used<br>a green gate rings consonance; a cracked gate is a dissonance you feel<br>the River reads from CP1, full-bleed to the bezel | nothing runs it | 8f6298ad |
| UNBOUND | `features/deploy-river/cp3_narration.feature`<br>CP3 narration engine | story mode names the journey's actual push time, gates, and merge time<br>live commentary speaks only at transitions, never speculates<br>a failed gate is named, never glossed over<br>unknown stages are read as unknown, never guessed<br>a sha with no recorded journey is BLIND, never a fabricated story<br>timestamps are deterministic; two runs produce byte-identical text<br>the narration engine never imports an LLM client<br>narration speaks in one or two sentences, no markdown, no lists<br>live commentary speaks only at transitions and never wakes the room | nothing runs it | 8f6298ad |
| UNBOUND | `features/deploy-river/cp4_replay.feature`<br>CP4 time-scrub and Holmes interrogation | dragging the river's edge back replays last week's deploys at 60x<br>focusing a comet stops the replay and opens the receipt<br>"why did this fail" routes the question to ask_holmes and speaks the answer<br>"why did this fail" on a green comet is BLIND, never a forced answer<br>a failed gate hologram names the check-run and the conclusion verbatim<br>scrubbing past the latest ledger row is BLIND, not an extrapolation<br>Holmes is called once per question; the same question is not re-routed<br>the subtitle proof is shown, not hidden, on every answer<br>time machine — "take me to last Thursday 4pm" flies the camera into the past | nothing runs it | 8f6298ad |
| UNBOUND | `features/deploy-river/cp5_observatory.feature`<br>CP5 Three Planes Observatory | ring 1 (Generative Swarm) animates every live agent as a star<br>ring 2 (Adversarial Crucible) renders JudgeWorker scores as scales<br>ring 3 (Physics Engine) renders Z3 proofs and Aevum seals<br>work passes outward through the three rings<br>a plane with no data shows a named gap, not a green tick<br>the Universal Write Boundary is drawn as a crack<br>the Observatory reads only from the one estate MCP server<br>zero chrome — the rings are the page<br>presence — the rings idle until interrogated<br>every named gap renders as a dark segment with a name, never a green tick<br>the soundtrack binds each plane to an instrument family | nothing runs it | 8f6298ad |
| UNBOUND | `features/deploy-river/cp6_voice_steering.feature`<br>CP6 voice steering on both pages | focus on a comet by saying "the one on the left"<br>address a session by spatial relation not by name<br>a steering phrase reaches the right MCP tool, named by intent<br>an unresolvable spatial reference is BLIND, never a guessed target<br>the same spatial phrase works on the Observatory<br>the voice router never invents a session id<br>voice steering is gated by the same quota as the human on Telegram | nothing runs it | 8f6298ad |
| UNBOUND | `features/estate-rebuild/rebuild-and-gates.feature`<br>The estate can be torn down and rebuilt from the phone, and no past error can recur | A stale local state file can never overwrite the shared remote state<br>a pull request's planned changes are the PR, not drift (incident 2026-08-26, run 32925504695)<br>a lost-state apply never re-creates a vault that already exists (incident 2026-08-26 02:26Z)<br>the secret store is never repointed at a vault while secrets still live in the current one (incident 2026-08-26 02:26Z)<br>a failing rebuild step shows its cause, not only its footer (incident 2026-08-26, run 32930359052)<br>node cycling is never asked of a BASIC cluster (incident 2026-08-26, run 32930359052) | nothing runs it | #123 |
| UNBOUND | `features/fleetview/cp3_signals.feature`<br>CP3 the four signals | stop ends a session<br>a signal from an unauthenticated caller is refused | nothing runs it | #2619 |
| UNBOUND | `features/fleetview/cp4_every_runtime.feature`<br>CP4 every runtime, enriched | three runtimes on one board<br>an adapter that breaks the schema fails CI | nothing runs it | #2619 |
| UNBOUND | `features/fleetview/cp5_offering.feature`<br>CP5 the offering | a tenant sees only its own fleet<br>history replays<br>the product has its pages | nothing runs it | #2619 |
| UNBOUND | `features/fleetview/cp6_adapters.feature`<br>CP6 — NATS adapters | Claude Code tool event reaches NATS<br>Stream endpoint relays NATS events as SSE frames<br>Stream falls back to heartbeat when NATS is not configured | nothing runs it | #3717 |
| UNBOUND | `features/fleetview/cp8_steering.feature`<br>CP8 — the steering wheel | Steer a sovereign session<br>Steer a claude-code session writes the directives mailbox<br>Steer an otto session publishes NATS steer event<br>Unsupported runtime returns 422<br>Steer with missing "by" field returns 400 | nothing runs it | #3717 |
| UNBOUND | `features/gates/ci-cryptographic-gateway.feature`<br>Cryptographic CI Gateway and Zero-Trust Agent Enforcement | An AI agent submits a Pull Request<br>The verifier tooling is unavailable or crashes<br>An agent submits a pre-verified, cryptographically sealed patch<br>An agent submits an unsealed patch (Hallucination/Bypass attempt)<br>A human submits an unsealed patch | nothing runs it | 89c068fb |
| UNBOUND | `features/gates/efficiency-gateway.feature`<br>The estate efficiency gateway cuts tokens before every vendor call, model-agnostic | A stable system prompt keeps the prefix cache alive<br>A drifted system prompt is flagged as a cache miss<br>Duplicate lines in a tool_result message are stripped<br>A tool description longer than the limit is truncated<br>A short tool description is not touched<br>Each call increments the call counter and the token estimate<br>A second identical large tool result is replaced with a reference handle<br>A duplicate tool_result with the same tool_call_id and content is pruned<br>A history longer than MAX_HISTORY_MSGS is truncated<br>System messages are preserved during compaction<br>An old assistant turn beyond the gist boundary is condensed<br>A recent assistant turn is not condensed<br>A realistic session with drift, duplicates, long history, and verbose tools is optimised | nothing runs it | #3703 |
| UNBOUND | `features/gates/egress-block.feature`<br>Egress block — direct AI vendor egress denied at the network layer | GlobalNetworkPolicy has correct name and order<br>GlobalNetworkSet covers all current AI vendor endpoints<br>Policy blocks all namespaces except the approved exempt set<br>Egress rule targets the vendor endpoint GlobalNetworkSet by label<br>A non-exempt namespace has no allow-egress rule to vendor endpoints<br>Policy manifest is valid YAML and passes structural schema check | nothing runs it | #3703 |
| UNBOUND | `features/gates/front-door-login.feature`<br>The front door is a federated login with no local password | No manifest holds a user database | nothing runs it | #144 crew#269 |
| UNBOUND | `features/gates/placement-published.feature`<br>placement is measured, published and read on the portal | the cluster publishes its placement receipt<br>the render carries the placement document forward<br>the portal reads it through the proxy it already has<br>a cluster at its request ceiling is explained, never just reported | nothing runs it | #3254 |
| UNBOUND | `features/portal-product/cp1-showcase-live.feature`<br>the showcase page reads live state, never a markdown file | the showcase page answers with data read on this load, not a stored file<br>every system on the showcase page carries a live health value<br>the five Otto capabilities are read from their proof file, not retyped | nothing runs it | #1918 |
| UNBOUND | `features/portal-product/cp2-sandbox-launch-countdown.feature`<br>a person launches the buyer sandbox from the showcase page, and watches it expire | the launch button dispatches the documented command, unchanged<br>the countdown reflects the Kustomization's own TTL label<br>after expiry the showcase page shows the sandbox is gone, not stale<br>no new agent capability is created | nothing runs it | #1918 |
| UNBOUND | `features/portal-product/cp3-vendor-proxies.feature`<br>the vendor dashboards show one live fact on the card, not only a link | a Langfuse proxy exists and answers<br>a SigNoz proxy exists and answers<br>a Superset proxy exists and answers<br>a proxy failure is shown, not hidden | nothing runs it | #1918 |
| UNBOUND | `features/portal-product/cp4-platform-layer-card.feature`<br>every platform layer's entity page draws its own live state | the layer card is one component reused by every platform layer<br>the layer card's Ready state matches the cluster, not the manifest<br>adding the card to one layer adds it to all | nothing runs it | #1918 |
| UNBOUND | `features/portal-product/cp5-no-link-only-founder-surfaces.feature`<br>no founder-surface entity ships with zero live content | every founder-surface entity carries a kubernetes-label-selector<br>the previously link-only entities render the layer card | nothing runs it | #1918 |
| UNBOUND | `features/portal-product/cp6-agent-door-status-pill.feature`<br>the agent doors show a live status pill, not a manifest link | the Otto door's pill reflects its own healthz answer<br>the MCP gateway's pill is read through a proxy, never a hardcoded host | nothing runs it | #1918 |
| UNBOUND | `features/sovereign-bus/cp10_dual_read.feature`<br>Dual-read router — every read runs twice, legacy and DAG, and is compared | Both paths execute<br>Reads never slow the caller past the key | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp11_consensus_check.feature`<br>Consensus check — a legacy/DAG mismatch is an alert, never a freeze | Mismatch is surfaced to the HUD<br>Match rate is measured | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp12_ai_sandbox.feature`<br>AI sandbox and zero-cost forks — agents query a copy-on-write fork, production untouched | Fork under a second<br>Agent writes land on the fork only<br>Switch and drop<br>The cap is a key | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp13_the_flip.feature`<br>The flip — the DAG becomes primary; the legacy DB becomes a read-only archive with a rollback path | Zero-downtime cutover<br>Rollback path | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp14_projection_views.feature`<br>Projection views — hot SQLite/Redis stores compiled from the immutable DAG | Views are rebuilt from the DAG after a crash<br>Boot check | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp15_cross_stack_root.feature`<br>Cross-stack root — one Merkle root over code, DB, policy and AI policy | One root, four children | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp16_orchestrator_commit.feature`<br>Orchestrator agent — one atomic commit across UI, API, schema and policy | A cross-stack change is one commit | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp17_global_cas_api.feature`<br>Global CAS API — one local blob router for frontend, backend and agents | Put and get by hash<br>No literal endpoints | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp18_budget_wall.feature`<br>Budget wall — the phase-1 exit test | An agent burns its budget and is halted<br>No session starts without a budget<br>A signed refill resumes from the same step | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp19_signed_receipt_chain.feature`<br>Receipts are a signed hash chain, not a JSON file | The chain verifies<br>A tampered line is detected<br>A deleted halt is detected<br>The signing key never leaves the keystore | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp1_durable_session.feature`<br>Durable session — a session outlives the process that runs it | A session survives its worker being killed<br>A session id is stable and listable | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp20_cross_platform.feature`<br>Cross-platform — macOS and Windows, one trust anchor, one presence gateway | One trust anchor, three backends<br>No OS branching outside the boundary<br>Windows paths do not change hashes<br>The engine runs on Windows | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp21_estate_attach.feature`<br>Estate attach — point the OS at any repo, directory or workspace | Attach a fresh repo<br>The scaffolded policy is conservative<br>Sessions run inside the attached estate<br>Global commands | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp22_everything_configurable.feature`<br>Everything is configurable — one config, defaults in one place, no literal in code | The whole config is listable, with its source<br>File overrides default, env overrides file, flag overrides env<br>No magic numbers outside config.py<br>Every spec threshold is a key<br>Setting a key is a command, and a receipt | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp2_stop_signal.feature`<br>Stop — the founder's 🛑 lands even when nothing is awake to hear it | Stop while the worker is alive<br>Stop while the worker is dead | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp30_consensus_and_policy.feature`<br>Cross-model consensus under a policy invariant | Two of three agree and the call is allowed<br>Consensus outside policy is blocked<br>A late vote does not count<br>Non-destructive ops use one cheap model<br>The default voters are three different models<br>sb model-consensus reaches the vote | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp3_approval_gate.feature`<br>Approval gate — a session that needs the founder waits for him, durably | Deny ends the session with a receipt<br>Approve lets the session continue<br>Silence is not consent | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp3_worker_registry.feature`<br>The live worker serves every workflow the CLI can start | Branch workflows are registered on the live worker<br>A real fork and merge runs on the registered worker | nothing runs it | #169 crew#284 |
| UNBOUND | `features/sovereign-bus/cp4_otto_card.feature`<br>Otto's card — the chat is a status line, edited in place, never appended | One pinned card, edited not re-sent<br>One message per live session, collapsed when it ends<br>The card never carries alerts | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp5_digest_schedule.feature`<br>The daily digest is a job on the estate scheduler | The digest job exists and fires at the configured hour | nothing runs it | #171 crew#284 |
| UNBOUND | `features/sovereign-bus/cp5_receipts_and_learning.feature`<br>Receipts and learning — every founder touch is recorded so Otto can learn from it | Every signal is a receipt<br>Every session is a Langfuse trace<br>Failure episodes are queryable | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp6_provider_agnostic.feature`<br>Provider agnostic — the runner and the model are configuration, never code | The engine runs a step with no vendor present<br>No vendor import in the engine<br>No hardcoded home, host or checkout<br>A new provider is one registry entry, not a code change | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp6_self_termination.feature`<br>Auto-termination acts, it does not only report | Langfuse unreachable for more than blind.halt_after_min minutes halts<br>Confidence below terminate.min_confidence for low_confidence_steps steps soft-halts<br>More than alerts.digest_over_per_hour alerts in an hour compresses to a signed digest<br>Enforcement stops running sessions on halt and nothing on continue<br>The check is a row on the estate scheduler<br>A critical session survives self-termination | nothing runs it | #174 crew#284 |
| UNBOUND | `features/sovereign-bus/cp7_cockpit.feature`<br>Cockpit — a screen, not a command list, on the phone and the laptop | The cockpit serves the truth from Temporal<br>A tap is a signal<br>The Inbox is on the screen, not in the chat<br>The menu button opens the cockpit | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp8_db_sidecar.feature`<br>DB sidecar — every legacy write is diffed, hashed, and passed through untouched | The legacy DB is named before anything is built<br>A write is mirrored, not altered<br>Sidecar failure never blocks the legacy write | nothing runs it | #70 crew#213 |
| UNBOUND | `features/sovereign-bus/cp9_shadow_root.feature`<br>Shadow root — a branch pointer that tracks legacy state one to one | Root advances with every write<br>Root is verifiable | nothing runs it | #70 crew#213 |
| PENDING | `features/gates/deterministic-verifier.feature`<br>Deterministic Verifier and Zero-Trust Agent Governance | The agent attempts to directly write to the active worktree<br>The agent successfully queues a change for verification<br>A proposed patch contains structural, symbolic, or execution flaws<br>A proposed patch achieves mathematical and execution correctness<br>An unverified payload attempts to bypass the Verifier and enter the estate<br>A mechanically verified payload enters the estate | `sovereign/tests/bdd/test_deterministic_verifier.py` | #3376 |
| PENDING | `sovereign/tests/fixtures/bdd/pending_unclaimed/pending_unclaimed.feature`<br>A pending feature nobody has claimed | Steps that were never written | `sovereign/tests/fixtures/bdd/pending_unclaimed/test_pending_unclaimed.py` | a85623f2 |
| PROSE | `docs/prose/catalogue-links-product.feature`<br>A product's UIs enter the catalogue from the product's own repository | A product's UIs enter the catalogue from the product's own repository | nothing runs it | #205 crew#297 |
| PROSE | `docs/prose/chaos-daemon-runtime.feature`<br>The chaos daemon speaks the container runtime the node actually runs | The daemon's runtime matches the node's containerRuntimeVersion<br>A runtime value that names a socket the node does not have is a red row, not a quiet one | nothing runs it | #215 crew#297 |
| PROSE | `docs/prose/chaos-live.feature`<br>A chaos experiment injects for real on the cluster | The first run is a receipt, not a promise | nothing runs it | #215 crew#297 |
| PROSE | `docs/prose/estate-gates-online.feature`<br>Every active estate repository runs the same merge-blocking gates | A leaked secret cannot reach main in any active repository<br>The rollout is one command, run once<br>A required check that cannot report never blocks a repository<br>The architecture diagram is drawn from the catalogue, never by hand<br>The live architecture page reaches main on a schedule, through a pull request<br>The npm audit gate is about what ships<br>The pip-audit gate reports an advisory, never a broken build environment | nothing runs it | #207 crew#297 |
| PROSE | `docs/prose/estate-rebuild-live.feature`<br>The estate rebuilds from an empty tenancy and every incident is a gate | Every component of the estate is covered, or a command says which is not<br>A full rebuild from an empty tenancy, started from the phone<br>The rebuild is rehearsed on a schedule<br>A broken workload is reported to the founder within ten minutes<br>Every past incident is a gate proved both ways<br>A deploy cannot point at an image nobody builds<br>Improvement is measured, not felt | nothing runs it | #219 crew#297 |
| PROSE | `docs/prose/fortress/cp1_data.feature`<br>Data — the catalog database is green before any governance layer is judged | idp-verify passes clean | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/fortress/cp2_litellm.feature`<br>Model routing — LiteLLM is the universal API layer | The proxy is up with a real fallback chain<br>Every non-CLI agent's base URL points at the proxy | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/fortress/cp3_langfuse.feature`<br>Observability and audit — OTel GenAI traces reach Langfuse | The primary receiver answers<br>A real agent run produces a queryable, complete trace | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/fortress/cp4_spiffe_on_k3d.feature`<br>Agent identity — SPIRE runs on the estate cluster and issues short-lived SVIDs | The SPIRE server has attested the node agent<br>A workload in the catalogue namespace receives an SVID from the Workload API | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/fortress/cp4_spiffe_on_oke.feature`<br>Agent identity — SPIRE runs on the estate cluster through Flux and issues short-lived SVIDs | The cluster carries the SPIRE row and Flux waits for it to be healthy<br>The cluster-state receipt shows the identity issuer running | nothing runs it | #292 crew#227 |
| PROSE | `docs/prose/fortress/cp5_mcp_agentgateway.feature`<br>Protocols — official MCP SDK replaces the proprietary board schema, fronted by Agentgateway | The board and estate.db are MCP tools, not a proprietary schema<br>Every MCP call passes through Agentgateway | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/fortress/cp6_agents_md.feature`<br>Living specs — AGENTS.md is the version-controlled boundary for this repo | AGENTS.md is a committed file, not a convention in someone's head<br>A gate reads it both ways | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/front-door-login-live.feature`<br>The live front door redirects to the identity domain and holds no password | An unauthenticated request is sent to the identity domain<br>The identity domain's signing keys are readable by the relying party<br>The signing keys are private<br>The door answers anything else<br>No zone is configured<br>The secrets reach the pod as one mounted file, never as environment variables<br>the login row refuses a placeholder client id (idp#149 review, 2026-08-26) | nothing runs it | #212 crew#297 |
| PROSE | `docs/prose/front-door-oidc-rollout.feature`<br>the front door OIDC client is applied by Terraform and used by the founder (crew#269, crew#281) | the identity module is valid and writes the two vault secrets the pod reads<br>the apply refuses a domain mismatch<br>the founder signs in with the identity he already has | nothing runs it | #210 crew#297 |
| PROSE | `docs/prose/github-app-online.feature`<br>The GitHub App manifest and the identity gh acts as | The manifest is a one-tap page with no webhook and no public listing<br>A session on a personal token is a FAIL | nothing runs it | #211 crew#297 |
| PROSE | `docs/prose/golden-path-cp2a-python-golden-path.feature`<br>Python golden path scaffolding | Provisioning a Python service produces a compliant repo | nothing runs it | #930 crew#627 |
| PROSE | `docs/prose/golden-path-cp2b-dotnet-golden-path.feature`<br>.NET golden path scaffolding | Provisioning a .NET service produces a compliant repo | nothing runs it | #930 crew#627 |
| PROSE | `docs/prose/golden-path-cp2c-node-golden-path.feature`<br>Node golden path scaffolding | Provisioning a Node service produces a compliant repo | nothing runs it | #930 crew#627 |
| PROSE | `docs/prose/golden-path-cp2d-template-opens-infra-pr.feature`<br>Template opens the platform infra pull request | A scaffolder run opens a pull request touching only the new service's platform folder | nothing runs it | #930 crew#627 |
| PROSE | `docs/prose/golden-path-cp2e-flux-reconciles-service.feature`<br>Flux reconciles a template-created service | Merging the platform pull request brings the service up | nothing runs it | #930 crew#627 |
| PROSE | `docs/prose/icm-cp1_gateway_auth.feature`<br>Gateway auth at the edge | Traefik ForwardAuth calls the identity primitive before any backend is reached<br>An invalid JWT is rejected at the edge<br>A lapsed subscriber is rejected at the edge<br>Spoofed trust headers from the internet are stripped | nothing runs it | ad12f38a crew#263 |
| PROSE | `docs/prose/icm-cp2_identity_primitive.feature`<br>Headless identity primitive | Kratos mints a JWT for a valid login<br>A password reset does not touch application code<br>User metadata and tier live only behind universal protocols | nothing runs it | ad12f38a crew#263 |
| PROSE | `docs/prose/icm-cp3_billing_primitive.feature`<br>Headless billing primitive | A successful payment upgrades the tier in identity<br>A lapsed payment revokes Pro without app code | nothing runs it | ad12f38a crew#263 |
| PROSE | `docs/prose/icm-cp4_prospector_store_headers_only.feature`<br>Prospector Store.Api reads trusted headers only | Store.Api has zero JWT, Stripe SDK or password-hashing references<br>Store.Api authorizes purely from trusted headers<br>A differential replay agrees between the old and new auth paths<br>Store subscriptions work changes coordinate through the storefront sync | nothing runs it | ad12f38a crew#263 |
| PROSE | `docs/prose/icm-cp5_new_startup_rule.feature`<br>New-startup rule | A 50-line Python API gets auth and tiering for free on day 1<br>The new API ships with a demo and an onboarding doc | nothing runs it | ad12f38a crew#263 |
| PROSE | `docs/prose/icm-cp6_nervous_system.feature`<br>The nervous system sees the tier | Gateway spans carry the subscription tier<br>Free-vs-Pro request counts are visible with no app code | nothing runs it | ad12f38a crew#263 |
| PROSE | `docs/prose/langfuse-traces-live.feature`<br>Agent sessions trace to the live Langfuse, tagged with their crew issue | The seed secrets have no human author<br>A tag query returns the session | nothing runs it | #214 crew#297 |
| PROSE | `docs/prose/litellm-up.feature`<br>bin/litellm-up brings the proxy up on the config that is on disk | The proxy is restarted when config.yaml changed after it started<br>The proxy is left alone when it already runs the config on disk | nothing runs it | #220 crew#297 |
| PROSE | `docs/prose/messaging-cp1.feature`<br>the messaging day-0 design is accepted before any code | the specification is the founder's document, saved to the estate standard<br>MiniMax builds only against a scenario, never against prose | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp10.feature`<br>the founder used it | one button on the portal runs the demo | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp2.feature`<br>the broker is on and admitted | JetStream answers from inside the cluster<br>every pod passes admission before it is applied<br>R3 is one values change when the pool has three nodes | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp3.feature`<br>the contracts are gated, not reviewed | a subject that breaks the grammar cannot be published<br>a breaking schema change fails CI (§11 test 7)<br>the first stream exists with the locked values | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp4.feature`<br>the relay is the only writer and it never duplicates | the outbox row and the state change are one transaction (§11 test 1)<br>a relay crash mid-batch leaves no duplicates and no gaps (§11 test 2)<br>the relay is measurable | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp5.feature`<br>a consumer has exactly-once effect | a consumer killed between commit and ack does not repeat its effect (§11 test 4)<br>long work heartbeats and permanent failure terminates<br>the library defaults are the spec's | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp6.feature`<br>permissions make the outbox the only path | an app credential cannot publish a business event (§11 test 6)<br>the relay credential is narrow | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp7.feature`<br>dead letters are captured, alerted and replayed by hand | a poison message lands in DLQ with its headers (§11 test 5)<br>replay is a manual audited action | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp8.feature`<br>the bus is observed in the estate collector, never by scanning files | every messaging workload is seen by the backend (LAW 50)<br>one trace spans request, commit, publish and consumer<br>the seven alerts route to alertmanager | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/messaging-cp9.feature`<br>the platform is accepted, backed up and demoed | the eight acceptance tests are green on the throwaway cluster<br>node loss is survived and two-node loss halts cleanly (§11 test 3)<br>a stream backup restores and replays with correct dedupe (§11 test 8)<br>the demo and the onboarding exist | nothing runs it | #838 crew#639 |
| PROSE | `docs/prose/operating-model-gate-callers.feature`<br>Every active estate repo runs the one operating-model gate | Every active estate repo runs the one gate, with the policy in one place | nothing runs it | #216 crew#297 |
| PROSE | `docs/prose/phone-idea/cp10_urgency.feature`<br>Urgency — open decision, default is never interrupt | An urgent phone message during an active session changes nothing about it<br>An urgent card is flagged, once confirmed | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp11_persistence.feature`<br>Persistence — the platform holds the record, not vendor chat | A draft survives losing the Telegram conversation | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp12_provider_agnostic.feature`<br>Provider agnostic — the model and the transport are both swappable | The flow's logic carries no vendor-specific import | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp1_continuity.feature`<br>Continuity — an active laptop session is never touched by the phone flow | An active session finishes on its own after he leaves<br>A phone message during the session injects nothing into it | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp2_phone_ingress.feature`<br>Phone ingress — hermes-v2 never touches the active laptop session | hermes-v2 and the active session are separate processes | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp3_preflight_dedup.feature`<br>Pre-flight dedup — hermes-v2 checks the board before drafting | A matching card already exists<br>No match exists | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp4_mode_detection.feature`<br>Mode detection — exploring stays talk, building drafts a feature | Exploratory phrasing enters sounding-board mode<br>Build phrasing enters PM mode | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp5_confirmation_gate.feature`<br>Confirmation gate — hermes-v2 never writes to the board unannounced | A draft is shown before any board write<br>The board write matches the button tapped<br>Drop writes nothing | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp6_icebox.feature`<br>Icebox — kept ideas do not enter the work queue | An Icebox card is never claimed | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp7_dispatch.feature`<br>Dispatch — the watched column starts work when the laptop is awake | A card starts within one poll cycle while awake<br>A card waits while the laptop sleeps | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp8_welcome_back.feature`<br>Welcome back — the two branches never conflict | Both branches coexist without conflict | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/phone-idea/cp9_later_activation.feature`<br>Later activation — an Icebox idea becomes work, still gated | Activating an Icebox card drafts a feature file<br>The move still requires the confirmation gate | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/self-aware-platform/cp4_get_catalog_drift.feature`<br>get_catalog_drift — dark matter is visible, on the laptop substrate | A launchd job with no catalog entity is reported as drift<br>A colima container with no catalog entity is reported as drift<br>A port bound outside catalog/ports.yaml is reported as drift<br>Everything cataloged and running is not flagged<br>Drift detection reads typed annotations, never string-greps | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/self-aware-platform/cp5_event_aggregation.feature`<br>Event aggregation behind Temporal — a storm becomes one event | 50 crashes in 10 seconds collapse to one event<br>Events outside the window are not merged<br>Storm test proves the collapse ratio<br>Aggregation does not drop information, only delivery count | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/self-aware-platform/cp6_propose_execute_state_hash.feature`<br>propose_action / execute_action carry a state hash, refuse on TOCTOU | A proposal carries the state hash it was computed against<br>Execute succeeds when state has not changed<br>Execute refuses when state changed between propose and approve — TOCTOU<br>A stale approval never mutates anything | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/self-aware-platform/cp7_token_receipt.feature`<br>Token receipt — the waste the founder named, measured before and after | The script defines three recurring questions<br>Before numbers are measured, not remembered<br>After numbers are measured against the new MCP surface<br>The receipt is the number, not a claim<br>The laptop path renders no key | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/self-aware-platform/cp8_k8s_adapter.feature`<br>k8s adapter — the same desired-vs-actual interface, deferred until a cluster exists | Desired-vs-actual is read through one interface, not two code paths<br>A k8s implementation of the interface can be registered without<br>The adapter is not activated against a live cluster yet | nothing runs it | #202 crew#297 |
| PROSE | `docs/prose/verification-identity-live.feature`<br>The platform verifies its own health every hour with no person logged in | A runner grades the cluster's own verdict on the machine identity<br>Twenty-four hours with nobody awake | nothing runs it | #269 crew#345 |
| BOUND | `features/assurance/estate-next.feature`<br>What is planned, what is blocking, when (crew#403 CP6) | An open checkpoint with no Expect line is a red row<br>An open checkpoint named on a lane's red line is BLOCKING and dated<br>A handoff older than the lane window does not make a checkpoint ACTIVE | `sovereign/tests/bdd/test_gate_estate_next.py` | #402 crew#403 |
| BOUND | `features/assurance/trace-matrix.feature`<br>Every scenario traces to the test that runs it | A feature nothing runs is listed first and counted as a finding<br>A repository where every feature is bound passes the check<br>A feature under docs/prose is counted as PROSE, never as a finding | `sovereign/tests/bdd/test_gate_trace_matrix.py` | #389 crew#495 |
| BOUND | `features/cloud-agnostic/disposable-compute-universal-state.feature`<br>Disposable compute, universal state | No provider-specific service or annotation in the platform | `sovereign/tests/bdd/test_gate_cloud_agnostic.py` | #124 |
| BOUND | `features/fleetview/cp1_contract.feature`<br>CP1 the session contract | the backend lists sessions from the sovereign engine<br>the stream sends a change<br>the route answers the spec's done-command<br>a catalogue that cannot be read leaves the rest of the board live | `sovereign/tests/bdd/test_fleetview_cp1.py` | #2619 |
| BOUND | `features/fleetview/cp2_board.feature`<br>CP2 the board | a running session is on the board<br>the board updates itself | `sovereign/tests/bdd/test_fleetview_cp2.py` | #2619 |
| BOUND | `features/gates/budget-governor.feature`<br>A reasoning run is halted the instant a ceiling trips, and the best-so-far plan survives | A run repeats tool calls past the per-goal branch budget<br>A run finishes its declared plan inside its budget<br>The transcript cannot be read | `sovereign/tests/bdd/test_budget_governor.py` | #3464 |
| BOUND | `features/gates/catalogue-links.feature`<br>Every Component in the catalogue carries a URL a person can open | The clean inventory yields a catalogue where every Component has a URL<br>A checkout with no remote is refused<br>No catalogue on this machine | `sovereign/tests/bdd/test_gate_catalogue_links.py` | #145 crew#269 |
| BOUND | `features/gates/compiled-helm-published.feature`<br>the compiled estate is published and read on the portal | CI compiles the estate on every pull request<br>the compiled document reaches the portal<br>a disagreement between a claim and a rendered value is visible | `sovereign/tests/bdd/test_compiled_helm_published.py` | #3284 |
| BOUND | `features/gates/compiled-helm.feature`<br>the estate is compiled from git before its numbers are believed | a rendered workload declares the resources the estate intends<br>every release renders, so no chart is a blind spot | `sovereign/tests/bdd/test_compiled_helm.py` | #3284 |
| BOUND | `features/gates/contract-executor.feature`<br>A tool call's post-condition is checked by a deterministic script, not the agent | The post-condition never holds by its deadline<br>Both conditions hold<br>A subject has no observation at all<br>The contract file cannot be read | `sovereign/tests/bdd/test_contract_executor.py` | 22268b89 |
| BOUND | `features/gates/epistemic-and-trajectory.feature`<br>An agent cannot lie about what it sees, and cannot forget what it was told to do | An agent claims completed work with no tool call behind it<br>The same claim, with a single reading behind it<br>A claim backed by three independent pieces of evidence<br>A claim backed by one source read three times<br>The transcript cannot be read<br>An agent acts outside the plan it declared<br>An agent stays inside its declared plan<br>An agent begins work with no plan at all<br>A goal burns its budget and the agent is halted<br>The escape hatches are never blocked<br>Four consecutive failed attempts and the context is reset | `sovereign/tests/bdd/test_epistemic_and_trajectory.py` | #3243 |
| BOUND | `features/gates/estate-zone.feature`<br>The estate zone is one configurable value | A platform manifest names the zone as a literal<br>A platform manifest refers to the zone by substitution<br>A pull request adds a line that spells the zone<br>A pull request adds a line that refers to the zone by substitution<br>No cluster declares a zone | `sovereign/tests/bdd/test_gate_estate_zone.py` | #140 |
| BOUND | `features/gates/forge-guard.feature`<br>Forge guard — model training runs are budget-gated and quality-gated | task.yaml carries all required contract fields<br>Shipped task.yaml fits within its own declared budget<br>A run within budget is not refused by the cost gate<br>A run over budget is refused with the spend numbers in the reason<br>An unpriced GPU is refused as un-budgetable<br>A compute plan with no GPU block defaults to T4<br>T4 GPU cost per hour is accurate<br>Dataset split is refused below 500 rows<br>Dataset split at 500 rows is deterministic and 80/20<br>Label probability abstain triggers when margin is below threshold<br>Grade counts only answered rows, not abstentions | `sovereign/tests/bdd/test_forge_guard_bdd.py` | #3703 |
| BOUND | `features/gates/github-app-per-lane.feature`<br>Agents act as a GitHub App, one narrowed token per lane | A lane never gets more than the App holds<br>An unknown lane is refused | `sovereign/tests/bdd/test_gate_github_app.py` | #168 crew#286 |
| BOUND | `features/gates/judge-drift.feature`<br>Judge drift — continuous calibration sentinel detects distribution shift | Jensen-Shannon divergence is zero for identical distributions<br>JS divergence is non-zero for different distributions<br>Partial window does not trigger a drift alert<br>Drift alert fires when JS divergence exceeds threshold<br>JudgeDriftLoop pre_llm always allows (not a blocking gate)<br>JudgeDriftLoop reports shadow mode in health check<br>ParEval loop halts when max_tasks turns are reached<br>ParEval loop allows when turn count is below max_tasks | `sovereign/tests/bdd/test_judge_drift_bdd.py` | #3703 |
| BOUND | `features/gates/langfuse-traces.feature`<br>Agent sessions trace to the estate's Langfuse, tagged with their crew issue | The release renders under the cluster's admission policies | `sovereign/tests/bdd/test_gate_langfuse_render.py` | #162 crew#286 |
| BOUND | `features/gates/llm-admission.feature`<br>The LiteLLM router is admitted by the cluster's policies before it is merged | platform/llm renders clean under the cluster's admission policies<br>The shape that was refused on the cluster is refused before the PR | `sovereign/tests/bdd/test_gate_llm_admission.py` | #253 crew#284 |
| BOUND | `features/gates/model-routing.feature`<br>Model routing is an estate service, not a laptop process | The cluster router carries every hosted model the laptop router carries<br>The router is reachable at llm.<zone> through the one edge<br>Upstream keys reach the pod only from the estate vault<br>The founder picks and adds models in the Admin UI, never by pull request | `sovereign/tests/bdd/test_gate_model_routing_bdd.py` | #225 crew#284 |
| BOUND | `features/gates/orchestrator-routing.feature`<br>Orchestrator proxy routing — all LLM calls route through the estate proxy | Orchestrator uses a configurable base URL, not a hardcoded vendor URL<br>Orchestrator does not import any vendor SDK directly<br>Teleological filter prunes 3 or more consecutive tool errors<br>Teleological filter leaves fewer than 3 consecutive errors untouched<br>Pre-LLM gate halts the agent when any hook signals halt<br>Pre-LLM gate passes through cleanly when no hook halts<br>Circuit breaker trips at the configured turn threshold<br>Orchestrator respects the proxy base URL from environment | `sovereign/tests/bdd/test_orchestrator_routing_bdd.py` | #3703 |
| BOUND | `features/gates/plist-gate.feature`<br>A launchd job that backgrounds children declares AbandonProcessGroup | A launchd job that backgrounds children declares AbandonProcessGroup | `sovereign/tests/bdd/test_gate_plist_gate.py` | #207 crew#297 |
| BOUND | `features/gates/reasoning-gateway.feature`<br>A reasoning step is graded as it happens, not only at the end of the transcript | A step claims verified state with zero evidence gathered so far<br>A step's claim is backed by independent evidence gathered earlier in the same run<br>The transcript cannot be read | `sovereign/tests/bdd/test_reasoning_gateway.py` | #3464 |
| BOUND | `features/gates/shadow-verify.feature`<br>Shadow verify — workload convergence assertion after change applied in vcluster | A ready Deployment with matching replicas passes<br>A Deployment that is not ready fails<br>An observation silent about readiness fails closed<br>A Deployment with fewer available replicas than required fails<br>An observation for an unverified kind fails closed<br>An observation without requiredReplicas fails closed<br>All supported workload kinds are accepted<br>A StatefulSet with probe failures fails | `sovereign/tests/bdd/test_shadow_verify_bdd.py` | #3703 |
| BOUND | `features/gates/typed-multidomain-mutation-ledger.feature`<br>The typed multi-domain mutation ledger | The agent calls propose_mutation with every domain empty<br>The agent proposes code, a manifest and a SQL migration together<br>Every domain in the bundle passes<br>The SQL domain fails while code and the manifest are sound<br>The agent calls seal_mutation before verify_mutation ever passed<br>An unattested bundle attempts to be admitted<br>A validly attested bundle is admitted to a new branch, never main<br>A proposal arrives with no reversibility envelope<br>A proposal carries an inverse that merely echoes the forward action<br>A proposal claims an irreversible exemption with a forged token<br>A proposal carries a deterministic inverse with a state probe<br>The inverse's probe is run against state the forward created | `sovereign/tests/bdd/test_typed_multidomain_mutation_ledger.py` | #3500 |
| BOUND | `features/identity/front-door-oidc-client.feature`<br>the front door's OIDC client lives in the estate's own identity domain (crew#269, crew#281) | the pod names no cloud (R36)<br>the Cloudflare broker is gone and nothing names it (crew#288 CP3) | `sovereign/tests/bdd/test_identity_front_door.py` | #163 crew#269 |
| BOUND | `features/self-aware-platform/cp1_inventory_tool.feature`<br>Inventory tool — one call answers what the estate is | One MCP call replaces the shell-command chain<br>The tool never shells out<br>A stale snapshot is disclosed, not hidden | `sovereign/tests/bdd/test_self_aware_cp1.py` | #74 crew#216 |
| BOUND | `features/self-aware-platform/cp2_get_workload_state.feature`<br>get_workload_state — one fat tool call, catalog plus metrics plus desired state | One call answers "why is X down"<br>The payload never carries raw logs or raw timeseries<br>Payload stays under the byte ceiling regardless of workload size | `sovereign/tests/bdd/test_self_aware_cp2.py` | #74 crew#216 |
| BOUND | `features/self-aware-platform/cp3_get_workload_logs.feature`<br>get_workload_logs — the separate drill-down tool for raw detail | An agent drills into logs only when it asks<br>tail is bounded even when a larger value is requested<br>get_workload_state and get_workload_logs are distinct tools | `sovereign/tests/bdd/test_self_aware_cp3.py` | #74 crew#216 |
| BOUND | `features/sovereign-bus/cp0_temporal_in_cluster.feature`<br>The workflow engine runs in the cluster, not on the Mac | The Temporal row renders and is pinned to the official chart<br>The Mac no longer owns the engine<br>Every object the chart renders passes the pinned policy set | `sovereign/tests/bdd/test_cp0_temporal_in_cluster.py` | #271 crew#396 |
| BOUND | `features/sovereign-bus/cp0b_worker_in_cluster.feature`<br>The sovereign worker runs in the cluster next to the engine | The worker image is on the estate's one image list<br>The worker Deployment polls the in-cluster frontend and rolls on every main build<br>The worker reports ready only while it is polling | `sovereign/tests/bdd/test_cp0b_worker_in_cluster.py` | #271 crew#396 |
| BOUND | `features/sovereign-bus/cp0c_kini_finish_workflow.feature`<br>The KINI checkpoints finish as one durable workflow | Every checkpoint verdict comes from pytest's exit, and the unknown branch is never silent<br>A checkpoint that flakes is retried and a platform fault is healed, not failed<br>The workflow and both activities are on the worker the cluster runs | `sovereign/tests/bdd/test_cp0c_kini_finish_workflow.py` | #277 crew#396 |
| BOUND | `features/sovereign-bus/cp0d_kini_finish_trigger.feature`<br>CP0d "Finish KINI" is one word, and the answer is a receipt (crew#396 step 4) | the receipt head is total and the reader grades every branch both ways<br>the trigger is wired end to end in git | `sovereign/tests/bdd/test_cp0d_kini_finish_trigger.py` | #280 crew#396 |
| BOUND | `features/sovereign-bus/cp23_presence_ghost.feature`<br>Presence — Ghost is the default; the chat is the fire alarm, not the control room | Routine execution sends nothing<br>A catastrophe is exactly one message<br>The daily digest is six lines, signed | `sovereign/tests/bdd/test_cp23.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp24_receipts_and_undo.feature`<br>Zero-noise receipts — one line, a hash, a budget delta, and undo | Every step that writes emits one receipt line<br>Undo reverts to the receipt's hash | `sovereign/tests/bdd/test_cp24.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp25_vision_to_repo.feature`<br>Vision to repo — a photo becomes a committed markdown file and one receipt | Photo with caption is committed silently<br>Model is configuration | `sovereign/tests/bdd/test_cp25.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp26_predictive_preauth.feature`<br>Predictive pre-authorization — approve futures, not events | A predicted budget boundary asks once, ahead of time<br>Shadow-founder auto-approves only inside policy and above confidence<br>Destructive ops are never auto-authorized | `sovereign/tests/bdd/test_cp26.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp27_temporal_branching.feature`<br>Temporal branching — fork silently, merge the winner, keep the losers | A decision point forks three silent branches<br>Stop during branches freezes all of them<br>A branch is capped at 10 percent of the parent budget and halts with a receipt | `sovereign/tests/bdd/test_cp27.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp28_distillation.feature`<br>Auto-distillation — every frontier success is a training row; routing follows measured accuracy | A successful frontier step lands in the dataset<br>Routing flips on measurement, not on hope | `sovereign/tests/bdd/test_cp28.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp29_trust_boundary.feature`<br>Trust boundary — the founder is the root certificate | A destructive approval requires a hardware signature<br>Replay is rejected<br>Degraded mode is logged, not silent | `sovereign/tests/bdd/test_cp29.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp2_litellm_real.feature`<br>LiteLLM is real | The kernel finds the router in the estate secret store<br>The live router serves every consensus voter<br>A destructive op reaches quorum through the live router | `sovereign/tests/bdd/test_cp2_litellm_real.py` | #179 crew#284 |
| BOUND | `features/sovereign-bus/cp31_fsm_budget.feature`<br>Finite state machine and budget enforcer | Budget exhaustion halts, state is kept, refill resumes<br>Cycle detection<br>Blind execution is refused<br>Concurrent spend cannot overdraw | `sovereign/tests/bdd/conftest.py`<br>`sovereign/tests/bdd/test_cp31.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp32_surfaces.feature`<br>Surfaces — Haptic, Spatial, Converse and Voice | Spatial graph reflects Temporal truth<br>Haptic patterns map to states<br>Siri status is read from the kernel<br>The system never opens Converse | `sovereign/tests/bdd/test_cp32.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp33_rewind.feature`<br>Atomic time-travel rollback — one command rewinds code, DB, policy and config to any hash | Rewind to a previous root | `sovereign/tests/bdd/test_cp33.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp34_audit_verify.feature`<br>Cryptographic auditing — SOC2 by default; the first feature delivered | An auditor verifies from a hash | `sovereign/tests/bdd/test_cp34.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp35_recover.feature`<br>Recover the last estate checkpoint — crash equals rebuild, never data loss | Recover after a crash | `sovereign/tests/bdd/test_cp35.py` | #70 crew#213 |
| BOUND | `features/sovereign-bus/cp36_telegram_kernel.feature`<br>cp36 Telegram is a receipt channel, not a chat (spec 2.2, 2.3; crew#284 CP1) | A photo with a caption becomes one DOC_COMMIT line and never reaches the model<br>A photo without a caption is dispatched normally<br>undo from the phone replies with the undo receipt line, not prose<br>stop from the phone replies with the session's newest receipt | `sovereign/tests/bdd/test_cp36.py` | #157 crew#284 |
| BOUND | `features/sovereign-bus/cp37_spatial_cockpit.feature`<br>The cockpit page is the presence model, not a log dump | Page in Ghost renders no session text and no inbox lines<br>Presence red renders the red dot and one emergency line | `sovereign/tests/bdd/test_cp37.py` | #158 crew#284 |
| BOUND | `features/stale_pr.feature`<br>a pull request with no live owner does not sit in the queue | a pull request idle for a day is closed<br>the close message names both ways back<br>activity or keep-open cancels the clock<br>issues are never touched | `sovereign/tests/bdd/test_stale_pr.py` | #194 crew#299 |
| BOUND | `features/twin/estate-twin.feature`<br>The estate twin knows what the estate actually is | A dead agent is visible without anybody running a command<br>A deployment scaled to zero is dead, not absent<br>A capability stranded on a branch is visible<br>A branch that only edits existing files is not stranded<br>A stopped emitter is visible as stale, not as healthy<br>Replaying the same events changes nothing<br>The twin adds no second store, bus or server | `sovereign/tests/bdd/test_estate_twin.py` | #3364 |
| BOUND | `sovereign/tests/fixtures/bdd/harness_selftest.feature`<br>The acceptance harness itself | The temporary estate is real, isolated and configured<br>The clock only moves when a step moves it<br>A budget cannot be overdrawn by two concurrent spends<br>An unsigned refill is refused<br>Ghost silence is observable<br>The scratch repository is a real git repository | `sovereign/tests/bdd/test_harness.py` | d1ea142d crew#219 |
| BOUND | `sovereign/tests/fixtures/bdd/unbound/unbound_step.feature`<br>A feature whose steps are not all bound | One step is bound and one is not | `sovereign/tests/fixtures/bdd/unbound/test_unbound.py` | d1ea142d crew#219 |
