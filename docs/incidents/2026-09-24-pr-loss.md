# Incident: 12 PRs closed without merge (2026-09-24)

## Timeline
- 09:36 → 15:55: `chidionyema` manually closed 12 PRs without merging, scattered across the day
- All closures confirmed via GitHub Events API: `actor: chidionyema`, `action: closed`
- No `merged: true` on any of the 12

## What was lost
| PR | Title | Actually lost? |
|----|-------|---------------|
| 3938 | llm: spire-key-broker | ✅ server.go, Dockerfile, Helm chart, docs |
| 3935 | llm: vault OCID, spire-key-broker | ✅ server.go, Dockerfile, Helm chart, config.local.yaml, docs |
| 3934 | rescued_patches2 deletion | ❌ intentionally orphaned (rescued_patches2 exists on main) |
| 3933 | llm: vault-ocid | ❌ platform/llm/litellm.yaml exists in main |
| 3931 | fix: postgres callbacks | ❌ main's config.yaml is newer |
| 3930 | feat: TTS pipeline | ❌ bin/voice-converse etc. exist in main |
| 3929 | feat: catalog-gen + ci | ❌ files exist in main (newer versions) |
| 3927 | feat: consolidate | ❌ files exist in main (newer versions) |
| 3939/3940 | syntax fix attempts | ❌ superseded by #3942 (already merged) |
| 3936 | fleetview device routes | ❌ staged locally, recovered |

**Genuinely missing from main before today**: `platform/llm/spire-key-broker/server.go`, `Dockerfile`, Helm chart, `config.local.yaml`, two docs. All recovered into main 2026-09-24.

## Root cause
Manual action by `chidionyema` — not merge-when-green, not an agent. The closures were scattered (09:40 → 15:55) across multiple sessions.

## Recovery
1. All 12 branches rescued via `git fetch origin refs/pull/<N>/head:rescue/pr-<N>`
2. Missing spire-key-broker files cherry-picked into main via PR #3947
3. Fleetview routes already staged locally

## Prevention (done 2026-09-24)
1. **`no-branch-loss` ruleset** (id 23949362): `deletion` rule on all branches except `main` and `agent-trunk`. Blocks branch deletion at the platform level. No bypass actor.
2. **`merge-when-green` workflow**: disabled. Founder re-enables when ready.
3. **`shadow-verify` removed from `platform/github/ruleset.idp.required-checks.json`**: the workflow was deleted from main but still listed as required, which would have blocked future PRs from merging.
4. **`AGENTS.md` rule**: "no `.github/workflows/` files without founder approval" — prevents agents from adding workflow files that add ghost CI checks.
5. **`estate-bootstrap-preflight.yml` + `shadow-verify.yml` deleted from main** — stale workflows causing ghost checks on PRs.

## What remains
- `merge-when-green` is disabled — no auto-merge until founder re-enables it
- 5 local branches with stale work (`fix/syntax-gates`, `llm/converged`, etc.) — these are untracked, no remote impact
- The `estate-idp-agent-trunk` ruleset has no rules — it protects the branch but enforces nothing
