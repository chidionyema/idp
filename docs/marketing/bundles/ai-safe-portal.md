# AI-Safe Portal

> The bundle for any company rolling out AI to production: every call
> traced, every action governed, every $ capped, every voice reviewed.
> Shipped in week 4, audited on a clock.

## The story the buyer tells their board

> "We shipped AI to production in week 4. Every model call is traced.
> Every action an agent takes is governed. Every dollar is capped. Every
> output is reviewed for our house voice. The auditor got one log file.
> The CFO got one dashboard."

That is the AI-Safe Portal. Five products, one bundle, one story.

## What you get

The AI-Safe Portal is the Platform + Voice Gate + MCP Gateway + LLM
Gateway + Spend Breaker, configured as a single deployment. Every
component is a product on its own; together, they are the answer a
buyer's board wants to hear when AI is on the agenda.

| Component | What it does | Why it is in the bundle |
|---|---|---|
| **The Platform** | The IDP substrate: catalogue, identity, edge, secrets, scheduling, supply chain, policy | The audit log is the platform's, not the vendor's |
| **Voice Gate** | A deterministic prose linter that enforces your house voice on every commit | Every AI output is reviewed before it lands |
| **MCP Gateway** | One policed door for every tool an agent can reach | Every agent action is governed, identity-aware, audited |
| **LLM Gateway** | A model router with per-lane budgets and a spend breaker | Every $ is capped, every call is traced, the breaker is on the proxy |

## What the buyer sees

- **One console.** The portal is the console. The auditor opens the
  portal; the trace is on the page they already know.
- **One dashboard.** The spend is on the dashboard. The CFO opens the
  dashboard; the cost is on the row they already read.
- **One gate.** Every model call goes through one gate. The security
  team reads the gate's policy bundle; the gate is the policy they
  already signed.
- **One voice.** Every output is graded by Voice Gate before it lands.
  The marketing team owns the policy; the policy is in git.

## How it works

The AI-Safe Portal is the install wedge with five products enabled.
The buyer runs `idp/quickstart`, the installer enables Voice Gate, the
MCP Gateway, and the LLM Gateway. The drill runs hourly. The receipts
land in the collector.

The buyer's first model call is on the LLM gateway. The first agent
action is on the MCP gateway. The first commit is graded by Voice
Gate. The first spend is on the dashboard. The first audit log is on
the collector.

## Why us

- **The bundle is the story.** Five products, one story, one buyer.
  Most vendors sell five products and call them a platform; we sell
  one story and back it with five products. *Benefit: the buyer is
  buying an outcome, not a stack.*

- **The drill is the SLO.** Every component has a drill on a clock.
  *Benefit: the SLO is a number a buyer can read.*

- **The dogfood chain.** The estate runs the bundle itself; the
  receipts are in the collector. *Benefit: a buyer can read the case
  study, not just the slide.*

## Pricing

| Tier | Price | What you get |
|---|---|---|
| Solo | $300/month | The install wedge, Voice Gate, the LLM Gateway, up to 1M tokens, the audit log |
| Team | $3,000/month, up to 100M tokens | Multi-tenant, the MCP Gateway, the spend breaker, Slack alerts, the drill |
| Enterprise | Contact us | Unlimited tokens, SOC 2 conversation, custom policy, dedicated support |

The Enterprise tier is required for any tenant with regulated
workloads.

## Get started

- **Run the install wedge.** `idp/quickstart` brings up the bundle in
  30 minutes. The drill runs hourly.
- **Read the showcase.** `/showcase` renders the live estate bar, the
  per-system health donuts, the buyer sandbox launch button. The
  bundle is the page they already know.
- **See it in action.** The estate runs the bundle; the receipts are
  in the collector.
- **Call us.** For the Enterprise tier, for a custom policy, or for a
  procurement-grade security one-pager.

The AI-Safe Portal is the bundle. The story is the bundle. The buyer
is the bundle.
