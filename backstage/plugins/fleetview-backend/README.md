# fleetview-backend

FleetView voice + sessions backend plugin for Backstage.

Exposes `/voice/*` routes (say, hear, stream, log, select) and `/api/fleetview/*` routes
(sessions, stream, notes, nudge, signals, mutations).

Install: `pip install -e backstage/plugins/fleetview-backend/`
