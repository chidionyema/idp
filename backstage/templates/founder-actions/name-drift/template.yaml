# GENERATED by bin/idp-portal-buttons from .github/workflows/name-drift.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-name-drift
  title: Check every repository for a hardcoded estate name
  description: Reads the main branch of every repository the estate App can see and refuses if any file
    spells the estate's domain instead of ${ESTATE_ZONE}. A red run is the drift alarm on the Ops dashboard.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Start Check every repository for a hardcoded estate name
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: name-drift.yml
      branchOrTagName: main
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/name-drift.yml
