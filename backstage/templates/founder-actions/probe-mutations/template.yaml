# GENERATED by bin/idp-portal-buttons from .github/workflows/probe-mutations.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-probe-mutations
  title: 'Run: probe-mutations'
  description: 'crew#631 CP8: weekly, every probe is run against a broken target (auth off, database down,
    an ingest that drops, wrong digest). A probe that passes a broken door is quarantined and the run
    is red. Then the last 30 real verdicts grade each assertion''s graduation: UNPROVEN until it has one
    real FAIL and one real PASS. No cluster, no vault: the doors are fakes with one thing wrong.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch probe-mutations.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: probe-mutations.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of probe-mutations
      url: https://github.com/chidionyema/idp/actions/workflows/probe-mutations.yml
