# GENERATED by bin/idp-portal-buttons from .github/workflows/pr-ownership-watch.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-pr-ownership-watch
  title: Grade every in-flight PR ownership receipt against GitHub
  description: 'Founder 2026-09-17: "we push a PR and move on, no accountability. we spray and pray."'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      reason:
        title: reason
        type: string
        default: hourly PR ownership state refresh
  steps:
  - id: dispatch
    name: Start Grade every in-flight PR ownership receipt against GitHub
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: pr-ownership-watch.yml
      branchOrTagName: main
      workflowInputs:
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/pr-ownership-watch.yml
