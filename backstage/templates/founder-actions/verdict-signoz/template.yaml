# GENERATED by bin/idp-portal-buttons from .github/workflows/verdict-signoz.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-verdict-signoz
  title: 'Run: verdict-signoz'
  description: 'crew#631 CP9: the prover for SigNoz, the third surface. Same machine identity as verdict-langfuse
    (GitHub OIDC -> OCI session token), so the service-account key, the drill password, the cluster read
    and the signing key are reachable here and nowhere an agent runs. L1/L2/NEGATIVE go through the edge''s
    signoz-api route with the key bin/idp-signoz-key minted; L3 is the login drill''s walk to signoz.<zone>.
    SigNoz community has no SSO (crew#718 CP2): until it does the verdict is BLOCKED with that reason
    and the check-run neutral, never green over a login screen.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run verdict-signoz
    properties:
      nonce:
        title: nonce
        type: string
        description: 'nonce issued by the control plane for this request (blank: the run mints one)'
        default: ''
  steps:
  - id: dispatch
    name: Dispatch verdict-signoz.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: verdict-signoz.yml
      branchOrTagName: main
      workflowInputs:
        nonce: ${{ parameters.nonce }}
  output:
    links:
    - title: Runs of verdict-signoz
      url: https://github.com/chidionyema/idp/actions/workflows/verdict-signoz.yml
