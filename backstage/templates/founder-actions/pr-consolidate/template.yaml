# GENERATED by bin/idp-portal-buttons from .github/workflows/pr-consolidate.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-pr-consolidate
  title: Consolidate the repo's open non-bot PRs hourly, delete the originals
  description: "Founder 2026-09-17: \"we should not even get here \u2014 limit no of open PRs 5 maximum\"\
    \ and"
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      max_open:
        title: Repo-wide open-PR cap that triggers consolidation
        type: string
        default: '5'
      dry_run:
        title: List what would be consolidated; do not act
        type: boolean
        default: false
      reason:
        title: Why (one line, kept in the run record)
        type: string
        default: hourly repo-wide PR sprawl cap enforcement
  steps:
  - id: dispatch
    name: Start Consolidate the repo's open non-bot PRs hourly, delete the originals
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: pr-consolidate.yml
      branchOrTagName: main
      workflowInputs:
        max_open: ${{ parameters.max_open }}
        dry_run: ${{ parameters.dry_run }}
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/pr-consolidate.yml
