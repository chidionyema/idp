# GENERATED by bin/idp-portal-buttons from .github/workflows/deploy-when-green.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-deploy-when-green
  title: Ship the portal and the shop
  description: Merges the pull request that carries a new image tag once its checks are green and nothing
    but the tag changed.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Start Ship the portal and the shop
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: deploy-when-green.yml
      branchOrTagName: main
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/deploy-when-green.yml
