# GENERATED by bin/idp-portal-buttons from .github/workflows/estate-secret-prune.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-estate-secret-prune
  title: List and prune old Kubernetes secrets to escape OKE's 2000 secret cap
  description: Cluster over Oracle's 2000-secret leak protection stops every ExternalSecret webhook and
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      dry_run:
        title: 'true: list only, do not delete'
        type: boolean
        default: true
      prune_helm_history:
        title: 'true: delete old sh.helm.release.v1.* secrets past helm_keep'
        type: boolean
        default: false
      helm_keep:
        title: How many recent revisions per Helm release to keep
        type: string
        default: '3'
      reason:
        title: Why (one line, kept in the run record)
        type: string
        default: OKE resource leak protection tripped at 2650 secrets
  steps:
  - id: dispatch
    name: Start List and prune old Kubernetes secrets to escape OKE's 2000 secret cap
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: estate-secret-prune.yml
      branchOrTagName: main
      workflowInputs:
        dry_run: ${{ parameters.dry_run }}
        prune_helm_history: ${{ parameters.prune_helm_history }}
        helm_keep: ${{ parameters.helm_keep }}
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/estate-secret-prune.yml
