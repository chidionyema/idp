# GENERATED by bin/idp-portal-buttons from .github/workflows/conscience.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-conscience
  title: Grade the estate against the founder's rules
  description: Reads the standing rules and grades the main branch against them, once an hour; keeps one
    running score.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      founder_line:
        title: Send the founder's Telegram line from this run too
        type: boolean
        default: false
  steps:
  - id: dispatch
    name: Start Grade the estate against the founder's rules
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: conscience.yml
      branchOrTagName: main
      workflowInputs:
        founder_line: ${{ parameters.founder_line }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/conscience.yml
