# GENERATED by bin/idp-portal-buttons from .github/workflows/forge-collect-ci-runs.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-forge-collect-ci-runs
  title: Collect the CI flake-triage dataset
  description: Reads this repo's failed and green workflow runs on GitHub's machines and commits the
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      since:
        title: first day of runs to read (YYYY-MM-DD)
        type: string
        default: '2026-08-01'
      until:
        title: last day (YYYY-MM-DD, default today)
        type: string
        default: ''
      top_up:
        title: keep the committed rows and add flakes until real:flake is 9:1 or better
        type: boolean
        default: true
      branch:
        title: branch the dataset commit is pushed to; a pull request to main is opened or refreshed
        type: string
        default: forge/dataset-ci-flake-triage
  steps:
  - id: dispatch
    name: Start Collect the CI flake-triage dataset
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: forge-collect-ci-runs.yml
      branchOrTagName: main
      workflowInputs:
        since: ${{ parameters.since }}
        until: ${{ parameters.until }}
        top_up: ${{ parameters.top_up }}
        branch: ${{ parameters.branch }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/forge-collect-ci-runs.yml
