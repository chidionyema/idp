# GENERATED by bin/idp-portal-buttons from .github/workflows/ticket-verification.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-ticket-verification
  title: Refresh ticket states
  description: Moves each ticket's label to match what the checks proved since the last run.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      apply:
        title: apply (move labels) or report only
        type: boolean
        default: true
  steps:
  - id: dispatch
    name: Start Refresh ticket states
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: ticket-verification.yml
      branchOrTagName: main
      workflowInputs:
        apply: ${{ parameters.apply }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/ticket-verification.yml
