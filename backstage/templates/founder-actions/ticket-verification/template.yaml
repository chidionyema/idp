# GENERATED by bin/idp-portal-buttons from .github/workflows/ticket-verification.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-ticket-verification
  title: 'Run: ticket-verification'
  description: 'crew#631 CP5: the ticket state machine. Runs after every prover run and hourly: a ticket
    labelled RESOLVED_PENDING_VERIFICATION moves to VERIFIED or REJECTED only here, on the prover''s verdict,
    as the estate''s GitHub App; a VERIFIED label set by anyone else, or one with no PASS verdict younger
    than 24 h behind it, is reverted. The App token is minted from the vault (lane ticket-verifier: issues
    write, actions read), which is why this needs the OCI session and lives in idp.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run ticket-verification
    properties:
      apply:
        title: apply
        type: boolean
        description: apply (move labels) or report only
        default: true
  steps:
  - id: dispatch
    name: Dispatch ticket-verification.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: ticket-verification.yml
      branchOrTagName: main
      workflowInputs:
        apply: ${{ parameters.apply }}
  output:
    links:
    - title: Runs of ticket-verification
      url: https://github.com/chidionyema/idp/actions/workflows/ticket-verification.yml
