# GENERATED by bin/idp-portal-buttons from .github/workflows/portability-drill.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-portability-drill
  title: 'Run: portability-drill'
  description: 'crew#488 CP1: the hydration scenario of docs/prose/cloud-agnostic-drills.feature, run
    for real.  A vanilla k3d cluster on a runner with no OCI identity, Flux installed from its own CLI,
    one GitRepository at this run''s commit, every Kustomization under clusters/oke/ applied. Then bin/idp-portability-drill
    counts how many layers came Ready and grades it against drills/portability-floor.txt. Layers that
    need OCI (the vault behind secret-store, DNS, the receipts bucket) are expected to stay red; the drill
    prints each with Flux''s own reason, and the count of everything else is the portability numbe'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch portability-drill.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: portability-drill.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of portability-drill
      url: https://github.com/chidionyema/idp/actions/workflows/portability-drill.yml
