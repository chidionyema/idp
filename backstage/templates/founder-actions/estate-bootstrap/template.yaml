# GENERATED by bin/idp-portal-buttons from .github/workflows/estate-bootstrap.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-estate-bootstrap
  title: Run the estate bootstrap (cloud-side, OIDC, no laptop)
  description: 'Same identity path as vault-reads.yml (crew#584, gtrevorrow/oci-token-exchange-action@v2):'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      scope:
        title: estate-seed (OCI half, cloud-side) or a vendor scope (laptop runbook path)
        type: string
        enum:
        - estate-seed
        - tailscale
        - cloudflare
        - vendors
        default: estate-seed
      reason:
        title: One-line reason (drift, fresh bring-up, region copy, key rotation)
        type: string
      mode:
        title: preflight (read-only) or live (writes the vault)
        type: string
        enum:
        - preflight
        - live
        default: preflight
    required:
    - reason
  steps:
  - id: dispatch
    name: Start Run the estate bootstrap (cloud-side, OIDC, no laptop)
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: estate-bootstrap.yml
      branchOrTagName: main
      workflowInputs:
        scope: ${{ parameters.scope }}
        reason: ${{ parameters.reason }}
        mode: ${{ parameters.mode }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/estate-bootstrap.yml
