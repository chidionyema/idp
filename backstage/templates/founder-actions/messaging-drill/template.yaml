# GENERATED by bin/idp-portal-buttons from .github/workflows/messaging-drill.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-messaging-drill
  title: 'Run: messaging-drill'
  description: 'Daily drill: the messaging demos (crew#639 CP9) run end to end on a runner with no cluster
    and no Docker, against the chart''s nats-server version and an embedded Postgres. Catalogued in drills/catalogue.yaml
    as messaging-demo; the drills row of bin/idp-verify grades freshness.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch messaging-drill.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: messaging-drill.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of messaging-drill
      url: https://github.com/chidionyema/idp/actions/workflows/messaging-drill.yml
