# GENERATED by bin/idp-portal-buttons from .github/workflows/concierge-deploy.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-concierge-deploy
  title: Deploy the concierge runtime
  description: Builds the vault sidecar and fetches the voice model, then deploys the concierge's ephemeral
    runtime to Modal and proves the sidecar is in the path; a dry run stops before deploy.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      dry_run:
        title: build and verify, deploy nothing
        type: boolean
        default: true
  steps:
  - id: dispatch
    name: Start Deploy the concierge runtime
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: concierge-deploy.yml
      branchOrTagName: main
      workflowInputs:
        dry_run: ${{ parameters.dry_run }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/concierge-deploy.yml
