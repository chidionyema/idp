# GENERATED by bin/idp-portal-buttons from .github/workflows/models-drill.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-models-drill
  title: Check the models still work
  description: Proves every model in the estate still answers, and says plainly what to do about any that
    does not. Runs on its own several times a day.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Start Check the models still work
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: models-drill.yml
      branchOrTagName: main
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/models-drill.yml
