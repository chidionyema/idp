# GENERATED by bin/idp-portal-buttons from .github/workflows/otto-lifeboat.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-otto-lifeboat
  title: Put Otto's lifeboat back in the water
  description: Rebuilds Otto's second home on Cloudflare's edge and reports whether it can answer, so
    that a turn still lands when the cluster is gone.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      reason:
        title: Why (one line, kept in the run record)
        type: string
        default: home 2 back in the water
  steps:
  - id: dispatch
    name: Start Put Otto's lifeboat back in the water
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: otto-lifeboat.yml
      branchOrTagName: main
      workflowInputs:
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/otto-lifeboat.yml
