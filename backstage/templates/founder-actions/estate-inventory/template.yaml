# GENERATED by bin/idp-portal-buttons from .github/workflows/estate-inventory.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-estate-inventory
  title: Inventory the estate from its control planes
  description: 'Reads what every cloud, the cluster, GitHub, Cloudflare and Tailscale actually run and
    grades each thing against what git declares: managed, drifted, orphan or ghost.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Start Inventory the estate from its control planes
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: estate-inventory.yml
      branchOrTagName: main
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/estate-inventory.yml
