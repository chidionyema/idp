# GENERATED by bin/idp-portal-buttons from .github/workflows/branch-archive.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-branch-archive
  title: Archive stale branches
  description: 'Every branch that is finished, or untouched for two weeks with no pull request, becomes
    a tag and leaves the branch list. Nothing is lost: the tag holds the tip and one command brings the
    branch back.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      mode:
        title: preview (the plan on the run summary, nothing changes) or apply; empty applies, as the
          clock does
        type: string
        default: ''
  steps:
  - id: dispatch
    name: Start Archive stale branches
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: branch-archive.yml
      branchOrTagName: main
      workflowInputs:
        mode: ${{ parameters.mode }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/branch-archive.yml
