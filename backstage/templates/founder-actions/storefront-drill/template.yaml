# GENERATED by bin/idp-portal-buttons from .github/workflows/storefront-drill.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-storefront-drill
  title: 'Run: storefront-drill'
  description: 'Opens the shop the way a stranger does and checks it is serving what the last merge built.
    Every other drill here grades the platform -- a login, a cluster, a collector. None of them opened
    the shop, so on 2026-08-30 the storefront served an eight-hour-old build with no pack pictures while
    the login drill, flux and the front door all reported green, and the founder found it by looking at
    the site. This holds no credential: it grades what a visitor gets. Refs crew#693.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch storefront-drill.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: storefront-drill.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of storefront-drill
      url: https://github.com/chidionyema/idp/actions/workflows/storefront-drill.yml
