# GENERATED by bin/idp-portal-buttons from .github/workflows/webhook-diagnose.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-webhook-diagnose
  title: Diagnose why external-secrets-webhook is stalling Flux
  description: Read-only surgical inspection. Dumps deploy state, pods, events, logs, and probes the
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      namespace:
        title: Namespace holding the webhook deployment
        type: string
        default: external-secrets
      deployment:
        title: Deployment name for the webhook
        type: string
        default: external-secrets-webhook
      reason:
        title: Why (one line, kept in the run record)
        type: string
        default: 53 Flux Kustomizations NotReady; webhook validate deadline exceeded
  steps:
  - id: dispatch
    name: Start Diagnose why external-secrets-webhook is stalling Flux
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: webhook-diagnose.yml
      branchOrTagName: main
      workflowInputs:
        namespace: ${{ parameters.namespace }}
        deployment: ${{ parameters.deployment }}
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/webhook-diagnose.yml
