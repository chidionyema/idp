# GENERATED by bin/idp-portal-buttons from .github/workflows/verdict-langfuse.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-verdict-langfuse
  title: Check Langfuse and sign the result
  description: Checks that Langfuse answers, answers its key, refuses a caller without one, and that a
    signed-in person reaches it; signs the result.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      nonce:
        title: request code for this run (leave blank; the run makes one)
        type: string
        default: ''
      lookup_trace:
        title: a trace id from an earlier run, read again now (shown, not checked)
        type: string
        default: ''
  steps:
  - id: dispatch
    name: Start Check Langfuse and sign the result
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: verdict-langfuse.yml
      branchOrTagName: main
      workflowInputs:
        nonce: ${{ parameters.nonce }}
        lookup_trace: ${{ parameters.lookup_trace }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/verdict-langfuse.yml
