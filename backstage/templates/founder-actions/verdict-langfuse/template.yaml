# GENERATED by bin/idp-portal-buttons from .github/workflows/verdict-langfuse.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-verdict-langfuse
  title: 'Run: verdict-langfuse'
  description: 'crew#631 CP1: the prover for Langfuse. Runs on the estate''s machine identity (the same
    GitHub OIDC -> OCI session token exchange as login-drill.yml), so the API key, the drill password,
    the cluster read and the signing key are all reachable here and nowhere an agent runs. The output
    is one signed verdict file, kept as the run''s artifact, and a check-run `verify/langfuse` on the
    commit under test, whose conclusion is the verdict''s outcome. An agent token cannot create a check-run
    and does not hold the key, so the only way to a green check is a Langfuse that works.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run verdict-langfuse
    properties:
      nonce:
        title: nonce
        type: string
        description: 'nonce issued by the control plane for this request (blank: the run mints one)'
        default: ''
      lookup_trace:
        title: lookup_trace
        type: string
        description: 'attribution: a trace id from an earlier run, read again now (info line, not graded)'
        default: ''
  steps:
  - id: dispatch
    name: Dispatch verdict-langfuse.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: verdict-langfuse.yml
      branchOrTagName: main
      workflowInputs:
        nonce: ${{ parameters.nonce }}
        lookup_trace: ${{ parameters.lookup_trace }}
  output:
    links:
    - title: Runs of verdict-langfuse
      url: https://github.com/chidionyema/idp/actions/workflows/verdict-langfuse.yml
