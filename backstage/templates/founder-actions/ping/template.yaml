# GENERATED by bin/idp-portal-buttons from .github/workflows/ping.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-ping
  title: Ping the dead man's switch
  description: Sends the heartbeat that proves the estate's monitoring is alive, from outside the cluster.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Start Ping the dead man's switch
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: ping.yml
      branchOrTagName: main
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/ping.yml
