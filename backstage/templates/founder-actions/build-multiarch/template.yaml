# GENERATED by bin/idp-portal-buttons from .github/workflows/build-multiarch.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-build-multiarch
  title: Build every container image
  description: Builds every container image in this repository for both kinds of machine (Intel and ARM)
    and publishes them.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      since:
        title: 'build the images changed since this commit (empty: every image)'
        type: string
        default: ''
  steps:
  - id: dispatch
    name: Start Build every container image
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: build-multiarch.yml
      branchOrTagName: main
      workflowInputs:
        since: ${{ parameters.since }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/build-multiarch.yml
