# GENERATED by bin/idp-portal-buttons from .github/workflows/build-multiarch.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-build-multiarch
  title: 'Run: build-multiarch'
  description: Dispatches .github/workflows/build-multiarch.yml on main.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run build-multiarch
    properties:
      since:
        title: since
        type: string
        description: 'build the images changed since this commit (empty: every image)'
        default: ''
  steps:
  - id: dispatch
    name: Dispatch build-multiarch.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: build-multiarch.yml
      branchOrTagName: main
      workflowInputs:
        since: ${{ parameters.since }}
  output:
    links:
    - title: Runs of build-multiarch
      url: https://github.com/chidionyema/idp/actions/workflows/build-multiarch.yml
