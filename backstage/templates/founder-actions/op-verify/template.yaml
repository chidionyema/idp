# GENERATED by bin/idp-portal-buttons from .github/workflows/op-verify.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-op-verify
  title: Prove which harnesses are actually running in the cluster
  description: One dispatch, one matrix. For every named harness the estate claims to have shipped, this
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      reason:
        title: Why (one line, kept in the run record)
        type: string
        default: prove every named harness is operational
  steps:
  - id: dispatch
    name: Start Prove which harnesses are actually running in the cluster
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: op-verify.yml
      branchOrTagName: main
      workflowInputs:
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/op-verify.yml
