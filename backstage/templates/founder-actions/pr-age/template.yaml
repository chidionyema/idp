# GENERATED by bin/idp-portal-buttons from .github/workflows/pr-age.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-pr-age
  title: 'Run: pr-age'
  description: 'crew#607 CP2/CP3: a green PR merges itself; a PR over four hours is told why on its own
    thread. Founder, 2026-08-28: "why do we have open PRs not merged" / "4 maximum". Hourly so no PR waits
    on a peer noticing; the job is red exactly when bin/idp-pr-age is (any row over PR_AGE_MAX_H).'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch pr-age.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: pr-age.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of pr-age
      url: https://github.com/chidionyema/idp/actions/workflows/pr-age.yml
