# GENERATED by bin/idp-portal-buttons from .github/workflows/matrix-review.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-matrix-review
  title: Review a pull request against the matrix
  description: Checks a pull request against the estate's review matrix and posts the result on its thread.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Start Review a pull request against the matrix
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: matrix-review.yml
      branchOrTagName: main
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/matrix-review.yml
