# GENERATED by bin/idp-portal-buttons from .github/workflows/mac-salvage.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-mac-salvage
  title: Salvage uncommitted work from the founder's Mac
  description: When the screen is dead, this workflow reaches the Mac over the tailnet through the
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      probe_only:
        title: Only probe hostname/whoami/uptime; do not commit or push anything
        type: boolean
        default: false
      target_dirs:
        title: Space-separated repo dirs on the Mac to salvage
        type: string
        default: ~/dev/code/idp ~/dev/code/hermes-v2
      branch_prefix:
        title: Branch name prefix for the WIP commit
        type: string
        default: salvage
      reason:
        title: Why (one line, kept in the run record)
        type: string
        default: broken screen; salvage uncommitted work
  steps:
  - id: dispatch
    name: Start Salvage uncommitted work from the founder's Mac
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: mac-salvage.yml
      branchOrTagName: main
      workflowInputs:
        probe_only: ${{ parameters.probe_only }}
        target_dirs: ${{ parameters.target_dirs }}
        branch_prefix: ${{ parameters.branch_prefix }}
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/mac-salvage.yml
