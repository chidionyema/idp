# GENERATED by bin/idp-portal-buttons from .github/workflows/webhook-diagnose-v2.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-webhook-diagnose-v2
  title: Three-source diagnostic for the external-secrets stall
  description: 'Founder 2026-09-17: "I''m sure we solved that problem for good, let''s get evidence from
    3'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      namespace:
        title: Namespace of the webhook under test
        type: string
        default: external-secrets
      service:
        title: Service name to hit
        type: string
        default: external-secrets-webhook
      port:
        title: Service port
        type: string
        default: '443'
      reason:
        title: Why (one line, kept in the run record)
        type: string
        default: 3-source convergence check on the estate stall
  steps:
  - id: dispatch
    name: Start Three-source diagnostic for the external-secrets stall
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: webhook-diagnose-v2.yml
      branchOrTagName: main
      workflowInputs:
        namespace: ${{ parameters.namespace }}
        service: ${{ parameters.service }}
        port: ${{ parameters.port }}
        reason: ${{ parameters.reason }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/webhook-diagnose-v2.yml
