# GENERATED by bin/idp-portal-buttons from .github/workflows/forge-train-kaggle.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-forge-train-kaggle
  title: Train a model in the Forge on a free Kaggle GPU.
  description: Fires forge/kaggle_app.py which pushes forge/kaggle/notebook.ipynb to Kaggle, polls it
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      task_file:
        title: task YAML under forge/ (name, base, labels, gates)
        type: string
        default: forge/tasks/ci-flake-triage.yaml
      max_steps:
        title: cap training steps (-1 = the epochs in the task file)
        type: string
        default: '-1'
  steps:
  - id: dispatch
    name: Start Train a model in the Forge on a free Kaggle GPU.
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: forge-train-kaggle.yml
      branchOrTagName: main
      workflowInputs:
        task_file: ${{ parameters.task_file }}
        max_steps: ${{ parameters.max_steps }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/forge-train-kaggle.yml
