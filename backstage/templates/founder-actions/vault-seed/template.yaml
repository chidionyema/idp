# GENERATED by bin/idp-portal-buttons from .github/workflows/vault-seed.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-vault-seed
  title: Write or remove a vault entry
  description: Writes one named entry into the vault under the machine identity, or schedules one for
    deletion.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      entry:
        title: which vault entry to write
        type: string
        enum:
        - all
        - prospector-engine-env
        - github-app
        - flux-writer
        - temporal-db
        - mcp-gateway
        - hindsight
        - k8sgpt
        - holmes
        - tailscale-operator
        - laptop
        - hermes
        - science
        - router-rows
        default: all
      delete:
        title: schedule deletion of one vault entry by name (for a mis-named entry); the entry field is
          ignored when this is set
        type: string
        default: ''
  steps:
  - id: dispatch
    name: Start Write or remove a vault entry
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: vault-seed.yml
      branchOrTagName: main
      workflowInputs:
        entry: ${{ parameters.entry }}
        delete: ${{ parameters.delete }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/vault-seed.yml
