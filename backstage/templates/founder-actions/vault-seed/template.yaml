# GENERATED by bin/idp-portal-buttons from .github/workflows/vault-seed.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-vault-seed
  title: 'Run: vault-seed'
  description: 'crew#284 CP1/CP2, crew#248: the estate vault is written by the machine identity, never
    a laptop (crew#227 deleted the OCI API key; crew#247: infra is never Mac-bound). crew#66 root trust
    (5453747447, docs/reference/policy/root-trust.md): an entry a bootstrapper births is never seeded
    from a pasted GitHub secret; this workflow now carries only github-app (the four keys bin/idp-github-app
    convert produces when no OCI session is live) and k8sgpt (a router key minted here, never pasted).
    Every other entry chosen by name is refused with the command that owns it. Values travel from the
    secret store '
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run vault-seed
    properties:
      entry:
        title: entry
        type: string
        description: which vault entry to write
        enum:
        - all
        - prospector-engine-env
        - github-app
        - flux-writer
        - temporal-db
        - mcp-gateway
        - hindsight
        - k8sgpt
        - tailscale-operator
        - laptop
        - hermes
        default: all
      delete:
        title: delete
        type: string
        description: 'schedule deletion of one vault secret by name (crew#503: a mis-named entry); entry
          is ignored when set'
        default: ''
  steps:
  - id: dispatch
    name: Dispatch vault-seed.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: vault-seed.yml
      branchOrTagName: main
      workflowInputs:
        entry: ${{ parameters.entry }}
        delete: ${{ parameters.delete }}
  output:
    links:
    - title: Runs of vault-seed
      url: https://github.com/chidionyema/idp/actions/workflows/vault-seed.yml
