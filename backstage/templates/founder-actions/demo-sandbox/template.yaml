# GENERATED by bin/idp-portal-buttons from .github/workflows/demo-sandbox.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-demo-sandbox
  title: Launch the buyer sandbox
  description: Starts a throwaway cluster-in-a-cluster with a demo shop at sandbox.<zone> for one hour
    or four; it removes itself when the hold runs out, and Ending it early works the same way.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      action:
        title: Launch a sandbox, or end the one that is running
        type: string
        enum:
        - launch
        - end
        default: launch
      hold:
        title: How long the sandbox stays before it removes itself
        type: string
        enum:
        - 1h
        - 4h
        default: 1h
  steps:
  - id: dispatch
    name: Start Launch the buyer sandbox
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: demo-sandbox.yml
      branchOrTagName: main
      workflowInputs:
        action: ${{ parameters.action }}
        hold: ${{ parameters.hold }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/demo-sandbox.yml
