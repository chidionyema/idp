# GENERATED by bin/idp-portal-buttons from .github/workflows/vault-reads.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-vault-reads
  title: 'Run: vault-reads'
  description: 'crew#584, founder 2026-08-29: the vault audit is monitoring, not a pull-request grade.
    It used to be the vault-reads row of bin/idp-oke-rebuild --check, where a 90-minute OCI audit query
    (427 s, run 33237964214) blocked every platform pull request. Now it runs on its own clock, never
    on pull_request, and its only outputs are a receipt line and a P1 issue: red opens or updates `P1:
    external-secrets is not reading the vault`, green closes it with the run as the receipt. Same machine
    identity as oke-check (GitHub OIDC -> OCI session token); no key on a laptop.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch vault-reads.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: vault-reads.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of vault-reads
      url: https://github.com/chidionyema/idp/actions/workflows/vault-reads.yml
