# GENERATED by bin/idp-portal-buttons from .github/workflows/otto-parity.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-otto-parity
  title: 'Run: otto-parity'
  description: 'crew#561 CP7: "Otto can do X" is a green row on a schedule, not a sentence in an issue.
    This is the otto-parity playbook of bin/idp-oke-break-glass on the estate''s clock (the drill-dispatcher
    fires it; the cron line below is the catalogue''s copy). The steps are oke-check.yml''s check job
    verbatim: the runner''s OIDC token becomes a one-hour OCI session, --break-glass admits the runner''s
    /32 to the API endpoint for the length of the playbook and removes it on every exit. One concurrency
    group with oke-check''s dispatches: two break-glass runs must never race on the NSG rule.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch otto-parity.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: otto-parity.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of otto-parity
      url: https://github.com/chidionyema/idp/actions/workflows/otto-parity.yml
