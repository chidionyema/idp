# GENERATED by bin/idp-portal-buttons from .github/workflows/catalog-render.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-catalog-render
  title: Refresh the catalogue
  description: Reads what is running and rewrites the Backstage catalogue to match, as a pull request
    or a preview.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      mode:
        title: Preview (the difference on the run summary) or commit (push the branch and open the pull
          request)
        type: string
        enum:
        - dry-run
        - commit
  steps:
  - id: dispatch
    name: Start Refresh the catalogue
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: catalog-render.yml
      branchOrTagName: main
      workflowInputs:
        mode: ${{ parameters.mode }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/catalog-render.yml
