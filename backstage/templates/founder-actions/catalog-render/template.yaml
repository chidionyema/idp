# GENERATED by bin/idp-portal-buttons from .github/workflows/catalog-render.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-catalog-render
  title: 'Run: catalog-render'
  description: 'crew#516 CP3 slice 2: the catalogue render runs off the Mac.  Until 2026-08-27 bin/catalog-render
    ran only as com.estate.catalog-render on the founder''s Mac, because its one input, ~/.estate/state/inventory.json,
    lived there. estate#9 made the inventory job publish that file to the estate bucket at state/inventory/latest.json,
    read back. This workflow is the reader: it takes the object, checks out crew beside idp (estate-showcase
    reads crew/docs/STANDARDS.md through ESTATE_CODE), and runs the same bin/catalog-render.  It started
    in --dry-run (the diff as the receipt, measured against the Mac r'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run catalog-render
    properties:
      mode:
        title: mode
        type: string
        description: dry-run (diff in the summary) or commit (push the branch, open the PR)
        enum:
        - dry-run
        - commit
        default: dry-run
  steps:
  - id: dispatch
    name: Dispatch catalog-render.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: catalog-render.yml
      branchOrTagName: main
      workflowInputs:
        mode: ${{ parameters.mode }}
  output:
    links:
    - title: Runs of catalog-render
      url: https://github.com/chidionyema/idp/actions/workflows/catalog-render.yml
