# GENERATED by bin/idp-portal-buttons from .github/workflows/estate-escrow.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-estate-escrow
  title: 'Run: estate-escrow'
  description: "crew#290 layer 4: GitHub is the only copy of every load-bearing repository, and the only\
    \ escrow writer ran on the founder's Mac. Six times a day a runner with no static credential exchanges\
    \ its OIDC token for the estate-ci session, mints a read-only App token, and bin/idp-escrow writes\
    \ a full `git bundle` of every repository the App can see to Cloudflare R2 (a second cloud), then\
    \ reads each one back and verifies it. bin/idp-recover-drill is the restore side. Cost: \xA30 (GitHub\
    \ runner, R2 ingress is free). Catalogued in drills/catalogue.yaml as estate-escrow."
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch estate-escrow.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: estate-escrow.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of estate-escrow
      url: https://github.com/chidionyema/idp/actions/workflows/estate-escrow.yml
