# GENERATED by bin/idp-portal-buttons from .github/workflows/forge-train.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-forge-train
  title: Train a model in the Forge
  description: Trains a small task model on a GPU from a task file and its labelled data, grades it against
    the task's gates, and files the run as an experiment record on a pull request; a dry run grades without
    publishing.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      task_file:
        title: task YAML under forge/ (name, base, labels, gates)
        type: string
        default: forge/task.yaml
      data:
        title: JSONL under forge/datasets/; blank = forge/datasets/<task>.jsonl
        type: string
        default: ''
      dry_run:
        title: grade the gates, push nothing
        type: boolean
        default: true
      max_steps:
        title: cap training steps (-1 = the epochs in the task file)
        type: string
        default: '-1'
  steps:
  - id: dispatch
    name: Start Train a model in the Forge
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: forge-train.yml
      branchOrTagName: main
      workflowInputs:
        task_file: ${{ parameters.task_file }}
        data: ${{ parameters.data }}
        dry_run: ${{ parameters.dry_run }}
        max_steps: ${{ parameters.max_steps }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/forge-train.yml
