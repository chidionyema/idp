# GENERATED by bin/idp-portal-buttons from .github/workflows/demo-render.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-demo-render
  title: Re-render every demo recording
  description: Replays every demo script against the real tools and commits fresh recordings, so a demo
    can never show what the software no longer does.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Start Re-render every demo recording
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: demo-render.yml
      branchOrTagName: main
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/demo-render.yml
