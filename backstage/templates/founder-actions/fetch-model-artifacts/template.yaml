# GENERATED by bin/idp-portal-buttons from .github/workflows/fetch-model-artifacts.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-fetch-model-artifacts
  title: Fetch open models to the estate
  description: Downloads the open-weight models from your list and publishes each one into the
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      models:
        title: space-separated keys from the matrix below
        type: string
        default: vibethinker-1.5b twil-lm3 vibethinker-3b nanbeige-4.2-3b falcon-h1-7b
  steps:
  - id: dispatch
    name: Start Fetch open models to the estate
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: fetch-model-artifacts.yml
      branchOrTagName: main
      workflowInputs:
        models: ${{ parameters.models }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/fetch-model-artifacts.yml
