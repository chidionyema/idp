# GENERATED by bin/idp-portal-buttons from .github/workflows/wake-blocked.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-wake-blocked
  title: 'Run: wake-blocked'
  description: Copied verbatim into .github/workflows/wake-blocked.yml of every active estate repository
    by bin/estate-security-rollout's sibling step (crew#504 CP7). A PR closed with a `Blocked-by:` line
    in its body reopens itself, branch updated, when every key it names has landed.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run wake-blocked
    properties:
      resolved:
        title: resolved
        type: string
        description: free-text Blocked-by key that has landed, e.g. secret signoz-root
        default: ''
  steps:
  - id: dispatch
    name: Dispatch wake-blocked.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: wake-blocked.yml
      branchOrTagName: main
      workflowInputs:
        resolved: ${{ parameters.resolved }}
  output:
    links:
    - title: Runs of wake-blocked
      url: https://github.com/chidionyema/idp/actions/workflows/wake-blocked.yml
