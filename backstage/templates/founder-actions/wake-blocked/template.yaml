# GENERATED by bin/idp-portal-buttons from .github/workflows/wake-blocked.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-wake-blocked
  title: Wake a blocked session
  description: Tells a session that the thing it was waiting for has landed, so it carries on.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      resolved:
        title: 'the thing that has now landed, in a few words (for example: the SigNoz root secret)'
        type: string
        default: ''
  steps:
  - id: dispatch
    name: Start Wake a blocked session
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: wake-blocked.yml
      branchOrTagName: main
      workflowInputs:
        resolved: ${{ parameters.resolved }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/wake-blocked.yml
