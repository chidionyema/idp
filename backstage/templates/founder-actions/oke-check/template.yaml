# GENERATED by bin/idp-portal-buttons from .github/workflows/oke-check.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-oke-check
  title: Check or apply the cluster
  description: Compares the cluster with what is declared, or applies the declared state and seeds the
    vault; runs under the machine identity.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      mode:
        title: check (compare and report drift) or apply (apply the declared state and seed the vault)
        type: string
        enum:
        - check
        - apply
        - surge-node
        - surge-finish
        - break-glass
        default: check
      playbook:
        title: 'Break-glass only: runs the one named recovery playbook while the runner is admitted to
          the cluster; diagnose and architect-doctor are read-only'
        type: string
        enum:
        - diagnose
        - architect-doctor
        - otto-parity
        - otto-lockdown
        - otto-restore
        - healthchecks-door
        - cilium-unchain
        - cilium-replace
        - helm-retry
        - webhooks-restart
        - namespace-unstick
        - ghcr-pull-restore
        - k8sgpt-analyze
        - dns-per-node
        - dns-per-namespace
        - tcp-per-node
        - node-drain
        - node-uncordon
        - chi-resize
        - catalogue-roll
        - router-spend
        - registries-conf
        - ns-fences
        default: diagnose
  steps:
  - id: dispatch
    name: Start Check or apply the cluster
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: oke-check.yml
      branchOrTagName: main
      workflowInputs:
        mode: ${{ parameters.mode }}
        playbook: ${{ parameters.playbook }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/oke-check.yml
