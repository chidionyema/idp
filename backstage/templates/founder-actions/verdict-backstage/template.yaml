# GENERATED by bin/idp-portal-buttons from .github/workflows/verdict-backstage.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-verdict-backstage
  title: Check the catalogue and sign the result
  description: Checks that the catalogue answers, answers its key, refuses a caller without one, and signs
    the result.
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Choices for this run
    properties:
      nonce:
        title: request code for this run (leave blank; the run makes one)
        type: string
        default: ''
  steps:
  - id: dispatch
    name: Start Check the catalogue and sign the result
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: verdict-backstage.yml
      branchOrTagName: main
      workflowInputs:
        nonce: ${{ parameters.nonce }}
  output:
    links:
    - title: Past runs
      url: https://github.com/chidionyema/idp/actions/workflows/verdict-backstage.yml
