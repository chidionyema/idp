# GENERATED by bin/idp-portal-buttons from .github/workflows/verdict-backstage.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-verdict-backstage
  title: 'Run: verdict-backstage'
  description: 'crew#631 CP9: the prover for the catalogue. Same machine identity as verdict-langfuse;
    no browser. One signed verdict, one check-run `verify/backstage`, one row in the verdict table.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters:
  - title: Run verdict-backstage
    properties:
      nonce:
        title: nonce
        type: string
        description: 'nonce issued by the control plane for this request (blank: the run mints one)'
        default: ''
  steps:
  - id: dispatch
    name: Dispatch verdict-backstage.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: verdict-backstage.yml
      branchOrTagName: main
      workflowInputs:
        nonce: ${{ parameters.nonce }}
  output:
    links:
    - title: Runs of verdict-backstage
      url: https://github.com/chidionyema/idp/actions/workflows/verdict-backstage.yml
