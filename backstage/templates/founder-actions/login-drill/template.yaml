# GENERATED by bin/idp-portal-buttons from .github/workflows/login-drill.yml -- edit the workflow, not this file.
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: run-login-drill
  title: 'Run: login-drill'
  description: 'crew#292 CP1, rewritten under crew#307. A drill is an adversary, not a downstream step:
    it runs on its own clock whether the platform pipeline is green, red or absent, because as a `needs:`
    of the tofu check it was skipped on every check red and the skip read as green while the catalogue
    served the founder an error. Same machine identity as oke-check (GitHub OIDC token exchanged for a
    one-hour OCI session token); the drill''s credential is the domain password Terraform put in the vault,
    read by name, never printed.'
  tags:
  - founder-action
  - estate
spec:
  owner: group:default/platform
  type: founder-action
  parameters: []
  steps:
  - id: dispatch
    name: Dispatch login-drill.yml on main
    action: github:actions:dispatch
    input:
      repoUrl: github.com?owner=chidionyema&repo=idp
      workflowId: login-drill.yml
      branchOrTagName: main
  output:
    links:
    - title: Runs of login-drill
      url: https://github.com/chidionyema/idp/actions/workflows/login-drill.yml
