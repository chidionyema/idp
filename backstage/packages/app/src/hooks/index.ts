/**
 * Hooks index — public exports for backstage app hooks.
 */

export {
  useSpeculativeVoice,
  type VoiceIntent,
  type SpeculativeVoiceState,
  type SpeculativeVoice,
  type ModelProgress,
  type TurnMetrics,
} from './useSpeculativeVoice';
