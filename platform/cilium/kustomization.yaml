apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No namespace transformer: Cilium is a kube-system citizen (the CNI plugin path and the
# chaining ConfigMap are read by the node's CNI, not by a tenant namespace).
resources:
  - cni-configuration.yaml
  - cilium.yaml
