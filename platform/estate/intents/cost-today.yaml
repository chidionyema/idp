name: cost-today
description: Where is my money going? Query spend for the calling key.
tools: [curl]
models: []
halt_on_failure: true
max_turns: 3
steps:
  - cmd: >
      curl -sf https://llm.mumchimp.com/key/info
      -H "Authorization: Bearer $ESTATE_AGENT_KEY"
    on_fail: halt
