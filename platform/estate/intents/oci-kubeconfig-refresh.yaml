name: oci-kubeconfig-refresh
description: >
  Refresh the OKE kubeconfig token. Run this when kubectl returns
  "server asked for credentials" or token expired.
tools: []
halt_on_failure: true
steps:
  - cmd: >
      bash ~/.estate/libexec/oci-kubeconfig-refresh.sh
