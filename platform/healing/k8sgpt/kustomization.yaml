# crew#539 CP7: K8sGPT explains a Pending wave through the estate's own router. Split out of
# ./.. on 2026-09-08 so its dependency on `llm` is its own and not the evictor's -- see the
# comment in ../kustomization.yaml. The namespace stays in the parent row, which this one
# depends on, so exactly one row owns it.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: healing
resources:
  - external-secret.yaml
  - k8sgpt.yaml
