# Chaos schedules only. The Chaos Mesh install is in mesh/, a separate Flux row this one
# dependsOn, so the Schedule CRD exists before this row is dry-run.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - backstage-pod-kill.yaml
  - backstage-pod-kill-first-run.yaml
  - langfuse-alert-drill.yaml
  - langfuse-alert-drill-first-run.yaml
