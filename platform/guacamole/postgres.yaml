# Guacamole persistence (users, the one connection, its permission). Same shape as
# platform/healthchecks/postgres.yaml: one StatefulSet, non-root, read-only root, password from a
# mounted Secret. The schema is applied by the web pod's init container (guacamole.yaml).
apiVersion: v1
kind: Service
metadata:
  name: guacamole-db
  namespace: guacamole
  labels: { app.kubernetes.io/name: guacamole-db, app.kubernetes.io/part-of: idp, backstage.io/kubernetes-id: founder-screen }
spec:
  type: ClusterIP
  clusterIP: None
  selector: { app.kubernetes.io/name: guacamole-db }
  ports:
    - { name: pg, port: 5432, targetPort: 5432 }
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: guacamole-db
  namespace: guacamole
  labels: { app.kubernetes.io/name: guacamole-db, app.kubernetes.io/part-of: idp }
spec:
  serviceName: guacamole-db
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: guacamole-db }
  template:
    metadata:
      labels: { app.kubernetes.io/name: guacamole-db, app.kubernetes.io/part-of: idp }
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 70   # postgres uid in the alpine image
        runAsGroup: 70
        fsGroup: 70
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: postgres
          image: docker.io/library/postgres:17.6-alpine
          env:
            - { name: POSTGRES_USER, value: guacamole }
            - { name: POSTGRES_DB, value: guacamole }
            - { name: PGDATA, value: /var/lib/postgresql/data/pgdata }
            - { name: POSTGRES_PASSWORD_FILE, value: /run/secrets/guacamole-db/password }
          ports:
            - { containerPort: 5432, name: pg }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          readinessProbe:
            exec: { command: ["pg_isready", "-U", "guacamole", "-d", "guacamole"] }
            periodSeconds: 10
          livenessProbe:
            exec: { command: ["pg_isready", "-U", "guacamole", "-d", "guacamole"] }
            periodSeconds: 30
            failureThreshold: 3
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
          volumeMounts:
            - { name: pgdata, mountPath: /var/lib/postgresql/data }
            - { name: secret, mountPath: /run/secrets/guacamole-db, readOnly: true }
            - { name: run, mountPath: /var/run/postgresql }
            - { name: tmp, mountPath: /tmp }
      volumes:
        - name: secret
          secret: { secretName: guacamole-db }
        - name: run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
