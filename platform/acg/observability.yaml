apiVersion: v1
kind: ServiceMonitor
metadata:
  name: gateway
  namespace: acg
spec:
  selector:
    matchLabels:
      app: gateway
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
