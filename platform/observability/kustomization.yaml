apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability
resources:
  - namespace.yaml
  - signoz.yaml
configMapGenerator:
  - name: signoz-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
