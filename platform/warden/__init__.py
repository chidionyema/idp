# API key lifecycle — vendor key proving and rotation (Decision 0020).
#
# prove(vendor, key) -> Proof: checks a key against the vendor's verify endpoint.
# rotate(vendor, old_key) -> Proof: rotates a key programmatically where the vendor allows it.
