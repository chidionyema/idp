# GENERATED by bin/idp-vendor-render -- do not edit; change platform/vendors/consoles.yaml.
#
# The seeding half of the human door. externalsecrets.yaml carries keys DOWN from the founder's
# Bitwarden Secrets Manager into the estate; this file carries the ones the estate already holds
# UP into Bitwarden once, so he never has to find and retype a key the platform is already using.
#
# updatePolicy: IfNotExists is the whole safety argument. A PushSecret with the default Replace
# would rewrite the founder's value from the estate's copy on every refresh, so the moment he
# rotated a key it would be silently reverted -- the exact opposite of what the door is for.
# IfNotExists writes only into an empty slot: it fills what he has never set and never touches
# what he has. deletionPolicy: None so that deleting this file leaves his vault as it is.
#
# A source Secret or field that does not exist fails this PushSecret alone, the same way a
# missing Bitwarden name fails one ExternalSecret alone (measured 2026-09-07: one absent key
# takes down the whole object it sits in, and nothing else).
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-telegram-alerts-bot-token
  namespace: flux-system
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: flux-telegram }
  data:
    - match:
        secretKey: token
        remoteRef: { remoteKey: TELEGRAM_ALERTS_BOT_TOKEN }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-cursor-api-key
  namespace: hermes-agent
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: hermes-agent-env }
  data:
    - match:
        secretKey: CURSOR_API_KEY
        remoteRef: { remoteKey: CURSOR_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-exa-api-key
  namespace: hermes-agent
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: hermes-agent-env }
  data:
    - match:
        secretKey: EXA_API_KEY
        remoteRef: { remoteKey: EXA_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-openrouter-api-key
  namespace: hermes-agent
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: hermes-agent-env }
  data:
    - match:
        secretKey: OPENROUTER_API_KEY
        remoteRef: { remoteKey: OPENROUTER_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-telegram-hermes-bot-token
  namespace: hermes-agent
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: hermes-agent-env }
  data:
    - match:
        secretKey: TELEGRAM_BOT_TOKEN
        remoteRef: { remoteKey: TELEGRAM_HERMES_BOT_TOKEN }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-gemini-api-key
  namespace: llm
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: litellm-upstream }
  data:
    - match:
        secretKey: GEMINI_API_KEY
        remoteRef: { remoteKey: GEMINI_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-groq-api-key
  namespace: llm
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: litellm-upstream }
  data:
    - match:
        secretKey: GROQ_API_KEY
        remoteRef: { remoteKey: GROQ_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-moonshot-api-key
  namespace: llm
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: litellm-upstream }
  data:
    - match:
        secretKey: MOONSHOT_API_KEY
        remoteRef: { remoteKey: MOONSHOT_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-openrouter-api-key
  namespace: llm
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: litellm-upstream }
  data:
    - match:
        secretKey: OPENROUTER_API_KEY
        remoteRef: { remoteKey: OPENROUTER_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-telegram-alerts-bot-token
  namespace: notify
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: notify-apprise-founder-telegram }
  data:
    - match:
        secretKey: token
        remoteRef: { remoteKey: TELEGRAM_ALERTS_BOT_TOKEN }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-telegram-alerts-chat-id
  namespace: notify
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: notify-apprise-founder-telegram }
  data:
    - match:
        secretKey: chat
        remoteRef: { remoteKey: TELEGRAM_ALERTS_CHAT_ID }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-deepseek-api-key
  namespace: prospector
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: prospector-engine-env }
  data:
    - match:
        secretKey: DEEPSEEK_API_KEY
        remoteRef: { remoteKey: DEEPSEEK_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-exa-api-key
  namespace: prospector
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: prospector-engine-env }
  data:
    - match:
        secretKey: EXA_API_KEY
        remoteRef: { remoteKey: EXA_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-google-oauth-client-id
  namespace: prospector
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: prospector-store-api-env }
  data:
    - match:
        secretKey: Authentication__Google__ClientId
        remoteRef: { remoteKey: GOOGLE_OAUTH_CLIENT_ID }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-google-oauth-client-secret
  namespace: prospector
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: prospector-store-api-env }
  data:
    - match:
        secretKey: Authentication__Google__ClientSecret
        remoteRef: { remoteKey: GOOGLE_OAUTH_CLIENT_SECRET }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-openrouter-api-key
  namespace: prospector
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: prospector-engine-env }
  data:
    - match:
        secretKey: OPENROUTER_API_KEY
        remoteRef: { remoteKey: OPENROUTER_API_KEY }
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: seed-stripe-secret-key
  namespace: prospector
spec:
  refreshInterval: 1h
  updatePolicy: IfNotExists
  deletionPolicy: None
  secretStoreRefs:
    - { kind: ClusterSecretStore, name: human-vault }
  selector:
    secret: { name: prospector-store-api-env }
  data:
    - match:
        secretKey: Stripe__ApiKey
        remoteRef: { remoteKey: STRIPE_SECRET_KEY }
