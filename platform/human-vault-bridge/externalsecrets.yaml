# GENERATED by bin/idp-vendor-render -- one ExternalSecret per vendor per namespace, from the
# `targets:` of every vendor in platform/vendors/consoles.yaml whose store_default is human-vault.
# Edit the registry, never this file; bin/idp-ci fails when this file and a fresh render differ.
#
# The human door, wired (decision 0017). A key the founder drops into Bitwarden Secrets Manager
# under the name in a target's `bw:` field lands in the cluster as a Secret named human-<vendor>,
# in the namespace that consumes it, within the refresh interval. Nothing is pasted into a portal
# and no value is ever typed into this repository.
#
# Why one ExternalSecret per vendor rather than one per namespace holding every key: External
# Secrets fails an ExternalSecret whole when any one of its `data` entries names a secret the store
# does not hold. Split this way, a vendor whose key has not been dropped in yet fails only its own
# Secret, and its status line names exactly which key is missing -- so the set of failing bridge
# ExternalSecrets IS the list of keys still to add, with no separate inventory to keep true.
#
# Addressing is by name, not by Bitwarden id, and that is load-bearing. `dataFrom.find` was
# measured on 2026-09-07 against bitwarden-sdk-server v0.6.0 and it IGNORES `name.regexp`: a find
# for `^NO_SUCH_KEY_AT_ALL$` returned the project's unrelated secret and `rewrite` then renamed it
# onto the field. A bridge built on find would quietly serve one vendor's value under every other
# vendor's key. `data[].remoteRef.key: <name>` resolves exactly, and refuses when the name is
# absent, which is the behaviour a credential path must have.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-apprise-telegram
  namespace: notify
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-apprise-telegram, creationPolicy: Owner }
  data:
    - secretKey: chat
      remoteRef: { key: TELEGRAM_ALERTS_CHAT_ID }
    - secretKey: token
      remoteRef: { key: TELEGRAM_ALERTS_BOT_TOKEN }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-cursor
  namespace: hermes-agent
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-cursor, creationPolicy: Owner }
  data:
    - secretKey: CURSOR_API_KEY
      remoteRef: { key: CURSOR_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-deepseek
  namespace: prospector
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-deepseek, creationPolicy: Owner }
  data:
    - secretKey: DEEPSEEK_API_KEY
      remoteRef: { key: DEEPSEEK_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-exa
  namespace: hermes-agent
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-exa, creationPolicy: Owner }
  data:
    - secretKey: EXA_API_KEY
      remoteRef: { key: EXA_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-exa
  namespace: prospector
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-exa, creationPolicy: Owner }
  data:
    - secretKey: EXA_API_KEY
      remoteRef: { key: EXA_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-gemini
  namespace: llm
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-gemini, creationPolicy: Owner }
  data:
    - secretKey: GEMINI_API_KEY
      remoteRef: { key: GEMINI_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-gemini
  namespace: otto-gateway
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-gemini, creationPolicy: Owner }
  data:
    - secretKey: GEMINI_API_KEY
      remoteRef: { key: GEMINI_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-google-oauth
  namespace: prospector
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-google-oauth, creationPolicy: Owner }
  data:
    - secretKey: Authentication__Google__ClientId
      remoteRef: { key: GOOGLE_OAUTH_CLIENT_ID }
    - secretKey: Authentication__Google__ClientSecret
      remoteRef: { key: GOOGLE_OAUTH_CLIENT_SECRET }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-groq
  namespace: llm
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-groq, creationPolicy: Owner }
  data:
    - secretKey: GROQ_API_KEY
      remoteRef: { key: GROQ_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-groq
  namespace: otto-gateway
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-groq, creationPolicy: Owner }
  data:
    - secretKey: GROQ_API_KEY
      remoteRef: { key: GROQ_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-kimi
  namespace: llm
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-kimi, creationPolicy: Owner }
  data:
    - secretKey: MOONSHOT_API_KEY
      remoteRef: { key: MOONSHOT_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-minimax
  namespace: llm
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-minimax, creationPolicy: Owner }
  data:
    - secretKey: MINIMAX_API_KEY
      remoteRef: { key: MINIMAX_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-minimax
  namespace: otto-gateway
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-minimax, creationPolicy: Owner }
  data:
    - secretKey: MINIMAX_API_KEY
      remoteRef: { key: MINIMAX_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-minimax
  namespace: prospector
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-minimax, creationPolicy: Owner }
  data:
    - secretKey: MINIMAX_API_KEY
      remoteRef: { key: MINIMAX_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-openrouter
  namespace: hermes-agent
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-openrouter, creationPolicy: Owner }
  data:
    - secretKey: OPENROUTER_API_KEY
      remoteRef: { key: OPENROUTER_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-openrouter
  namespace: llm
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-openrouter, creationPolicy: Owner }
  data:
    - secretKey: OPENROUTER_API_KEY
      remoteRef: { key: OPENROUTER_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-openrouter
  namespace: otto-gateway
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-openrouter, creationPolicy: Owner }
  data:
    - secretKey: OPENROUTER_API_KEY
      remoteRef: { key: OPENROUTER_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-openrouter
  namespace: prospector
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-openrouter, creationPolicy: Owner }
  data:
    - secretKey: OPENROUTER_API_KEY
      remoteRef: { key: OPENROUTER_API_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-stripe
  namespace: prospector
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-stripe, creationPolicy: Owner }
  data:
    - secretKey: Stripe__ApiKey
      remoteRef: { key: STRIPE_SECRET_KEY }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-telegram
  namespace: flux-system
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-telegram, creationPolicy: Owner }
  data:
    - secretKey: token
      remoteRef: { key: TELEGRAM_ALERTS_BOT_TOKEN }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: human-telegram
  namespace: hermes-agent
spec:
  refreshInterval: 1m
  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
  target: { name: human-telegram, creationPolicy: Owner }
  data:
    - secretKey: TELEGRAM_BOT_TOKEN
      remoteRef: { key: TELEGRAM_HERMES_BOT_TOKEN }
