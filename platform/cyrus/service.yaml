# Cyrus service (crew#834).
apiVersion: v1
kind: Service
metadata:
  name: cyrus
  namespace: cyrus
  labels:
    backstage.io/kubernetes-id: layer-cyrus
spec:
  selector:
    app.kubernetes.io/name: cyrus
  ports:
    - name: http
      port: 3456
      targetPort: 3456
  type: ClusterIP
