# The graded drill catalogue (crew#292 CP2).
#
# Founder, 2026-08-26: "we have drills? ... when we get it working we need to drill".
# bin/idp-verify graded static rows: a file exists, a redirect is right, a port is declared.
# None of them said whether the scheduled drills had actually run. A drill that is scheduled
# and silently failing to fire looks exactly like a drill that is passing, and the first time
# anyone finds out is the day the thing it drills is needed.
#
# Every entry here is a scheduled GitHub workflow in this repository. The `drills` row of
# bin/idp-verify asks GitHub for the last successful run of each one and FAILs when it is
# older than max_age_hours. Nothing is invented: `schedule` is copied verbatim from the
# workflow's own cron line, so a drill that is quietly unscheduled shows up as a diff here.
#
# Fields
#   name            short name printed by the drills row
#   workflow        the workflow file under .github/workflows/
#   job             optional: the job inside that workflow the drill is (documentation only;
#                   the freshness check grades the workflow run)
#   schedule        the cron string exactly as the workflow declares it
#   max_age_hours   how stale the last green run may be before the row is FAIL
#                   (26 for a daily drill: 24 plus GitHub's scheduling slack)
#   proves          one sentence: what a green run of this drill actually demonstrates
#   pending         optional: true means the drill is not merged yet; the row prints n/a
#                   and never FAILs. Remove the line the moment the job lands.

drills:
  - name: oke-check
    workflow: oke-check.yml
    job: check
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      A machine identity with no OCI API key can still exchange its GitHub OIDC token for an
      OCI session and read the live cluster, so the platform can be rebuilt without a laptop.

  - name: login-drill
    workflow: login-drill.yml
    job: login-drill
    schedule: "7 * * * *"
    max_age_hours: 26
    proves: >-
      A sign-in to the front door completes end to end from a runner that holds no password,
      so the federated login is exercised daily rather than only when the founder tries it.

  - name: chaos-pod-kill
    workflow: oke-check.yml
    job: chaos-drill
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      In the last week Chaos Mesh killed one Backstage pod while /healthcheck stayed 200, the
      Workflow wrote its receipt from the node's own identity, and a runner holding no key read
      it back (bin/idp-chaos-drill grades the receipt's age, max 194h).

  - name: alert-drill
    workflow: oke-check.yml
    job: alert-drill
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      In the last week Chaos Mesh failed every langfuse-web pod for 8 minutes and, while it was
      down, Alertmanager held an active FounderSurfaceDown for langfuse and its telegram receiver
      delivered it (crew#539 DoD 2: the founder's phone learns of a dead surface within 5 minutes
      of it dying); the receipt is written from the node's identity and graded by
      bin/idp-chaos-drill langfuse-alert-drill alert-drill (age max 194h).

  - name: trace-drill
    workflow: trace-drill.yml
    job: trace-drill
    schedule: "23 * * * *"
    max_age_hours: 3
    proves: >-
      One chat completion went through llm.<zone> with the master key from the vault and its
      trace came back out of langfuse.<zone> by id within 90 s, so model routing and the trace
      store work end to end from a runner that holds no key (crew#325 showcase).

  - name: kyverno-secrets-drill
    workflow: kyverno-secrets-drill.yml
    job: kyverno-secrets-drill
    schedule: "41 7 * * *"
    max_age_hours: 26
    proves: >-
      The secrets-not-from-env-vars Kyverno policy still refuses a Pod that reads a secret via
      env.valueFrom.secretKeyRef or envFrom.secretRef, and still passes the real vault-backed
      volume-mount pattern already shipped (langfuse) plus two false-positive-risk cases
      (no env at all, ConfigMap-sourced env) -- so a future dependency bump or CEL engine
      change cannot silently reopen the class crew#341 closed.

  - name: cluster-state
    workflow: oke-check.yml
    job: cluster-state
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      The cluster wrote its own state receipt (nodes, pods not Ready) to Object Storage from the
      node's identity within the last hour and a runner with no kube path and no laptop session
      token read it back through OIDC (crew#345: bin/idp-cluster-state grades freshness and Ready);
      the cluster holds PrometheusRules and Alertmanager shows the Watchdog alert, so the alert
      pipeline is evaluating and delivering (crew#539 DoD 1).
  - name: verify-drill
    workflow: verify-drill.yml
    job: verify-drill
    schedule: "23 * * * *"
    max_age_hours: 3
    proves: >-
      In the last hour a runner holding no key, on the estate-ci machine identity, named its own
      session subject as that service user, saw the cluster and node pools ACTIVE through the OCI
      API, and graded the cluster's own state/cluster receipt (platform/state, bin/idp-cluster-state)
      written from inside on the node's instance principal. Twenty-four green rows in a row is crew#345's acceptance
      criterion: live verification with nobody logged in. Since crew#66 root trust the same run also
      grades the root-trust row (bin/idp-root-trust): every vault entry an ExternalSecret reads is
      registered in docs/policy/root-trust.md with a bootstrapper on disk or a ticket, hourly.

  - name: kini-finish
    workflow: oke-check.yml
    job: kini-state
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      The KINI run's own receipt (state/kini, written every 15 minutes by the kini-state CronJob
      from the engine's answer) is fresh and the last KiniFinishWorkflow is green, running, or not
      yet requested; a red checkpoint or a stale receipt fails it (crew#396: bin/idp-kini-state).
  - name: telemetry-coverage
    workflow: oke-check.yml
    job: telemetry-coverage
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      Every pod Running for more than ten minutes wrote a log line or a metric sample that SigNoz
      stored in the last hour, measured by querying ClickHouse and diffing against the API server
      (crew#320, LAW 50: coverage is proved by the backend, never by scanning files).
  - name: portability
    workflow: portability-drill.yml
    job: hydrate
    schedule: "23 5 * * 1"
    max_age_hours: 194
    proves: >-
      In the last week the whole clusters/oke/ Flux tree was applied to a vanilla k3d cluster on a
      runner with no OCI identity and at least drills/portability-floor.txt layers came Ready, so
      the platform hydrates a foreign cluster from git alone (crew#488, LAW 19).
  - name: portability-k3s
    workflow: portability-drill.yml
    job: k3s
    schedule: "23 5 * * 1"
    max_age_hours: 194
    proves: >-
      In the last week the same clusters/oke/ tree hydrated a k3s node on a second provider (the
      GitHub-hosted Azure runner, no docker, no OCI) from the same commit, met the same floor, and
      the run printed its wall-clock and its cost (crew#488 CP2, LAW 19).
  - name: free-tier
    workflow: oke-check.yml
    job: free-tier
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      The limits API granted the tenancy at least the A1 cores and memory platform/oci/variables.tf
      assumes free and enough to hold what it provisions, so the allowance OCI halved once
      (ADR 0004) has not moved again without a red row (crew#488 CP3).

  - name: stale
    workflow: stale.yml
    job: stale
    schedule: "17 * * * *"
    max_age_hours: 3
    proves: >-
      A pull request nobody has touched for 24 hours is closed by the platform with the two
      ways back in the comment, so the queue cannot pile up behind ended sessions (crew#504 CP2).

  - name: wake-blocked
    workflow: wake-blocked.yml
    job: wake-blocked
    schedule: "7-59/30 * * * *"
    max_age_hours: 3
    pending: true
    proves: >-
      A pull request closed with a Blocked-by line is updated and reopened within 30 minutes of
      its blocker landing, so dependency waits never need a person to remember (crew#504 CP7).
  - name: science-facts
    workflow: oke-check.yml
    job: science-facts
    schedule: "17 6 * * *"
    max_age_hours: 26
    proves: >-
      The science lane's rows reached the estate collector: an hourly job inside the cluster counts
      rows per science.source in ClickHouse over the last 24 hours and writes the count as a receipt;
      at least one source is the only green, and a backend that cannot answer is BLIND
      (crew#516 CP5, LAW 50: the lane is graded by querying the backend, never by scanning files).
