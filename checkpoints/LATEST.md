# LATEST checkpoint — session 2c88870e, lane .wt-vendor-probe, 2026-09-03 04:5xZ

## RESUME HERE

Two threads, both live.

**1. Estate snapshot version 2 — PR 1213 (idp), branch `docs/founder-estate-snapshot-mandatory`, commit ca51de42.**
Built and proved locally: eight new keys inside the five tabs, 22 tests green, schema valid, first local
build named the two refused vendor roots and the kimi lane 500 without a re-run. Background watcher
`b4c51cpt4` polls the checks. When settled: red check → read the log, fix in one pass, push; green → do not
tell the founder about the PR until green, then INVENTORY: five lines and wait for his merge word.
If the `pr-evidence` check is red, run `pr-evidence.py attach` (memory: gh pr edit clobbers the link).

**2. The gate half of the ruling — claude-guards `opa-hook.py`.** Founder 2026-09-03 04:1xZ: "no agent can
proceed without it". Plan: worktree of `~/.claude/scripts` in the scratchpad (`guards-wt`, branch
`guards/relay-refuses-blind-session`), PreToolUse denies tool calls when `~/.estate/estate-state.json` is blind
or stale after one `estate-state-relay.py --fetch` retry; message names that command; one test; push; PR.
Do not commit other sessions' uncommitted edits in `~/.claude/scripts` (founder-deliver.py etc.).

**Parked: Kimi lane.** Waits on the founder re-setting SEED_KIMI_API_KEY and SEED_DEEPSEEK_API_KEY from his own
tab (Telegram 21834) and saying go → `gh workflow run oke-check.yml --ref main -f mode=apply`, read the seeder
kimi line, ~10 min, one router call with model kimi, report MEASURED.

Feed handoff every 15 min: `python3 ~/.claude/scripts/feed-guard.py append --session 2c88870e --lane .wt-vendor-probe`
(last 04:48Z). Never `git push -q`; no STAGED countdowns; agents never deploy.
