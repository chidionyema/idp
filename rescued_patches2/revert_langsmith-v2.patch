diff --git a/.github/workflows/vault-seed.yml b/.github/workflows/vault-seed.yml
index 9e16291e3..43f5e147c 100644
--- a/.github/workflows/vault-seed.yml
+++ b/.github/workflows/vault-seed.yml
@@ -15,7 +15,7 @@ on:
       entry:
         description: "which vault entry to write"
         type: choice
-        options: [all, prospector-engine-env, github-app, flux-writer, temporal-db, mcp-gateway, hindsight, k8sgpt, holmes, tailscale-operator, laptop, hermes, science, router-rows, langsmith]
+        options: [all, prospector-engine-env, github-app, flux-writer, temporal-db, mcp-gateway, hindsight, k8sgpt, holmes, tailscale-operator, laptop, hermes, science, router-rows]
         default: all
       delete:
         description: "schedule deletion of one vault entry by name (for a mis-named entry); the entry field is ignored when this is set"
@@ -67,7 +67,6 @@ jobs:
           SEED_GITHUB_APP_SLUG: ${{ secrets.SEED_GITHUB_APP_SLUG }}
           SEED_GITHUB_APP_CLIENT_ID: ${{ secrets.SEED_GITHUB_APP_CLIENT_ID }}
           SEED_GITHUB_APP_PEM_B64: ${{ secrets.SEED_GITHUB_APP_PEM_B64 }}
-          SEED_LANGSMITH_API_KEY: ${{ secrets.SEED_LANGSMITH_API_KEY }}
         run: |
           set -euo pipefail
           bin/idp-oci-login --check
@@ -191,22 +190,6 @@ jobs:
             fi
             rm -rf "$RUNNER_TEMP/estate-secrets"
           fi
-          if [ "$ENTRY" = langsmith ]; then
-            # LangSmith tracing key (external vendor key, not a router key). Read from
-            # SEED_LANGSMITH_API_KEY GitHub secret and deliver through the sops vault so every
-            # shell that sources estate-secrets gets it without holding it in plaintext.
-            if [ -z "$SEED_LANGSMITH_API_KEY" ]; then echo "FAIL    SEED_LANGSMITH_API_KEY secret is empty"; exit 1; fi
-            tok=$(bin/idp-github-app token vault-writer); echo "::add-mask::$tok"
-            git clone -q --depth 1 "https://x-access-token:${tok}@github.com/${GITHUB_REPOSITORY_OWNER}/estate-secrets" "$RUNNER_TEMP/estate-secrets"
-            printf '%s' "$SEED_LANGSMITH_API_KEY" | SECRET_REPLACE=1 "$RUNNER_TEMP/estate-secrets/scripts/secret-add" dev LANGSMITH_API_KEY LANGSMITH_API_KEY
-            git -C "$RUNNER_TEMP/estate-secrets" add secrets/dev/LANGSMITH_API_KEY.yaml
-            if git -C "$RUNNER_TEMP/estate-secrets" -c user.name=vault-seed -c user.email=vault-seed@users.noreply.github.com \
-                 commit -q -m "langsmith api key (vault-seed run $GITHUB_RUN_ID)" 2>/dev/null; then
-              GH_TOKEN="$tok" git -C "$RUNNER_TEMP/estate-secrets" push -q origin HEAD || { echo "error   langsmith key written but the push to estate-secrets was refused"; exit 1; }
-              echo "ok      langsmith key delivered to estate-secrets as LANGSMITH_API_KEY"
-            else
-              echo "info    langsmith key unchanged in estate-secrets"
-            fi
             rm -rf "$RUNNER_TEMP/estate-secrets"
           fi
           rm -f "$ESTATE_ENV_FILE"
diff --git a/backstage/packages/app/src/global.d.ts b/backstage/packages/app/src/global.d.ts
new file mode 100644
index 000000000..291f0dcea
--- /dev/null
+++ b/backstage/packages/app/src/global.d.ts
@@ -0,0 +1,7 @@
+interface BackstageAnalytics {
+  track(event: string, properties?: Record<string, unknown>): void;
+}
+
+interface Window {
+  __BACKSTAGE_ANALYTICS__?: BackstageAnalytics;
+}
diff --git a/backstage/templates/founder-actions/vault-seed/template.yaml b/backstage/templates/founder-actions/vault-seed/template.yaml
index 702f84ce9..ced300e74 100644
--- a/backstage/templates/founder-actions/vault-seed/template.yaml
+++ b/backstage/templates/founder-actions/vault-seed/template.yaml
@@ -33,7 +33,6 @@ spec:
         - hermes
         - science
         - router-rows
-        - langsmith
         default: all
       delete:
         title: schedule deletion of one vault entry by name (for a mis-named entry); the entry field is
diff --git a/docs/reference/security-policy.md b/docs/reference/security-policy.md
index 304185ce2..ee220cc22 100644
--- a/docs/reference/security-policy.md
+++ b/docs/reference/security-policy.md
@@ -62,6 +62,7 @@ PR alongside these.
 | `require-auto-reload` | Audit 2, Enforce 2 | Every workload restarts when its config changes | `edge` | `platform/edge/require-auto-reload.yaml` |
 | `require-availability` | Enforce | Founder-facing workloads survive losing one node | `scheduling` | `platform/scheduling/require-availability.yaml` |
 | `require-catalogue-entity` | Enforce | Everything that serves a port names its catalogue entity | `edge` | `platform/edge/require-catalogue-entity.yaml` |
+| `require-otel-endpoint` | Enforce | Every application workload declares an OTel endpoint | `edge` | `platform/edge/require-otel-endpoint.yaml` |
 | `require-priority-class` | Audit 1, Enforce 2 | Require a PriorityClass on platform workloads | `scheduling` | `platform/scheduling/require-priority-class.yaml` |
 | `require-registry-host` | Audit | Every image names its registry | `edge` | `platform/edge/require-registry-host.yaml` |
 | `secrets-not-from-env-vars` | Audit | Disallow Secrets from Env Vars in CEL expressions | `edge` | `platform/edge/kyverno-secrets-policy.yaml` |
diff --git a/platform/backstage/base/catalogue.yaml b/platform/backstage/base/catalogue.yaml
index 4e1fb71ae..c796e34b0 100644
--- a/platform/backstage/base/catalogue.yaml
+++ b/platform/backstage/base/catalogue.yaml
@@ -138,13 +138,3 @@ spec:
             optional: false
         - name: tmp
           emptyDir: {}
----
-apiVersion: policy/v1
-kind: PodDisruptionBudget
-metadata:
-  name: catalogue
-spec:
-  minAvailable: 2   # Layer 4 Prevention: keep 2 pods up even during node drains
-  selector:
-    matchLabels:
-      app.kubernetes.io/name: catalogue
diff --git a/sovereign/tests/bdd/test_gate_vault_seed.py b/sovereign/tests/bdd/test_gate_vault_seed.py
index 3b791980d..3a235428b 100644
--- a/sovereign/tests/bdd/test_gate_vault_seed.py
+++ b/sovereign/tests/bdd/test_gate_vault_seed.py
@@ -64,7 +64,6 @@ def _dispatch(state: dict) -> None:
         "hermes",
         "science",
         "router-rows",
-        "langsmith",
     ]
 
 
diff --git a/tests/test_crew539_dod1_prometheus_alertmanager_page_the_founder.py b/tests/test_crew539_dod1_prometheus_alertmanager_page_the_founder.py
index da31edd23..5757cb9e5 100644
--- a/tests/test_crew539_dod1_prometheus_alertmanager_page_the_founder.py
+++ b/tests/test_crew539_dod1_prometheus_alertmanager_page_the_founder.py
@@ -95,6 +95,7 @@ def test_flux_row_substitutes_the_zone_and_waits_on_both_releases():
         "capacity.yaml",
         "canary-servicemonitor.yaml",
         "api-key-warden.yaml",
+        "kyverno.yaml",
     }  # capacity.yaml: crew#645 CP5; K8sGPT findings PrometheusRule, idp#696;
     # canary-servicemonitor.yaml: the verification canary's gauge, crew#656 CP4;
     # api-key-warden.yaml: VendorKeyInvalid/Unchecked/JobFailed alerts, crew#832 CP3
