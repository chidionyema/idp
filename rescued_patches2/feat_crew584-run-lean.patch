diff --git a/clusters/oke/alerts.yaml b/clusters/oke/alerts.yaml
index f7cd76263..302ec6c15 100644
--- a/clusters/oke/alerts.yaml
+++ b/clusters/oke/alerts.yaml
@@ -6,6 +6,7 @@ metadata:
   name: alerts
   namespace: flux-system
 spec:
+  suspend: true
   interval: 10m
   dependsOn:
     - name: alerts-secret
@@ -28,6 +29,7 @@ metadata:
   name: alerts-secret
   namespace: flux-system
 spec:
+  suspend: true
   interval: 10m
   dependsOn:
     - name: secret-store
@@ -49,6 +51,7 @@ metadata:
   name: alerts-github
   namespace: flux-system
 spec:
+  suspend: false
   interval: 10m
   dependsOn:
     - name: secret-store
diff --git a/clusters/oke/platform.yaml b/clusters/oke/platform.yaml
index a52231b8d..60beaaee1 100644
--- a/clusters/oke/platform.yaml
+++ b/clusters/oke/platform.yaml
@@ -69,6 +69,7 @@ metadata:
   name: chaos-mesh
   namespace: flux-system
 spec:
+  suspend: true
   interval: 1m
   # Platform rows never wait on the portal (crew#539, 2026-08-28: one stalled catalogue rollout held
   # 15 rows — observability, spire, temporal, keda, monitoring, science — behind Deployment/backstage/catalogue).
@@ -96,6 +97,7 @@ metadata:
   name: chaos
   namespace: flux-system
 spec:
+  suspend: true
   interval: 1m
   dependsOn:
     - name: chaos-mesh
@@ -119,6 +121,7 @@ metadata:
   name: observability-collector
   namespace: flux-system
 spec:
+  suspend: false
   # crew#584 traces/lean: the node agent alone. Enterprise adds the `observability` row (SigNoz
   # store + Langfuse) on top; lean points otelCollectorEndpoint at an external OTLP endpoint.
   interval: 1m
@@ -229,6 +232,7 @@ metadata:
   name: healthchecks
   namespace: flux-system
 spec:
+  suspend: true
   interval: 1m
   sourceRef:
     kind: GitRepository
@@ -426,6 +430,7 @@ metadata:
   name: temporal
   namespace: flux-system
 spec:
+  suspend: true
   interval: 1m
   dependsOn:
     - name: edge
@@ -649,6 +654,7 @@ metadata:
   name: healing
   namespace: flux-system
 spec:
+  suspend: true
   interval: 1m
   dependsOn:
     - name: scheduling
@@ -679,6 +685,7 @@ metadata:
   name: healing-analyzer
   namespace: flux-system
 spec:
+  suspend: true
   interval: 1m
   dependsOn:
     - name: healing
@@ -704,6 +711,7 @@ metadata:
   name: robusta
   namespace: flux-system
 spec:
+  suspend: true
   interval: 1m
   dependsOn:
     - name: secret-store
@@ -729,6 +737,7 @@ metadata:
   name: monitoring
   namespace: flux-system
 spec:
+  suspend: false
   interval: 1m
   dependsOn:
     - name: edge
@@ -779,6 +788,7 @@ metadata:
   name: monitoring-rules
   namespace: flux-system
 spec:
+  suspend: false
   interval: 1m
   dependsOn:
     - name: monitoring
@@ -802,6 +812,7 @@ metadata:
   name: drills
   namespace: flux-system
 spec:
+  suspend: false
   interval: 1m
   sourceRef:
     kind: GitRepository
