diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 409553743..43ff4feb0 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -17,6 +17,19 @@ jobs:
     steps:
       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
         with: { fetch-depth: 0 }   # bin/law32-gate diffs the PR against origin/main
+      # crew#539, 2026-08-28: bin/idp-kyverno-render judges renders against this estate's
+      # ClusterPolicies, and two of them -- no-optional-secret-references and
+      # money-rail-single-writer -- live in prospector, not here. CI had no copy, so it silently
+      # judged 24 of 26 policies and called platform/robusta clean; admission then refused the
+      # same Deployment, the release went Failed, and monitoring stayed down 8h. The repo is
+      # public, so the default token reads it and no secret is involved. The script is BLIND
+      # (never a pass) without this checkout.
+      - name: estate ClusterPolicies (outside the workspace on purpose)
+        # multiarch-gate and the other repo-root sweeps walk the filesystem, not git, so a tree
+        # parked under $GITHUB_WORKSPACE makes idp CI grade prospector's shell scripts, and
+        # .gitignore does not help a gate that globs. actions/checkout refuses a path outside the
+        # workspace, so this is a clone. The repo is public: no credential, no secret.
+        run: git clone --depth 1 https://github.com/chidionyema/prospector "$RUNNER_TEMP/kyverno-policies"
       - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
         with: { python-version: "3.12" }
       - name: tools
@@ -37,6 +50,7 @@ jobs:
           "$HOME/.temporalio/bin/temporal" --version
       - run: bin/idp-ci
         env:
+          IDP_KYVERNO_POLICIES: ${{ runner.temp }}/kyverno-policies/deploy/k8s/policies
           MIGRATE_HEALTH_WAIT: "240"
 
   # crew#286: the Estate-as-Platform operating model as a gate. One job, one script, the
@@ -78,6 +92,19 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
+      # crew#539, 2026-08-28: bin/idp-kyverno-render judges renders against this estate's
+      # ClusterPolicies, and two of them -- no-optional-secret-references and
+      # money-rail-single-writer -- live in prospector, not here. CI had no copy, so it silently
+      # judged 24 of 26 policies and called platform/robusta clean; admission then refused the
+      # same Deployment, the release went Failed, and monitoring stayed down 8h. The repo is
+      # public, so the default token reads it and no secret is involved. The script is BLIND
+      # (never a pass) without this checkout.
+      - name: estate ClusterPolicies (outside the workspace on purpose)
+        # multiarch-gate and the other repo-root sweeps walk the filesystem, not git, so a tree
+        # parked under $GITHUB_WORKSPACE makes idp CI grade prospector's shell scripts, and
+        # .gitignore does not help a gate that globs. actions/checkout refuses a path outside the
+        # workspace, so this is a clone. The repo is public: no credential, no secret.
+        run: git clone --depth 1 https://github.com/chidionyema/prospector "$RUNNER_TEMP/kyverno-policies"
       - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
         with: { python-version: "3.12" }
       - name: tools
@@ -95,10 +122,17 @@ jobs:
       - name: acceptance suite (strict on PRs to main)
         env:
           SB_BDD_STRICT: ${{ github.base_ref == 'main' && '1' || '0' }}
+          # test_gate_langfuse_render and test_gate_llm_admission run bin/idp-kyverno-render for
+          # real, so this step is a judging step and needs the estate-only policies like the
+          # others. Without it the script is BLIND (rc 2), which is the correct verdict and a
+          # failing one -- crew#539 is the 8h outage that happened when it was a pass instead.
+          IDP_KYVERNO_POLICIES: ${{ runner.temp }}/kyverno-policies/deploy/k8s/policies
         run: |
           echo "base_ref=${{ github.base_ref }} SB_BDD_STRICT=$SB_BDD_STRICT"
           cd sovereign && python -m pytest tests/bdd -q -p no:cacheprovider
       - name: ruff (crew#325 step 3; the rule set is pyproject.toml)
         run: pip install -q ruff==0.15.18 && ruff check .
       - name: pytest tests (crew#325 step 2, every file under tests/ in one run)
+        env:
+          IDP_KYVERNO_POLICIES: ${{ runner.temp }}/kyverno-policies/deploy/k8s/policies
         run: python -m pytest tests -q -p no:cacheprovider
diff --git a/.gitignore b/.gitignore
index e52161e32..f7397d928 100644
--- a/.gitignore
+++ b/.gitignore
@@ -52,3 +52,5 @@ platform/**/backend_override.tf
 platform/oci/identity/backend_override.tf
 platform/oci/identity/.terraform*
 platform/oci/identity/*.tfstate*
+
+# CI checks prospector's ClusterPolicies out here so idp-kyverno-render judges all 26 (crew#539).
diff --git a/bin/idp-ci b/bin/idp-ci
index f6b2c9d64..7a32238af 100755
--- a/bin/idp-ci
+++ b/bin/idp-ci
@@ -556,9 +556,21 @@ if command -v kyverno >/dev/null && command -v helm >/dev/null; then
   # Deployment was never judged and the cluster refused it after merge; crew#539 2026-08-28: the
   # robusta release carried no postRenderers, was never judged, and admission refused it on 15 rules).
   dirs=$(cd "$IDP" && { grep -lE '^kind: HelmRelease' platform/*/*.yaml platform/*/*/*.yaml 2>/dev/null | xargs -n1 dirname | sort -u | while read -r d; do [ -f "$d/kustomization.yaml" ] && echo "$d"; done; grep -lE '^kind: (Deployment|StatefulSet|DaemonSet|Job|CronJob)$' platform/*/*.yaml 2>/dev/null | xargs -n1 dirname | sort -u | while read -r d; do [ -f "$d/kustomization.yaml" ] && echo "$d"; done; } | sort -u)
-  if [ -n "$dirs" ] && (cd "$IDP" && bin/idp-kyverno-render $dirs) && ! (cd "$IDP" && bin/idp-kyverno-render tests/fixtures/kyverno/must-fail >/dev/null 2>&1); then
-    say "ok    kyverno  $(echo "$dirs" | tr '\n' ' ')render clean; the unpatched-chart fixture is refused"
-  else say "FAIL  kyverno  a HelmRelease render fails admission policy, or the must-fail fixture passed"; fail=1; fi
+  # rc 2 is BLIND: the judge ran but not against the whole policy set, and crew#539 is what that
+  # costs -- CI printed "render clean" for a Deployment admission refused, because the two
+  # estate-only policies live in prospector and CI had no copy. Blind and failing are different
+  # facts and the operator needs to be told which one this is.
+  kyv=$( (cd "$IDP" && bin/idp-kyverno-render $dirs) 2>&1 ); kyv_rc=$?
+  if [ -z "$dirs" ]; then say "FAIL  kyverno  no platform dir shipped a HelmRelease or a workload; the judge had nothing"; fail=1
+  elif [ "$kyv_rc" = 2 ]; then
+    say "BLIND kyverno  the judge did not see the whole policy set; no render was judged"
+    printf '%s\n' "$kyv" | grep '^BLIND' | sed 's/^/      /'; fail=1
+  elif [ "$kyv_rc" != 0 ]; then
+    say "FAIL  kyverno  a HelmRelease or workload render fails admission policy"
+    printf '%s\n' "$kyv" | grep -E '^(FAIL|      )' | sed 's/^/      /'; fail=1
+  elif (cd "$IDP" && bin/idp-kyverno-render tests/fixtures/kyverno/must-fail >/dev/null 2>&1); then
+    say "FAIL  kyverno  the must-fail fixture passed; the judge admits an unpatched chart"; fail=1
+  else say "ok    kyverno  $(echo "$dirs" | tr '\n' ' ')render clean; the unpatched-chart fixture is refused"; fi
 else say "BLIND kyverno  helm or kyverno CLI not installed; HelmRelease renders were not judged"; fail=1; fi
 
 # 9b. crew#483: Flux post-build substitution judged with the tool Flux runs. Every Kustomization
diff --git a/bin/idp-kyverno-render b/bin/idp-kyverno-render
index c1c9315d3..f025bc158 100755
--- a/bin/idp-kyverno-render
+++ b/bin/idp-kyverno-render
@@ -23,6 +23,21 @@ kubectl kustomize "$src" > "$S/policies.yaml"
 n=$(grep -c '^kind: ClusterPolicy' "$S/policies.yaml")
 echo "policies  $n ClusterPolicies from $src"
 
+# The fallback set is the pinned upstream library and nothing else. The two policies this estate
+# wrote itself -- no-optional-secret-references and money-rail-single-writer -- live in the
+# prospector checkout, and they are the two that encode incidents this estate actually had, so a
+# verdict reached without them is the one verdict that cannot be trusted.
+# crew#539, 2026-08-28: CI has never had that checkout. It judged platform/robusta against 24
+# policies, printed "render clean", and the cluster then refused the same Deployment on
+# no-optional-secret-references -- the release went Failed, Flux could not remediate it, and
+# monitoring and monitoring-rules stayed down for 8h. BLIND, never a pass (LAW 28).
+for want in no-optional-secret-references money-rail-single-writer; do
+  grep -q "^  name: $want$" "$S/policies.yaml" && continue
+  echo "BLIND  policies  $want is not in the set from $src; the estate-only policies were not"
+  echo "                 judged. Point IDP_KYVERNO_POLICIES at prospector deploy/k8s/policies."
+  exit 2
+done
+
 # PolicyExceptions are handed to the CLI as admission sees them: Kyverno honours only the ones in
 # namespace kyverno (platform/edge/kyverno.yaml features.policyExceptions), wherever in the estate
 # they are declared, so every path a clusters/oke Kustomization points at is rendered once here.
diff --git a/platform/robusta/robusta.yaml b/platform/robusta/robusta.yaml
index 57c9b38ac..acaa5f662 100644
--- a/platform/robusta/robusta.yaml
+++ b/platform/robusta/robusta.yaml
@@ -147,3 +147,25 @@ spec:
                       - name: runner
                         readinessProbe: { httpGet: { path: /healthz, port: 5000 }, periodSeconds: 15 }
                         livenessProbe: { httpGet: { path: /healthz, port: 5000 }, initialDelaySeconds: 60, periodSeconds: 30, failureThreshold: 5 }
+                        # crew#539, 2026-08-28: chart 0.48.0 marks three secret references
+                        # `optional: true`, and `no-optional-secret-references` (the 2026-08-24
+                        # incident written as a rule) refuses every one. Admission denied the
+                        # upgrade, the release went Failed, and Flux could not remediate it
+                        # ("missing target release for rollback"), which held monitoring and
+                        # monitoring-rules down. The rule is right, so nothing is excused here:
+                        # the two secrets the chart itself renders are declared required, and
+                        # the third is not a secret at all.
+                        envFrom:
+                          - secretRef: { name: robusta-runner-secret }
+                    volumes:
+                      # The chart renders robusta-playbooks-config-secret, so it is required.
+                      - name: playbooks-config-secret
+                        secret: { secretName: robusta-playbooks-config-secret, optional: null }
+                      # robusta-auth-config-secret is rendered by nothing in this estate and
+                      # created by nothing: the Robusta UI is off (enableHolmesGPT false, the
+                      # only sink is telegram). Today the kubelet mounts an empty directory at
+                      # /etc/robusta/auth because the reference is optional; an emptyDir mounts
+                      # the same empty directory, and refers to no secret to be optional about.
+                      - name: auth-config-secret
+                        secret: null
+                        emptyDir: {}
diff --git a/tests/fixtures/kyverno/robusta-chart/runner.yaml b/tests/fixtures/kyverno/robusta-chart/runner.yaml
new file mode 100644
index 000000000..be336aaa8
--- /dev/null
+++ b/tests/fixtures/kyverno/robusta-chart/runner.yaml
@@ -0,0 +1,31 @@
+# robusta chart 0.48.0, the parts of Deployment/robusta-runner that carry a secret
+# reference. Vendored so the postRenderer patch can be proved offline; the CI kyverno
+# rung renders the real chart and catches drift (crew#539).
+apiVersion: apps/v1
+kind: Deployment
+metadata:
+  name: robusta-runner
+  namespace: robusta
+spec:
+  template:
+    spec:
+      containers:
+      - name: runner
+        envFrom:
+        - secretRef:
+            name: robusta-runner-secret
+            optional: true
+        volumeMounts:
+        - name: auth-config-secret
+          mountPath: /etc/robusta/auth
+        - name: playbooks-config-secret
+          mountPath: /etc/robusta/config
+      volumes:
+      - name: playbooks-config-secret
+        secret:
+          secretName: robusta-playbooks-config-secret
+          optional: true
+      - name: auth-config-secret
+        secret:
+          secretName: robusta-auth-config-secret
+          optional: true
diff --git a/tests/test_incident_crew539_kyverno_judged_24_of_26_policies.py b/tests/test_incident_crew539_kyverno_judged_24_of_26_policies.py
new file mode 100644
index 000000000..7b30cf5c5
--- /dev/null
+++ b/tests/test_incident_crew539_kyverno_judged_24_of_26_policies.py
@@ -0,0 +1,160 @@
+"""Incident 2026-08-28 (crew#539): `robusta/robusta` sat Ready=False for 8h with
+`admission webhook "validate.kyverno.svc-fail" denied the request` on Deployment/robusta-runner,
+and Flux could not remediate it (`missing target release for rollback`), which held
+`monitoring` and `monitoring-rules` down behind it.
+
+Two defects, one incident.
+
+The manifest: chart robusta 0.48.0 marks three secret references `optional: true`, and
+`no-optional-secret-references` -- the 2026-08-24 incident written as a rule -- refuses every one.
+Nothing here is excused: the two secrets the chart itself renders are declared required, and the
+third (`robusta-auth-config-secret`, which nothing in this estate creates -- the Robusta UI is off)
+stops being a secret reference at all and becomes the empty directory the kubelet was mounting for
+it anyway.
+
+The judge: `bin/idp-kyverno-render` falls back to the pinned upstream library when prospector's
+checkout is absent, and CI never had that checkout. So CI judged 24 of 26 policies -- missing
+exactly the two this estate wrote from its own incidents -- and printed `render clean` for the
+Deployment admission then refused. Rule (rung 4): the judge is BLIND, never a pass, when the
+estate-only policies are not in the set, and CI checks them out so it is not blind.
+"""
+import os
+import re
+import shutil
+import subprocess
+from pathlib import Path
+
+import pytest
+import yaml
+
+ROOT = Path(__file__).resolve().parents[1]
+ESTATE_ONLY = ("no-optional-secret-references", "money-rail-single-writer")
+
+
+def _kustomize(tmp_path: Path, resource: Path, patches: list) -> list:
+    (tmp_path / "runner.yaml").write_text(resource.read_text())
+    (tmp_path / "kustomization.yaml").write_text(yaml.safe_dump(
+        {"apiVersion": "kustomize.config.k8s.io/v1beta1", "kind": "Kustomization",
+         "resources": ["runner.yaml"], "patches": patches}))
+    out = subprocess.run(["kubectl", "kustomize", str(tmp_path)],
+                         capture_output=True, text=True, check=True).stdout
+    return [d for d in yaml.safe_load_all(out) if d]
+
+
+def _runner_patches() -> list:
+    docs = [d for d in yaml.safe_load_all((ROOT / "platform/robusta/robusta.yaml").read_text()) if d]
+    hr = next(d for d in docs if d["kind"] == "HelmRelease")
+    return [p for pr in hr["spec"]["postRenderers"] for p in pr["kustomize"]["patches"]
+            if p["target"].get("name") == "robusta-runner"]
+
+
+@pytest.mark.skipif(not shutil.which("kubectl"), reason="kubectl builds the postRenderer patch")
+def test_the_postrenderer_leaves_no_optional_secret_reference(tmp_path: Path) -> None:
+    """The chart's three `optional: true` references, put through the release's own patches."""
+    fixture = ROOT / "tests/fixtures/kyverno/robusta-chart/runner.yaml"
+    assert "optional: true" in fixture.read_text(), "the fixture must still carry what we fix"
+    dep = _kustomize(tmp_path, fixture, _runner_patches())[0]
+    spec = dep["spec"]["template"]["spec"]
+
+    assert spec["containers"][0]["envFrom"] == [{"secretRef": {"name": "robusta-runner-secret"}}]
+    vols = {v["name"]: v for v in spec["volumes"]}
+    assert vols["playbooks-config-secret"]["secret"] == {"secretName": "robusta-playbooks-config-secret"}
+    # Nothing creates robusta-auth-config-secret, so requiring it would deadlock the pod; the
+    # kubelet already mounts an empty directory there and an emptyDir mounts the same one.
+    assert vols["auth-config-secret"] == {"name": "auth-config-secret", "emptyDir": {}}
+    assert "optional" not in yaml.safe_dump(dep)
+
+
+@pytest.mark.skipif(not shutil.which("kubectl"), reason="kubectl builds the postRenderer patch")
+def test_the_auth_directory_is_still_mounted(tmp_path: Path) -> None:
+    """LAW 38 control: the fix must not remove a path the runner reads."""
+    dep = _kustomize(tmp_path, ROOT / "tests/fixtures/kyverno/robusta-chart/runner.yaml", _runner_patches())[0]
+    mounts = dep["spec"]["template"]["spec"]["containers"][0]["volumeMounts"]
+    assert {"name": "auth-config-secret", "mountPath": "/etc/robusta/auth"} in mounts
+
+
+@pytest.mark.skipif(not (shutil.which("kyverno") and shutil.which("helm") and shutil.which("kubectl")),
+                    reason="the judge needs helm, kubectl and the kyverno CLI")
+def test_the_judge_is_blind_not_green_without_the_estate_policies(tmp_path: Path) -> None:
+    """The defect itself: with only the upstream library the judge used to print `render clean`."""
+    env = {**os.environ, "IDP_KYVERNO_POLICIES": str(tmp_path / "absent")}
+    r = subprocess.run([str(ROOT / "bin/idp-kyverno-render"), "platform/robusta"],
+                       cwd=ROOT, capture_output=True, text=True, env=env)
+    assert r.returncode == 2, f"a partial policy set must be BLIND, got rc={r.returncode}\n{r.stdout}"
+    assert "BLIND  policies" in r.stdout, r.stdout + r.stderr
+    assert ESTATE_ONLY[0] in r.stdout
+    assert "ok    render" not in r.stdout, "nothing may be graded once the set is known short"
+
+
+def _judging_steps(job: dict) -> list:
+    """Steps that actually invoke the judge, found by the command, not by the step's name.
+
+    The first version of this guard matched the whole `run:` string against "bin/idp-ci" or the
+    exact text "pytest tests", so the bdd job -- which spells it `cd sovereign && python -m pytest
+    tests/bdd`, and whose test_gate_llm_admission shells bin/idp-kyverno-render for real -- was not
+    counted as judging. It had no policies, the judge went BLIND, and this test stayed green while
+    CI went red: the same blindness the PR closes, one level up.
+    """
+    out = []
+    for s in job.get("steps", []):
+        for line in str(s.get("run", "")).splitlines():
+            line = line.strip()
+            if line.startswith("pip ") or line.startswith("curl "):
+                continue
+            if re.search(r"(^|&&\s*)(cd \S+ && )?(python -m pytest|pytest |bin/idp-ci\b)", line):
+                out.append(s)
+                break
+    return out
+
+
+def test_every_step_that_runs_the_judge_is_pointed_at_the_estate_policies() -> None:
+    """And the reason it was blind for months: no job ever had prospector's policies."""
+    wf = yaml.safe_load((ROOT / ".github/workflows/ci.yml").read_text())
+    judging = {name: _judging_steps(job) for name, job in wf["jobs"].items()}
+    judging = {n: s for n, s in judging.items() if s}
+    assert judging, "no job runs the judge at all"
+    for name, steps in judging.items():
+        clones = [str(s.get("run", "")) for s in wf["jobs"][name]["steps"]]
+        assert any("chidionyema/prospector" in c for c in clones), \
+            f"job {name} judges with no estate policies"
+        for s in steps:
+            env = (s.get("env") or {}).get("IDP_KYVERNO_POLICIES", "")
+            assert "kyverno-policies/deploy/k8s/policies" in env, \
+                f"job {name} step {s.get('name', s.get('run'))!r} runs the judge blind"
+
+
+def test_the_policies_are_never_cloned_into_the_workspace() -> None:
+    """offline-gate went red the first time this landed: multiarch-gate walks the filesystem, not
+    git, so prospector's deploy/targets/k8s.sh became an idp CI finding. A .gitignore entry does
+    not help a gate that globs -- the tree has to be somewhere the sweeps do not reach."""
+    wf = yaml.safe_load((ROOT / ".github/workflows/ci.yml").read_text())
+    for name, job in wf["jobs"].items():
+        for s in job.get("steps", []):
+            run = str(s.get("run", ""))
+            if "chidionyema/prospector" in run:
+                assert "$RUNNER_TEMP/" in run, f"job {name} clones prospector into the workspace"
+            with_ = s.get("with") or {}
+            assert with_.get("repository") != "chidionyema/prospector", \
+                f"job {name} uses actions/checkout, which cannot place it outside the workspace"
+
+
+def test_the_guard_names_both_estate_policies() -> None:
+    """A third estate-only policy added later must be added here too, or it is judged by nobody."""
+    guard = (ROOT / "bin/idp-kyverno-render").read_text()
+    for want in ESTATE_ONLY:
+        assert want in guard, f"{want} is not in the BLIND check"
+
+
+def test_the_auth_emptydir_is_only_honest_while_the_robusta_ui_is_off() -> None:
+    """The emptyDir replaces a secret the chart renders only when the UI is on. Turn the UI on and
+    this patch silently keeps mounting an empty directory at /etc/robusta/auth: an unauthenticated
+    runner, no denial, no crash, no alert. A comment 120 lines from the value it depends on is not
+    a guard, so the values it rests on are asserted here (code-2d, review 5449915505)."""
+    doc = [d for d in yaml.safe_load_all((ROOT / "platform/robusta/robusta.yaml").read_text())
+           if d and d.get("kind") == "HelmRelease"][0]
+    values = doc["spec"]["values"]
+    assert values["enableHolmesGPT"] is False, \
+        "the UI is on: robusta-auth-config-secret is now real and the emptyDir must go"
+    sinks = [next(iter(s)) for s in values["sinksConfig"]]
+    assert sinks == ["telegram_sink"], \
+        f"a sink other than telegram is configured ({sinks}); re-check whether the UI is reached"
