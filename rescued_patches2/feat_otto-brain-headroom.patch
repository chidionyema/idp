diff --git a/bin/idp-vendor-render b/bin/idp-vendor-render
index 9e8f20645..1a1a14d13 100755
--- a/bin/idp-vendor-render
+++ b/bin/idp-vendor-render
@@ -129,6 +129,14 @@ def seed(vendors: dict) -> str:
         for t in v.get("targets") or []:
             if not t.get("bw") or not t.get("ns") or t.get("derived"):
                 continue
+            # No `entry:` means the vendor has no vault copy on purpose, so there is nothing
+            # to seed Bitwarden FROM and nothing that could become a stale second key. MiniMax
+            # is the reason the field is optional: its vault copies held a revoked key while
+            # the human door held the live one, and only the consumers that read the vault
+            # alone could see it (2026-09-10; the measurement is on the minimax block of
+            # platform/vendors/consoles.yaml).
+            if not t.get("entry"):
+                continue
             # Keyed by (namespace, name), not by name: one Bitwarden secret is often held in
             # more than one place -- TELEGRAM_ALERTS_BOT_TOKEN sits in flux-system/flux-telegram
             # and in notify/notify-apprise-founder-telegram. Seeding from every holder costs
diff --git a/platform/human-vault-bridge/externalsecrets.yaml b/platform/human-vault-bridge/externalsecrets.yaml
index da45181ce..d88d8ad5c 100644
--- a/platform/human-vault-bridge/externalsecrets.yaml
+++ b/platform/human-vault-bridge/externalsecrets.yaml
@@ -155,6 +155,19 @@ spec:
 ---
 apiVersion: external-secrets.io/v1
 kind: ExternalSecret
+metadata:
+  name: human-minimax
+  namespace: otto-gateway
+spec:
+  refreshInterval: 1m
+  secretStoreRef: { kind: ClusterSecretStore, name: human-vault }
+  target: { name: human-minimax, creationPolicy: Owner }
+  data:
+    - secretKey: MINIMAX_API_KEY
+      remoteRef: { key: MINIMAX_API_KEY }
+---
+apiVersion: external-secrets.io/v1
+kind: ExternalSecret
 metadata:
   name: human-minimax
   namespace: prospector
diff --git a/platform/human-vault-bridge/pushsecrets.yaml b/platform/human-vault-bridge/pushsecrets.yaml
index d9c0f09ef..db5eed13e 100644
--- a/platform/human-vault-bridge/pushsecrets.yaml
+++ b/platform/human-vault-bridge/pushsecrets.yaml
@@ -141,24 +141,6 @@ spec:
 ---
 apiVersion: external-secrets.io/v1alpha1
 kind: PushSecret
-metadata:
-  name: seed-minimax-api-key
-  namespace: llm
-spec:
-  refreshInterval: 1h
-  updatePolicy: IfNotExists
-  deletionPolicy: None
-  secretStoreRefs:
-    - { kind: ClusterSecretStore, name: human-vault }
-  selector:
-    secret: { name: litellm-upstream }
-  data:
-    - match:
-        secretKey: MINIMAX_API_KEY
-        remoteRef: { remoteKey: MINIMAX_API_KEY }
----
-apiVersion: external-secrets.io/v1alpha1
-kind: PushSecret
 metadata:
   name: seed-moonshot-api-key
   namespace: llm
@@ -303,24 +285,6 @@ spec:
 ---
 apiVersion: external-secrets.io/v1alpha1
 kind: PushSecret
-metadata:
-  name: seed-minimax-api-key
-  namespace: prospector
-spec:
-  refreshInterval: 1h
-  updatePolicy: IfNotExists
-  deletionPolicy: None
-  secretStoreRefs:
-    - { kind: ClusterSecretStore, name: human-vault }
-  selector:
-    secret: { name: prospector-engine-env }
-  data:
-    - match:
-        secretKey: MINIMAX_API_KEY
-        remoteRef: { remoteKey: MINIMAX_API_KEY }
----
-apiVersion: external-secrets.io/v1alpha1
-kind: PushSecret
 metadata:
   name: seed-openrouter-api-key
   namespace: prospector
diff --git a/platform/otto-gateway/deployment.yaml b/platform/otto-gateway/deployment.yaml
index 66b0ec49e..17a298811 100644
--- a/platform/otto-gateway/deployment.yaml
+++ b/platform/otto-gateway/deployment.yaml
@@ -331,9 +331,30 @@ spec:
             - { name: HTTP_PROXY, value: "http://127.0.0.1:1055" }
             - { name: HTTPS_PROXY, value: "http://127.0.0.1:1055" }
             - { name: NO_PROXY, value: "127.0.0.1,localhost,api.minimax.io,.svc,.svc.cluster.local,.cluster.local,10.0.0.0/8" }
+          # 2026-09-10: 512Mi was not a tight fit, it was the wrong order of magnitude, and
+          # it kept Otto on one home for a day. The lifeboat never reached its first request
+          # -- `exitCode 137 OOMKilled` at 26 seconds (started 09:49:25Z, killed 09:49:51Z),
+          # five times on one ReplicaSet and thirteen on another, while the old
+          # single-container pod otto-gateway-7d8b47fc79-b2fwm kept answering with home 1 and
+          # nothing else. A lifeboat that cannot finish importing is not a lifeboat.
+          #
+          # The number has a receipt rather than a guess: the SAME binary at the SAME version
+          # elsewhere in this estate, measured 2026-09-10, `kubectl top pod -n llm
+          # --containers` -- litellm-77f5fbf88d-vnlkl 1417Mi, litellm-77f5fbf88d-zk66s 956Mi.
+          # Those replicas carry a Postgres, a Redis, callbacks and a far longer model_list
+          # than this one does, so the lifeboat should sit under them; the ceiling is set
+          # ABOVE the higher of the two anyway, because the cost of guessing low is another
+          # crash loop and another day on one home, and the cost of guessing high is limit
+          # headroom on a node whose limits are already 174% overcommitted and whose requests
+          # are what the scheduler actually counts.
+          #
+          # cpu 500m -> 1 for the same reason in the other direction: the startupProbe allows
+          # 120s (24 x 5s) and a throttled litellm import can spend most of it. The request
+          # stays 50m -- both nodes are at 95% of requested cpu, and the steady draw measured
+          # on those same two replicas is 83m and 58m.
           resources:
-            requests: { cpu: 50m, memory: 256Mi }
-            limits: { cpu: 500m, memory: 512Mi }
+            requests: { cpu: 50m, memory: 384Mi }
+            limits: { cpu: 1, memory: 1536Mi }
           securityContext:
             allowPrivilegeEscalation: false
             readOnlyRootFilesystem: true
@@ -443,7 +464,11 @@ spec:
         # Home 2's key. Optional so a vault entry that is late costs Otto home 2 and leaves
         # homes 1 and 3 answering, rather than holding the whole door closed.
         - name: brain-secrets
-          secret: { secretName: otto-gateway-direct, optional: true }
+          # human-minimax, not otto-gateway-direct: the vault copy of this key was revoked at
+          # the vendor and the founder's live key only ever reaches the cluster through the
+          # Bitwarden bridge (2026-09-10, the note in three-homes-secrets.yaml). `optional`
+          # stays: a missing key must leave home 2 unusable, never keep the door from starting.
+          secret: { secretName: human-minimax, optional: true }
         - name: tailscale-authkey
           secret: { secretName: otto-gateway-tailscale, optional: true }
         # Step 5: the fork's env dir, one file per credential name. The MCP key is optional so a
diff --git a/platform/otto-gateway/quota.yaml b/platform/otto-gateway/quota.yaml
index 0a7da078d..b46f5b223 100644
--- a/platform/otto-gateway/quota.yaml
+++ b/platform/otto-gateway/quota.yaml
@@ -12,7 +12,13 @@ spec:
     - type: Container
       default: { cpu: 200m, memory: 256Mi }
       defaultRequest: { cpu: 50m, memory: 128Mi }
-      max: { cpu: 500m, memory: 512Mi }
+      # 2026-09-10: this ceiling, not the container's own limits, is what actually held
+      # otto-brain at 512Mi -- a LimitRange max is a namespace-wide refusal, so raising the
+      # container alone would have been rejected at admission and the crash loop would have
+      # looked identical. Both move together or neither does. The value is the measured peak
+      # of the same litellm binary elsewhere in this estate (1417Mi, `kubectl top pod -n llm
+      # --containers`, 2026-09-10), rounded up.
+      max: { cpu: 1, memory: 1536Mi }
 ---
 apiVersion: v1
 kind: ResourceQuota
@@ -37,12 +43,27 @@ spec:
   #   limits.cpu       3 x 1000m = 3000m + 500m job = 3500m  -> 3500m
   #   limits.memory    3 x 1024Mi = 3072Mi + 512Mi  = 3584Mi -> 3584Mi
   #
+  # 2026-09-10, the OOM: the lifeboat sized above was given 512Mi and the litellm binary
+  # needs roughly three times that (measured 956Mi and 1417Mi on the two `llm` replicas of
+  # the same version, `kubectl top pod -n llm --containers`). otto-brain moves to 384Mi
+  # requested / 1536Mi limited and 1 cpu limited, so the pod's own arithmetic changes and
+  # this ceiling has to follow it -- a quota left at the old sum would refuse the fixed pod
+  # exactly as the LimitRange refused it, and Otto would still be on one home:
+  #
+  #   requests.cpu     3 x 150m   = 450m   + 50m job  = 500m   -> 700m   (unchanged)
+  #   requests.memory  3 x 640Mi  = 1920Mi + 128Mi    = 2048Mi -> 2304Mi
+  #   limits.cpu       3 x 1500m  = 4500m  + 500m job = 5000m  -> 5500m
+  #   limits.memory    3 x 2048Mi = 6144Mi + 512Mi    = 6656Mi -> 7168Mi
+  #
+  # per pod: gateway 50m/192Mi req, 400m/384Mi lim; otto-brain 50m/384Mi, 1/1536Mi;
+  # tailscale 50m/64Mi, 100m/128Mi. Two replicas plus one surge is the three.
+  #
   # Headroom on each line rather than the exact sum, for the reason the note above records:
   # a ceiling sized to the exact fit refuses the next correct thing, and LAW 38 calls a fence
   # that refuses correct work an outage.
   hard:
     requests.cpu: 700m
-    requests.memory: 1792Mi
-    limits.cpu: 3500m
-    limits.memory: 3584Mi
+    requests.memory: 2304Mi
+    limits.cpu: 5500m
+    limits.memory: 7168Mi
     pods: "8"
diff --git a/platform/otto-gateway/three-homes-secrets.yaml b/platform/otto-gateway/three-homes-secrets.yaml
index 1af820361..913476bb0 100644
--- a/platform/otto-gateway/three-homes-secrets.yaml
+++ b/platform/otto-gateway/three-homes-secrets.yaml
@@ -5,25 +5,26 @@
 # envFrom.secretRef on a pod (crew#341), and platform/llm/litellm.yaml already carries the same
 # shape for the same reason.
 ---
-# Home 2's key: Otto's own copy of the direct vendor line. Same vault entry the estate router
-# reads (platform/vendors/consoles.yaml:97, entry litellm-upstream, field MINIMAX_API_KEY), so
-# the founder rotating it in the console reaches the router and the lifeboat together -- one
-# rotation, not two, and no second place for a key to go stale (R52).
-apiVersion: external-secrets.io/v1
-kind: ExternalSecret
-metadata:
-  name: otto-gateway-direct
-  namespace: otto-gateway
-spec:
-  refreshInterval: 10m
-  secretStoreRef: { kind: ClusterSecretStore, name: estate-vault }
-  target:
-    name: otto-gateway-direct
-    creationPolicy: Owner
-  data:
-    - secretKey: MINIMAX_API_KEY
-      remoteRef: { key: litellm-upstream, property: MINIMAX_API_KEY }
----
+# Home 2's key is NOT here, and its absence is the fix for a dead second home (2026-09-10).
+#
+# This file used to carry an ExternalSecret `otto-gateway-direct` reading vault entry
+# `litellm-upstream`, field MINIMAX_API_KEY, and claimed that made rotation reach the router
+# and the lifeboat together. It did the opposite. MiniMax is a human-vault vendor: the founder
+# rotates it in Bitwarden Secrets Manager, and the vault copy was a second credential that was
+# seeded once and never rotated. Measured from inside the router pod against api.minimax.io on
+# 2026-09-10, values never printed:
+#
+#   vault  litellm-upstream  -> HTTP 401  {"message":"invalid api key (2049)"}
+#   human  bitwarden door    -> ANSWERED
+#
+# The router never noticed, because litellm.yaml exports the human mount after the vault mount
+# and the later export wins. Otto's lifeboat read the vault alone, so home 2 answered 401 on
+# every fall-through for as long as it has existed.
+#
+# So the key now arrives the one way it arrives everywhere else: Secret `human-minimax` in this
+# namespace, rendered into platform/human-vault-bridge/externalsecrets.yaml from the
+# `targets:` of the minimax vendor in platform/vendors/consoles.yaml. One key, one door, one
+# rotation -- and no file here to go stale, because there is no second copy to go stale.
 # The Tailscale device key for this pod, so home 3 has a road to the founder's laptop.
 # Identical mechanism to platform/hermes-agent/tailscale.yaml: the one estate Tailscale OAuth
 # client doubles as a TS_AUTHKEY (containerboot mints a fresh ephemeral device key from a
diff --git a/platform/vendors/consoles.yaml b/platform/vendors/consoles.yaml
index 846436bac..15dca190d 100644
--- a/platform/vendors/consoles.yaml
+++ b/platform/vendors/consoles.yaml
@@ -93,9 +93,30 @@ vendors:
     verify: { method: GET, url: https://api.minimax.io/v1/models, headers: { Authorization: "Bearer {key}" } }
     rotation: assisted  # MiniMax publishes no create-key API (R52)
     store_default: human-vault
+    # No `entry:` on any of these three rows, and that absence is the fix for a week of
+    # silent 401s (2026-09-10). MiniMax is a human-vault vendor: the founder rotates the key
+    # in Bitwarden Secrets Manager and the bridge carries it to the cluster within a minute.
+    # The vault entries beside it were a SECOND copy of the same credential, seeded once and
+    # never rotated again, and by 2026-09-10 they held a key the vendor had revoked:
+    #
+    #   llm/litellm-upstream/MINIMAX_API_KEY        -> 401 invalid api key (2049)
+    #   otto-gateway/otto-gateway-direct/...        -> 401 invalid api key (2049)
+    #   prospector/prospector-engine-env/...        -> 401 invalid api key (2049)
+    #   llm/human-minimax/MINIMAX_API_KEY           -> answered
+    #
+    # (measured from inside litellm-77f5fbf88d-vnlkl against api.minimax.io, values never
+    # printed, fingerprints only). The router survived because litellm.yaml exports the human
+    # mount last and the human door wins -- so the dead copy was invisible there and fatal
+    # anywhere that read the vault alone, which is exactly what Otto's lifeboat did. Founder,
+    # 2026-09-10: "we are using it now in prod so there must be 2 keys or ur wrong", then "get
+    # rid of dead one". A target with no `entry:` gets the bridge and no push-back, so there
+    # is one MiniMax key in this estate and one place it is rotated.
     targets:
-      - { entry: litellm-upstream, ns: llm, field: MINIMAX_API_KEY, bw: MINIMAX_API_KEY }
-      - { entry: prospector-engine-env, ns: prospector, field: MINIMAX_API_KEY, bw: MINIMAX_API_KEY }
+      - { ns: llm, field: MINIMAX_API_KEY, bw: MINIMAX_API_KEY }
+      - { ns: prospector, field: MINIMAX_API_KEY, bw: MINIMAX_API_KEY }
+      # Otto's home 2, the direct vendor line his lifeboat falls to when the estate router
+      # cannot answer. It read the vault copy and so had a dead second home all along.
+      - { ns: otto-gateway, field: MINIMAX_API_KEY, bw: MINIMAX_API_KEY }
     router:
       lanes: |
         # --- the moat's cheap primary --------------------------------------------------------
