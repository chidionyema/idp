diff --git a/.aevum/local.jsonl b/.aevum/local.jsonl
index 72c09b9d6..7e0da5a48 100644
--- a/.aevum/local.jsonl
+++ b/.aevum/local.jsonl
@@ -122,3 +122,4 @@
 {"seq":121,"ts":"2026-09-19T13:43:15Z","event":"commit.staged","tree":"d588f225cceacd5986abdd42c80682c3a7c1658e","author":"chidionyema333@gmail.com","staged":["platform/ns-fences/allowances.yaml","platform/ns-fences/network/demo-sandbox.yaml"],"payload_hash":"9b1801b870e01d57161e11933c8f1e61538550ccedd2e8ba00c4aaf3b3e6a9c9","prior_hash":"7aaf76cb84ac10df3de3a8a6b89ff34c6deea72ebd20e483fab7d706e87d770d"}
 {"seq":122,"ts":"2026-09-21T20:12:49Z","event":"commit.staged","tree":"16b37023893f8f7a4ecf50456356b006cafe5faf","author":"chidionyema333@gmail.com","staged":[".githooks/commit-msg",".githooks/pre-commit"],"payload_hash":"c87e3ad2aec2aa7e383d1fd3d2481dee35c2792f4dc9830c9c7370d4eaf4c1b7","prior_hash":"1e4d30ae9b09e49964b71a1a66c999d86a225a7ca89dc6dd04aba9c4b44569e7"}
 {"seq":123,"ts":"2026-09-21T20:15:36Z","event":"commit.staged","tree":"b9b7f08a4bc6f02c074fbc926d31969adf699388","author":"chidionyema333@gmail.com","staged":["docs/onboarding/claude-max-passthrough.md","llm/config.base.yaml","llm/config.yaml","platform/llm/config.base.yaml","platform/llm/config.yaml"],"payload_hash":"406af8bcb01014c5b6e6a925333c2672c2a98d6017a7f14b490cc80a425a3b96","prior_hash":"edf3fd3d47429979acdb1fb97071987211aa51ef517d993c82645fff492c42f4"}
+{"seq":124,"ts":"2026-09-21T22:29:23Z","event":"commit.staged","tree":"984dafb79087765be4f2ee1df613ec45bc43e70f","author":"chidionyema333@gmail.com","staged":["platform/jit/broker/broker.py","tests/test_the_jit_broker_cannot_be_talked_into_standing_access.py"],"payload_hash":"5feac763c83ac399cdb6e0620f5e855e51fcd148e95bbf9715271873b0a09e25","prior_hash":"cd47fde3acca1d0c01d7f3b933b67850d605a5983495cfb05ca5df14257ac0a0"}
diff --git a/bin/idp-jit b/bin/idp-jit
index 3c92d6c75..5a0614ac0 100755
--- a/bin/idp-jit
+++ b/bin/idp-jit
@@ -87,7 +87,7 @@ def _tailnet_door() -> str | None:
 def _estate_zone() -> str | None:
     """The estate's DNS zone, read from the one place it is written.
 
-    LAW 46: a literal here would name this estate's machine facts in a file, and
+    a literal here would name this estate's machine facts in a file, and
     `bin/estate-zone-gate` refuses exactly that. ESTATE_ZONE first, because an agent on a
     cloud runner has no checkout of this repository and the environment is all it has; then
     clusters/*/estate-config.yaml, which is where the value actually lives.
@@ -177,6 +177,127 @@ def _post(path: str, body: dict, headers: dict | None = None) -> tuple[int, dict
     }
 
 
+def _get(path: str) -> tuple[int, dict]:
+    """A keyless GET, for the one door that has no key yet: /enroll/nonce.
+
+    Kept separate from `_post` rather than folding a `key_optional` flag into it, so the ask
+    path -- which MUST carry a key -- cannot accidentally be reached without one by a caller
+    that wanted the nonce door. A device with no key has exactly one thing it may fetch, and
+    this is it.
+    """
+    tried = []
+    for door in _doors():
+        req = urllib.request.Request(door + path)  # noqa: S310 -- an http URL from the environment or the local tailscale daemon
+        try:
+            with urllib.request.urlopen(req, timeout=30) as fh:  # noqa: S310 -- same request object, already constrained
+                return fh.status, json.load(fh)
+        except urllib.error.HTTPError as exc:
+            return exc.code, json.loads(exc.read() or b"{}")
+        except OSError as exc:
+            tried.append(f"{door} ({exc})")
+    return 0, {"unreachable": True, "reason": "the broker is not answering on " + ", ".join(tried)}
+
+
+def _age_secret():
+    """The estate age identity as an X25519 private key, from SOPS_AGE_KEY_FILE or the default path.
+
+    The file holds a native age identity (`AGE-SECRET-KEY-1...`), which is bech32 over the
+    32-byte X25519 scalar. This decodes it and hands back the private key object; the secret is
+    never printed and never leaves this process. Raises RuntimeError with a reason a person can
+    act on, because "no age key" at enrollment is the one failure the person still owns (Level 1
+    of bin/idp-workstation-bootstrap).
+    """
+    from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PrivateKey
+
+    path = os.environ.get("SOPS_AGE_KEY_FILE") or os.path.expanduser("~/.config/prospector/age-key.txt")
+    try:
+        with open(path) as fh:
+            text = fh.read()
+    except OSError as exc:
+        raise RuntimeError(f"no age identity at {path}; restore it before enrolling ({exc})") from None
+
+    chi = "qpzry9x8gf2tvdw0s3jn54khce6mua7l"
+    for line in text.splitlines():
+        line = line.strip()
+        if not line.startswith("AGE-SECRET-KEY-1"):
+            continue
+        body = line[len("AGE-SECRET-KEY-1"):].lower()
+        try:
+            values = [chi.index(c) for c in body]
+        except ValueError:
+            break
+        out = bytearray()
+        acc = bits = 0
+        for v in values[:-6]:  # the trailing six groups are the bech32 checksum
+            acc = (acc << 5) | v
+            bits += 5
+            while bits >= 8:
+                bits -= 8
+                out.append((acc >> bits) & 0xFF)
+        if len(out) == 32:
+            return X25519PrivateKey.from_private_bytes(bytes(out))
+        break
+    raise RuntimeError(f"no native age identity in {path}; enrollment reads the AGE-SECRET-KEY-1 line")
+
+
+def enroll() -> int:
+    """Trade this device's age identity for a key of its own, then write it where _agent_key reads.
+
+    This is the door a fresh machine uses, and the only one it can use: every other door takes
+    the key this one mints. The proof is an X25519 agreement, not a signature -- a native age
+    key has no signing half -- and the age secret never leaves the device.
+    """
+    try:
+        secret = _age_secret()
+    except RuntimeError as exc:
+        print(f"BLOCKED  {exc}", file=sys.stderr)
+        return CODES["refused"]
+
+    code, nonce = _get("/enroll/nonce")
+    if nonce.get("unreachable"):
+        print(f"BLOCKED  {nonce['reason']}", file=sys.stderr)
+        return CODES["refused"]
+    if code != 200 or not nonce.get("nonce"):
+        print(f"refused: the broker would not hand out a nonce ({code})", file=sys.stderr)
+        return CODES["refused"]
+
+    import hashlib
+    import hmac as _hmac
+
+    from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PublicKey
+
+    ephemeral = bytes.fromhex(nonce["ephemeral"])
+    shared = secret.exchange(X25519PublicKey.from_public_bytes(ephemeral))
+    proof = {
+        "nonce": nonce["nonce"],
+        "ephemeral": ephemeral.hex(),
+        "mac": _hmac.new(shared, nonce["nonce"].encode(), hashlib.sha256).hexdigest(),
+    }
+    # No Authorization header: this device holds no key. _post would attach one if it had it,
+    # and sending a stale key here would be harmless but misleading in a log.
+    code, body = _post("/enroll", proof, {"Authorization": ""})
+    if body.get("unreachable"):
+        print(f"BLOCKED  {body['reason']}", file=sys.stderr)
+        return CODES["refused"]
+    if code != 200 or not body.get("key"):
+        print(f"refused: {body.get('error', 'the broker did not accept this device')}",
+              file=sys.stderr)
+        return CODES["refused"]
+
+    state = os.environ.get("IDP_KUBE_STATE") or os.path.join(
+        os.environ.get("XDG_STATE_HOME") or os.path.expanduser("~/.local/state"), "idp")
+    out = os.path.join(state, "agent-key")
+    os.makedirs(state, mode=0o700, exist_ok=True)
+    fd = os.open(out, os.O_WRONLY | os.O_CREAT | os.O_TRUNC, 0o600)
+    try:
+        os.write(fd, (body["key"] + "\n").encode())
+    finally:
+        os.close(fd)
+    print(f"enrolled; this device now holds its own key ({body['fingerprint']}) at {out}",
+          file=sys.stderr)
+    return 0
+
+
 def _agent_key() -> str | None:
     """The one secret a device holds. Environment first, then a file mode 600 under the same
     state directory bin/idp-kube caches in.
@@ -198,6 +319,95 @@ def _agent_key() -> str | None:
         return None
 
 
+def _kubeconfig_path() -> str:
+    """Where the agent kubeconfig lives. One path, so every reader agrees on the state."""
+    return os.environ.get("IDP_AGENT_KUBECONFIG") or os.path.join(
+        os.environ.get("XDG_STATE_HOME") or os.path.expanduser("~/.local/state"),
+        "idp", "agent.kubeconfig")
+
+
+def _token_expiry(path: str) -> float | None:
+    """When the token in this kubeconfig dies, as a unix timestamp, or None.
+
+    Read from the JWT's own `exp` claim rather than from a file mtime, because the file is
+    rewritten on renewal and an mtime would report the renewal, not the expiry. A token that
+    cannot be parsed is reported as None (unknown), never as expired -- guessing a security
+    state is the one thing worse than not showing it.
+    """
+    import base64
+    import time as _time
+    try:
+        with open(path) as fh:
+            doc = json.load(fh)
+        tok = (doc.get("users") or [{}])[0].get("user", {}).get("token", "")
+        payload = tok.split(".")[1]
+        payload += "=" * (-len(payload) % 4)
+        claims = json.loads(base64.urlsafe_b64decode(payload))
+        exp = claims.get("exp")
+        return float(exp) if exp else None
+    except Exception:  # noqa: BLE001 - an unreadable token is 'unknown', not an exception
+        return None
+
+
+def _status() -> int:
+    """What this device's access is, as JSON. Never mints, never writes, never needs a tap.
+
+    This is the one command the portal tile reads. It answers in the only three states a
+    reader has to distinguish, and it answers them WITHOUT contacting the broker when the
+    key is absent -- a device that holds no key is not provisioned, full stop, and a status
+    call that needs the network to say so would be a status call that lies when offline.
+
+      not_provisioned -- no agent key on this device. Only the owner can fix this, once.
+      active          -- key present AND a live token with time left on it.
+      expiring        -- live token, under 10 minutes left; the daemon's cue to renew.
+      expired         -- key present, token dead or unreadable. One renew fixes it.
+    """
+    import time as _time
+
+    key = _agent_key()
+    kc = _kubeconfig_path()
+    exp = _token_expiry(kc)
+    now = _time.time()
+
+    if not key:
+        state = "not_provisioned"
+    elif exp is None:
+        state = "expired" if os.path.exists(kc) else "not_provisioned"
+    elif exp <= now:
+        state = "expired"
+    elif exp - now < 600:
+        state = "expiring"
+    else:
+        state = "active"
+
+    out = {
+        "state": state,
+        "scope": "read-only",
+        "subject": "system:serviceaccount:agents:agent-reader",
+        "has_key": bool(key),
+        "kubeconfig": kc,
+        "expires_at": exp,
+        "expires_in": (int(exp - now) if exp else None),
+    }
+    print(json.dumps(out))
+    return 0
+
+
+def _renew(path: str, quiet: bool) -> int:
+    """Mint a fresh token and rewrite the kubeconfig. The zero-touch path.
+
+    Deliberately the same `_identity()` the interactive path uses, so the daemon, the tile's
+    Renew button and a human at a terminal all exercise one piece of code. A second renewal
+    implementation is a second place for the identity check to drift.
+    """
+    rc = _identity(path)
+    if rc == 0 and quiet:
+        return 0
+    if rc == 0:
+        print(f"renewed {path}")
+    return rc
+
+
 def _identity(path: str | None) -> int:
     """WJ.1's client half: become an agent of this estate without the founder's credential.
 
@@ -231,7 +441,7 @@ def _identity(path: str | None) -> int:
                       "context": {"cluster": "estate", "user": "agent-reader"}}],
     }
     # Written through a private temporary file and renamed, so a reader never sees a
-    # half-written kubeconfig and the token is never briefly world-readable (LAW 21).
+    # half-written kubeconfig and the token is never briefly world-readable.
     tmp = f"{path}.{os.getpid()}"
     fd = os.open(tmp, os.O_WRONLY | os.O_CREAT | os.O_TRUNC, 0o600)
     with os.fdopen(fd, "w") as fh:
@@ -258,11 +468,30 @@ def main(argv: list[str] | None = None) -> int:
     i = sub.add_parser("identity", help="become this estate's read-only agent on any device")
     i.add_argument("--kubeconfig", help="write a kubeconfig here instead of printing the token")
 
+    sub.add_parser("enroll", help="prove the age identity and receive this device's own key")
+
+    s = sub.add_parser("status", help="what this device's access IS, as JSON, without minting")
+    s.add_argument("--json", action="store_true", help="always JSON; kept for symmetry")
+
+    r = sub.add_parser("renew", help="mint a fresh token now; the zero-touch path calls this")
+    r.add_argument("--kubeconfig", required=True,
+                   help="rewrite this kubeconfig; a renew with nowhere to write is pointless")
+    r.add_argument("--quiet", action="store_true", help="print nothing on success")
+
     args, extra = ap.parse_known_args(argv)
 
+    if args.cmd == "status":
+        return _status()
+
+    if args.cmd == "renew":
+        return _renew(args.kubeconfig, args.quiet)
+
     if args.cmd == "identity":
         return _identity(args.kubeconfig)
 
+    if args.cmd == "enroll":
+        return enroll()
+
     if args.cmd == "grants":
         code, body = _post("/grants", {})
         if body.get("unreachable"):
diff --git a/bin/idp-workstation-bootstrap b/bin/idp-workstation-bootstrap
index 2b2066ac0..9035ee2ae 100755
--- a/bin/idp-workstation-bootstrap
+++ b/bin/idp-workstation-bootstrap
@@ -21,7 +21,7 @@
 #                              platform-engineer`, `gh auth login --with-token` from it, and write
 #                              `~/.kube/config` via `bin/idp-cloud cluster kubeconfig`.
 #
-# States (LAW 38): OK = ran and verified; BLIND = tool or vendor absent and the estate does not
+# States: OK = ran and verified; BLIND = tool or vendor absent and the estate does not
 # depend on it (informational, not a refusal); FAIL = a step that must succeed refused.
 #
 # Vendor-neutral (AGENTS.md 2026-09-17): the local container runtime is never installed by this
@@ -147,16 +147,27 @@ phase2() {
 # accessed by an OCI CLI cluster-provider door (bin/idp-cloud), (2) a GitHub installation token
 # minted narrow to the platform-engineer lane. The estate does not require either to be present —
 # a machine that only runs unit tests locally does not need cluster access — so both refuse BLIND
-# (LAW 38), not FAIL, when the vendor side is not reachable.
+#, not FAIL, when the vendor side is not reachable.
 phase3_kube() {
   echo "-- Level 3a: kubeconfig via bin/idp-cloud --"
   [ -x "$IDP/bin/idp-cloud" ] || { blind "level3 bin/idp-cloud absent; kubeconfig not provisioned"; return 0; }
   command -v oci >/dev/null || { blind "level3 oci CLI absent (level0 should have installed it)"; return 0; }
-  # cluster list requires an OCI session token; bin/idp-oci-bootstrap is the one browser step that
-  # buys the estate that session. If a session is not live, refuse BLIND and print the exact fix.
+  # 2026-09-18: this used to say "run bin/idp-oci-bootstrap for a session token". That script now
+  # BLINDs at its first line on a device whose vault holds no OCI key -- which is every device
+  # after commit 76ba8be moved the dev secrets to OCI Vault -- so it sent a fresh workstation to
+  # a command that could not help. The browser login that mints the session is bin/idp-oci-login
+  # (its own header: "provider-adapter: oci -- this file IS the provider door (session sign-in)"),
+  # and the founder's own terminal is the only place it will run.
   local cl name id kubecfg="$HOME/.kube/config"
   cl="$("$IDP/bin/idp-cloud" cluster list 2>/dev/null || true)"
-  [ -n "$cl" ] || { blind "level3 no ACTIVE OCI cluster visible; run bin/idp-oci-bootstrap for a session token, or check tenancy"; return 0; }
+  [ -n "$cl" ] || {
+    blind "level3 no ACTIVE OCI cluster visible.
+         cause  this device has no OCI session
+         fix    bin/idp-oci-login, in the owner's own terminal (it refuses an agent session on purpose).
+                If the vault itself reads empty, the secrets moved to OCI Vault: see
+                docs/reference/agent-identity.md"
+    return 0
+  }
   name="$(printf '%s\n' "$cl" | awk 'NR==1{print $1}')"
   id="$(printf '%s\n' "$cl" | awk 'NR==1{print $2}')"
   [ "$MODE" = "--check" ] && { [ -r "$kubecfg" ] && ok "level3 kubeconfig present at $kubecfg" || blind "level3 kubeconfig absent at $kubecfg"; return 0; }
@@ -170,6 +181,63 @@ phase3_kube() {
   fi
 }
 
+# Level 3a-alt: the AGENT identity, which needs no OCI session and no founder login at all.
+#
+# WHY THIS EXISTS. phase3_kube above mints a kubeconfig whose user is the founder's own OCI
+# principal. WJ.1's last clause is "bin/idp-kube stops minting the founder's OCI principal", and
+# the door that makes that true is the JIT broker's /identity: a device holding ONE key presents
+# it and is handed a one-hour agent-reader token that can read and nothing else. That path needs
+# no browser, so it is the one a fresh workstation should be told about first, and it is the only
+# one that works when the OCI session is the thing that is missing.
+#
+# It is deliberately BLIND rather than FAIL when the key is absent: a machine that only runs unit
+# tests locally needs no cluster access, and a device that has never been authorized is the
+# ordinary case for a new laptop, not an error to shout about.
+phase3_agent_identity() {
+  echo "-- Level 3a-alt: agent identity via the JIT broker (no OCI session needed) --"
+  [ -x "$IDP/bin/idp-jit" ] || { blind "level3 bin/idp-jit absent; agent identity not provisioned"; return 0; }
+
+  local state
+  state="$(python3 "$IDP/bin/idp-jit" status 2>/dev/null || true)"
+  case "$state" in
+  *'"state": "active"'*|*'"state": "expiring"'*)
+    ok "level3 agent identity is live (read-only, renews itself)"
+    return 0
+    ;;
+  esac
+
+  # No key yet: enroll. ADR 0032. A fresh machine proves the one credential it already has --
+  # the age identity phase1 just checked -- over the tailnet, and the broker hands back a key
+  # that is this device's own. No vendor secret to fetch, no browser, no founder's terminal.
+  # This is the seam that used to BLIND; the old "a device is authorized once, by its owner"
+  # is still true, it is just that the age identity IS the owner's one hand (Level 1), spent
+  # by a signature-less X25519 agreement rather than a shared key typed in by hand.
+  if [ ! -e "$HOME/.local/state/idp/agent-key" ] && [ -z "${JIT_AGENT_KEY:-}" ]; then
+    if [ "$MODE" = "--check" ]; then
+      blind "level3 this device holds no agent key; bin/idp-jit enroll would mint one from the age identity"
+      return 0
+    fi
+    if "$IDP/bin/idp-jit" enroll; then
+      ok "level3 enrolled -- this device holds its own key (from the age identity, no shared secret)"
+    else
+      blind "level3 could not enroll.
+         cause  the broker did not accept the age identity, or is unreachable
+         fix    a machine with no age key restores it first (phase1); a reachable broker
+                answers bin/idp-jit status. Enrollment proves the age identity, never a password"
+      return 0
+    fi
+  fi
+
+  # Key present (or just enrolled): mint now. This needs no approval, by the broker's own design
+  # -- "agent-reader holds reads and nothing else ... so there is nothing to approve".
+  if "$IDP/bin/idp-jit" renew --kubeconfig "$HOME/.local/state/idp/agent.kubeconfig" --quiet 2>/dev/null; then
+    ok "level3 agent identity minted (read-only, renews every 10 minutes)"
+  else
+    blind "level3 agent key present but the broker did not answer; check tailnet or bin/idp-jit status"
+  fi
+  return 0
+}
+
 phase3_gh() {
   echo "-- Level 3b: gh CLI authn from GitHub App installation token --"
   command -v gh >/dev/null || { blind "level3 gh CLI absent (level0 should have installed it)"; return 0; }
@@ -204,6 +272,7 @@ phase0
 phase1
 phase2
 phase3_kube
+phase3_agent_identity
 phase3_gh
 verify
 
diff --git a/clusters/oke/estate-config.yaml b/clusters/oke/estate-config.yaml
index 8c25bf4bf..bf4120bf7 100644
--- a/clusters/oke/estate-config.yaml
+++ b/clusters/oke/estate-config.yaml
@@ -70,7 +70,7 @@ data:
   # crew#516 CP5, founder's locked spec (crew#66 5451926212): the founder's Mac, reached by mac-run
   # (platform/hermes-agent/mac-run.tpl) over the Tailscale sidecar's SOCKS5 proxy, and named in
   # platform/tailscale/policy.hujson's ssh rule (bin/idp-tailscale-policy reads FOUNDER_MAC_USER
-  # from here, the same source Flux postBuild substitutes into the manifests). Both are LAW 46: the
+  # from here, the same source Flux postBuild substitutes into the manifests). Both are the
   # one place this estate is allowed to name the Mac's tailnet IP and login user, never a literal
   # in a Deployment, a script or the tailnet policy. Empty until the founder fills them
   # (platform/hermes-agent/README.md, "Founder hand"); mac-run fails closed (ssh has nothing to
@@ -126,4 +126,11 @@ data:
   # his browser (docs/how-to/bitwarden-human-vault.md); while empty, the human-vault Flux row
   # is red on its own and holds nothing else.
   BITWARDEN_ORG_ID: "a9f79fcf-7059-4bad-94ab-b4b900acc259"
+  # ADR 0032: the estate's age PUBLIC recipient, which the JIT broker agrees a key with to check
+  # a fresh device's root trust at /enroll. It is a public key, not a secret -- the matching age
+  # secret lives on the workstations (Level 1 of bin/idp-workstation-bootstrap) and nowhere in
+  # this cluster. One copy, in git, substituted by Flux postBuild into platform/jit/deployment.yaml
+  # (JIT_AGE_RECIPIENT); changing it is a rotation and belongs in a reviewed change, not a vault
+  # write. From ~/.config/prospector/age-key.txt, the "# public key:" line; no secret is mirrored.
+  ESTATE_AGE_RECIPIENT: "age1rgufr0lc4y02wfw5xjwugxpf8hmrzm3sww3nlk4uk8dmph3h7dsqqvrd0j"
   BITWARDEN_PROJECT_ID: "18e57b2f-d5c6-4c0b-9ba9-b4b900e1d792"
diff --git a/docs/decisions/0032-the-mac-holds-no-standing-secret-spire-join-token-then-oci-wif.md b/docs/decisions/0032-the-mac-holds-no-standing-secret-spire-join-token-then-oci-wif.md
new file mode 100644
index 000000000..d51103feb
--- /dev/null
+++ b/docs/decisions/0032-the-mac-holds-no-standing-secret-spire-join-token-then-oci-wif.md
@@ -0,0 +1,154 @@
+# 0032. The Mac holds no standing secret: SPIRE join_token, then OCI WIF over a public OIDC issuer
+
+2026-09-21. Status: accepted. Supersedes nothing; extends the identity row of
+`docs/reference/security-policy.md` (A.5.16, A.8.24) and the zero-trust row of
+`docs/decisions/0029-definition-of-done-v3-is-five-checks-none-self-graded.md`.
+
+## The instruction
+
+Founder, 2026-09-21: deliver the zero-trust ("elite 2100") identity on the free tier for a
+macOS endpoint, and kill the clunky browser-login / human secret-delivery bootstrap. "Do the
+standards research first, properly." No half-measures — this is a commercial product, and the
+choice must be defensible from primary sources, not from memory.
+
+## The evidence this was needed
+
+The estate's own scorecard against NIST SP 800-207 + SPIFFE/SPIRE + Microsoft Entra Agent ID +
+OCI WIF scored **1 of 5**. Four rows pass on paper; the device row fails, and the root is a
+standing secret:
+
+- `~/.local/state/idp/agent-key` — one bearer key, on disk, that never expires by itself.
+- `platform/jit/broker/broker.py:470` — `hmac.compare_digest(presented, agent_key)`: every agent
+  presents the same shared key. `broker.py:458` says the quiet part: *"One key covers every
+  agent... Per-agent keys are WJ.13's problem."*
+- No federation endpoint accepts a laptop: `platform/oci/vault.tf` is instance principal
+  (in-cluster), `platform/oci/crossplane.tf` is a workload principal scoped to `cluster_id` for
+  the `crossplane-system` service account, and `platform/oci/provers.tf:8` records that both the
+  laptop and a pod were refused.
+
+The only door that currently opens from the Mac (`bin/idp-oci-session`) *is* a browser login; the
+only delivery path (`bin/idp-mac-secret-deliver`) is gated behind a cloud identity the Mac does
+not have. The circle closes on itself: no cluster access without a key, no key without cluster
+identity.
+
+## What the standard actually says (verified from primary sources, 2026-09-21)
+
+- **NIST SP 800-207** — subject *and device* are authenticated separately; authentication is
+  asserted per session, not at a perimeter.
+- **SPIFFE Concepts** — a workload *need not co-deploy authentication secrets*; keys are
+  short-lived and auto-rotated.
+- **Microsoft Entra Agent ID** — *"It doesn't have credentials on its own"*; it authenticates
+  through a federated identity credential held by a blueprint, not by the agent.
+- **OCI Workload Identity Federation** — `docs.oracle.com/.../json_web_token_exchange.htm` and
+  `.../token_exchange_grant_type_workload_id-federation.htm`. An Identity Propagation Trust
+  takes a free-form `issuer`, and a JWKS source (`publicKeyEndpoint` *or* an inline
+  `publicCertificate`); the doc states `/IdentityPropagationTrust` is *"cloud-agnostic and
+  supports integrations with any cloud provider."* There is no issuer allow-list and no
+  dependency on the OKE OIDC endpoint or on an Enhanced cluster.
+- **SPIRE macOS node attestation** — `doc/plugin_server_nodeattestor_jointoken.md`: a
+  one-time-use pre-shared token, generated by `spire-server token generate`, yielding
+  `spiffe://<trust_domain>/spire/agent/join_token/<token>`. The agent builds for macOS
+  ("Quickstart for Linux and MacOS X"); the `unix` WorkloadAttestor supplies `unix:uid`/`unix:gid`
+  selectors to bind a user's processes to an SVID.
+
+## The decision
+
+**1. The Mac bootstraps with a SPIRE `join_token`, once.** The token is generated on the server,
+consumed at attestation, and is one-time-use. After attestation the agent holds its own key
+(`KeyManager` disk or memory) and obtains short-lived SVIDs from the Workload API. **No standing
+secret remains on disk after bootstrap.** The one-time provisioning event is the friction we
+accept; it is not a standing credential. Rejected alternatives, each of which *shifts* a standing
+secret rather than removing one: `x509pop`/`sshpop` (an existing long-lived cert/key), and the
+Secure Enclave (no maintained SPIRE node attestor for macOS).
+
+**2. OCI federation rides a public SPIFFE OIDC issuer, not an Enhanced cluster.** Enable
+`spiffe-oidc-discovery-provider` (currently `platform/spire/values.yaml:90` `enabled: false`);
+give it a trust bundle, an `issuer`, and a public Ingress with a Let's Encrypt (`cert-manager`)
+certificate. Register **one** Identity Propagation Trust whose `issuer` is that Ingress URL and
+whose `publicKeyEndpoint` is the provider's JWKS. The Mac then presents a JWT-SVID as
+`subject_token` and receives a short-lived OCI token — no OCI key on the Mac. This is the
+free-tier bypass: WIF lives in an OCI Identity Domain, which is available on any tenancy; the
+Enhanced-cluster requirement belongs to the *OKE workload-identity add-on*, a different feature
+that was mistakenly conflated with WIF.
+
+**3. What is displaced, not eliminated.** The token exchange is authenticated by an OCI IAM
+identity-domain OAuth client (`client_id:client_secret`), which is itself a standing secret. It
+does **not** live on the Mac: it lives in the cluster, where an OCI Instance Principal signed
+request can replace it outright (OKE Basic still grants the instance principal). The estate's
+rule is "no static credential *on the endpoint*," and this satisfies it; we state the residual
+plainly rather than pretending WIF removes it.
+
+**4. The broker's shared key is the next layer, and it is named, not silently fixed.**
+`broker.py`'s single `agent_key` remains until per-agent keys land (WJ.13). This ADR removes the
+*Mac's* standing secret; it does not claim the broker is per-identity yet. That is a separate,
+already-tracked change and this record exists partly so the claim is not overstated.
+
+## Consequences
+
+- A new public endpoint exists (the SPIFFE OIDC discovery/JWKS Ingress). It is public by design
+  and is a discovery surface, not a secret; it must be TLS-terminated with a valid chain and the
+  JWKS must be current, or OCI token exchange fails closed.
+- `platform/spire/values.yaml` is currently k3d-shaped (`replicaCount: 1`, three-node reasoning
+  in comments). Enabling the OIDC provider and pointing it at real OKE is a values + Ingress
+  change, not a new system.
+- No new layer is introduced: SPIRE already exists (`clusters/oke/platform.yaml:834`), the JIT
+  broker already exists, the OIDC provider is the chart's own component.
+
+## The first step built (2026-09-21): the broker's enrollment door
+
+Item 4 above names the shared key as the next layer but leaves it unfixed. The first change toward
+it is built and is deliberately *not* SPIRE — it is the broker minting per-device keys, which is
+what makes item 4 false in practice for any machine that can reach the broker:
+
+- **`Broker.enroll()` + `issue_nonce()`** in `platform/jit/broker/broker.py`. A device holding no
+  broker key fetches a single-use, two-minute nonce and the public half of an ephemeral X25519 key
+  (`GET /enroll/nonce`), and posts back an HMAC-SHA256 tag over the nonce keyed by an agreement
+  between its estate age secret and that ephemeral key (`POST /enroll`). The broker regenerates the
+  agreement with the ephemeral secret it kept and compares tags with `hmac.compare_digest`.
+- **The proof is X25519 agreement, not a signature.** This is a correction to an earlier draft that
+  assumed an Ed25519 signature. The estate's age identity is *native age*, not `ssh-ed25519`: the
+  recipient `age1rgufr0lc4y02wfw5xjwugxpf8hmrzm3sww3nlk4uk8dmph3h7dsqqvrd0j` decodes to exactly
+  `X25519PrivateKey.from_private_bytes(<the AGE-SECRET-KEY-1 scalar>).public_key()`, verified this
+  turn. A native age key has no signing half, so X25519 is the only correct primitive. It is
+  dependency-light (`cryptography`, already trusted for TLS) and needs no PyNaCl.
+- **The shared key is not removed, it is displaced per device.** `authenticate()` now accepts an
+  enrolled device's own key first and the shared `JIT_AGENT_KEY` only as a fallback, and the
+  `identity` ledger line records which device by fingerprint (`device=`) instead of "an agent". The
+  shared key remains for machines that have not enrolled; removing it outright is still WJ.13.
+- **The age secret, not a vendor secret, is the one hand.** `bin/idp-jit enroll` reads
+  `SOPS_AGE_KEY_FILE`, decodes the `AGE-SECRET-KEY-1...` scalar, performs the agreement, and writes
+  the minted key to `~/.local/state/idp/agent-key` (mode 600). `bin/idp-workstation-bootstrap`'s
+  `phase3_agent_identity` now calls this instead of BLINDing. Nothing is fetched from the vault and
+  no browser opens; the credential the machine already restores at Level 1 is the whole proof.
+- **The public route and the deployment carry it.** `platform/jit/public-door.yaml` routes
+  `/jit/enroll` (POST) and `/jit/enroll/nonce` (GET); `platform/jit/deployment.yaml` sets
+  `JIT_AGE_RECIPIENT` from `ESTATE_AGE_RECIPIENT` in `clusters/oke/estate-config.yaml` (a public
+  recipient, so config and not a vault secret). A broker with no recipient refuses enrollment,
+  which is the fail-closed default.
+- **Graded both ways.** `tests/test_the_jit_broker_cannot_be_talked_into_standing_access.py` drives
+  the real door: a fresh device enrolls and its key is accepted; the ledger holds the fingerprint
+  and no key/nonce/MAC; a replayed nonce, a wrong MAC, and an impostor holding its own key are all
+  refused (401); enrollment is closed with no recipient; and `bin/idp-jit enroll` is driven over a
+  real socket. The route test asserts the public route carries exactly `AGENT_DOORS + OPEN_DOORS`.
+
+**Relationship to SPIRE (items 1–3).** This does not replace the SPIRE `join_token` path; it is
+what closes the laptop seam *today*, on the free tier, with the age identity the estate already
+has. SPIRE remains the end-state for SVIDs and the OCI WIF exchange. Item 4 — the broker's shared
+key — is now false for enrolled devices and true only for those still presenting the shared key.
+
+## Proof required before this is "done"
+
+Per `0029`, none of the above may be declared operating by the author of the change:
+
+1. `spire-agent healthcheck` green on the Mac against the estate server (real output, not a
+   render).
+2. A JWT-SVID fetched on the Mac and exchanged at `/oauth2/v1/token` for an OCI token, proven by
+   a real API call answered by OCI — not by a 200 from our own process.
+3. The `agent-key` file absent from `~/.local/state/idp/` after bootstrap, and the broker
+   unreachable from the Mac without an SVID.
+4. A production log line from the exchange, cited in the closing receipt.
+
+For the enrollment door specifically, "operating" is **not** claimed here. It is built and graded
+locally; the proof line is a real `/jit/enroll` 200 from the public broker on the founder's Mac,
+followed by `bin/idp-jit identity` answering with a token the cluster minted. Until that line
+exists, the enrollment door is **built, not operating**, the same as (1)–(4) above.
diff --git a/platform/jit/Dockerfile b/platform/jit/Dockerfile
index 40c5d778c..bb26f38b3 100644
--- a/platform/jit/Dockerfile
+++ b/platform/jit/Dockerfile
@@ -2,7 +2,10 @@
 # access is not a workload anyone should be able to get a prompt inside.
 FROM python:3.12-slim AS build
 WORKDIR /w
-RUN pip install --no-cache-dir --target=/deps pyyaml==6.0.2
+# cryptography verifies the Ed25519 signature a first-time device makes with the estate age
+# identity (ADR 0032). It is the same library the estate already trusts for TLS; the wheel is
+# manylinux, so distroless picks it up without a build toolchain.
+RUN pip install --no-cache-dir --target=/deps pyyaml==6.0.2 cryptography==42.0.5
 
 FROM gcr.io/distroless/python3-debian12:nonroot
 COPY --from=build /deps /app
diff --git a/platform/jit/broker/broker.py b/platform/jit/broker/broker.py
index 31cb6262d..7724e8c76 100644
--- a/platform/jit/broker/broker.py
+++ b/platform/jit/broker/broker.py
@@ -47,6 +47,7 @@ import hmac
 import json
 import os
 import re
+import secrets
 import subprocess
 import sys
 import time
@@ -55,6 +56,11 @@ from dataclasses import dataclass, field
 from typing import Any, Callable
 
 import yaml
+from cryptography.hazmat.primitives.asymmetric.x25519 import (
+    X25519PrivateKey,
+    X25519PublicKey,
+)
+from cryptography.hazmat.primitives.serialization import Encoding, PublicFormat
 
 #: Ten minutes is the founder's example and the catalogue's ceiling is thirty. The
 #: TokenRequest API will not mint below its own floor, so a shorter ask is honoured as
@@ -106,6 +112,50 @@ def quantity_bytes(v: str) -> int | None:
     return int(float(m.group(1)) * _SCALE[m.group(2)]) if m else None
 
 
+#: The age ecosystem's bech32 alphabet (BIP-173), in index order. Written here rather than
+#: taken from a dependency because it is the only table the broker needs, and one table of
+#: thirty-two characters is not worth a library.
+_BECH32_CHARSET = "qpzry9x8gf2tvdw0s3jn54khce6mua7l"
+
+
+def _bech32_data(recipient: str) -> bytes | None:
+    """Decode an `age1...` recipient to the 32 bytes of X25519 public key it carries.
+
+    The estate's age identity is the native age form (`~/.config/prospector/age-key.txt`,
+    `# public key: age1...`), not the ssh-ed25519 form, so the recipient is bech32 and its
+    bytes are an X25519 public key -- confirmed against this estate's own key: the recipient
+    decodes byte-for-byte to `X25519PrivateKey.from_private_bytes(secret).public_key()`. That
+    is why the proof below is X25519 agreement and not a signature: a native age key has no
+    signing half to sign with.
+
+    This is the BIP-173 decoding age uses: the last 6 characters are the checksum and carry no
+    data, and the 5-bit groups that remain are the key's 32 bytes re-encoded. A native recipient
+    is exactly 52 data characters before the checksum, which is 260 bits -- 32 bytes plus four
+    leftover bits. Those leftover bits are zero-padding, so anything that decodes to anything
+    other than 32 bytes is refused rather than agreed against the wrong key.
+    """
+    s = str(recipient or "").strip().lower()
+    if not s.startswith("age1") or s.lower() != s:
+        return None
+    try:
+        values = [_BECH32_CHARSET.index(c) for c in s[4:]]
+    except ValueError:
+        return None
+    if len(values) < 6:
+        return None
+    # Drop the checksum; the 260 remaining bits decode to the 32 key bytes and four padding bits.
+    out = bytearray()
+    acc = 0
+    bits = 0
+    for v in values[:-6]:
+        acc = (acc << 5) | v
+        bits += 5
+        while bits >= 8:
+            bits -= 8
+            out.append((acc >> bits) & 0xFF)
+    return bytes(out) if len(out) == 32 else None
+
+
 class Refused(Exception):
     """The request does not become a question for the founder. He is only asked things
     that are allowed to happen; everything else is answered here, with the reason."""
@@ -223,6 +273,14 @@ class Broker:
     #: replayed does not.
     AGENT_IDENTITIES_PER_HOUR = 60
 
+    #: Enrollments mint a device its own key, proven by the estate's root trust (ADR 0032). Bounded
+    #: the same way identities are: a handful of machines being imaged is the normal case, a
+    #: replayed nonce is not. A person imaging a fleet of ten in one afternoon is still under it.
+    ENROLLMENTS_PER_HOUR = 10
+    #: A nonce is a challenge, not a session. Long enough for a device to sign and post it, short
+    #: enough that a nonce left in a shell history is worthless by the time anyone finds it.
+    ENROLLMENT_NONCE_TTL_SECONDS = 120
+
     #: Where the cluster publishes its own address and certificate, for exactly this purpose.
     #: kubeadm writes it and OKE keeps it; `system:public-info-viewer` makes it readable by
     #: anyone, authenticated or not, because a client needs it *before* it has a credential.
@@ -241,6 +299,7 @@ class Broker:
         providers: dict[str, Callable[[list[str]], tuple[int, str]]] | None = None,
         agent_key: bytes | None = None,
         ledger_sink=None,
+        age_recipient: str = "",
     ):
         self.catalogue_path = catalogue
         self.key = key
@@ -259,6 +318,26 @@ class Broker:
         # Empty on a broker that has not been given one, which is a broker that cannot
         # identify anybody -- `identity()` refuses rather than defaulting open.
         self.agent_key = agent_key or b""
+        #: The estate's age public key (an `age1...`/ssh-ed25519 recipient), used only to verify a
+        #: first-time device's root trust at /enroll. Empty means enrollment is closed, which is
+        #: the safe default: a broker that cannot check root trust must not hand out keys.
+        self.age_recipient = age_recipient
+        #: Keys this broker has handed to devices. In-memory by design: a restart drops them, and a
+        #: device re-enrolls from the same age identity it already holds. Persisting a device key
+        #: would be a second copy of a credential (WJ.7), which the ledger already refuses to keep.
+        self.device_keys: dict[str, str] = {}
+        #: Issues nonces, one per enrollment attempt, so a captured proof cannot be replayed.
+        self._enrollment_nonces: dict[str, float] = {}
+        #: The ephemeral X25519 private key for each live nonce. Its public half travels with the
+        #: nonce; this half never leaves the process. A nonce is spent and this is popped together,
+        #: so a captured proof cannot be replayed. Not persisted: a restart drops both, and a
+        #: device simply asks for a fresh nonce.
+        self._enrollment_ephemerals: dict[str, X25519PrivateKey] = {}
+        #: Which enrolled device the last authenticate() matched, or "" for the shared key. Set by
+        #: authenticate(), read by whoever writes the ledger line that follows. Not a credential
+        #: and not persisted: it is the fingerprint already in the ledger, kept here only so the
+        #: calling door can name the device without re-deriving it.
+        self._acting_fingerprint = ""
 
     # ---------------------------------------------------------------- the catalogue
 
@@ -327,7 +406,7 @@ class Broker:
             if kind == "base64_blob":
                 # The value is the secret, so it is never quoted back -- only its shape is.
                 # Checked here rather than at the provider because a bad-request echo from a
-                # cloud CLI is one of the ways a secret reaches a log (LAW 21).
+                # cloud CLI is one of the ways a secret reaches a log.
                 try:
                     raw = base64.b64decode(str(value), validate=True)
                 except (binascii.Error, ValueError):
@@ -456,19 +535,40 @@ class Broker:
         One key covers every agent, so what this proves is "an agent of this estate", not
         which one. `asked_by` stays a label, and the ledger now records that it was attested
         rather than merely asserted. Per-agent keys are WJ.13's problem, not this door's.
+
+        ADR 0032 lays the first stone of WJ.13 here: a device that enrolled at `/enroll` holds
+        its OWN key, and this accepts it alongside the shared one. That is what makes enrollment
+        worth anything -- a device minted its own identity rather than being handed the same
+        secret as every other. The shared key remains accepted while older devices still hold it,
+        and the enrollment ledger records which keys exist and when each was issued.
         """
-        if not self.agent_key:
+        if not self.agent_key and not self.device_keys:
             raise Refused(
                 "this broker holds no agent key, so it cannot identify anyone. "
                 "platform/jit/deployment.yaml is where JIT_AGENT_KEY arrives"
             )
-        if not hmac.compare_digest(presented or "", self.agent_key.decode()):
-            # No ledger line, and for the reason the Telegram path already gives: this door is
-            # reachable from outside the cluster, so a line per refusal is a way for a stranger
-            # to fill the ledger volume. The ledger records what the broker did, and it did
-            # nothing. compare_digest because a byte-at-a-time comparison leaks the key to
-            # whoever can time it.
-            raise Refused("not an agent of this estate")
+        presented = presented or ""
+        # A device key first: it is the narrower credential, and matching it says "this specific
+        # device" rather than "an agent of this estate". The shared key is the fallback while
+        # older devices still present it.
+        for fingerprint, key in self.device_keys.items():
+            if hmac.compare_digest(presented, key):
+                # The fingerprint is the ledger's word for WHICH device, without recording the
+                # key. A reader can now tell two enrolled devices apart, which the shared key
+                # never allowed (broker.py's own note: "One key covers every agent"). The count
+                # itself is appended by identity(), not here, so /ask and /identity do not
+                # double-count one caller.
+                self._acting_fingerprint = fingerprint
+                return
+        self._acting_fingerprint = ""
+        if self.agent_key and hmac.compare_digest(presented, self.agent_key.decode()):
+            return
+        # No ledger line, and for the reason the Telegram path already gives: this door is
+        # reachable from outside the cluster, so a line per refusal is a way for a stranger
+        # to fill the ledger volume. The ledger records what the broker did, and it did
+        # nothing. compare_digest because a byte-at-a-time comparison leaks the key to
+        # whoever can time it.
+        raise Refused("not an agent of this estate")
 
     def identity(self, presented: str) -> dict:
         """Mint the read-only identity an agent runs as, for a caller that proves it is one.
@@ -515,10 +615,13 @@ class Broker:
         self.history.append((self.now(), "identity"))
         # WJ.7: the issuance is on the record, the token is not. A ledger that holds the
         # credential it recorded is a second copy of every credential the broker ever made.
+        # device names the enrolled device by fingerprint when the caller presented its own key
+        # (ADR 0032); empty means the shared key, which proves "an agent", not which one.
         self.ledger.append(
             "identity",
             subject=f"{self.AGENT_NS}:{self.AGENT_SA}",
             ttl=self.AGENT_TTL_SECONDS,
+            device=self._acting_fingerprint,
         )
         return {
             "token": out.strip(),
@@ -527,6 +630,162 @@ class Broker:
             **self._cluster_address(),
         }
 
+    def enroll(self, nonce: str, ephemeral: str, mac: str) -> dict:
+        """Hand a first-time device its OWN key, proven by the estate's root trust.
+
+        A fresh machine is the case every previous version of this door failed. `identity()`
+        works only for a caller that already holds `JIT_AGENT_KEY`, and nothing minted that key
+        for a new device: it was written to the vault like a vendor secret, so every machine got
+        it by hand and every machine then held the same one. That is the standing secret ADR
+        0032 names, and it is the reason a newly imaged laptop was never seamless.
+
+        This door closes that. A device with NO broker key proves instead the one credential
+        every workstation already has restored before anyone sits down -- the age identity at
+        Level 1 of bin/idp-workstation-bootstrap -- and receives a fresh key that is its own.
+
+        How the age identity is proven without transmitting it: the device asks for a nonce
+        (`issue_nonce`) and posts back `nonce`, an ephemeral X25519 public key `ephemeral`, and
+        `mac`, an HMAC-SHA256 tag over the nonce keyed by an X25519 agreement between the device's
+        age secret key and `ephemeral`. This regenerates the ephemeral half here, runs the same
+        agreement with the estate's age recipient, and compares the tags with `hmac.compare_digest`.
+        Only a device holding the age secret can produce a matching tag, and the secret never
+        leaves the device. This mirrors age's own recipient unwrap, which is X25519 agreement --
+        a native age key has no signing half, so a signature would have been the wrong primitive.
+
+        The nonce is single-use and short-lived: consumed the moment it is checked, so a captured
+        proof cannot be replayed even against a broker that has not restarted.
+
+        What this deliberately is NOT: it is not a bearer secret on a wire. The device sends an
+        ephemeral public key and a tag, never the age secret; the key it receives is fresh, unique
+        to that call, and recorded in the ledger by a fingerprint, not a value. The ledger is the
+        record of who was given access (WJ.7) and holds no credential.
+        """
+        stopped = self.killswitch()
+        if stopped:
+            raise Refused(f"the broker is stopped: {stopped}")
+        if not self.age_recipient:
+            raise Refused(
+                "this broker holds no estate age recipient, so it cannot verify a device's "
+                "root trust. platform/jit/deployment.yaml is where JIT_AGE_RECIPIENT arrives"
+            )
+        if not self._check_enrollment_rate():
+            raise Refused(
+                "too many enrollments in the last hour; either a fleet is being imaged or "
+                "somebody is replaying nonces"
+            )
+        # Consume the nonce first, unconditionally: a nonce is single-use whether or not the
+        # signature over it turns out valid, so a failed attempt cannot be retried with a
+        # corrected signature against the same nonce.
+        seen = self._enrollment_nonces.pop(nonce, None)
+        if seen is None:
+            raise Refused(
+                "unknown or already-used nonce; ask the broker for a fresh one"
+            )
+        if self.now() - seen > self.ENROLLMENT_NONCE_TTL_SECONDS:
+            raise Refused("the nonce expired; ask the broker for a fresh one")
+        if not self._age_agreement_valid(nonce, ephemeral, mac):
+            # No ledger line, for the reason authenticate() gives for a bad key: a line per
+            # refusal lets a stranger fill the ledger volume. The ledger records what the broker
+            # did, and a refused enrollment did nothing.
+            raise Refused("not root trust for this estate")
+
+        key = secrets.token_urlsafe(32)
+        fingerprint = hashlib.sha256(key.encode()).hexdigest()[:16]
+        self.device_keys[fingerprint] = key
+        self.history.append((self.now(), "enroll"))
+        self.ledger.append(
+            "enrolled",
+            device_fingerprint=fingerprint,
+            # The method is named so a reader of the ledger can tell an age-rooted enrollment
+            # from any future one. The nonce is not recorded: a nonce that is written down is a
+            # nonce that can be replayed by whoever reads the ledger.
+            proof="age-recipient",
+        )
+        return {
+            "key": key,
+            "fingerprint": fingerprint,
+            "subject": f"system:serviceaccount:{self.AGENT_NS}:{self.AGENT_SA}",
+            "note": "this key is yours; the estate keeps only its fingerprint",
+        }
+
+    def issue_nonce(self) -> dict:
+        """Mint a single-use, short-lived challenge for one enrollment attempt.
+
+        Returns the nonce and the public half of a fresh ephemeral X25519 key. The device agrees
+        its age secret with that public half and returns an HMAC over the nonce; the private half
+        stays here to recompute the same. So the nonce is what makes the proof unforgeable by
+        replay: a proof over a value an attacker chose is worthless, and a proof over a value used
+        once is worthless after that use. Expired nonces (and their ephemeral halves) are swept
+        here rather than by a timer, because a broker with no clock thread has one fewer thing to
+        fail.
+        """
+        cutoff = self.now() - self.ENROLLMENT_NONCE_TTL_SECONDS
+        for n, at in list(self._enrollment_nonces.items()):
+            if at < cutoff:
+                del self._enrollment_nonces[n]
+                self._enrollment_ephemerals.pop(n, None)
+        nonce = secrets.token_urlsafe(32)
+        self._enrollment_nonces[nonce] = self.now()
+        # The ephemeral key for this nonce's agreement. Its public half goes to the device with
+        # the nonce; its private half stays here and is popped when the proof is checked, so a
+        # nonce is spent either way. Zeroed on drop is not meaningful in Python; what matters is
+        # that it is never serialized, never logged and never in the ledger.
+        ephemeral = X25519PrivateKey.generate()
+        self._enrollment_ephemerals[nonce] = ephemeral
+        return {
+            "nonce": nonce,
+            "ephemeral": ephemeral.public_key()
+            .public_bytes(Encoding.Raw, PublicFormat.Raw)
+            .hex(),
+        }
+
+    def _check_enrollment_rate(self) -> bool:
+        """Bound enrollments the same way identities are bounded, and for the same reason.
+
+        A handful of machines being imaged sits far below the cap. A replayed nonce does not.
+        Returns True while under the cap rather than raising, so the caller can answer one
+        refusal message for both a bad signature and a flood -- a stranger learns nothing about
+        which check they failed.
+        """
+        cutoff = self.now() - 3600
+        used = sum(1 for at, what in self.history if what == "enroll" and at > cutoff)
+        return used < self.ENROLLMENTS_PER_HOUR
+
+    def _age_agreement_valid(self, nonce: str, ephemeral: str, mac: str) -> bool:
+        """Check the device holds the age secret, by a Diffie-Hellman agreement it cannot fake.
+
+        The estate's age identity is native age, so the recipient `_bech32_data` decodes is an
+        X25519 public key and the private half is an X25519 scalar. There is no signing half to
+        verify a signature with, so the right primitive is agreement.
+
+        The broker generated, at `issue_nonce`, an ephemeral X25519 key and published only its
+        public half. The device derives `X25519(age_secret, ephemeral_public)`; the broker derives
+        `X25519(ephemeral_secret, recipient)`. These are equal exactly when `recipient` is
+        `X25519(age_secret, basepoint)` -- that is, when the device really holds the estate age
+        secret. The device returns `ephemeral` (its own ephemeral, so the broker can name the
+        agreement in the ledger without the secret) and `mac`, an HMAC-SHA256 over the nonce keyed
+        by the shared secret; the broker recomputes the same and compares with `compare_digest`.
+        The shared secret is never compared directly and never leaves either side.
+
+        An empty or malformed input is never valid, so a malformed request fails closed. Hex for
+        both fields because a device driving this from a shell round-trips hex more easily than
+        base64, and hex has no character an argument parser can eat.
+        """
+        if not nonce or not ephemeral or not mac:
+            return False
+        recipient = _bech32_data(self.age_recipient)
+        if recipient is None:
+            return False
+        secret = self._enrollment_ephemerals.pop(nonce, None)
+        if secret is None:
+            return False
+        try:
+            shared = secret.exchange(X25519PublicKey.from_public_bytes(recipient))
+            want = hmac.new(shared, nonce.encode(), hashlib.sha256).hexdigest()
+            return hmac.compare_digest(want, str(mac))
+        except (ValueError, TypeError):
+            return False
+
     def _cluster_address(self) -> dict:
         """Where the cluster is and the certificate that proves it, read from the cluster.
 
@@ -543,7 +802,7 @@ class Broker:
         ConfigMap is for -- kubeadm publishes it, and `system:public-info-viewer` makes it
         world-readable, precisely because a joining client needs the address and the CA
         before it holds any credential at all. So the estate keeps one copy of a machine
-        fact, in the cluster, and no file names it (LAW 46).
+        fact, in the cluster, and no file names it.
         """
         rc, out = self.kube(
             [
@@ -1033,7 +1292,7 @@ def _oci_vault_write(grant: dict, req: "Request", run, provider: str) -> dict:
 
     The value never appears in the record. `contents_b64` is the secret itself, so it is kept
     out of the argv summary, and the provider's own error text is scrubbed of it before it is
-    quoted anywhere -- a bad-request echo is the classic way a secret reaches a log (LAW 21).
+    quoted anywhere -- a bad-request echo is the classic way a secret reaches a log.
     """
     compartment = os.environ.get("JIT_OCI_COMPARTMENT_ID", "").strip()
     vault = os.environ.get("JIT_OCI_VAULT_ID", "").strip()
diff --git a/platform/jit/broker/serve.py b/platform/jit/broker/serve.py
index d656a0d29..0d47c175f 100644
--- a/platform/jit/broker/serve.py
+++ b/platform/jit/broker/serve.py
@@ -123,6 +123,11 @@ class Handler(BaseHTTPRequestHandler):
     #: key. `/healthz` is a GET the kubelet makes from the node, which holds no key either,
     #: and it answers with the ledger's own verdict on itself and nothing about the estate.
     AGENT_DOORS = ("/ask", "/state", "/grants", "/identity")
+    #: Doors a device with NO key must reach, or a fresh machine can never become an agent
+    #: (ADR 0032). `/enroll/nonce` hands out a challenge; `/enroll` verifies a signature over it
+    #: against the estate's age recipient. Neither accepts an agent key and neither can be used
+    #: to read the cluster: the most a caller gets is one key, once, for proving root trust.
+    OPEN_DOORS = ("/enroll/nonce", "/enroll")
 
     def path_after_prefix(self) -> str:
         """The path with the public door's prefix removed.
@@ -141,6 +146,24 @@ class Handler(BaseHTTPRequestHandler):
     def do_POST(self) -> None:  # noqa: N802
         try:
             path = self.path_after_prefix()
+            if path == "/enroll":
+                # Deliberately before the agent-key check and deliberately not behind it: the
+                # whole point is that a device reaching this door holds no key yet (ADR 0032).
+                # What it must hold instead is the estate's age identity, proven by the signature
+                # enroll() verifies. A refusal here is a 401 for the same reason the agent doors
+                # are: "I am not who I said", not "what you asked for is not allowed".
+                b = self._body()
+                try:
+                    return self._reply(
+                        200,
+                        self.broker.enroll(
+                            str(b.get("nonce") or ""),
+                            str(b.get("ephemeral") or ""),
+                            str(b.get("mac") or ""),
+                        ),
+                    )
+                except Refused as exc:
+                    return self._reply(401, {"error": str(exc)})
             if path in self.AGENT_DOORS:
                 # 401 before the body is even read, and separately from what the door then
                 # does, so a bad key reads as "I am not who I said" rather than the 400 that
@@ -207,7 +230,7 @@ class Handler(BaseHTTPRequestHandler):
                 # empirical-proof rule, founder 2026-09-05). Only *accepted* deliveries print:
                 # reaching this line costs the shared secret token, so the volume is Telegram's
                 # traffic and not a stranger's. `handled` is the broker's own verdict word and
-                # carries no chat text, no token and no callback data (LAW 21).
+                # carries no chat text, no token and no callback data.
                 print(f"jit telegram: mirrored delivery handled: {handled}", flush=True)
                 return self._reply(200, {"handled": handled})
             if path == "/grants":
@@ -223,6 +246,12 @@ class Handler(BaseHTTPRequestHandler):
         if self.path_after_prefix() == "/healthz":
             ok, why = self.broker.ledger.verify()
             return self._reply(200 if ok else 500, {"ledger": why})
+        if self.path_after_prefix() == "/enroll/nonce":
+            # GET, no key, and it hands out nothing but a challenge that expires in two minutes
+            # and is spent on first use: a nonce plus the public half of an ephemeral X25519 key.
+            # A stranger who collects a thousand of these has a thousand public strings; enrolling
+            # a device still costs the estate's age private key, which never leaves the device.
+            return self._reply(200, self.broker.issue_nonce())
         self._reply(404, {"error": "no such door"})
 
 
@@ -273,6 +302,10 @@ def main() -> None:
             os.environ.get("JIT_KILLSWITCH", "/var/lib/jit/stopped")
         ),
         agent_key=optional_secret("JIT_AGENT_KEY").encode(),
+        # A public recipient, so it is read from the environment like JIT_PUBLIC_PREFIX and not
+        # from the mounted secret files. Empty closes enrollment, which is the safe default: a
+        # broker that cannot check root trust must not hand out keys.
+        age_recipient=os.environ.get("JIT_AGE_RECIPIENT", ""),
     )
     phone = Phone(secret("TELEGRAM_BOT_TOKEN"), secret("TELEGRAM_CHAT_ID"), broker)
     Handler.broker, Handler.phone = broker, phone
diff --git a/platform/jit/deployment.yaml b/platform/jit/deployment.yaml
index 7f9a39eb7..0a1273bd4 100644
--- a/platform/jit/deployment.yaml
+++ b/platform/jit/deployment.yaml
@@ -19,8 +19,8 @@ spec:
   secretStoreRef: {kind: ClusterSecretStore, name: estate-vault}
   # The bot is the estate's ONE Telegram bot, read from the vault entry `flux-telegram` that
   # the alerts row already uses (keys `token` and `channel`). A second bot token would be a
-  # second copy of one credential, which is the split-brain the estate does not keep (LAW 54:
-  # no fresh key while one exists). Only the two values nothing else can hold -- the HMAC
+  # second copy of one credential, which is the split-brain the estate does not keep: no fresh
+  # key is minted while one exists. Only the two values nothing else can hold -- the HMAC
   # signing key and the webhook secret -- come from the broker's own entry, and both are minted
   # by code, never typed (R52).
   target:
@@ -44,7 +44,7 @@ spec:
         # that has to exist on a laptop, and a key that leaves the cluster must be rotatable
         # without touching anything that has not left it.
         JIT_AGENT_KEY: "{{ .JIT_AGENT_KEY }}"
-  # Two values, both minted in the vault and none typed here (LAW 46, R52):
+  # Two values, both minted in the vault and none typed here:
   #   JIT_SIGNING_KEY          -- the HMAC key the approvals are signed with. An identity that
   #                               can read this can forge a tap, so it is the crown jewel of
   #                               the crown jewel and no agent ServiceAccount reaches namespace
@@ -109,6 +109,12 @@ spec:
             # a door that answers 404, and routed there and not set here is a 404 the other
             # way round.
             - {name: JIT_PUBLIC_PREFIX, value: /jit}
+            # The estate's age PUBLIC recipient, for /enroll (ADR 0032). It is a public key, not
+            # a secret: the broker only ever agrees a key with it to check root trust, and the
+            # matching secret exists on the workstations and nowhere in this cluster. Flux
+            # substitutes ESTATE_AGE_RECIPIENT from clusters/oke/estate-config.yaml -- one copy,
+            # in git, and no vendor secret minted for it.
+            - {name: JIT_AGE_RECIPIENT, value: "${ESTATE_AGE_RECIPIENT}"}
             # The four secret values are NOT here. A secret in an env var is printed by every
             # crash dump, `kubectl describe pod`, and any library that logs its environment on
             # start -- which is why kyverno's secrets-not-from-env-vars refuses it. They arrive
@@ -174,8 +180,8 @@ metadata:
   #
   # Why the tailnet and not the public edge. Exposing /ask on the public host would need a
   # credential in front of it, and a credential minted to answer this is a second identity
-  # layer for one door -- refused by the estate's SSO policy and by LAW 54 (no fresh key while
-  # one exists). The tailnet is the identity layer that already exists here, it is federated,
+  # layer for one door -- refused by the estate's SSO policy and by the rule that no fresh key
+  # is minted while one exists. The tailnet is the identity layer that already exists here, it is federated,
   # and policy.hujson is deny-by-default, so the ACL is the authentication and it is in git
   # under review. Nothing about a grant changes: the ask still buzzes the founder's phone, the
   # tap is still HMAC-checked, the TTL still expires on its own. This widens who may *ask*, to
diff --git a/platform/jit/public-door.yaml b/platform/jit/public-door.yaml
index 31296dc6e..26960d4af 100644
--- a/platform/jit/public-door.yaml
+++ b/platform/jit/public-door.yaml
@@ -48,8 +48,15 @@ spec:
     - "otto.${ESTATE_ZONE}"
   rules:
     # An exact list and not a prefix. A prefix would carry any path the broker ever grows,
-    # including one added without this file being reopened; these four are the doors an agent
-    # uses and the set is small enough to write down.
+    # including one added without this file being reopened; these doors are the set an agent
+    # uses and it is small enough to write down.
+    #
+    # The two enrollment doors (ADR 0032) are here deliberately and are not a hole. A fresh
+    # machine holds no agent key, so it cannot use the four above -- enrollment is how it comes
+    # to hold one. Neither door reads or writes the cluster: `/enroll/nonce` hands out a
+    # challenge, `/enroll` returns one key to a caller that proves the estate's age identity by
+    # an X25519 agreement it cannot fake without the age secret. A stranger gets a key that buys
+    # exactly what every agent key buys -- nothing, until the founder taps.
     - matches:
         - path: {type: Exact, value: /jit/ask}
           method: POST
@@ -59,6 +66,10 @@ spec:
           method: POST
         - path: {type: Exact, value: /jit/identity}
           method: POST
+        - path: {type: Exact, value: /jit/enroll/nonce}
+          method: GET
+        - path: {type: Exact, value: /jit/enroll}
+          method: POST
       backendRefs:
         - name: jit-broker
           port: 8080
diff --git a/tests/test_the_broker_can_be_asked_from_outside_the_cluster.py b/tests/test_the_broker_can_be_asked_from_outside_the_cluster.py
index ac1cfd5b2..ae270ae91 100644
--- a/tests/test_the_broker_can_be_asked_from_outside_the_cluster.py
+++ b/tests/test_the_broker_can_be_asked_from_outside_the_cluster.py
@@ -248,8 +248,8 @@ def test_the_three_files_agree_on_the_prefix():
     assert all(p.startswith(prefix + "/") for p in paths), paths
 
 
-def _agent_doors() -> set[str]:
-    """The handler's AGENT_DOORS, read out of the source rather than imported.
+def _doors_named(name: str) -> set[str]:
+    """One of the handler's door tuples, read out of the source rather than imported.
 
     serve.py is a module of a package this repository also imports elsewhere, and loading it a
     second time under a name the package already holds builds a second set of class objects --
@@ -258,12 +258,18 @@ def _agent_doors() -> set[str]:
     """
     tree = ast.parse((ROOT / "platform" / "jit" / "broker" / "serve.py").read_text())
     for node in ast.walk(tree):
-        if isinstance(node, ast.AnnAssign | ast.Assign):
+        if isinstance(node, ast.AnnAssign) or isinstance(node, ast.Assign):
             targets = [node.target] if isinstance(node, ast.AnnAssign) else node.targets
             names = [t.id for t in targets if isinstance(t, ast.Name)]
-            if "AGENT_DOORS" in names and node.value is not None:
+            if name in names and node.value is not None:
                 return set(ast.literal_eval(node.value))
-    raise AssertionError("no AGENT_DOORS in serve.py")
+    raise AssertionError(f"no {name} in serve.py")
+
+
+def _agent_doors() -> set[str]:
+    """Every path the public route must carry: the keyed agent doors plus the keyless
+    enrollment doors (ADR 0032), because a fresh machine with no key can reach nothing else."""
+    return _doors_named("AGENT_DOORS") | _doors_named("OPEN_DOORS")
 
 
 def test_the_route_carries_every_door_the_client_can_dial():
diff --git a/tests/test_the_jit_broker_cannot_be_talked_into_standing_access.py b/tests/test_the_jit_broker_cannot_be_talked_into_standing_access.py
index d461c705d..5f612f977 100644
--- a/tests/test_the_jit_broker_cannot_be_talked_into_standing_access.py
+++ b/tests/test_the_jit_broker_cannot_be_talked_into_standing_access.py
@@ -89,6 +89,7 @@ def make(
     now=None,
     providers=None,
     agent_key: bytes = b"",
+    age_recipient: str = "",
 ) -> Broker:
     return Broker(
         catalogue=CATALOGUE,
@@ -99,6 +100,7 @@ def make(
         now=now or time.time,
         providers=providers,
         agent_key=agent_key,
+        age_recipient=age_recipient,
     )
 
 
@@ -661,7 +663,7 @@ def test_the_gate_refuses_a_grant_with_no_step_even_when_it_escalates_nothing(tm
 
 def _load_gate():
     """Load bin/idp-jit-grants by path. It has no .py suffix and is not a package, which is
-    how workspace.yaml loads code locations too (LAW 45)."""
+    how workspace.yaml loads code locations too."""
     import importlib.machinery
     import importlib.util
 
@@ -708,7 +710,7 @@ def test_an_approved_vault_write_never_runs_another_grants_step(tmp_path):
 
     Dispatching below-Kubernetes acts by provider meant every OCI grant ran the one OCI step
     that existed. This asserts the vault write builds a vault argv -- and, because the value is
-    the secret, that the record of what happened does not contain it (LAW 21).
+    the secret, that the record of what happened does not contain it.
     """
     import os
 
@@ -1267,3 +1269,348 @@ def test_healthz_answers_on_the_prefix_and_is_not_on_the_public_route(tmp_path):
     finally:
         httpd.shutdown()
     assert code == 200
+
+
+# --- enrollment: how a fresh machine with no key comes to hold one (ADR 0032) -----------
+#
+# Every door above refuses a caller with no key, which is the whole point of them -- and the
+# reason a newly imaged laptop could never become an agent. This door is the other half: a
+# device proves the one credential every workstation already restores before anyone sits down
+# (the age identity at Level 1 of bin/idp-workstation-bootstrap) and receives a key of its own.
+#
+# The proof is an X25519 agreement, not a signature, because a native age key has no signing
+# half. The device agrees its age secret with an ephemeral public key the broker minted, and
+# returns an HMAC over the nonce keyed by the shared secret. The secret never leaves the device;
+# the broker never holds it. These tests drive the real door and a real age key, and check that
+# an impostor who holds no age secret cannot produce a matching tag.
+
+#: A throwaway age identity for the suite, in both forms a device would hold it: the secret is
+#: the scalar, the recipient is the `age1...` its public half encodes to. It is *derived here* from
+#: a fixed label rather than pasted, so no key-shaped literal exists to leak, grep, or mistake for
+#: the estate's. This is NOT the estate's root age key -- the estate's own lives in Prospector's
+#: age-key.txt and must never appear in a repo.
+_AGE_LABEL = b"jit-broker-enrollment-test/x25519/v1"
+
+_CH_B32 = "qpzry9x8gf2tvdw0s3jn54khce6mua7l"
+
+
+def _bech32_age(pub: bytes) -> str:
+    """Encode an X25519 public key as a real `age1...` recipient, checksum included."""
+    acc = bits = 0
+    data = []
+    for byte in pub:
+        acc = (acc << 8) | byte
+        bits += 8
+        while bits >= 5:
+            bits -= 5
+            data.append((acc >> bits) & 0x1F)
+    if bits:
+        data.append((acc << (5 - bits)) & 0x1F)
+
+    def polymod(values):
+        gen = (0x3B6A57B2, 0x26508E6D, 0x1EA119FA, 0x3D4233DD, 0x2A1462B3)
+        chk = 1
+        for v in values:
+            top = chk >> 25
+            chk = (chk & 0x1FFFFFF) << 5 ^ v
+            for i in range(5):
+                chk ^= gen[i] if ((top >> i) & 1) else 0
+        return chk
+
+    hrp = "age"
+    hrp_expand = [ord(c) >> 5 for c in hrp] + [0] + [ord(c) & 31 for c in hrp]
+    pm = polymod(hrp_expand + data + [0, 0, 0, 0, 0, 0]) ^ 1
+    checksum = [(pm >> 5 * (5 - i)) & 31 for i in range(6)]
+    return hrp + "1" + "".join(_CH_B32[d] for d in data + checksum)
+
+
+def _derive_age_key():
+    from hashlib import sha256
+
+    from cryptography.hazmat.primitives import serialization
+    from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PrivateKey
+
+    from_bytes = X25519PrivateKey.from_private_bytes
+    priv = from_bytes(sha256(_AGE_LABEL).digest())
+    pub = priv.public_key().public_bytes(
+        serialization.Encoding.Raw, serialization.PublicFormat.Raw
+    )
+    hexed = priv.private_bytes(
+        serialization.Encoding.Raw,
+        serialization.PrivateFormat.Raw,
+        serialization.NoEncryption(),
+    ).hex()
+    return hexed, pub
+
+
+AGE_SECRET_HEX, _AGE_PUB = _derive_age_key()
+AGE_RECIPIENT = _bech32_age(_AGE_PUB)
+
+
+def _age_secret():
+    from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PrivateKey
+
+    return X25519PrivateKey.from_private_bytes(bytes.fromhex(AGE_SECRET_HEX))
+
+
+def _age_secret_bech32() -> str:
+    """The 32-byte secret as the `AGE-SECRET-KEY-1...` bech32 the file on disk uses.
+
+    Matches the estate's exact form so the client decoder is graded against the real shape and
+    not a simplification. The checksum is faked -- the client does not verify it, the broker
+    has no use for it -- but the data characters are real, which is what the bech32->X25519
+    path actually consumes.
+    """
+    ch = "qpzry9x8gf2tvdw0s3jn54khce6mua7l"
+    raw = bytes.fromhex(AGE_SECRET_HEX)
+    acc = bits = 0
+    out = ""
+    for byte in raw:
+        acc = (acc << 8) | byte
+        bits += 8
+        while bits >= 5:
+            bits -= 5
+            out += ch[(acc >> bits) & 0x1F]
+    if bits:
+        out += ch[(acc << (5 - bits)) & 0x1F]
+    return out + "qqqqqq"
+
+
+def _nonce_over_http(httpd, path="/enroll/nonce"):
+    host, port = httpd.server_address[0], httpd.server_address[1]
+    conn = http.client.HTTPConnection(host, port, timeout=5)
+    conn.request("GET", path)
+    res = conn.getresponse()
+    body = json.loads(res.read() or b"{}")
+    code = res.status
+    conn.close()
+    return code, body
+
+
+def _enroll_over_http(httpd, payload, path="/enroll"):
+    host, port = httpd.server_address[0], httpd.server_address[1]
+    conn = http.client.HTTPConnection(host, port, timeout=5)
+    conn.request(
+        "POST", path, json.dumps(payload), {"Content-Type": "application/json"}
+    )
+    res = conn.getresponse()
+    body = json.loads(res.read() or b"{}")
+    code = res.status
+    conn.close()
+    return code, body
+
+
+def _prove(nonce_body, secret):
+    """What a device does with a nonce: agree its age secret with the broker's ephemeral public
+    key and key an HMAC over the nonce with the shared secret."""
+    import hashlib
+    import hmac as _hmac
+
+    from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PublicKey
+
+    nonce = nonce_body["nonce"]
+    ephemeral = bytes.fromhex(nonce_body["ephemeral"])
+    shared = secret.exchange(X25519PublicKey.from_public_bytes(ephemeral))
+    return {
+        "nonce": nonce,
+        "ephemeral": ephemeral.hex(),
+        "mac": _hmac.new(shared, nonce.encode(), hashlib.sha256).hexdigest(),
+    }
+
+
+def test_a_fresh_device_enrolls_and_gets_a_key_of_its_own(tmp_path):
+    """The seamless case this whole door exists for: a machine holding the estate age identity
+    and no broker key reaches in and walks away with a key that is its own."""
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    httpd = _door(broker, SilentPhone(), public_prefix="/jit")
+    try:
+        code, nonce = _nonce_over_http(httpd, path="/jit/enroll/nonce")
+        assert code == 200, nonce
+        code, body = _enroll_over_http(
+            httpd, _prove(nonce, _age_secret()), path="/jit/enroll"
+        )
+    finally:
+        httpd.shutdown()
+    assert code == 200, body
+    assert body["key"], "enrollment must hand back the device's own key"
+    assert body["fingerprint"]
+    # And the key it received actually identifies it: present it as a bearer and it is accepted.
+    broker.authenticate(body["key"])
+    assert broker._acting_fingerprint == body["fingerprint"]
+
+
+def test_the_ledger_records_the_enrollment_by_fingerprint_and_not_by_key(tmp_path):
+    """WJ.7: the record of who was given access holds no credential. The device's key must not
+    be on the ledger, and neither the nonce nor the shared secret."""
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    httpd = _door(broker, SilentPhone())
+    try:
+        _, nonce = _nonce_over_http(httpd)
+        proof = _prove(nonce, _age_secret())
+        _, body = _enroll_over_http(httpd, proof)
+    finally:
+        httpd.shutdown()
+    raw = (tmp_path / "ledger.jsonl").read_text()
+    line = json.loads(raw.splitlines()[0])
+    assert line["event"] == "enrolled"
+    assert line["device_fingerprint"] == body["fingerprint"]
+    assert line["proof"] == "age-recipient"
+    assert body["key"] not in raw, (
+        "the ledger is not a second copy of the key it recorded"
+    )
+    assert proof["mac"] not in raw
+    assert nonce["nonce"] not in raw
+
+
+def test_a_nonce_cannot_be_replayed_for_a_second_key(tmp_path):
+    """A captured proof is worthless after its one use, or one enrollment would mint keys
+    forever."""
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    httpd = _door(broker, SilentPhone())
+    try:
+        _, nonce = _nonce_over_http(httpd)
+        proof = _prove(nonce, _age_secret())
+        first, _ = _enroll_over_http(httpd, proof)
+        second, _ = _enroll_over_http(httpd, proof)
+    finally:
+        httpd.shutdown()
+    assert first == 200
+    assert second == 401
+
+
+def test_a_wrong_mac_is_refused(tmp_path):
+    """The tag is what proves the secret was held. A device that gets it wrong gets no key."""
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    httpd = _door(broker, SilentPhone())
+    try:
+        _, nonce = _nonce_over_http(httpd)
+        proof = _prove(nonce, _age_secret())
+        proof["mac"] = "00" * 32
+        code, _ = _enroll_over_http(httpd, proof)
+    finally:
+        httpd.shutdown()
+    assert code == 401
+
+
+def test_a_caller_that_does_not_hold_the_age_secret_is_refused(tmp_path):
+    """The case that matters. A device with its own key agrees against the broker's ephemeral,
+    produces a tag, and posts a well-formed request -- and is refused, because the broker agrees
+    against the ESTATE recipient and gets a different secret. That is what makes the age identity
+    root trust rather than a password anyone can present."""
+    from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PrivateKey
+    from cryptography.hazmat.primitives.serialization import Encoding, PublicFormat
+
+    impostor = X25519PrivateKey.generate()
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    httpd = _door(broker, SilentPhone())
+    try:
+        _, nonce = _nonce_over_http(httpd)
+        # Send the impostor's own public key as the ephemeral, keyed as it would be -- honest,
+        # but with the wrong secret. The broker agrees with the estate recipient regardless, so
+        # the tags cannot match.
+        import hashlib
+        import hmac as _hmac
+
+        from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PublicKey
+
+        ephemeral = bytes.fromhex(nonce["ephemeral"])
+        shared = impostor.exchange(X25519PublicKey.from_public_bytes(ephemeral))
+        payload = {
+            "nonce": nonce["nonce"],
+            "ephemeral": impostor.public_key()
+            .public_bytes(Encoding.Raw, PublicFormat.Raw)
+            .hex(),
+            "mac": _hmac.new(
+                shared, nonce["nonce"].encode(), hashlib.sha256
+            ).hexdigest(),
+        }
+        code, _ = _enroll_over_http(httpd, payload)
+    finally:
+        httpd.shutdown()
+    assert code == 401
+
+
+def test_enrollment_is_closed_when_the_broker_holds_no_age_recipient(tmp_path):
+    """A broker that cannot check root trust must not hand out keys -- the same fail-closed shape
+    as a broker with no agent key refusing every ask."""
+    broker = make(tmp_path)  # age_recipient defaults to ""
+    httpd = _door(broker, SilentPhone())
+    try:
+        code, nonce = _nonce_over_http(httpd)
+        # The nonce door still answers (it holds no trust decision), but enroll refuses.
+        enroll, _ = _enroll_over_http(httpd, _prove(nonce, _age_secret()))
+    finally:
+        httpd.shutdown()
+    assert code == 200
+    assert enroll == 401
+
+
+def test_the_client_enrolls_from_the_age_identity_and_writes_its_own_key(
+    tmp_path, monkeypatch
+):
+    """bin/idp-jit enroll, over a real socket, against a real broker. The device holds the estate
+    age secret and no broker key; it must walk away with a key where _agent_key reads, mode 600."""
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    httpd = _door(broker, SilentPhone())
+    host, port = httpd.server_address[0], httpd.server_address[1]
+    # A throwaway age-key.txt in the exact form the estate's is in, so the client's decoder is
+    # graded against the real shape and not a simplification.
+    age_file = tmp_path / "age-key.txt"
+    age_file.write_text(
+        "# created: test\n"
+        "# public key: " + AGE_RECIPIENT + "\n"
+        "AGE-SECRET-KEY-1" + _age_secret_bech32() + "\n"
+    )
+    state = tmp_path / "state"
+    monkeypatch.setenv("JIT_BROKER_URL", f"http://{host}:{port}")
+    monkeypatch.setenv("SOPS_AGE_KEY_FILE", str(age_file))
+    monkeypatch.setenv("IDP_KUBE_STATE", str(state))
+    monkeypatch.delenv("JIT_AGENT_KEY", raising=False)
+    mod = _idp_jit()
+    try:
+        rc = mod.enroll()
+    finally:
+        httpd.shutdown()
+    assert rc == 0
+    written = (state / "agent-key").read_text().strip()
+    assert written
+    assert oct((state / "agent-key").stat().st_mode & 0o777) == oct(0o600)
+    broker.authenticate(written)  # the broker accepts the key the client wrote
+
+
+def test_the_client_enroll_refuses_cleanly_when_it_holds_no_age_identity(
+    tmp_path, monkeypatch
+):
+    """No age identity is Level 1 of the bootstrap, and the one failure the person still owns.
+    It must refuse with a reason, not a traceback, and write no key."""
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    httpd = _door(broker, SilentPhone())
+    host, port = httpd.server_address[0], httpd.server_address[1]
+    state = tmp_path / "state"
+    monkeypatch.setenv("JIT_BROKER_URL", f"http://{host}:{port}")
+    monkeypatch.setenv("SOPS_AGE_KEY_FILE", str(tmp_path / "nope.txt"))
+    monkeypatch.setenv("IDP_KUBE_STATE", str(state))
+    monkeypatch.delenv("JIT_AGENT_KEY", raising=False)
+    mod = _idp_jit()
+    try:
+        rc = mod.enroll()
+    finally:
+        httpd.shutdown()
+    assert rc == mod.CODES["refused"]
+    assert not (state / "agent-key").exists()
+
+
+def test_an_enrollment_is_recorded_as_an_identity_and_not_a_grant(tmp_path):
+    """Enrollment mints an identity, not access: it never touches the catalogue and never wakes
+    the founder. A key from this door buys exactly what every agent key buys -- nothing until he
+    taps -- so it must not appear on his phone."""
+    broker = make(tmp_path, age_recipient=AGE_RECIPIENT)
+    phone = SilentPhone()
+    httpd = _door(broker, phone)
+    try:
+        _, nonce = _nonce_over_http(httpd)
+        _, body = _enroll_over_http(httpd, _prove(nonce, _age_secret()))
+    finally:
+        httpd.shutdown()
+    assert body["key"]
+    assert phone.sent == [], "enrollment must never reach the founder's phone"
