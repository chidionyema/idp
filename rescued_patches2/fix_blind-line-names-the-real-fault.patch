diff --git a/bin/idp-cloud b/bin/idp-cloud
index 71da280de..1a43a300a 100755
--- a/bin/idp-cloud
+++ b/bin/idp-cloud
@@ -11,6 +11,7 @@
 #   bin/idp-cloud secret delete NAME                  schedule deletion; absent is exit 0
 #   bin/idp-cloud secret describe NAME                JSON {"id","vault-id","key-id"} of the ACTIVE secret in the compartment; exit 1 NotFound, 3 Split
 #   bin/idp-cloud secret list [--vault VAULT_ID]      one ACTIVE secret name per line; --vault restricts to that vault only
+#   bin/idp-cloud identity env                       `export OCI_CLI_PROFILE=... OCI_CLI_AUTH=...` for eval; the one door to the estate's OCI identity
 #   bin/idp-cloud vault list                         one ACTIVE vault per line as "<display-name> <id>", sorted by name
 #   bin/idp-cloud bucket head NAME                   exit 0 "ok" if the bucket exists in the tenancy namespace; exit 1 with NotFound on stderr when absent, exit 2 BLIND when the namespace cannot be read
 #   bin/idp-cloud cluster list                       one ACTIVE cluster per line as "<name> <id>", sorted by name
@@ -82,28 +83,94 @@ done
 file_object_path() { printf '%s' "$IDP_CLOUD_FILE_ROOT/objects/$BUCKET/$NAME"; }
 file_secret_path() { printf '%s' "$IDP_CLOUD_FILE_ROOT/secrets/$NAME"; }
 
-# Resolve $C the same way every oci verb does: whoami, the security_token auth, the sops envelope,
-# the env override. Centralised so vault-list and cluster-list share the exact same shape (crew#66 CP5d).
-oci_compartment() {
-  local MAIN
-  MAIN=$(dirname "$(git -C "$IDP" rev-parse --path-format=absolute --git-common-dir)")
-  if [ -z "${OCI_CLI_PROFILE:-}" ]; then
-    # shellcheck source=idp-oci-whoami
-    source "$IDP/bin/idp-oci-whoami" >&2 || return 2
+# One value out of the tfvars the provisioner already rendered. That file is the estate's own
+# record of how THIS machine talks to OCI -- compartment_ocid, oci_profile, oci_auth, region -- and
+# both the laptop and the runner have it (the runner has it and no age key, idp#490).
+oci_tfvar() {
+  [ -f "$IDP/platform/oci/terraform.tfvars" ] || return 0
+  awk -F'"' -v k="$1" '$0 ~ "^"k"[ \t]*=" {print $2; exit}' "$IDP/platform/oci/terraform.tfvars"
+}
+
+# Which auth mode a profile can actually use. This used to be the hard default `security_token`,
+# and on 2026-08-31 that default was the whole of the wrong BLIND line: the laptop's working
+# profile is DEFAULT, an API-key profile, and security_token is a mode it cannot use. The tfvars
+# spelling ("APIKey") is the estate's own recorded answer; the profile's shape in ~/.oci/config is
+# the fallback, because a session-token profile is exactly the one that names a security_token_file.
+# When neither is knowable (a runner with no config file and no oci_auth key) the old default stands.
+oci_auth_for_profile() {
+  local a cfg
+  # tfvars records ONE machine's answer, so it answers for the profile it names and no other.
+  a=""
+  [ "$1" = "$(oci_tfvar oci_profile)" ] && a=$(printf '%s' "$(oci_tfvar oci_auth)" | tr '[:upper:]' '[:lower:]')
+  case "$a" in
+    apikey|api_key) printf 'api_key'; return 0;;
+    securitytoken|security_token) printf 'security_token'; return 0;;
+    instanceprincipal|instance_principal) printf 'instance_principal'; return 0;;
+  esac
+  cfg=${OCI_CLI_CONFIG_FILE:-$HOME/.oci/config}
+  [ -f "$cfg" ] || { printf 'security_token'; return 0; }
+  awk -v p="[$1]" '$0==p{f=1;next} /^\[/{f=0} f&&/^[ \t]*security_token_file/{s=1} END{print s?"security_token":"api_key"}' "$cfg"
+}
+
+# Export the profile and auth mode into THIS shell -- not into a $(...) subshell. An export inside
+# a command substitution dies with it, which is why the old `export OCI_CLI_AUTH` on line 94 never
+# reached the `oci` call that needed it.
+oci_identity() {
+  export SUPPRESS_LABEL_WARNING=True
+  export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt}
+  local p
+  p=${OCI_CLI_PROFILE:-}
+  [ -n "$p" ] || p=$(oci_tfvar oci_profile)
+  if [ -n "$p" ]; then
+    export OCI_CLI_PROFILE="$p"
+    export OCI_CLI_AUTH=${OCI_CLI_AUTH:-$(oci_auth_for_profile "$p")}
+    return 0
   fi
-  export OCI_CLI_AUTH=${OCI_CLI_AUTH:-security_token}
-  export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-  # Order: env, the tfvars the provisioner already rendered (the runner has that and no age key —
-  # idp#490 check job: nodes went BLIND for this), then the sops envelope on a laptop.
+  # Last resort: discover a live browser session. As a SUBPROCESS, never `source`. idp-oci-whoami
+  # ends in `exit 2`; sourced, that kills the caller instead of returning a status, and sourced
+  # inside a command substitution it killed only the subshell and left $C silently empty.
+  p=$("$IDP/bin/idp-oci-whoami" 2>/dev/null) || return 2
+  [ -n "$p" ] || return 2
+  export OCI_CLI_PROFILE="$p" OCI_CLI_AUTH=security_token
+  return 0
+}
+
+# Resolve $C: the env override, the tfvars the provisioner already rendered (the runner has that
+# and no age key -- idp#490 check job: nodes went BLIND for this), then the sops envelope on a
+# laptop. Centralised so vault-list and cluster-list share the exact same shape (crew#66 CP5d).
+oci_compartment() {
+  local MAIN T
   if [ -n "${OCI_COMPARTMENT_OCID:-}" ]; then printf '%s' "$OCI_COMPARTMENT_OCID"; return 0; fi
-  if [ -f "$IDP/platform/oci/terraform.tfvars" ]; then
-    local T; T=$(awk -F'"' '/^compartment_ocid/{print $2}' "$IDP/platform/oci/terraform.tfvars")
-    if [ -n "$T" ]; then printf '%s' "$T"; return 0; fi
-  fi
+  T=$(oci_tfvar compartment_ocid)
+  if [ -n "$T" ]; then printf '%s' "$T"; return 0; fi
+  MAIN=$(dirname "$(git -C "$IDP" rev-parse --path-format=absolute --git-common-dir)")
   sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$MAIN/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml"
 }
 
+# Both halves, each named separately when it is the one that is missing. The old code ran
+# `C=$(oci_compartment)` and checked nothing, so a failed identity became an empty --compartment-id
+# and OCI answered `InvalidParameter: compartmentId is not available` -- which the caller then
+# reported as an expired session (2026-08-31, bin/idp-kube get nodes).
+oci_ready() {
+  oci_identity || blind "no OCI identity: OCI_CLI_PROFILE is unset, platform/oci/terraform.tfvars names no oci_profile, and no profile in ~/.oci/sessions has a valid token -- export OCI_CLI_PROFILE, or run bin/idp-oci-login"
+  C=$(oci_compartment 2>/dev/null)
+  [ -n "$C" ] || blind "no compartment id as profile ${OCI_CLI_PROFILE:-<unset>}: OCI_COMPARTMENT_OCID is unset, platform/oci/terraform.tfvars names no compartment_ocid, and the sops envelope did not decrypt -- export OCI_COMPARTMENT_OCID"
+}
+
 case "$prim" in
+  # bin/idp-vault-put and anything else outside this file used to source bin/idp-oci-whoami and
+  # then hardcode `OCI_CLI_AUTH=security_token` themselves, which is the same defect in a second
+  # place. `eval "$(bin/idp-cloud identity env)"` is the one door (crew#66: every other bin/idp-*
+  # goes through bin/idp-cloud). It prints assignments and nothing else, so eval is safe to read.
+  identity)
+    case "$verb" in
+      env)
+        oci_identity || blind "no OCI identity: OCI_CLI_PROFILE is unset, platform/oci/terraform.tfvars names no oci_profile, and no profile in ~/.oci/sessions has a valid token -- export OCI_CLI_PROFILE, or run bin/idp-oci-login"
+        printf 'export OCI_CLI_PROFILE=%s OCI_CLI_AUTH=%s SUPPRESS_LABEL_WARNING=True\n' "$OCI_CLI_PROFILE" "$OCI_CLI_AUTH"
+        ;;
+      *) blind "unknown identity $verb";;
+    esac
+    ;;
   object)
     case "$verb" in
       head)
@@ -206,13 +273,7 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
             MAIN=$(dirname "$(git -C "$IDP" rev-parse --path-format=absolute --git-common-dir)")
-            if [ -z "${OCI_CLI_PROFILE:-}" ]; then
-              # shellcheck source=idp-oci-whoami
-              source "$IDP/bin/idp-oci-whoami" >&2 || exit 2
-            fi
-            export OCI_CLI_AUTH=${OCI_CLI_AUTH:-security_token}
-            export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-            C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$MAIN/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
+            oci_ready
             ids=$(oci vault secret list --compartment-id "$C" --name "$NAME" --all --query "data[?\"lifecycle-state\"=='ACTIVE'].id" --output json 2>/dev/null | python3 -c 'import json,sys; [print(i) for i in json.load(sys.stdin)]' 2>/dev/null) || ids=""
             n=$(printf '%s\n' "$ids" | grep -c .)
             [ "$n" -le 1 ] || { echo "Split: secret $NAME is ACTIVE in $n vaults of the compartment; bin/idp-vault-split-guard names them" >&2; exit 3; }
@@ -241,13 +302,7 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
             MAIN=$(dirname "$(git -C "$IDP" rev-parse --path-format=absolute --git-common-dir)")
-            if [ -z "${OCI_CLI_PROFILE:-}" ]; then
-              # shellcheck source=idp-oci-whoami
-              source "$IDP/bin/idp-oci-whoami" >&2 || exit 2
-            fi
-            export OCI_CLI_AUTH=${OCI_CLI_AUTH:-security_token}
-            export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-            C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$MAIN/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
+            oci_ready
             ids=$(oci vault secret list --compartment-id "$C" --name "$NAME" --all --query "data[?\"lifecycle-state\"=='ACTIVE'].id" --output json 2>/dev/null | python3 -c 'import json,sys; [print(i) for i in json.load(sys.stdin)]' 2>/dev/null) || ids=""
             n=$(printf '%s\n' "$ids" | grep -c .)
             [ "$n" -le 1 ] || { echo "Split: secret $NAME is ACTIVE in $n vaults of the compartment; bin/idp-vault-split-guard names them" >&2; exit 3; }
@@ -280,13 +335,7 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
             MAIN=$(dirname "$(git -C "$IDP" rev-parse --path-format=absolute --git-common-dir)")
-            if [ -z "${OCI_CLI_PROFILE:-}" ]; then
-              # shellcheck source=idp-oci-whoami
-              source "$IDP/bin/idp-oci-whoami" >&2 || exit 2
-            fi
-            export OCI_CLI_AUTH=${OCI_CLI_AUTH:-security_token}
-            export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-            C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$MAIN/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
+            oci_ready
             ids=$(oci vault secret list --compartment-id "$C" --name "$NAME" --all --query "data[?\"lifecycle-state\"=='ACTIVE'].id" --output json 2>/dev/null | python3 -c 'import json,sys; [print(i) for i in json.load(sys.stdin)]' 2>/dev/null) || ids=""
             n=$(printf '%s\n' "$ids" | grep -c .)
             [ "$n" -le 1 ] || { echo "Split: secret $NAME is ACTIVE in $n vaults of the compartment; bin/idp-vault-split-guard names them" >&2; exit 3; }
@@ -313,13 +362,7 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
             MAIN=$(dirname "$(git -C "$IDP" rev-parse --path-format=absolute --git-common-dir)")
-            if [ -z "${OCI_CLI_PROFILE:-}" ]; then
-              # shellcheck source=idp-oci-whoami
-              source "$IDP/bin/idp-oci-whoami" >&2 || exit 2
-            fi
-            export OCI_CLI_AUTH=${OCI_CLI_AUTH:-security_token}
-            export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-            C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$MAIN/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
+            oci_ready
             list=$(oci vault secret list --compartment-id "$C" --name "$NAME" --all --query "data[?\"lifecycle-state\"=='ACTIVE']" --output json 2>/dev/null) || list=""
             n=$(printf '%s\n' "$list" | jq 'length // 0' 2>/dev/null) || n=0
             [ "$n" -le 1 ] || { echo "Split: secret $NAME is ACTIVE in $n vaults of the compartment; bin/idp-vault-split-guard names them" >&2; exit 3; }
@@ -346,13 +389,7 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
             MAIN=$(dirname "$(git -C "$IDP" rev-parse --path-format=absolute --git-common-dir)")
-            if [ -z "${OCI_CLI_PROFILE:-}" ]; then
-              # shellcheck source=idp-oci-whoami
-              source "$IDP/bin/idp-oci-whoami" >&2 || exit 2
-            fi
-            export OCI_CLI_AUTH=${OCI_CLI_AUTH:-security_token}
-            export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-            C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$MAIN/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
+            oci_ready
             if [ -n "$VAULT" ]; then
               V=$VAULT
             else
@@ -382,8 +419,11 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
             ;;
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
-            C=$(oci_compartment)
-            oci kms management vault list --compartment-id "$C" --all --query "data[?\"lifecycle-state\"=='ACTIVE'].[\"display-name\",id]" --output json | jq -r '.[] | "\(.[0]) \(.[1])"' | sort || blind "oci kms management vault list failed"
+            oci_ready
+            out=$(oci kms management vault list --compartment-id "$C" --all --query "data[?\"lifecycle-state\"=='ACTIVE'].[\"display-name\",id]" --output json 2>&1) \
+              || blind "oci kms management vault list failed as profile ${OCI_CLI_PROFILE:-<unset>} auth ${OCI_CLI_AUTH:-<unset>}: $(oneline "$(printf '%s' "$out" | grep -oE '"(code|message)": "[^"]*"' | paste -sd' ' -)")"
+            [ -n "$out" ] || exit 0
+            printf '%s' "$out" | jq -r '.[] | "\(.[0]) \(.[1])"' | sort
             ;;
           *) blind "unknown backend $BACKEND";;
         esac
@@ -432,8 +472,14 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
             ;;
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
-            C=$(oci_compartment)
-            oci ce cluster list --compartment-id "$C" --lifecycle-state ACTIVE --query 'data[].[name,id]' --output json | jq -r '.[] | "\(.[0]) \(.[1])"' | sort || blind "oci ce cluster list failed"
+            oci_ready
+            # The CLI's own diagnosis, not a fixed string. Piping straight into jq threw the 400
+            # body away, so every cause -- wrong profile, wrong auth mode, empty compartment, a
+            # genuinely expired token -- reached the operator as the same six words (2026-08-31).
+            out=$(oci ce cluster list --compartment-id "$C" --lifecycle-state ACTIVE --query 'data[].[name,id]' --output json 2>&1) \
+              || blind "oci ce cluster list failed as profile ${OCI_CLI_PROFILE:-<unset>} auth ${OCI_CLI_AUTH:-<unset>}: $(oneline "$(printf '%s' "$out" | grep -oE '"(code|message)": "[^"]*"' | paste -sd' ' -)")"
+            [ -n "$out" ] || exit 0
+            printf '%s' "$out" | jq -r '.[] | "\(.[0]) \(.[1])"' | sort
             ;;
           *) blind "unknown backend $BACKEND";;
         esac
@@ -451,8 +497,11 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
             ;;
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
-            C=$(oci_compartment)
-            oci ce node-pool list --compartment-id "$C" --query 'data[].[name,"lifecycle-state"]' --output json | jq -r '.[] | "\(.[0]) \(.[1])"' | sort || blind "oci ce node-pool list failed"
+            oci_ready
+            out=$(oci ce node-pool list --compartment-id "$C" --query 'data[].[name,"lifecycle-state"]' --output json 2>&1) \
+              || blind "oci ce node-pool list failed as profile ${OCI_CLI_PROFILE:-<unset>} auth ${OCI_CLI_AUTH:-<unset>}: $(oneline "$(printf '%s' "$out" | grep -oE '"(code|message)": "[^"]*"' | paste -sd' ' -)")"
+            [ -n "$out" ] || exit 0
+            printf '%s' "$out" | jq -r '.[] | "\(.[0]) \(.[1])"' | sort
             ;;
           *) blind "unknown backend $BACKEND";;
         esac
@@ -469,6 +518,10 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
             ;;
           oci)
             command -v oci >/dev/null || blind "oci CLI is not installed"
+            # This branch needs no compartment, but it does need the same profile and auth mode as
+            # every other one. Without this it ran on whatever `oci` defaults to, and it worked only
+            # because that default happened to be a usable profile on this laptop (2026-08-31).
+            oci_identity || blind "no OCI identity for create-kubeconfig: OCI_CLI_PROFILE is unset, platform/oci/terraform.tfvars names no oci_profile, and no profile in ~/.oci/sessions has a valid token -- export OCI_CLI_PROFILE, or run bin/idp-oci-login"
             # crew#292: `|| blind "create-kubeconfig failed"` threw away the only sentence that
             # said why. bin/idp-kube then took tail -1 of the combined output, which is this
             # line, so the CLI's ServiceError never reached the receipt: 7 of 7 verify-drill runs
@@ -478,10 +531,10 @@ print(json.dumps({"last-modified": format_datetime(datetime.fromtimestamp(mtime,
             # the kubeconfig goes to --file, and a token must never land in a log.
             if [ -n "$REGION" ]; then
               why=$(oci ce cluster create-kubeconfig --cluster-id "$NAME" --file "$FILE" --region "$REGION" --token-version 2.0.0 --kube-endpoint PUBLIC_ENDPOINT 2>&1 >/dev/null) \
-                || blind "oci ce cluster create-kubeconfig failed: $(oneline "$why")"
+                || blind "oci ce cluster create-kubeconfig failed as profile ${OCI_CLI_PROFILE:-<unset>} auth ${OCI_CLI_AUTH:-<unset>}: $(oneline "$why")"
             else
               why=$(oci ce cluster create-kubeconfig --cluster-id "$NAME" --file "$FILE" --token-version 2.0.0 --kube-endpoint PUBLIC_ENDPOINT 2>&1 >/dev/null) \
-                || blind "oci ce cluster create-kubeconfig failed: $(oneline "$why")"
+                || blind "oci ce cluster create-kubeconfig failed as profile ${OCI_CLI_PROFILE:-<unset>} auth ${OCI_CLI_AUTH:-<unset>}: $(oneline "$why")"
             fi
             ;;
           *) blind "unknown backend $BACKEND";;
diff --git a/bin/idp-kube b/bin/idp-kube
index 3a0442f79..5e3ea75bd 100755
--- a/bin/idp-kube
+++ b/bin/idp-kube
@@ -12,9 +12,10 @@
 #   bin/idp-kube --refresh get nodes          # rebuild the kubeconfig now
 #   bin/idp-kube                              # print the kubeconfig path only
 # Grading, same shape as every bin/idp-*: `BLIND   kube  <why>` exit 2 when no cluster could be
-# reached; the why names bin/idp-oci-login when the session is the gap. kubectl's own exit code
-# otherwise. rule-guard (claude-guards policy/command.rego, bare_kubectl) refuses bare kubectl and
-# names this file.
+# reached; the why is whatever bin/idp-cloud actually diagnosed -- the missing profile, the missing
+# compartment id, or the CLI's own error -- and this file adds no remedy of its own. kubectl's own
+# exit code otherwise. rule-guard (claude-guards policy/command.rego, bare_kubectl) refuses bare
+# kubectl and names this file.
 set -uo pipefail
 IDP=$(cd "$(dirname "$0")/.." && pwd)
 blind() { printf 'BLIND   kube  %s\n' "$1" >&2; exit 2; }
@@ -27,7 +28,12 @@ command -v kubectl >/dev/null || blind "kubectl is not installed"
 fresh=0
 if [ "$refresh" -eq 0 ] && [ -f "$KC" ] && [ -n "$(find "$KC" -mmin "-$MAX_MIN" 2>/dev/null)" ]; then fresh=1; fi
 if [ "$fresh" -eq 0 ]; then
-  cl=$("$IDP/bin/idp-cloud" cluster list 2>&1) || blind "cluster list failed: $(printf '%s' "$cl" | tail -1 | cut -c1-160) -- refresh the session with bin/idp-oci-login"
+  # idp-cloud names its own cause now, so relay it instead of appending one. The old tail said
+  # "refresh the session with bin/idp-oci-login" on every failure, including the ones a session
+  # cannot fix: on 2026-08-31 the real fault was an empty --compartment-id, and a browser login
+  # cannot supply a compartment id. The one instruction the operator got was the one that could
+  # not work. An instrument that names the wrong action is worse than one that names none.
+  cl=$("$IDP/bin/idp-cloud" cluster list 2>&1) || blind "cluster list failed: $(printf '%s' "$cl" | tail -1 | sed -E 's/^BLIND +cloud +//' | cut -c1-240)"
   # crew#345: the cluster is picked by name (OCI_CLUSTER_NAME, default estate), never by position;
   # a compartment with several ACTIVE clusters made NR==1 mint a kubeconfig for the wrong one.
   want="${OCI_CLUSTER_NAME:-estate}"
@@ -38,4 +44,8 @@ if [ "$fresh" -eq 0 ]; then
   chmod 600 "$KC"
 fi
 if [ $# -eq 0 ]; then printf 'ok      kube  kubeconfig %s\n' "$KC"; exit 0; fi
-KUBECONFIG="$KC" exec kubectl "$@"
+# The kubeconfig authenticates through an exec credential plugin, so kubectl runs `oci ce cluster
+# generate-token` itself and that child prints OCI's API-key label warning onto this command's
+# stderr, above kubectl's own output, on every single call. It is not a diagnosis and it is not
+# ours to emit; suppressing it is what the warning text itself asks for.
+KUBECONFIG="$KC" SUPPRESS_LABEL_WARNING=True exec kubectl "$@"
diff --git a/bin/idp-vault-put b/bin/idp-vault-put
index 53573595e..eb39644c4 100755
--- a/bin/idp-vault-put
+++ b/bin/idp-vault-put
@@ -12,17 +12,16 @@ set -euo pipefail
 IDP=$(cd "$(dirname "$0")/.." && pwd)
 MERGE=0; [ "${1:-}" = --merge ] && { MERGE=1; shift; }
 DELETE=0; [ "${1:-}" = --delete ] && { DELETE=1; shift; }
-# SECRET_NAME, not `name`: bin/idp-oci-whoami is sourced below and its profile loop assigned `name`,
-# so the github-app entry was created as "DEFAULT" (vault-seed run 33089277235, crew#503).
+# SECRET_NAME, not `name`: bin/idp-oci-whoami used to be sourced below and its profile loop assigned
+# `name`, so the github-app entry was created as "DEFAULT" (vault-seed run 33089277235, crew#503).
+# It is a subprocess behind `idp-cloud identity env` now and can no longer reach this shell at all.
 SECRET_NAME=$1; shift
-# No profile is a hard stop: ~/.oci/config has no DEFAULT, only session-token profiles. Discover the
-# live one the way every other bin/idp-* does (crew#503, 2026-08-27: an unset profile failed the write
-# after a one-time GitHub manifest code had already been spent).
-if [ -z "${OCI_CLI_PROFILE:-}" ]; then
-  # shellcheck source=idp-oci-whoami
-  source "$IDP/bin/idp-oci-whoami" >&2 || exit 2
-fi
-export OCI_CLI_AUTH=${OCI_CLI_AUTH:-security_token}
+# No profile is a hard stop (crew#503, 2026-08-27: an unset profile failed the write after a
+# one-time GitHub manifest code had already been spent). This used to source bin/idp-oci-whoami and
+# then hardcode security_token; both were wrong by 2026-08-31, when ~/.oci/config grew a DEFAULT
+# API-key profile that whoami cannot see (it scans ~/.oci/sessions only) and that security_token
+# cannot use. bin/idp-cloud is the one door to the estate's OCI identity (crew#66).
+eval "$("$IDP/bin/idp-cloud" identity env)" || exit 2
 ENVF=${ESTATE_ENV_FILE:-$HOME/.estate/.env}
 [ "${VAULT_PUT_PREFLIGHT:-}" = 1 ] || [ -f "$ENVF" ] || { echo "BLIND: no env file at $ENVF"; exit 2; }
 export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
diff --git a/tests/test_incident_20260831_blind_line_named_a_remedy_that_could_not_work.py b/tests/test_incident_20260831_blind_line_named_a_remedy_that_could_not_work.py
new file mode 100644
index 000000000..b57d7fee8
--- /dev/null
+++ b/tests/test_incident_20260831_blind_line_named_a_remedy_that_could_not_work.py
@@ -0,0 +1,237 @@
+"""A BLIND line must name the fault it found, and must never prescribe an action that cannot fix it.
+
+2026-08-31: `bin/idp-kube get nodes` printed
+
+    BLIND   kube  cluster list failed: BLIND   cloud  oci ce cluster list failed
+                  -- refresh the session with bin/idp-oci-login
+
+on a laptop whose session was irrelevant. The real answer, from the CLI's own 400 body, was
+`InvalidParameter: compartmentId is not available` -- `--compartment-id` was the empty string,
+because `oci_compartment` had sourced `bin/idp-oci-whoami`, that script had `exit 2`ed inside a
+command substitution, and nothing checked the status. A browser login cannot supply a compartment
+id, so the one instruction the operator was given was the one that could not work.
+
+Three separate defects produced one sentence, and this file grades each of them:
+  - the cause was thrown away    (`oci ... | jq` swallowed the body; the `||` bound to the pipeline)
+  - the remedy was hardcoded     (idp-kube appended the session line to every failure)
+  - the auth mode was hardcoded  (`security_token` on a laptop whose working profile is API-key)
+
+Every case runs against a stub `oci` on PATH and a fixture ~/.oci/config, so the whole file is
+cloud-free and runs on a bare runner.
+"""
+
+import os
+import re
+import subprocess
+from pathlib import Path
+
+import pytest
+
+ROOT = Path(__file__).resolve().parents[1]
+CLOUD = ROOT / "bin" / "idp-cloud"
+KUBE = ROOT / "bin" / "idp-kube"
+
+# The literal 400 body OCI returned on 2026-08-31, trimmed to the two keys the blind line quotes.
+REAL_400 = """{
+  "code": "InvalidParameter",
+  "message": "compartmentId is not available",
+  "status": 400,
+  "target_service": "container_engine"
+}"""
+
+API_KEY_PROFILE = """[DEFAULT]
+user=ocid1.user.oc1..fixture
+fingerprint=aa:bb
+key_file=/dev/null
+tenancy=ocid1.tenancy.oc1..fixture
+region=uk-london-1
+"""
+
+SESSION_PROFILE = """[otto]
+security_token_file=/dev/null
+key_file=/dev/null
+tenancy=ocid1.tenancy.oc1..fixture
+region=uk-london-1
+"""
+
+
+def _estate(
+    tmp_path, *, oci_exit=0, oci_stdout="[]", config=API_KEY_PROFILE, tfvars=None
+):
+    """A throwaway checkout, HOME and PATH: a stub `oci`, a fixture ~/.oci/config, optional tfvars.
+
+    The scripts take $IDP from their own location, so the fixture checkout is a directory of
+    symlinks to the real bin/. That is what makes these cases independent of whether this machine
+    happens to have a rendered platform/oci/terraform.tfvars -- the file is generated and
+    gitignored, so a test that reads the real one passes or fails by accident.
+    """
+    idp = tmp_path / "checkout"
+    (idp / "bin").mkdir(parents=True)
+    for f in (ROOT / "bin").iterdir():
+        if f.is_file() and f.name.startswith("idp-"):
+            (idp / "bin" / f.name).symlink_to(f.resolve())
+    if tfvars is not None:
+        (idp / "platform" / "oci").mkdir(parents=True)
+        (idp / "platform" / "oci" / "terraform.tfvars").write_text(tfvars)
+
+    home = tmp_path / "home"
+    (home / ".oci").mkdir(parents=True)
+    (home / ".oci" / "config").write_text(config)
+
+    bindir = tmp_path / "stub"
+    bindir.mkdir()
+    stub = bindir / "oci"
+    # The stub answers, so the script under test reaches its own error handling rather than
+    # dying on a missing binary. A failing call writes the body to stderr, as the real CLI does.
+    stub.write_text(
+        "#!/usr/bin/env bash\n"
+        f"cat <<'EOF' >&{'2' if oci_exit else '1'}\n{oci_stdout}\nEOF\n"
+        f"exit {oci_exit}\n"
+    )
+    stub.chmod(0o755)
+
+    env = dict(os.environ)
+    for k in (
+        "OCI_CLI_PROFILE",
+        "OCI_CLI_AUTH",
+        "OCI_COMPARTMENT_OCID",
+        "OCI_LAPTOP_KEY",
+    ):
+        env.pop(k, None)
+    env["HOME"] = str(home)
+    env["PATH"] = f"{bindir}:{env['PATH']}"
+    env["IDP_CLOUD_BACKEND"] = "oci"
+    env["_IDP_FIXTURE"] = str(idp)
+    return env
+
+
+def _cloud(env, *args):
+    return subprocess.run(
+        [str(Path(env["_IDP_FIXTURE"]) / "bin" / "idp-cloud"), *args],
+        env=env,
+        capture_output=True,
+        text=True,
+    )
+
+
+def _shell_func(name, arg, env):
+    """Call one function out of bin/idp-cloud without running the script's argument parser."""
+    src = CLOUD.read_text()
+    block = "\n".join(
+        m.group(0)
+        for m in re.finditer(
+            r"^oci_(?:tfvar|auth_for_profile)\(\) \{.*?^\}", src, re.S | re.M
+        )
+    )
+    assert "oci_auth_for_profile" in block, (
+        "oci_auth_for_profile is no longer a top-level function"
+    )
+    script = f'IDP={env["_IDP_FIXTURE"]}\nblind() {{ printf "%s" "$1"; exit 2; }}\n{block}\n{name} "{arg}"\n'
+    return subprocess.run(
+        ["bash", "-c", script], env=env, capture_output=True, text=True
+    ).stdout.strip()
+
+
+# --- the remedy that could not work -------------------------------------------------------------
+
+
+def test_the_relayed_cause_carries_no_remedy_of_its_own():
+    """idp-kube must not append a fix to a failure it did not diagnose."""
+    line = next(
+        ln
+        for ln in KUBE.read_text().splitlines()
+        if "cluster list failed" in ln and "blind" in ln
+    )
+    assert "idp-oci-login" not in line, (
+        "bin/idp-kube is again naming a remedy on a cause it did not read. It relays whatever "
+        "bin/idp-cloud diagnosed; a session refresh cannot supply a compartment id.\n"
+        + line
+    )
+
+
+def test_a_missing_compartment_is_named_as_a_missing_compartment(tmp_path):
+    env = _estate(tmp_path, config=SESSION_PROFILE)
+    env["OCI_CLI_PROFILE"] = "otto"
+    env["OCI_COMPARTMENT_OCID"] = ""
+    r = _cloud(env, "cluster", "list")
+    assert r.returncode == 2
+    assert "compartment" in r.stderr, r.stderr
+    assert "session" not in r.stderr.lower(), (
+        "an absent compartment id is being reported as a session problem again:\n"
+        + r.stderr
+    )
+
+
+def test_a_missing_identity_is_named_as_a_missing_identity(tmp_path):
+    """No profile anywhere: that IS the session case, and it may say so."""
+    env = _estate(tmp_path, config="")
+    (tmp_path / "home" / ".oci" / "sessions").mkdir()
+    r = _cloud(env, "cluster", "list")
+    assert r.returncode == 2
+    assert "no OCI identity" in r.stderr, r.stderr
+    assert "idp-oci-login" in r.stderr, (
+        "the one case a login does fix must still say so"
+    )
+
+
+# --- the cause that was thrown away -------------------------------------------------------------
+
+
+def test_the_cli_diagnosis_reaches_the_blind_line(tmp_path):
+    env = _estate(tmp_path, oci_exit=1, oci_stdout=REAL_400)
+    env["OCI_CLI_PROFILE"] = "DEFAULT"
+    env["OCI_COMPARTMENT_OCID"] = "ocid1.compartment.oc1..fixture"
+    r = _cloud(env, "cluster", "list")
+    assert r.returncode == 2
+    for owed in (
+        "InvalidParameter",
+        "compartmentId is not available",
+        "DEFAULT",
+        "api_key",
+    ):
+        assert owed in r.stderr, (
+            f"the blind line no longer carries {owed!r}:\n{r.stderr}"
+        )
+
+
+def test_no_oci_call_pipes_its_failure_into_jq():
+    """`oci ... | jq ... || blind` grades the pipeline and discards the body that said why."""
+    offenders = [
+        ln.strip()
+        for ln in CLOUD.read_text().splitlines()
+        if re.search(r"^\s*oci\s+\S+.*\|\s*jq", ln)
+    ]
+    assert not offenders, (
+        "an OCI failure is being read through a pipe again:\n" + "\n".join(offenders)
+    )
+
+
+# --- the caller-killer, and the hardcoded auth mode ----------------------------------------------
+
+
+def test_nothing_sources_idp_oci_whoami():
+    """It ends in `exit 2`. Sourced, that kills the caller; sourced inside $(...) it kills the
+    subshell and hands the caller a silent empty string, which is exactly how $C went empty."""
+    hits = [
+        f"{p.relative_to(ROOT)}:{i}"
+        for p in sorted((ROOT / "bin").iterdir())
+        if p.is_file() and not p.is_symlink()
+        for i, ln in enumerate(p.read_text(errors="ignore").splitlines(), 1)
+        if re.search(r"^\s*(source|\.)\s+.*idp-oci-whoami", ln)
+    ]
+    assert not hits, "idp-oci-whoami is being sourced again: " + ", ".join(hits)
+
+
+@pytest.mark.parametrize(
+    "config,profile,expected",
+    [
+        (API_KEY_PROFILE, "DEFAULT", "api_key"),
+        (SESSION_PROFILE, "otto", "security_token"),
+        ("", "DEFAULT", "api_key"),
+    ],
+)
+def test_the_auth_mode_is_read_off_the_profile_not_assumed(
+    tmp_path, config, profile, expected
+):
+    env = _estate(tmp_path, config=config)
+    assert _shell_func("oci_auth_for_profile", profile, env) == expected
