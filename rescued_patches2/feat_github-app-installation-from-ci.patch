diff --git a/.github/workflows/oke-check.yml b/.github/workflows/oke-check.yml
index 361923c17..7e9d4363a 100644
--- a/.github/workflows/oke-check.yml
+++ b/.github/workflows/oke-check.yml
@@ -72,7 +72,19 @@ jobs:
           OKE_SSH_PUBKEY_TEXT: ${{ vars.OKE_SSH_PUBKEY }}
           OKE_ALLOWED_CIDRS: ${{ vars.OKE_ALLOWED_CIDRS }}
           ESTATE_FOUNDER_EMAIL: ${{ vars.ESTATE_FOUNDER_EMAIL }}   # platform/oci/langfuse.tf seeds the Langfuse login
+          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # bin/idp-drill-heartbeat reads login-drill runs; without it the row is BLIND (run 32988930880)
         run: bin/idp-oke-rebuild --${{ inputs.mode || 'check' }}
+      # crew#267: the Flux GitRepository idp-writer needs githubAppInstallationID in the vault secret
+      # github-app. This ran once from a founder laptop (bin/idp-github-app installation); now the
+      # apply run does it with the same machine identity. Exit 2 is BLIND (App not installed yet).
+      - name: bin/idp-github-app installation (crew#267)
+        if: ${{ inputs.mode == 'apply' }}
+        env:
+          OCI_CLI_AUTH: security_token
+          OCI_COMPARTMENT_OCID: ${{ vars.OCI_COMPARTMENT_OCID }}
+          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+        run: |
+          bin/idp-github-app installation || { rc=$?; [ "$rc" = 2 ] && echo "BLIND   github-app has no installation yet; install the App on chidionyema and re-run apply" || exit "$rc"; }
 
   # crew#292 CP4: the in-cluster chaos experiment leaves a receipt in Object Storage; this reads
   # it with the same machine identity and grades its age. A runner has no kube path, so the
diff --git a/bin/idp-github-app b/bin/idp-github-app
index 816bccc12..b0ecad6c8 100755
--- a/bin/idp-github-app
+++ b/bin/idp-github-app
@@ -46,7 +46,7 @@ token)
   lane=${1:?lane name from platform/github-app/lanes.json}
   perms=$(jq -c --arg l "$lane" '.[$l] // empty' "$D/lanes.json"); [ -n "$perms" ] || { echo "REFUSED: no lane '$lane' in $D/lanes.json"; exit 2; }
   export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-  C=$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$IDP/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")
+  C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$IDP/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
   V=$(cd "$IDP/platform/oci" && tofu output -raw vault_id)
   sid=$(oci vault secret list --compartment-id "$C" --vault-id "$V" --all --query "data[?\"secret-name\"=='$SECRET' && \"lifecycle-state\"=='ACTIVE'].id | [0]" --raw-output)
   [ -n "$sid" ] && [ "$sid" != null ] || { echo "BLIND   github-app no vault secret $SECRET; run: bin/idp-github-app convert <code>"; exit 2; }
@@ -63,7 +63,7 @@ installation)
   # githubAppInstallationID beside the id and key; platform/secret-store/github-app.yaml reads all
   # three from this one vault secret. Re-puts the secret with every existing key plus installation_id.
   export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-  C=$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$IDP/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")
+  C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$IDP/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
   V=$(cd "$IDP/platform/oci" && tofu output -raw vault_id)
   sid=$(oci vault secret list --compartment-id "$C" --vault-id "$V" --all --query "data[?\"secret-name\"=='$SECRET' && \"lifecycle-state\"=='ACTIVE'].id | [0]" --raw-output)
   [ -n "$sid" ] && [ "$sid" != null ] || { echo "BLIND   github-app no vault secret $SECRET; run: bin/idp-github-app convert <code>"; exit 2; }
diff --git a/bin/idp-iam-policy-drift b/bin/idp-iam-policy-drift
index f9cc8b130..62681be8e 100755
--- a/bin/idp-iam-policy-drift
+++ b/bin/idp-iam-policy-drift
@@ -1,26 +1,55 @@
 #!/usr/bin/env bash
-# Grade the live estate-operators policy against platform/oci/policy/estate-operators.statements.json.
-# crew#311 / crew#301: the policy lives in the tenancy root and only bin/idp-oci-bootstrap (a founder
-# browser sign-in) can change it, so a statement added to the file is not live until that run. Every
-# apply between the two fails with a 409/404 that names nothing. This row names the missing statement.
+# Grade the live estate-operators grants against platform/oci/policy/estate-operators.statements.json.
+# crew#311 / crew#301: the tenancy-root policy is written only by bin/idp-oci-bootstrap (a founder
+# browser sign-in). Since idp#227 the compartment-scoped lines of the file are ALSO applied from CI by
+# platform/oci/iam.tf as the compartment policy estate-operators-compartment, so a statement is live
+# when EITHER policy carries it. This grader unions both; grading the tenancy policy alone failed the
+# first apply after #227 (run 32988930880) on a grant that was live in the compartment policy.
 #   ok      iam-policy  22 statements, live matches platform/oci/policy/estate-operators.statements.json
+#   ok      iam-policy  ... (1 stale tenancy line superseded: allow ... read buckets ...)
 #   FAIL    iam-policy  1 missing from the live policy: ... | fix: founder runs bin/idp-oci-bootstrap
-#   BLIND   iam-policy  <reason>                 (cannot read the policy: never a verdict, LAW 45)
-# Usage: idp-iam-policy-drift [<statements.json>]   env: OCI_TENANCY_OCID, POLICY_NAME (default below)
+#   FAIL    iam-policy  1 live statement(s) not in the file: ... | fix: add to the file or remove with bin/idp-oci-bootstrap
+#   BLIND   iam-policy  <reason>                 (cannot read a policy: never a verdict, LAW 45)
+# A live line that is a weaker verb (inspect<read<use<manage) of a wanted line on the same resource and
+# scope is stale, not drift: the bootstrap wrote it before the file was raised and only the founder can
+# delete it. It is named in the ok row so the next bootstrap run clears it.
+# Usage: idp-iam-policy-drift [<statements.json>]
+#   env: OCI_TENANCY_OCID (required), OCI_COMPARTMENT_OCID (optional: also read the compartment policy),
+#        POLICY_NAME (default below), COMPARTMENT_POLICY_NAME (default estate-operators-compartment)
 set -u
 HERE="$(cd "$(dirname "$0")/.." && pwd)"
 FILE="${1:-$HERE/platform/oci/policy/estate-operators.statements.json}"
 NAME="${POLICY_NAME:-estate-operators-manage-estate}"
+CNAME="${COMPARTMENT_POLICY_NAME:-estate-operators-compartment}"
 [ -f "$FILE" ] || { echo "BLIND   iam-policy  no statements file at $FILE"; exit 2; }
 [ -n "${OCI_TENANCY_OCID:-}" ] || { echo "BLIND   iam-policy  OCI_TENANCY_OCID unset; the policy lives in the tenancy root"; exit 2; }
 live=$(oci iam policy list --compartment-id "$OCI_TENANCY_OCID" --name "$NAME" --all --query 'data[0].statements' 2>&1) || { echo "BLIND   iam-policy  oci iam policy list failed: $(printf %s "$live" | grep -iE 'code|message' | head -1 | cut -c1-120)"; exit 2; }
 case "$live" in null|"") echo "FAIL    iam-policy  no policy named $NAME in the tenancy root | fix: founder runs bin/idp-oci-bootstrap"; exit 1;; esac
+have=$(printf %s "$live" | jq -c 'map(ascii_downcase)' 2>/dev/null) || { echo "BLIND   iam-policy  policy list answered no JSON: $(printf %s "$live" | head -1 | cut -c1-120)"; exit 2; }
+note=""
+if [ -n "${OCI_COMPARTMENT_OCID:-}" ]; then
+  clive=$(oci iam policy list --compartment-id "$OCI_COMPARTMENT_OCID" --name "$CNAME" --all --query 'data[0].statements' 2>&1) || { echo "BLIND   iam-policy  oci iam policy list ($CNAME) failed: $(printf %s "$clive" | grep -iE 'code|message' | head -1 | cut -c1-120)"; exit 2; }
+  case "$clive" in
+    null|"") note="; $CNAME not applied yet (oke-check apply creates it)";;
+    *) chave=$(printf %s "$clive" | jq -c --arg c "$OCI_COMPARTMENT_OCID" 'map(ascii_downcase|sub(" in compartment id " + ($c|ascii_downcase) + "$"; " in compartment estate"))' 2>/dev/null) || { echo "BLIND   iam-policy  policy list ($CNAME) answered no JSON: $(printf %s "$clive" | head -1 | cut -c1-120)"; exit 2; }
+       have=$(jq -nc --argjson a "$have" --argjson b "$chave" '$a+$b|unique');;
+  esac
+fi
 want=$(jq -c 'map(ascii_downcase)|sort' "$FILE")
-have=$(printf %s "$live" | jq -c 'map(ascii_downcase)|sort' 2>/dev/null) || { echo "BLIND   iam-policy  policy list answered no JSON: $(printf %s "$live" | head -1 | cut -c1-120)"; exit 2; }
+have=$(jq -c 'sort' <<<"$have")
 missing=$(jq -nc --argjson w "$want" --argjson h "$have" '$w-$h')
-extra=$(jq -nc --argjson w "$want" --argjson h "$have" '$h-$w')
-nm=$(jq -n --argjson m "$missing" '$m|length'); ne=$(jq -n --argjson e "$extra" '$e|length')
-if [ "$nm" -eq 0 ] && [ "$ne" -eq 0 ]; then echo "ok      iam-policy  $(jq length "$FILE") statements, live matches ${FILE#$HERE/}"; exit 0; fi
-[ "$nm" -eq 0 ] || echo "FAIL    iam-policy  $nm missing from the live policy: $(jq -r 'join("; ")' <<<"$missing") | fix: founder runs bin/idp-oci-bootstrap"
+# an extra live line whose verb is weaker than a wanted line on the same resource and scope is stale, not drift
+JQ_STALE='def key: sub("^(?<a>allow group .* to )(inspect|read|use|manage)(?<b> .*)$"; "\(.a)*\(.b)");
+  def rank: if test(" to manage ") then 4 elif test(" to use ") then 3 elif test(" to read ") then 2 else 1 end;
+  ($h-$w) | map(. as $e | {s: $e, stale: (any($w[]; key == ($e|key) and rank >= ($e|rank)))})'
+graded=$(jq -nc --argjson w "$want" --argjson h "$have" "$JQ_STALE")
+extra=$(jq -c 'map(select(.stale|not)|.s)' <<<"$graded")
+stale=$(jq -c 'map(select(.stale)|.s)' <<<"$graded")
+nm=$(jq length <<<"$missing"); ne=$(jq length <<<"$extra"); ns=$(jq length <<<"$stale")
+if [ "$nm" -eq 0 ] && [ "$ne" -eq 0 ]; then
+  [ "$ns" -eq 0 ] || note="$note; $ns stale tenancy line(s) superseded, cleared by the next bin/idp-oci-bootstrap: $(jq -r 'join("; ")' <<<"$stale")"
+  echo "ok      iam-policy  $(jq length "$FILE") statements, live matches ${FILE#$HERE/}$note"; exit 0
+fi
+[ "$nm" -eq 0 ] || echo "FAIL    iam-policy  $nm missing from the live policy: $(jq -r 'join("; ")' <<<"$missing") | fix: compartment lines land with oke-check apply (platform/oci/iam.tf); tenancy lines need the founder to run bin/idp-oci-bootstrap$note"
 [ "$ne" -eq 0 ] || echo "FAIL    iam-policy  $ne live statement(s) not in ${FILE#$HERE/}: $(jq -r 'join("; ")' <<<"$extra") | fix: add to the file or remove with bin/idp-oci-bootstrap"
 exit 1
diff --git a/bin/idp-vault-put b/bin/idp-vault-put
index 74e3395eb..0ba1ba2f2 100755
--- a/bin/idp-vault-put
+++ b/bin/idp-vault-put
@@ -9,7 +9,7 @@ name=$1; shift
 ENVF=${ESTATE_ENV_FILE:-$HOME/.hermes/.env}
 [ -f "$ENVF" ] || { echo "BLIND: no env file at $ENVF"; exit 2; }
 export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/prospector/age-key.txt} SUPPRESS_LABEL_WARNING=True
-C=$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$IDP/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")
+C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' "${ESTATE_SECRETS:-${ESTATE_CODE:-$IDP/..}/estate-secrets}/secrets/dev/OCI_COMPARTMENT_OCID.yaml")}
 V=$(cd "$IDP/platform/oci" && tofu output -raw vault_id); K=$(cd "$IDP/platform/oci" && tofu output -raw vault_key_id)
 payload=$(python3 - "$ENVF" "$@" <<'PY'
 import sys,json,base64
diff --git a/tests/test_incident_crew267_bin_reads_compartment_from_env.py b/tests/test_incident_crew267_bin_reads_compartment_from_env.py
new file mode 100644
index 000000000..aa3155f29
--- /dev/null
+++ b/tests/test_incident_crew267_bin_reads_compartment_from_env.py
@@ -0,0 +1,43 @@
+"""Incident crew#267 / crew#301 (2026-08-26): bin/idp-github-app installation could only run from a
+founder laptop because it read OCI_COMPARTMENT_OCID through sops from estate-secrets, which a
+runner does not have, so crew#267 sat not-live behind an expired OCI session. Rule: every bin
+script that reads OCI_COMPARTMENT_OCID via sops honours the OCI_COMPARTMENT_OCID environment
+variable first, the same variable .github/workflows/oke-check.yml exports. Rung 4 (incident).
+Both ways: the live scripts pass; a script text with the bare sops read is caught."""
+import glob
+import os
+import re
+
+ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
+SOPS = re.compile(r"""sops -d --extract '\["OCI_COMPARTMENT_OCID"\]'""")
+ENV_FIRST = re.compile(r"\$\{OCI_COMPARTMENT_OCID:-")
+
+
+def _offenders(texts: dict) -> list:
+    """A script offends when it reads the secret through sops and no `${OCI_COMPARTMENT_OCID:-`
+    default precedes that read (inline `${X:-$(sops ...)}` or an env-first `C="${X:-}"` block)."""
+    out = []
+    for p, t in texts.items():
+        m = SOPS.search(t)
+        if m and not ENV_FIRST.search(t[: m.start()]):
+            out.append(p)
+    return sorted(out)
+
+
+def _bin_texts() -> dict:
+    out = {}
+    for p in glob.glob(os.path.join(ROOT, "bin", "*")):
+        if os.path.isfile(p):
+            with open(p, errors="replace") as f:
+                out[os.path.relpath(p, ROOT)] = f.read()
+    return out
+
+
+def test_every_bin_sops_compartment_read_honours_env():
+    assert _offenders(_bin_texts()) == []
+
+
+def test_bare_sops_read_is_caught():
+    bad = {"bin/x": """C=$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' f.yaml)"""}
+    good = {"bin/y": """C=${OCI_COMPARTMENT_OCID:-$(sops -d --extract '["OCI_COMPARTMENT_OCID"]' f.yaml)}"""}
+    assert _offenders(bad) == ["bin/x"] and _offenders(good) == []
diff --git a/tests/test_incident_iam_policy_drift_unnamed.py b/tests/test_incident_iam_policy_drift_unnamed.py
index c6cef5848..6f8844215 100644
--- a/tests/test_incident_iam_policy_drift_unnamed.py
+++ b/tests/test_incident_iam_policy_drift_unnamed.py
@@ -2,20 +2,30 @@
 bootstrap list, the live tenancy-root policy never got it, and three applies failed 409 with no
 row naming the missing statement. bin/idp-iam-policy-drift grades live statements against the file.
 Both ways in one run: a matching policy is ok; a missing statement is FAIL and names it; an
-unreadable policy is BLIND, never a verdict."""
+unreadable policy is BLIND, never a verdict.
+
+Second incident, run 32988930880 (first apply after idp#227): the compartment policy carried
+`manage buckets`, the tenancy policy carried a stale `read buckets`, and the grader read only the
+tenancy policy, so it failed a grant that was live. The union test below is that run's exact shape."""
 import json, os, stat, subprocess
 from pathlib import Path
 
 GUARD = Path(__file__).resolve().parents[1] / "bin" / "idp-iam-policy-drift"
 FILE = Path(__file__).resolve().parents[1] / "platform" / "oci" / "policy" / "estate-operators.statements.json"
+COMP = "ocid1.compartment.oc1..test"
 
 
-def _run(tmp: Path, live, rc_list: int = 0) -> subprocess.CompletedProcess:
+def _run(tmp: Path, live, rc_list: int = 0, comp=None) -> subprocess.CompletedProcess:
     b = tmp / "bin"; b.mkdir(exist_ok=True)
     fake = b / "oci"
-    fake.write_text(f"#!/usr/bin/env bash\ncase \"$*\" in *\"iam policy list\"*) printf '%s' '{json.dumps(live)}'; exit {rc_list};; *) echo unexpected >&2; exit 9;; esac\n")
+    fake.write_text(
+        "#!/usr/bin/env bash\ncase \"$*\" in "
+        f"*\"--name estate-operators-compartment\"*) printf '%s' '{json.dumps(comp)}'; exit 0;; "
+        f"*\"iam policy list\"*) printf '%s' '{json.dumps(live)}'; exit {rc_list};; "
+        "*) echo unexpected >&2; exit 9;; esac\n")
     fake.chmod(fake.stat().st_mode | stat.S_IEXEC)
-    env = {**os.environ, "PATH": f"{b}:{os.environ['PATH']}", "OCI_TENANCY_OCID": "ocid1.tenancy.oc1..test"}
+    env = {**os.environ, "PATH": f"{b}:{os.environ['PATH']}", "OCI_TENANCY_OCID": "ocid1.tenancy.oc1..test",
+           "OCI_COMPARTMENT_OCID": COMP if comp is not None else ""}
     return subprocess.run(["bash", str(GUARD)], env=env, capture_output=True, text=True)
 
 
@@ -30,6 +40,22 @@ def test_incident_missing_statement_is_named(tmp_path: Path) -> None:
     assert "bin/idp-oci-bootstrap" in r.stdout
 
 
+def test_incident_run_32988930880_compartment_policy_counts_as_live(tmp_path: Path) -> None:
+    want = json.loads(FILE.read_text())
+    tenancy = [s.replace("manage buckets", "read buckets") for s in want]          # stale bootstrap line
+    comp = [s.replace(" in compartment estate", f" in compartment id {COMP}")
+            for s in want if s.endswith(" in compartment estate")]                 # what iam.tf applies
+    r = _run(tmp_path, tenancy, comp=comp)
+    assert r.returncode == 0 and r.stdout.startswith("ok      iam-policy"), r.stdout + r.stderr
+    assert "1 stale tenancy line(s)" in r.stdout and "read buckets" in r.stdout, r.stdout
+    # the other way: a live line with no stronger wanted twin is still drift
+    r2 = _run(tmp_path, tenancy + ["Allow group estate-operators to manage users in tenancy"], comp=comp)
+    assert r2.returncode == 1 and "manage users in tenancy" in r2.stdout, r2.stdout
+    # and a compartment policy not applied yet does not hide a missing grant
+    r3 = _run(tmp_path, tenancy, comp=[])
+    assert r3.returncode == 1 and "manage buckets" in r3.stdout and "oke-check apply" in r3.stdout, r3.stdout
+
+
 def test_unreadable_policy_is_blind_not_a_verdict(tmp_path: Path) -> None:
     r = _run(tmp_path, {"code": "NotAuthorizedOrNotFound"}, rc_list=1)
     assert r.returncode == 2 and r.stdout.startswith("BLIND   iam-policy"), r.stdout
