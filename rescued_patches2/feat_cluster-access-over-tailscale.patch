diff --git a/platform/tailscale/kustomization.yaml b/platform/tailscale/kustomization.yaml
index 3d2a4145d..60bf33ae5 100644
--- a/platform/tailscale/kustomization.yaml
+++ b/platform/tailscale/kustomization.yaml
@@ -8,3 +8,4 @@ resources:
   - namespace.yaml
   - external-secret.yaml
   - operator.yaml
+  - rbac.yaml
diff --git a/platform/tailscale/operator.yaml b/platform/tailscale/operator.yaml
index 4c8eeb345..9eeaea368 100644
--- a/platform/tailscale/operator.yaml
+++ b/platform/tailscale/operator.yaml
@@ -35,10 +35,14 @@ spec:
     # No `oauth:` block here on purpose (see the header comment): the chart falls back to reading
     # the pre-created `operator-oauth` Secret (external-secret.yaml) when this is left unset.
     apiServerProxyConfig:
-      # This estate's kube-apiserver access is not routed through Tailscale (bin/idp-oke-rebuild,
-      # bin/idp-oke-break-glass are the existing paths); the operator is deployed for its ACL/policy
-      # management and to authenticate the hermes-agent sidecar's tag:k8s identity, nothing more.
-      mode: "false"
+      # Founder, 2026-08-29 (crew#307): "we need to be able to debug the cluster", "from the Mac
+      # also", "the point is we should be portable". Before this, every look at a pod needed a pull
+      # request into the break-glass playbook. The operator now fronts the API server on the
+      # tailnet: any device on the tailnet reaches it as its tailnet identity, the grant in
+      # policy.hujson maps that identity to the Kubernetes group `estate:founder`, and
+      # rbac.yaml binds that group. No cloud token, no IP allow-list, works the same on any
+      # cloud (LAW 19, LAW 43). From a machine:  tailscale configure kubeconfig tailscale-operator
+      mode: "auth"
     # The chart ships no default securityContext/resources (verified: templates/deployment.yaml
     # only emits them `{{- with .Values.operatorConfig.* }}`, i.e. never, when unset) -- every
     # ClusterPolicy that reads them refuses the operator Pod otherwise (bin/idp-kyverno-render
diff --git a/platform/tailscale/policy.hujson b/platform/tailscale/policy.hujson
index b0b17a830..e4f676308 100644
--- a/platform/tailscale/policy.hujson
+++ b/platform/tailscale/policy.hujson
@@ -54,6 +54,7 @@
   "tagOwners": {
     "tag:k8s":          [],
     "tag:founder-mac":  [],
+    "tag:k8s-operator": [],
   },
   "acls": [
     {"action": "accept", "src": ["tag:k8s"], "dst": ["tag:founder-mac:22"]},
@@ -62,6 +63,19 @@
     // The :22 here is not redundant with the ssh rule below — see the header, kb/1193.
     {"action": "accept", "src": ["group:founder"],
      "dst":    ["tag:founder-mac:22", "tag:founder-mac:47984-48010"]},
+    // crew#307: the cluster API server, fronted by the operator on the tailnet (443).
+    {"action": "accept", "src": ["group:founder"], "dst": ["tag:k8s-operator:443"]},
+  ],
+  // crew#307: the founder reaches the API server as Kubernetes group `estate:founder`;
+  // platform/tailscale/rbac.yaml binds that group. Every request carries the tailnet identity.
+  "grants": [
+    {
+      "src": ["group:founder"],
+      "dst": ["tag:k8s-operator"],
+      "app": {
+        "tailscale.com/cap/kubernetes": [{"impersonate": {"groups": ["estate:founder"]}}],
+      },
+    },
   ],
   "ssh": [
     {
diff --git a/platform/tailscale/rbac.yaml b/platform/tailscale/rbac.yaml
new file mode 100644
index 000000000..6178b7582
--- /dev/null
+++ b/platform/tailscale/rbac.yaml
@@ -0,0 +1,15 @@
+# crew#307 (2026-08-29): the founder's tailnet identity arrives at the API server as the group
+# `estate:founder` (policy.hujson grant, operator apiServerProxyConfig.mode auth). This binds it to
+# cluster-admin: it is the break-glass identity, and every request is logged with the tailnet user.
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: estate-founder-over-tailscale
+roleRef:
+  apiGroup: rbac.authorization.k8s.io
+  kind: ClusterRole
+  name: cluster-admin
+subjects:
+  - apiGroup: rbac.authorization.k8s.io
+    kind: Group
+    name: estate:founder
diff --git a/tests/test_incident_crew307_every_look_at_the_cluster_needed_a_pull_request.py b/tests/test_incident_crew307_every_look_at_the_cluster_needed_a_pull_request.py
new file mode 100644
index 000000000..403aa59a5
--- /dev/null
+++ b/tests/test_incident_crew307_every_look_at_the_cluster_needed_a_pull_request.py
@@ -0,0 +1,45 @@
+"""crew#307, 2026-08-29. The founder's surfaces vanished from the portal and nobody could read the
+catalogue pod without merging a pull request into the break-glass playbook: the laptop key was
+retired (crew#227) and no other door existed. Founder: "we need to be able to debug the cluster",
+"from the Mac also", "the point is we should be portable". The door is the Tailscale operator's
+API-server proxy: tailnet identity in, Kubernetes group out, bound by RBAC. This pins the three
+pieces so none can be switched off alone."""
+import json
+import re
+from pathlib import Path
+
+import yaml
+
+ROOT = Path(__file__).resolve().parents[1]
+TS = ROOT / "platform" / "tailscale"
+
+
+def _hujson(path):
+    text = re.sub(r"//[^\n]*", "", path.read_text())
+    text = re.sub(r",(\s*[}\]])", r"\1", text)
+    return json.loads(text)
+
+
+def test_the_operator_fronts_the_api_server_on_the_tailnet():
+    docs = list(yaml.safe_load_all((TS / "operator.yaml").read_text()))
+    hr = next(d for d in docs if d and d["kind"] == "HelmRelease")
+    assert hr["spec"]["values"]["apiServerProxyConfig"]["mode"] == "auth"
+
+
+def test_the_founder_group_is_granted_a_kubernetes_group():
+    pol = _hujson(TS / "policy.hujson")
+    assert "tag:k8s-operator" in pol["tagOwners"]
+    grant = next(g for g in pol["grants"] if "tag:k8s-operator" in g["dst"])
+    assert "group:founder" in grant["src"]
+    caps = grant["app"]["tailscale.com/cap/kubernetes"]
+    assert caps[0]["impersonate"]["groups"] == ["estate:founder"]
+    assert any("tag:k8s-operator:443" in a["dst"] and "group:founder" in a["src"] for a in pol["acls"])
+
+
+def test_the_kubernetes_group_is_bound_and_wired():
+    crb = yaml.safe_load((TS / "rbac.yaml").read_text())
+    assert crb["kind"] == "ClusterRoleBinding"
+    assert crb["roleRef"]["name"] == "cluster-admin"
+    assert crb["subjects"] == [{"apiGroup": "rbac.authorization.k8s.io", "kind": "Group", "name": "estate:founder"}]
+    kust = yaml.safe_load((TS / "kustomization.yaml").read_text())
+    assert "rbac.yaml" in kust["resources"]
diff --git a/tests/test_incident_crew516_otto_hands_on_the_mac.py b/tests/test_incident_crew516_otto_hands_on_the_mac.py
index 69946bcc1..3b9128943 100644
--- a/tests/test_incident_crew516_otto_hands_on_the_mac.py
+++ b/tests/test_incident_crew516_otto_hands_on_the_mac.py
@@ -182,6 +182,8 @@ def test_policy_acl_and_tag_owners_match_the_locked_spec():
     assert pol["acls"] == [
         {"action": "accept", "src": ["tag:k8s"], "dst": ["tag:founder-mac:22"]},
         {"action": "accept", "src": ["group:founder"], "dst": ["tag:founder-mac:22", "tag:founder-mac:47984-48010"]},
+        # crew#307: the founder reaches the cluster API server through the operator on the tailnet
+        {"action": "accept", "src": ["group:founder"], "dst": ["tag:k8s-operator:443"]},
     ]
     open_sources = {"*", "autogroup:member", "autogroup:members", "autogroup:tagged"}
     for section in ("acls", "ssh"):
