diff --git a/AGENTS.md b/AGENTS.md
index a3dcf4695..82c00ae97 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -16,6 +16,7 @@ Row format, one rule per row. `gate` is a shell function or command defined in `
 | No scheduled job on this laptop that runs in the sleep window or is never pinged (LAW 28) | policy_gate | policy/fixtures/placement-misplaced.json | policy/fixtures/placement-ok.json |
 | Only the gateway binds a non-loopback address; everything else is 127.0.0.1 or nothing (R20) | bind_audit | tests/fixtures/listeners.bad.txt | tests/fixtures/listeners.good.txt |
 | Every scheduled job reaches the Dagster UI with a description of what it does (LAW 28) | job_described | tests/fixtures/schedule-undescribed.yml | tests/fixtures/schedule-described.yml |
+| A code location loads the way workspace.yaml loads it: by file path, not as a package (LAW 45) | defs_validate | tests/fixtures/definitions/relative-import.py | tests/fixtures/definitions/loads-by-path.py |
 
 Rules that are already types or tools, and so need no row: compose files must parse
 (`docker compose config`), the gateway config must match its release schema
diff --git a/bin/idp-ci b/bin/idp-ci
index f8b74f632..2afba8ce5 100755
--- a/bin/idp-ci
+++ b/bin/idp-ci
@@ -12,6 +12,8 @@ say() { printf '%s\n' "$*"; }
 need() { command -v "$1" >/dev/null || { say "FAIL  $1 is not installed"; exit 2; }; }
 need shellcheck; need docker; need conftest; need check-jsonschema; need sqlite-utils; need jq; need python3
 
+MPY="$IDP/.venv/bin/python"; [ -x "$MPY" ] || MPY=python3
+
 # 1. Every script parses and passes shellcheck at warning level.
 for f in "$IDP"/bin/*; do
   head -1 "$f" | grep -q bash || continue
@@ -272,7 +274,6 @@ else say "FAIL  kustomize base does not render clean or the NodePort fixture pas
 #     applies healthy, applies again without changing state, and rolls back to
 #     the pre-state, on a pristine temp home. Skipped only if dagster is absent,
 #     and then it says BLIND rather than ok.
-MPY="$IDP/.venv/bin/python"; [ -x "$MPY" ] || MPY=python3
 if "$IDP/bin/migration-gate" "$IDP/tests/fixtures/migration-not-idempotent" >/dev/null 2>&1; then
   say "FAIL  migration gate accepted a migration whose second apply adds a resource"; fail=1
 else say "ok    migration gate refuses the non-idempotent fixture"; fi
@@ -299,6 +300,11 @@ else say "FAIL  schema   check-jsonschema rejected a generated entity"; fail=1;
 policy_gate() { conftest test --parser json -p "$IDP/policy" "$1" >/dev/null 2>&1; }
 bind_audit() { "$IDP/bin/bind-audit" --table "$1" >/dev/null 2>&1; }
 job_described() { ( cd "$IDP/scheduler" && python3 -m estate_scheduler.describe "$1" ) >/dev/null 2>&1; }
+# A code location is loaded by PATH by workspace.yaml, never as a package. Loading
+# it any other way here would grade a proxy: on 2026-08-25 definitions.py imported a
+# sibling with `from .describe import`, passed every offline check, and put the live
+# code location in PythonError the moment it reloaded. So: load it the way it runs.
+defs_validate() { ( cd "$(dirname "$1")" && DAGSTER_HOME="$(mktemp -d)" "$MPY" -m dagster definitions validate -f "$(basename "$1")" ) >/dev/null 2>&1; }
 rows=0
 while IFS='|' read -r _ rule gate bad good _; do
   rule=$(echo "$rule" | xargs); gate=$(echo "$gate" | xargs); bad=$(echo "$bad" | xargs); good=$(echo "$good" | xargs)
@@ -326,6 +332,13 @@ done < <(grep -E '^\| .* \| .* \| .* \| .* \|$' "$IDP/AGENTS.md" | grep -vE '^\|
 #    derived from the target script's docstring, so the selftest checks the derivation
 #    -- that it looks behind a wrapper, and that it refuses to invent prose from a
 #    command line. Coverage over the real 40 needs the scripts, so it lives in idp-verify.
+# 7b. The real code location loads the way workspace.yaml loads it: by file path.
+if ! "$MPY" -c "import dagster" 2>/dev/null; then
+  say "BLIND codeloc dagster not importable; the code location was not loaded"; fail=1
+elif defs_validate "$IDP/scheduler/estate_scheduler/definitions.py"; then
+  say "ok    codeloc  scheduler/estate_scheduler/definitions.py loads by file path, as workspace.yaml loads it"
+else say "FAIL  codeloc  the code location does not load by file path -- the dashboard will show PythonError"; fail=1; fi
+
 if out=$( ( cd "$IDP/scheduler" && python3 -m estate_scheduler.describe --selftest ) 2>&1); then
   say "ok    describe job descriptions derive from the script, and are refused when absent"
 else say "FAIL  describe estate_scheduler.describe --selftest"; printf '%s\n' "$out" | sed 's/^/      /'; fail=1; fi
diff --git a/scheduler/estate_scheduler/definitions.py b/scheduler/estate_scheduler/definitions.py
index 399406f60..f43d0cdbc 100644
--- a/scheduler/estate_scheduler/definitions.py
+++ b/scheduler/estate_scheduler/definitions.py
@@ -24,6 +24,7 @@ import json
 import os
 import shlex
 import subprocess
+import sys
 import time
 from pathlib import Path
 
@@ -63,7 +64,16 @@ os.environ.setdefault("IDP", str(IDP))
 os.environ.setdefault("CODE", os.environ.get("CODE_ROOT", str(IDP.parent)))
 
 
-from .describe import describe as describe_job
+# workspace.yaml loads this file by path, so it is a top-level module and not a
+# package member, and the working directory it resolves imports against is not
+# ours to choose -- the running webserver resolved them against a directory
+# that does not exist. Put this file's own directory on the path and the import
+# works under any of them.
+sys.path.insert(0, str(Path(__file__).resolve().parent))
+try:
+    from .describe import describe as describe_job
+except ImportError:  # loaded as a file, not as a package member
+    from describe import describe as describe_job
 
 
 def _expand(s: str) -> str:
diff --git a/tests/fixtures/definitions/loads-by-path.py b/tests/fixtures/definitions/loads-by-path.py
new file mode 100644
index 000000000..dedee24cf
--- /dev/null
+++ b/tests/fixtures/definitions/loads-by-path.py
@@ -0,0 +1,16 @@
+"""must-pass fixture: a code location that loads however it is loaded."""
+
+from dagster import Definitions, job, op
+
+
+@op
+def _noop():
+    pass
+
+
+@job(description="A job that carries a description, loaded by file path.")
+def a_job():
+    _noop()
+
+
+defs = Definitions(jobs=[a_job])
diff --git a/tests/fixtures/definitions/relative-import.py b/tests/fixtures/definitions/relative-import.py
new file mode 100644
index 000000000..48da5a5bc
--- /dev/null
+++ b/tests/fixtures/definitions/relative-import.py
@@ -0,0 +1,25 @@
+"""must-fail fixture: a code location that only loads as a package.
+
+This is the 2026-08-25 break. definitions.py imported a sibling with `from
+.describe import ...`, which is fine when something imports it as a package and
+fails with "attempted relative import with no known parent package" when
+workspace.yaml loads it by path -- which is how it actually runs. Every offline
+check passed; the live code location went to PythonError on reload.
+"""
+
+from dagster import Definitions, job, op
+
+from .describe import describe  # noqa: F401  -- the break, on purpose
+
+
+@op
+def _noop():
+    pass
+
+
+@job
+def a_job():
+    _noop()
+
+
+defs = Definitions(jobs=[a_job])
