diff --git a/.github/actions/security-scan/action.yml b/.github/actions/security-scan/action.yml
new file mode 100644
index 000000000..528275cb2
--- /dev/null
+++ b/.github/actions/security-scan/action.yml
@@ -0,0 +1,26 @@
+name: estate security scan
+description: >
+  Runs bin/estate-security-scan from chidionyema/idp against the calling
+  repository: gitleaks over the whole git history, pip-audit over every tracked
+  requirements*.txt, npm audit over every tracked package-lock.json, and the
+  security policy gate where one exists. Non-zero exit fails the job; the
+  ruleset `estate-security-scan` then refuses the merge.
+runs:
+  using: composite
+  steps:
+    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
+      with: { fetch-depth: 0 }   # gitleaks reads the whole history
+    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
+      with: { repository: chidionyema/idp, path: .estate-idp }
+    - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
+      with: { python-version: "3.12" }
+    - name: tools (gitleaks 8.30.1, pip-audit 2.10.1)
+      shell: bash
+      run: |
+        curl -sSL -o /tmp/gitleaks.tgz https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
+        echo "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb  /tmp/gitleaks.tgz" | sha256sum -c -
+        sudo tar -xz -C /usr/local/bin -f /tmp/gitleaks.tgz gitleaks
+        pip install -q pip-audit==2.10.1
+    - name: estate-security-scan
+      shell: bash
+      run: .estate-idp/bin/estate-security-scan --source "$GITHUB_WORKSPACE"
diff --git a/.github/actions/spec-gate/action.yml b/.github/actions/spec-gate/action.yml
new file mode 100644
index 000000000..7a8507be7
--- /dev/null
+++ b/.github/actions/spec-gate/action.yml
@@ -0,0 +1,19 @@
+name: spec-gate
+description: >
+  Founder ruling R29 (2026-08-25): a pull request that changes code must also
+  change the executable spec (a *.feature scenario, a test, or the live-diagram
+  generator). Runs bin/spec-gate from chidionyema/idp against the calling
+  repository. On a push (no pull request) it reports ok and does nothing.
+runs:
+  using: composite
+  steps:
+    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
+      with: { fetch-depth: 0 }
+    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
+      with: { repository: chidionyema/idp, path: .estate-idp }
+    - name: spec-gate
+      shell: bash
+      env: { BASE_SHA: "${{ github.event.pull_request.base.sha }}" }
+      run: |
+        if [ -z "$BASE_SHA" ]; then echo "ok    spec-gate not a pull request; nothing to compare"; exit 0; fi
+        .estate-idp/bin/spec-gate "$BASE_SHA"
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 19e40067e..8d37f1f44 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -43,16 +43,13 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
-        with: { fetch-depth: 0 }   # gitleaks reads the whole history
-      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
-        with: { python-version: "3.12" }
-      - name: tools (gitleaks 8.30.1, pip-audit 2.10.1)
-        run: |
-          curl -sSL -o /tmp/gitleaks.tgz https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
-          echo "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb  /tmp/gitleaks.tgz" | sha256sum -c -
-          sudo tar -xz -C /usr/local/bin -f /tmp/gitleaks.tgz gitleaks
-          pip install -q pip-audit==2.10.1
-      - run: bin/estate-security-scan
+      - uses: ./.github/actions/security-scan   # the steps every active estate repo runs
+
+  spec-gate:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
+      - uses: ./.github/actions/spec-gate       # R29: code changes carry an executable spec
 
   bdd:
     runs-on: ubuntu-latest
diff --git a/bin/estate-security-rollout b/bin/estate-security-rollout
new file mode 100755
index 000000000..f9ddca3b5
--- /dev/null
+++ b/bin/estate-security-rollout
@@ -0,0 +1,55 @@
+#!/usr/bin/env bash
+# estate-security-rollout [--apply] [--merge]: every repository pushed in the
+# last ACTIVE_DAYS (30) days carries .github/workflows/security-scan.yml, the
+# caller for the two idp composite actions (security-scan, spec-gate). Report
+# mode by default. --apply opens one pull request per repository that lacks
+# the file or carries a different one; --merge squash-merges those pull
+# requests whose checks are green. After that, `repo-rulesets --apply` makes
+# both checks required (platform/github/ruleset.@active.security-scan.json).
+#
+# The mature tool for fleet-wide file sync is a GitHub App such as
+# repo-file-sync-action; it needs an App or PAT with contents:write on every
+# repo and a workflow in each target, which is the very thing being installed
+# here. The gh CLI already holds that permission on this account.
+set -uo pipefail
+IDP="$(cd "$(dirname "$0")/.." && pwd)"
+OWNER="${ESTATE_GH_OWNER:-$(gh api /user -q .login)}"
+SRC="$IDP/platform/github/workflows/security-scan.yml"
+DEST=".github/workflows/security-scan.yml"
+BRANCH="estate/security-scan"
+ACTIVE_DAYS="${ACTIVE_DAYS:-30}"
+apply=0; merge=0
+for a in "$@"; do case "$a" in --apply) apply=1 ;; --merge) merge=1 ;; *) echo "usage: estate-security-rollout [--apply] [--merge]" >&2; exit 3 ;; esac; done
+since="$(date -u -v-"${ACTIVE_DAYS}"d +%Y-%m-%d 2>/dev/null || date -u -d "-${ACTIVE_DAYS} days" +%Y-%m-%d)"
+want="$(base64 < "$SRC" | tr -d '\n')"
+ok=0; todo=0; opened=0; merged=0; blocked=0
+while read -r repo pushed; do
+  [ "$pushed" \> "$since" ] || continue
+  [ "$repo" = "idp" ] && { echo "ok      idp: runs the actions from ./.github/actions in ci.yml"; ok=$((ok+1)); continue; }
+  default="$(gh api "/repos/$OWNER/$repo" -q .default_branch 2>/dev/null)" || { echo "BLOCKED $repo: cannot read repository"; blocked=$((blocked+1)); continue; }
+  have="$(gh api "/repos/$OWNER/$repo/contents/$DEST?ref=$default" -q .content 2>/dev/null | tr -d '\n')"
+  if [ "$have" = "$want" ]; then echo "ok      $repo: $DEST in place on $default"; ok=$((ok+1)); continue; fi
+  pr="$(gh pr list --repo "$OWNER/$repo" --head "$BRANCH" --state open --json number,url -q '.[0] | "\(.number) \(.url)"' 2>/dev/null)"
+  if [ -n "$pr" ] && [ "${pr% *}" != "null" ]; then
+    if [ $merge = 1 ]; then
+      if gh pr checks "${pr% *}" --repo "$OWNER/$repo" >/dev/null 2>&1 && gh pr merge "${pr% *}" --repo "$OWNER/$repo" --squash --delete-branch >/dev/null 2>&1; then
+        echo "MERGED  $repo: ${pr#* }"; merged=$((merged+1)); continue
+      fi
+      echo "OPEN    $repo: ${pr#* } (checks not green, not merged)"; todo=$((todo+1)); continue
+    fi
+    echo "OPEN    $repo: ${pr#* }"; todo=$((todo+1)); continue
+  fi
+  if [ $apply = 0 ]; then echo "MISSING $repo: no $DEST on $default"; todo=$((todo+1)); continue; fi
+  sha="$(gh api "/repos/$OWNER/$repo/git/ref/heads/$default" -q .object.sha)"
+  gh api -X POST "/repos/$OWNER/$repo/git/refs" -f ref="refs/heads/$BRANCH" -f sha="$sha" >/dev/null 2>&1 || true
+  existing="$(gh api "/repos/$OWNER/$repo/contents/$DEST?ref=$BRANCH" -q .sha 2>/dev/null || true)"
+  args=(-f message="ci: estate security-scan and spec-gate (chidionyema/idp actions)" -f content="$want" -f branch="$BRANCH")
+  [ -n "$existing" ] && args+=(-f sha="$existing")
+  gh api -X PUT "/repos/$OWNER/$repo/contents/$DEST" "${args[@]}" >/dev/null || { echo "BLOCKED $repo: could not write $DEST on $BRANCH"; blocked=$((blocked+1)); continue; }
+  url="$(gh pr create --repo "$OWNER/$repo" --base "$default" --head "$BRANCH" \
+    --title "ci: estate security-scan and spec-gate" \
+    --body "$(printf 'Installs the two estate gates as one job each, calling the composite actions in chidionyema/idp: security-scan (gitleaks over history, pip-audit, npm audit, policy gate) and spec-gate (a code change must carry a *.feature, test or diagram-generator change; founder ruling R29, 2026-08-25). Opened by bin/estate-security-rollout from idp; tracked on crew#236 and crew#229.\n\nAuthor-session: estate-security-rollout\nNo-Issue: fleet rollout tracked on crew#236')" 2>&1 | tail -1)"
+  echo "OPENED  $repo: $url"; opened=$((opened+1))
+done < <(gh repo list "$OWNER" --limit 200 --no-archived --json name,pushedAt -q '.[] | .name+" "+.pushedAt[:10]' | sort)
+echo "$([ $todo = 0 ] && [ $blocked = 0 ] && echo 'ok  ' || echo FAIL)  rollout $ok in place, $opened opened, $merged merged, $todo open or missing, $blocked blocked"
+[ $todo = 0 ] && [ $blocked = 0 ]
diff --git a/bin/estate-security-scan b/bin/estate-security-scan
index f7d14a2d9..9c697c086 100755
--- a/bin/estate-security-scan
+++ b/bin/estate-security-scan
@@ -20,16 +20,31 @@
 # here: they are git-ignored and never leave the machine. `gitleaks protect
 # --staged` is the pre-commit fence for what is about to become the codebase.
 #
-#   estate-security-scan            run all three, print the receipt
-#   estate-security-scan --quiet    receipt only, no tool chatter
+#   estate-security-scan                 run all checks on this checkout, print the receipt
+#   estate-security-scan --quiet         receipt only, no tool chatter
+#   estate-security-scan --source DIR    scan another repository (the composite action
+#                                        .github/actions/security-scan runs it this way
+#                                        from every active estate repo)
+#
+# Checks 2 and 3 adapt to the repository: pip-audit runs only where a tracked
+# requirements*.txt exists, npm audit only where a tracked package-lock.json
+# exists, and the policy gate only where bin/security-policy-gate exists. A
+# check with nothing to scan prints "ok ... none tracked", never BLIND.
 set -uo pipefail
 IDP="$(cd "$(dirname "$0")/.." && pwd)"
-cd "$IDP" || exit 3
-quiet=0; [ "${1:-}" = "--quiet" ] && quiet=1
+SRC="$IDP"; quiet=0
+while [ $# -gt 0 ]; do
+  case "$1" in
+    --quiet) quiet=1 ;;
+    --source) SRC="$(cd "$2" && pwd)" || exit 3; shift ;;
+    *) echo "usage: estate-security-scan [--quiet] [--source DIR]" >&2; exit 3 ;;
+  esac; shift
+done
+cd "$SRC" || exit 3
 fail=0; blind=0
 say() { printf '%s\n' "$*"; }
 
-say "SECURITY-SCAN $(date -u +%Y-%m-%dT%H:%M:%SZ) commit=$(git rev-parse HEAD) tree=$(git status --porcelain | wc -l | tr -d ' ')-dirty-paths"
+say "SECURITY-SCAN $(date -u +%Y-%m-%dT%H:%M:%SZ) commit=$(git rev-parse HEAD) tree=$(git status --porcelain | grep -vc '^?? .estate-idp/')-dirty-paths"
 
 # 1. secrets, over git history: every tracked byte in every commit.
 if command -v gitleaks >/dev/null; then
@@ -66,8 +81,30 @@ else
   fi
 fi
 
+# 2b. Node dependencies: every tracked package-lock.json, npm audit at High+.
+locks=$(git ls-files | grep -E '(^|/)package-lock\.json$')
+if [ -z "$locks" ]; then
+  say "ok    npm       no package-lock.json tracked"
+elif ! command -v npm >/dev/null; then
+  say "BLIND npm       npm not on PATH"; blind=1
+else
+  nfail=0
+  for l in $locks; do
+    d=$(dirname "$l")
+    if ! out=$(cd "$d" && timeout 120 npm audit --package-lock-only --audit-level=high 2>&1); then
+      rc=$?
+      if [ $rc = 124 ]; then say "BLIND npm       $d: npm audit exceeded 120s (network); rerun"; blind=1
+      else say "FAIL  npm       $d: high or critical advisories"; [ $quiet = 1 ] || printf '%s\n' "$out" | tail -15 | sed 's/^/      /'; nfail=1; fi
+    fi
+  done
+  [ $nfail = 1 ] && fail=1
+  [ $nfail = 0 ] && say "ok    npm       npm audit over $(echo "$locks" | wc -l | tr -d ' ') package-lock.json: no high or critical advisories"
+fi
+
 # 3. policy: every control row on the security policy page names a proof that exists.
-if out=$(python3 bin/security-policy-gate 2>&1); then
+if [ ! -x bin/security-policy-gate ]; then
+  say "ok    policy    no bin/security-policy-gate in this repository (idp carries the estate policy)"
+elif out=$(python3 bin/security-policy-gate 2>&1); then
   say "ok    policy    security-policy-gate: every control row has a proof"
 else
   say "FAIL  policy    security-policy-gate"; [ $quiet = 1 ] || printf '%s\n' "$out" | tail -10 | sed 's/^/      /'; fail=1
diff --git a/bin/law32-gate b/bin/law32-gate
index 6e7d817dc..9bd4d4324 100755
--- a/bin/law32-gate
+++ b/bin/law32-gate
@@ -47,8 +47,9 @@ def feature_names():
 
 
 def has_pair(stem, names):
-    stem = re.sub(r"-(gate|report)$", "", stem)
-    cands = [n for n in names if stem == n or stem.startswith(n + "-")]
+    # accept the full stem too: bin/spec-gate is documented as docs/demo/spec-gate.md
+    stems = {stem, re.sub(r"-(gate|report)$", "", stem)}
+    cands = [n for n in names if any(s == n or s.startswith(n + "-") for s in stems)]
     for n in sorted(cands, key=len, reverse=True):
         if all(os.path.isfile(os.path.join(ROOT, "docs", d, n + ".md")) for d in ("demo", "onboarding")):
             return n
diff --git a/bin/repo-rulesets b/bin/repo-rulesets
index cf706b88f..f8369bc3d 100755
--- a/bin/repo-rulesets
+++ b/bin/repo-rulesets
@@ -3,7 +3,10 @@
 # platform/github/ruleset.default-branch.json, and a repository named in an
 # overlay file platform/github/ruleset.<repo>.<name>.json carries that ruleset
 # too (idp: ruleset.idp.required-checks.json makes offline-gate, bdd and
-# security-scan required before anything lands on main). Report mode by default.
+# security-scan required before anything lands on main). An overlay named
+# ruleset.@active.<name>.json applies to every repository pushed in the last
+# ACTIVE_DAYS (30) days: the estate-wide gates security-scan and spec-gate,
+# which bin/estate-security-rollout installs first. Report mode by default.
 #
 # Why per repo: the account is a User (gh api /users/<owner> -q .type), and
 # org-wide rulesets ("~ALL") exist only on organizations. On the free plan a
@@ -31,11 +34,18 @@ apply_spec() {
     echo "MISSING $repo: no ruleset $NAME"; drift=$((drift+1))
   fi
 }
-while read -r repo; do apply_spec "$repo" "$DEFAULT"; done < <(gh repo list "$OWNER" --limit 200 --no-archived --json name -q '.[].name' | sort)
+ACTIVE_DAYS="${ACTIVE_DAYS:-30}"
+since="$(date -u -v-"${ACTIVE_DAYS}"d +%Y-%m-%d 2>/dev/null || date -u -d "-${ACTIVE_DAYS} days" +%Y-%m-%d)"
+repos="$(gh repo list "$OWNER" --limit 200 --no-archived --json name,pushedAt -q '.[] | .name+" "+.pushedAt[:10]' | sort)"
+while read -r repo _; do apply_spec "$repo" "$DEFAULT"; done <<< "$repos"
 for f in "$IDP"/platform/github/ruleset.*.*.json; do
   [ -e "$f" ] || break
   r="$(basename "$f")"; r="${r#ruleset.}"; r="${r%%.*}"
-  apply_spec "$r" "$f"
+  if [ "$r" = "@active" ]; then
+    while read -r repo pushed; do [ "$pushed" \> "$since" ] && apply_spec "$repo" "$f"; done <<< "$repos"
+  else
+    apply_spec "$r" "$f"
+  fi
 done
 echo "$([ $drift = 0 ] && echo 'ok  ' || echo FAIL)  rulesets $ok in place, $drift missing or drifted, $blocked private repos blocked by plan"
 [ $drift = 0 ]
diff --git a/bin/spec-gate b/bin/spec-gate
new file mode 100755
index 000000000..ee068a470
--- /dev/null
+++ b/bin/spec-gate
@@ -0,0 +1,32 @@
+#!/usr/bin/env bash
+# spec-gate [BASE_REF]: a change to code must come with a change to the
+# executable spec. Founder ruling R29, 2026-08-25: "Every PR must update the
+# BDD scenario or the auto-gen script. If the PR changes code but not the
+# executable spec, the CI blocks merge. No manual doc updates."
+#
+# Code     any tracked file that ends in .py .ts .tsx .js .jsx .go .rs .sh .rb
+#          .java .kt .swift, or lives under bin/, that is not itself a spec.
+# Spec     a *.feature file; anything under tests/, test/, spec/, features/;
+#          a file named test_*.py, *_test.*, *.test.*, *.spec.*; the live
+#          diagram generator bin/estate-diagram.
+#
+# Compares BASE_REF (default: origin/main, or the PR base in CI) to HEAD.
+# Exit 0 when no code changed or a spec changed with it; 1 when code changed
+# alone; 3 (BLIND) when the base ref cannot be resolved. Runs in every active
+# estate repository through .github/actions/spec-gate.
+set -uo pipefail
+base="${1:-${SPEC_GATE_BASE:-origin/main}}"
+git rev-parse --verify -q "$base^{commit}" >/dev/null || { echo "BLIND spec-gate: base ref '$base' not found (fetch-depth 0?)"; exit 3; }
+mb="$(git merge-base "$base" HEAD)" || { echo "BLIND spec-gate: no merge base with $base"; exit 3; }
+changed="$(git diff --name-only "$mb" HEAD)"
+spec_re='(^|/)(tests?|spec|features)/|\.feature$|(^|/)test_[^/]*\.py$|_test\.[a-z]+$|\.test\.[a-z]+$|\.spec\.[a-z]+$|(^|/)bin/estate-diagram$'
+code_re='\.(py|ts|tsx|js|jsx|go|rs|sh|rb|java|kt|swift)$|(^|/)bin/[^/]+$'
+specs="$(printf '%s\n' "$changed" | grep -E "$spec_re" || true)"
+code="$(printf '%s\n' "$changed" | grep -Ev "$spec_re" | grep -E "$code_re" || true)"
+ncode=$(printf '%s' "$code" | grep -c . || true); nspec=$(printf '%s' "$specs" | grep -c . || true)
+if [ "$ncode" = 0 ]; then echo "ok    spec-gate no code changed since $mb ($(printf '%s' "$changed" | grep -c . || true) files)"; exit 0; fi
+if [ "$nspec" -gt 0 ]; then echo "ok    spec-gate $ncode code file(s) changed with $nspec spec file(s)"; exit 0; fi
+echo "FAIL  spec-gate $ncode code file(s) changed and no executable spec changed (R29):"
+printf '%s\n' "$code" | sed 's/^/      /'
+echo "      add or change a *.feature scenario, a test, or bin/estate-diagram in this PR"
+exit 1
diff --git a/docs/demo/estate-security-rollout.md b/docs/demo/estate-security-rollout.md
new file mode 100644
index 000000000..ad78e8f43
--- /dev/null
+++ b/docs/demo/estate-security-rollout.md
@@ -0,0 +1,28 @@
+# Demo: estate-security-rollout
+
+One command installs the two estate gates in every active repository.
+Report mode first (founder machine, 2026-08-25, before the rollout):
+
+```
+$ bin/estate-security-rollout | tail -4
+MISSING sentinel-loop: no .github/workflows/security-scan.yml on main
+MISSING survival-stack: no .github/workflows/security-scan.yml on main
+MISSING tailwind-css-starter-blog: no .github/workflows/security-scan.yml on main
+FAIL  rollout 1 in place, 0 opened, 0 merged, 27 open or missing, 0 blocked
+```
+
+Then open the pull requests, and merge the green ones:
+
+```
+$ bin/estate-security-rollout --apply | tail -2
+OPENED  survival-stack: https://github.com/chidionyema/survival-stack/pull/N
+FAIL  rollout 1 in place, 27 opened, 0 merged, 0 open or missing, 0 blocked
+$ bin/estate-security-rollout --merge | tail -1
+ok    rollout 28 in place, 0 opened, 27 merged, 0 open or missing, 0 blocked
+$ bin/repo-rulesets --apply | grep estate-security-scan | head -2
+FIXED   crew: created #NNN
+```
+
+A repository whose history holds a leaked secret stays `OPEN`: its
+`security-scan` check is red and the pull request is not merged. That is the
+gate working; clean the history, then run `--merge` again.
diff --git a/docs/demo/spec-gate.md b/docs/demo/spec-gate.md
new file mode 100644
index 000000000..2cf2a69cc
--- /dev/null
+++ b/docs/demo/spec-gate.md
@@ -0,0 +1,30 @@
+# Demo: spec-gate
+
+Founder ruling R29 (2026-08-25): a pull request that changes code must also
+change the executable spec. `bin/spec-gate` compares the PR base to HEAD and
+refuses code that arrives alone.
+
+In a throwaway repository, change code only:
+
+```
+$ git init -q -b main t && cd t && echo 'x = 1' > app.py && git add . && git commit -qm base && git branch base
+$ echo 'x = 2' > app.py && git commit -qam 'code only'
+$ ~/dev/code/idp/bin/spec-gate base
+FAIL  spec-gate 1 code file(s) changed and no executable spec changed (R29):
+      app.py
+      add or change a *.feature scenario, a test, or bin/estate-diagram in this PR
+```
+
+Add the scenario and run it again:
+
+```
+$ mkdir features && echo 'Feature: x is 2' > features/app.feature && git add . && git commit -qm spec
+$ ~/dev/code/idp/bin/spec-gate base
+ok    spec-gate 1 code file(s) changed with 1 spec file(s)
+```
+
+Both runs are the incident test `sovereign/tests/test_incident_r29_spec_gate.py`,
+so CI proves the gate refuses and permits on every push. In every active
+estate repository the same script runs as the `spec-gate` job through
+`chidionyema/idp/.github/actions/spec-gate`; the ruleset
+`estate-security-scan` makes it a required check.
diff --git a/docs/onboarding/estate-security-rollout.md b/docs/onboarding/estate-security-rollout.md
new file mode 100644
index 000000000..68fcacd66
--- /dev/null
+++ b/docs/onboarding/estate-security-rollout.md
@@ -0,0 +1,50 @@
+# Onboarding: estate-security-rollout
+
+## What it is
+
+`bin/estate-security-rollout [--apply] [--merge]` makes sure every repository
+pushed in the last `ACTIVE_DAYS` (30) days carries
+`.github/workflows/security-scan.yml`, byte-identical to
+`platform/github/workflows/security-scan.yml` in idp. That file is two jobs,
+`security-scan` and `spec-gate`, each one `uses:` line calling a composite
+action in idp. Report mode prints `ok`, `MISSING`, `OPEN` or `BLOCKED` per
+repository. `--apply` writes the file on branch `estate/security-scan` through
+the GitHub contents API and opens one pull request per repository. `--merge`
+squash-merges the pull requests whose checks are green. Idempotent: a second
+run reports `ok` for what is in place and touches nothing.
+
+## Why it exists
+
+Founder, 2026-08-25: the security scan and the executable-spec rule apply to
+the whole estate, "only active ones", and the ways of working are unified in
+one shot. 50 repositories, 28 active; 15 of the active ones had no CI at all.
+A required status check is matched by job name, so the job has to exist in a
+repository before `repo-rulesets` can require it there. This command is the
+first half; `bin/repo-rulesets --apply` with
+`platform/github/ruleset.@active.security-scan.json` is the second.
+
+## The deviation from actions-pinned
+
+The caller uses `chidionyema/idp/.github/actions/...@main`, a moving ref on a
+same-owner repository, where `bin/actions-pinned` demands a commit SHA for
+third-party actions. A SHA would have to be re-pinned in 27 repositories on
+every idp change. The risk a SHA guards against is the action's owner moving a
+tag after review; the owner here is the estate.
+
+## What it cannot see
+
+Repositories that answer 403 (private on the free plan) print `BLOCKED`. A
+repository with a `review-gate` of its own (crew) needs its reviewer comment
+before `--merge` can land it; the command reports it `OPEN`.
+
+## Related files
+
+```
+bin/estate-security-rollout                       this command
+platform/github/workflows/security-scan.yml       the caller every repo carries
+.github/actions/security-scan/action.yml          gate 1
+.github/actions/spec-gate/action.yml              gate 2
+platform/github/ruleset.@active.security-scan.json
+                                                  makes both checks required on active repos
+bin/repo-rulesets                                 applies the rulesets
+```
diff --git a/docs/onboarding/estate-security-scan.md b/docs/onboarding/estate-security-scan.md
index d9ab2eb8d..5837cc8b0 100644
--- a/docs/onboarding/estate-security-scan.md
+++ b/docs/onboarding/estate-security-scan.md
@@ -2,6 +2,13 @@
 
 ## What it is
 
+`--source DIR` scans another repository; the composite action
+`.github/actions/security-scan` runs it that way from every active estate
+repository. Checks adapt to what the repository tracks: pip-audit only where a
+`requirements*.txt` exists, `npm audit --audit-level=high` only where a
+`package-lock.json` exists, the policy gate only where `bin/security-policy-gate`
+exists. Nothing to scan prints `ok ... none tracked`, never BLIND.
+
 `bin/estate-security-scan [--quiet]` runs three checks and prints one receipt
 with a timestamp and commit hash:
 
diff --git a/docs/onboarding/spec-gate.md b/docs/onboarding/spec-gate.md
new file mode 100644
index 000000000..02924a9d3
--- /dev/null
+++ b/docs/onboarding/spec-gate.md
@@ -0,0 +1,50 @@
+# Onboarding: spec-gate
+
+## What it is
+
+`bin/spec-gate [BASE_REF]` lists the files changed between the merge base of
+`BASE_REF` (default `origin/main`) and `HEAD`, and applies one rule:
+
+- **code**: `.py .ts .tsx .js .jsx .go .rs .sh .rb .java .kt .swift` files and
+  anything under `bin/` that is not itself a spec
+- **spec**: `*.feature` files; anything under `tests/`, `test/`, `spec/` or
+  `features/`; `test_*.py`, `*_test.*`, `*.test.*`, `*.spec.*`; the live
+  diagram generator `bin/estate-diagram`
+
+Code changed and no spec changed: exit 1, the code files are printed. No code
+changed, or a spec changed with it: exit 0. Base ref not found: exit 3, BLIND.
+
+## Why it exists
+
+Founder ruling R29, 2026-08-25, after a peer agent answered "can you
+guarantee the documentation stays current" with "no": documentation that lives
+outside the execution path rots, so the only spec that counts is one CI runs.
+"Every PR must update the BDD scenario or the auto-gen script. If the PR
+changes code but not the executable spec, the CI blocks merge. No manual doc
+updates." Tracked on crew#236.
+
+## Where it runs
+
+- idp: the `spec-gate` job in `.github/workflows/ci.yml`, required by ruleset
+  `idp-required-checks`.
+- Every repository pushed in the last 30 days: the same job from
+  `.github/workflows/security-scan.yml` (installed by
+  `bin/estate-security-rollout`), required by ruleset `estate-security-scan`.
+- By hand before pushing: `bin/spec-gate` on the branch.
+
+## What it cannot see
+
+It grades file names, not content. A one-line no-op edit to a `.feature` file
+satisfies it. The reviewer session (crew review-gate) is the check on that;
+this gate removes the case where nobody touched the spec at all.
+
+## Related files
+
+```
+bin/spec-gate                                   the rule
+.github/actions/spec-gate/action.yml            the composite action other repos call
+sovereign/tests/test_incident_r29_spec_gate.py  proves refuse and permit in one run
+features/gates/estate_gates.feature             the scenarios
+platform/github/ruleset.@active.security-scan.json
+                                                required checks for active repos
+```
diff --git a/features/gates/estate_gates.feature b/features/gates/estate_gates.feature
new file mode 100644
index 000000000..8dc6d566a
--- /dev/null
+++ b/features/gates/estate_gates.feature
@@ -0,0 +1,26 @@
+Feature: Every active estate repository runs the same merge-blocking gates
+  Founder, 2026-08-25: the security scan and the executable-spec rule (R29)
+  apply estate-wide, to every repository pushed in the last 30 days, not to
+  idp alone. One composite action per gate lives in idp; each repository
+  calls it from a job whose name is the required status check.
+
+  Scenario: A leaked secret cannot reach main in any active repository
+    Given a repository pushed in the last 30 days
+    When a pull request adds a committed credential
+    Then the security-scan job ends with "SECURITY-SCAN FAIL"
+    And the ruleset estate-security-scan refuses the merge
+
+  Scenario: Code without an executable spec cannot reach main
+    Given a pull request that changes a .py, .ts or bin/ file
+    And it changes no *.feature, test or bin/estate-diagram file
+    Then the spec-gate job fails naming the code files
+    And the ruleset estate-security-scan refuses the merge
+
+  Scenario: The gate permits code that arrives with its spec
+    Given a pull request that changes a .py file and a *.feature file
+    Then the spec-gate job passes
+
+  Scenario: The rollout is one command, run once
+    When bin/estate-security-rollout --apply runs
+    Then every active repository without the caller workflow gets one pull request
+    And bin/repo-rulesets --apply then requires both checks on each of them
diff --git a/mkdocs.yml b/mkdocs.yml
index 0c01c7a5f..601b77eb5 100644
--- a/mkdocs.yml
+++ b/mkdocs.yml
@@ -42,6 +42,8 @@ nav:
       - LAW 32 gate: demo/law32.md
       - Actions pinned: demo/actions-pinned.md
       - Estate security scan: demo/estate-security-scan.md
+      - Estate security rollout: demo/estate-security-rollout.md
+      - Spec gate: demo/spec-gate.md
       - owner-account: demo/owner-account.md
       - static-secret: demo/static-secret.md
       - Bind audit: demo/bind-audit.md
@@ -77,6 +79,8 @@ nav:
       - LAW 32 gate: onboarding/law32.md
       - Actions pinned: onboarding/actions-pinned.md
       - Estate security scan: onboarding/estate-security-scan.md
+      - Estate security rollout: onboarding/estate-security-rollout.md
+      - Spec gate: onboarding/spec-gate.md
       - owner-account: onboarding/owner-account.md
       - static-secret: onboarding/static-secret.md
       - Bind audit: onboarding/bind-audit.md
diff --git a/platform/github/ruleset.@active.security-scan.json b/platform/github/ruleset.@active.security-scan.json
new file mode 100644
index 000000000..4087df9a3
--- /dev/null
+++ b/platform/github/ruleset.@active.security-scan.json
@@ -0,0 +1,18 @@
+{
+  "name": "estate-security-scan",
+  "target": "branch",
+  "enforcement": "active",
+  "conditions": { "ref_name": { "include": ["~DEFAULT_BRANCH"], "exclude": [] } },
+  "rules": [
+    {
+      "type": "required_status_checks",
+      "parameters": {
+        "strict_required_status_checks_policy": false,
+        "required_status_checks": [
+          { "context": "security-scan" },
+          { "context": "spec-gate" }
+        ]
+      }
+    }
+  ]
+}
diff --git a/platform/github/ruleset.idp.required-checks.json b/platform/github/ruleset.idp.required-checks.json
index 74a512682..1445407b0 100644
--- a/platform/github/ruleset.idp.required-checks.json
+++ b/platform/github/ruleset.idp.required-checks.json
@@ -11,7 +11,8 @@
         "required_status_checks": [
           { "context": "offline-gate" },
           { "context": "bdd" },
-          { "context": "security-scan" }
+          { "context": "security-scan" },
+          { "context": "spec-gate" }
         ]
       }
     }
diff --git a/platform/github/workflows/security-scan.yml b/platform/github/workflows/security-scan.yml
new file mode 100644
index 000000000..d85a23cfb
--- /dev/null
+++ b/platform/github/workflows/security-scan.yml
@@ -0,0 +1,19 @@
+# Copied verbatim into .github/workflows/security-scan.yml of every active
+# estate repository by bin/estate-security-rollout. The job name is the
+# required status check named in platform/github/ruleset.@active.security-scan.json.
+# `@main` on a same-owner action is the stated deviation from bin/actions-pinned:
+# a SHA would need re-pinning in every repository on every idp change.
+name: security-scan
+on:
+  pull_request:
+  push:
+    branches: [main, master]
+jobs:
+  security-scan:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: chidionyema/idp/.github/actions/security-scan@main
+  spec-gate:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: chidionyema/idp/.github/actions/spec-gate@main
diff --git a/sovereign/tests/test_incident_r29_spec_gate.py b/sovereign/tests/test_incident_r29_spec_gate.py
new file mode 100644
index 000000000..f598666fd
--- /dev/null
+++ b/sovereign/tests/test_incident_r29_spec_gate.py
@@ -0,0 +1,49 @@
+"""Incident test, R29 (founder 2026-08-25): code changed without an executable spec must be
+refused, and code that arrives with its spec must be permitted, in the same run (LAW 38)."""
+from __future__ import annotations
+
+import subprocess
+from pathlib import Path
+
+GATE = Path(__file__).resolve().parents[2] / "bin" / "spec-gate"
+
+
+def _git(repo: Path, *args: str) -> str:
+    return subprocess.run(["git", "-c", "user.email=t@t", "-c", "user.name=t", *args],
+                          cwd=repo, check=True, capture_output=True, text=True).stdout
+
+
+def _repo(tmp: Path) -> Path:
+    _git(tmp, "init", "-q", "-b", "main")
+    (tmp / "app.py").write_text("x = 1\n")
+    _git(tmp, "add", "."); _git(tmp, "commit", "-qm", "base")
+    _git(tmp, "branch", "base")
+    return tmp
+
+
+def _gate(repo: Path) -> tuple[int, str]:
+    p = subprocess.run([str(GATE), "base"], cwd=repo, capture_output=True, text=True)
+    return p.returncode, p.stdout
+
+
+def test_incident_r29_code_alone_is_refused_and_code_with_spec_is_permitted(tmp_path: Path) -> None:
+    repo = _repo(tmp_path)
+    (repo / "app.py").write_text("x = 2\n")
+    _git(repo, "commit", "-qam", "code only")
+    rc, out = _gate(repo)
+    assert rc == 1 and "FAIL  spec-gate" in out and "app.py" in out, out
+
+    (repo / "features").mkdir()
+    (repo / "features" / "app.feature").write_text("Feature: x is 2\n")
+    _git(repo, "add", "."); _git(repo, "commit", "-qm", "spec")
+    rc, out = _gate(repo)
+    assert rc == 0 and "ok    spec-gate" in out, out
+
+
+def test_incident_r29_no_code_change_passes_and_missing_base_is_blind(tmp_path: Path) -> None:
+    repo = _repo(tmp_path)
+    (repo / "README.md").write_text("docs\n")
+    _git(repo, "add", "."); _git(repo, "commit", "-qm", "docs")
+    assert _gate(repo)[0] == 0
+    p = subprocess.run([str(GATE), "no-such-ref"], cwd=repo, capture_output=True, text=True)
+    assert p.returncode == 3 and "BLIND" in p.stdout
