diff --git a/.github/workflows/conscience.yml b/.github/workflows/conscience.yml
index aead918cc..5ad614a2f 100644
--- a/.github/workflows/conscience.yml
+++ b/.github/workflows/conscience.yml
@@ -208,6 +208,7 @@ jobs:
               "Author-session: com.estate.conscience" "Standard: Definition of Done row, docs/STANDARDS.md" "" \
               "Generated by bin/idp-conscience --page on the daily run. Auto-merge is on; it lands when the gates pass." "" \
               "Optimised: 1 -> 1 steps (one render of the conscience page), 1 -> 1 round trips (one push, one pull request); cut: nothing, a scheduled render has no plan to shrink (operating-model-gate rule optimised_plan, LAW 51)" "" \
+              "Cleanup: nothing to clean, the render replaces its own page and the branch is deleted on merge (operating-model-gate rule cleanup_section, ruling R62)" "" \
               "## Architecture laws" \
               "- LAW 1 zero-gravity: n/a: generated page only, docs/CONSCIENCE.md" \
               "- LAW 2 fractal: bin/idp-conscience -> --page -> docs/CONSCIENCE.md" \
diff --git a/.github/workflows/kini-finish.yml b/.github/workflows/kini-finish.yml
index 3b6452c78..8d3e57a11 100644
--- a/.github/workflows/kini-finish.yml
+++ b/.github/workflows/kini-finish.yml
@@ -59,10 +59,11 @@ jobs:
           git push -q "git@github.com:${GITHUB_REPOSITORY}.git" "$b"
           gh pr create --head "$b" --base main \
             --title "kini: FINISH request $REQUEST (crew#396)" \
-            --body "$(printf '%s\n\n%s\n%s\n%s\n\n%s\n%s\n%s\n%s\n%s\n\n%s\n' \
+            --body "$(printf '%s\n\n%s\n%s\n%s\n%s\n\n%s\n%s\n%s\n%s\n%s\n\n%s\n' \
               "Board word FINISH: KINI (${{ github.event_name }}, run $REQUEST). Flux applies the renamed Job; it starts KiniFinishWorkflow and waits. Receipt: state/kini via bin/idp-kini-state." \
               "Spec: features/sovereign-bus/cp0d_kini_finish_trigger.feature" \
               "Drill: kini-finish" \
+              "Cleanup: nothing to clean, the Job rename replaces itself and the branch is deleted on merge (operating-model-gate rule cleanup_section, ruling R62)" \
               "Cost-delta-usd-month: 0" \
               "## Architecture laws" \
               "- LAW 1 zero-gravity: n/a: Job name only" \
diff --git a/bin/catalog-render b/bin/catalog-render
index c4fae4d31..8d839df13 100755
--- a/bin/catalog-render
+++ b/bin/catalog-render
@@ -146,6 +146,9 @@ def main(argv: list[str]) -> int:
     # Same two-part fix as bin/idp-image-update-pr: the create path writes the line, and an
     # existing body that lacks it gains it.
     OPTIMISED = ("Optimised: 1 -> 1 steps (one render of the catalogue), 1 -> 1 round trips (one push, one pull request); cut: nothing, a scheduled render has no plan to shrink (operating-model-gate rule optimised_plan, LAW 51)")
+    # Ruling R62 (founder 2026-08-31): every body carries a Cleanup section; same two-part
+    # fix as the Optimised line so an existing render branch gains it too.
+    CLEANUP = "Cleanup: nothing to clean, the render replaces its own page and leaves no worktree or branch behind (operating-model-gate rule cleanup_section, ruling R62)"
     rc, existing = sh(["gh", "pr", "list", "--head", BRANCH, "--state", "open", "--json", "number", "--jq", ".[0].number"], WT)
     if not existing:
         step("open PR", ["gh", "pr", "create", "--base", "main", "--head", BRANCH,
@@ -156,7 +159,7 @@ def main(argv: list[str]) -> int:
                                    "it lands when security-scan and spec-gate pass.\n\n"
                                    # crew#474: the operating-model gate refuses a body with no laws section
                                    # (idp#376 sat BLOCKED for a day), so the generated body carries one.
-                                   f"{OPTIMISED}\n\n"
+                                   f"{OPTIMISED}\n{CLEANUP}\n\n"
                                    "## Architecture laws\n"
                                    f"- LAW 1 zero-gravity: n/a: generated pages only, {PAGE} and {SHOWCASE}\n"
                                    "- LAW 2 fractal: bin/catalog-render -> bin/estate-showcase -> bin/trace-matrix\n"
@@ -164,8 +167,10 @@ def main(argv: list[str]) -> int:
                                    "- LAW 4 calibration: n/a: no prediction made\n"], WT)
         rc, existing = sh(["gh", "pr", "list", "--head", BRANCH, "--state", "open", "--json", "number", "--jq", ".[0].number"], WT)
     rc, body = sh(["gh", "pr", "view", existing, "--json", "body", "--jq", ".body"], WT)
-    if not any(l.startswith("Optimised: ") for l in body.splitlines()):
-        step("refresh body", ["gh", "pr", "edit", existing, "--body", body + "\n\n" + OPTIMISED + "\n"], WT)
+    gained = [line for line in (OPTIMISED, CLEANUP)
+              if not any(l.startswith(line.split(":", 1)[0] + ":") for l in body.splitlines())]
+    if gained:
+        step("refresh body", ["gh", "pr", "edit", existing, "--body", body + "\n\n" + "\n".join(gained) + "\n"], WT)
 
     # crew#684: bin/idp-pr-age drafts a conflicting PR at 4h; a fresh render clears the conflict
     # but `gh pr merge --auto` refuses a draft (runs 33278169055, 33294242647). Un-draft first.
diff --git a/bin/estate-security-rollout b/bin/estate-security-rollout
index f9ddca3b5..a813f91b1 100755
--- a/bin/estate-security-rollout
+++ b/bin/estate-security-rollout
@@ -48,7 +48,7 @@ while read -r repo pushed; do
   gh api -X PUT "/repos/$OWNER/$repo/contents/$DEST" "${args[@]}" >/dev/null || { echo "BLOCKED $repo: could not write $DEST on $BRANCH"; blocked=$((blocked+1)); continue; }
   url="$(gh pr create --repo "$OWNER/$repo" --base "$default" --head "$BRANCH" \
     --title "ci: estate security-scan and spec-gate" \
-    --body "$(printf 'Installs the two estate gates as one job each, calling the composite actions in chidionyema/idp: security-scan (gitleaks over history, pip-audit, npm audit, policy gate) and spec-gate (a code change must carry a *.feature, test or diagram-generator change; founder ruling R29, 2026-08-25). Opened by bin/estate-security-rollout from idp; tracked on crew#236 and crew#229.\n\nAuthor-session: estate-security-rollout\nNo-Issue: fleet rollout tracked on crew#236')" 2>&1 | tail -1)"
+    --body "$(printf 'Installs the two estate gates as one job each, calling the composite actions in chidionyema/idp: security-scan (gitleaks over history, pip-audit, npm audit, policy gate) and spec-gate (a code change must carry a *.feature, test or diagram-generator change; founder ruling R29, 2026-08-25). Opened by bin/estate-security-rollout from idp; tracked on crew#236 and crew#229.\n\nAuthor-session: estate-security-rollout\nCleanup: nothing to clean, one workflow file is added and the branch is deleted on merge (ruling R62)\nNo-Issue: fleet rollout tracked on crew#236')" 2>&1 | tail -1)"
   echo "OPENED  $repo: $url"; opened=$((opened+1))
 done < <(gh repo list "$OWNER" --limit 200 --no-archived --json name,pushedAt -q '.[] | .name+" "+.pushedAt[:10]' | sort)
 echo "$([ $todo = 0 ] && [ $blocked = 0 ] && echo 'ok  ' || echo FAIL)  rollout $ok in place, $opened opened, $merged merged, $todo open or missing, $blocked blocked"
diff --git a/bin/idp-image-update-pr b/bin/idp-image-update-pr
index 7bcc08f9c..83aa56e7f 100755
--- a/bin/idp-image-update-pr
+++ b/bin/idp-image-update-pr
@@ -14,14 +14,18 @@ branch=${IMAGE_UPDATE_BRANCH:-flux/image-updates}
 # shellcheck disable=SC2016  # backticks are the gate's own syntax, not a substitution
 VERIFY='Verify: `grep -rn imagepolicy platform/ clusters/ --include=*.yaml`'
 OPTIMISED="Optimised: 1 -> 1 steps (a newTag line written by the image controller), 1 -> 1 round trips (one push, one pull request); cut: nothing, a machine-written tag bump has no plan to shrink (operating-model-gate rule optimised_plan, LAW 51)"
+# Ruling R62 (founder 2026-08-31): every body carries a Cleanup section; same two-part fix
+# as the Optimised line so an already-open controller branch gains it too.
+CLEANUP="Cleanup: nothing to clean, the controller rewrites one newTag line and leaves no worktree or branch behind (operating-model-gate rule cleanup_section, ruling R62)"
 n=$(gh pr list --head "$branch" --base main --state open --json number -q '.[0].number')
 if [ -z "$n" ]; then
   gh pr create --head "$branch" --base main \
     --title "$(git log -1 --format=%s)" \
-    --body "$(printf '%s\n\n%s\n%s\n%s\n%s\n%s\n%s\n\n%s\n%s\n%s\n%s\n%s\n' \
+    --body "$(printf '%s\n\n%s\n%s\n%s\n%s\n%s\n%s\n%s\n\n%s\n%s\n%s\n%s\n%s\n' \
       "$(git log -1 --format=%b)" \
       "$VERIFY" \
       "$OPTIMISED" \
+      "$CLEANUP" \
       "Spec: tests/test_incident_catalogue_image_never_repulled.py" \
       "Drill: login-drill" \
       "Cost-delta-usd-month: 0" \
@@ -44,6 +48,15 @@ case "$body" in
     echo "refresh image-update-pr #$n body gained the Optimised line (LAW 51)"
     ;;
 esac
+# Ruling R62: an already-open controller branch gains the Cleanup line the same way.
+body=$(gh pr view "$n" --json body -q .body)
+case "$body" in
+  "Cleanup: "*|*$'\n'"Cleanup: "*|*$'\n'"## Cleanup"*) ;;
+  *)
+    gh pr edit "$n" --body "$(printf '%s\n\n%s\n' "$body" "$CLEANUP")"
+    echo "refresh image-update-pr #$n body gained the Cleanup line (ruling R62)"
+    ;;
+esac
 # The same backfill as above: idp#1011 was opened before this line existed and keeps its old body
 # on every controller push, so `verify` would stay red on it forever without this.
 case "$body" in
diff --git a/docs/policy/cleanup-section.md b/docs/policy/cleanup-section.md
new file mode 100644
index 000000000..7c0134d57
--- /dev/null
+++ b/docs/policy/cleanup-section.md
@@ -0,0 +1,15 @@
+# Every pull request says what it cleans up
+
+Founder ruling, 2026-08-31: every pull request must have a mandatory cleanup section. His words: "we never cleannup" and "i will defie what it says" — he defines the section's required content.
+
+The day the ruling landed, 13 leftover work folders sat inside the idp checkout and 325 were registered against the repository. Work piles up because no pull request accounts for what it leaves behind.
+
+## The rule
+
+Every pull request body carries a `## Cleanup` section (a `Cleanup: ...` line also counts). It says what the change removes or leaves behind: work folders, branches, dead files, stored state. If there is nothing to clean, the section says `nothing to clean` — explicitly, never by omission.
+
+The operating-model gate enforces presence (rule `cleanup_section` in `policy/operating_model.rego`). The founder defines what the section must say; until that definition lands, the gate refuses nothing about the content. A pull request opened before the rule existed is not refused.
+
+## Why presence only
+
+The founder said he will define the required content himself. Inventing required fields before his definition would put words in his mouth and refuse correct work — and a guard that refuses correct work is an outage, by the estate's own law on guardrails.
diff --git a/policy/fixtures/opmodel-ok.json b/policy/fixtures/opmodel-ok.json
index de3eefd9d..510d868ae 100644
--- a/policy/fixtures/opmodel-ok.json
+++ b/policy/fixtures/opmodel-ok.json
@@ -7,7 +7,7 @@
    "tests/test_incident_crew287_zcp_grant_in_the_same_pr.py"
   ],
   "added": "+resource \"oci_identity_domains_app\" \"front_door\" {\n+resource \"oci_identity_domains_grant\" \"founder\" {\n",
-  "body": "Front door OIDC client by Terraform.\nApproval-word: front-door\nCost-delta-usd-month: 0\nUse: bin/idp-identity-apply apply\n\nDrill: oke-check\n\n## Architecture laws\n- LAW 1 zero-gravity: bin/cloud-agnostic-gate -> 0\n- LAW 2 fractal: n/a: no service boundary changes\n- LAW 3 nervous system: n/a: no workload changes\n- LAW 4 calibration: n/a: no prediction made\n\nOptimised: 3 steps -> 2, 1 round trip -> 1; cut: the second fixture pass, it proved nothing new\n\nControl: tests/test_incident_crew287_zcp_grant_in_the_same_pr.py\n",
+  "body": "Front door OIDC client by Terraform.\nApproval-word: front-door\nCost-delta-usd-month: 0\nUse: bin/idp-identity-apply apply\n\nDrill: oke-check\n\n## Architecture laws\n- LAW 1 zero-gravity: bin/cloud-agnostic-gate -> 0\n- LAW 2 fractal: n/a: no service boundary changes\n- LAW 3 nervous system: n/a: no workload changes\n- LAW 4 calibration: n/a: no prediction made\n\nOptimised: 3 steps -> 2, 1 round trip -> 1; cut: the second fixture pass, it proved nothing new\n\nControl: tests/test_incident_crew287_zcp_grant_in_the_same_pr.py\n\n## Cleanup\nnothing to clean\n",
   "labels": [
    "canary"
   ],
diff --git a/policy/operating_model.rego b/policy/operating_model.rego
index 991cb1052..f37ad9e3b 100644
--- a/policy/operating_model.rego
+++ b/policy/operating_model.rego
@@ -308,6 +308,36 @@ deny contains msg if {
 	msg := "rule=optimised_plan | the PR body has no counted `Optimised:` line (LAW 51) | fix: plan first, then add `Optimised: <steps before> -> <after>, <round trips before> -> <after>; cut: <what, why>` — numbers on both sides of the arrow and a cut clause; the procedure is in ~/AGENTS-FULL.md"
 }
 
+# --- cleanup_section (ruling R62, founder 2026-08-31) ---------------------------------------
+# Founder, 2026-08-31, verbatim: "we never cleannup every pr nust have cleanup section
+# nandatory ... and i will defie what it says". Said while 13 leftover worktrees sat inside
+# the idp folder and 325 were registered against the repository: work piles up because no
+# pull request accounts for what it leaves behind. Every body carries a `## Cleanup` heading
+# (or a `Cleanup:` line) naming what the change removes or leaves behind.
+#
+# Presence only, deliberately: the founder said he will define what the section must say,
+# so until his definition lands this rule refuses nothing about the content. Same
+# grandfather clause as optimised_plan: a pull request opened before the rule existed is
+# not refused for lacking a section nobody had asked for.
+
+cleanup_section_ok if {
+	regex.match(`(?mi)^(##+ +Cleanup\b|Cleanup: \S)`, input.pr.body)
+}
+
+cleanup_landed := "2026-08-31T17:10:00Z"
+
+opened_before_cleanup if {
+	is_string(input.pr.createdAt)
+	input.pr.createdAt < cleanup_landed
+}
+
+deny contains msg if {
+	is_string(input.pr.body)
+	not cleanup_section_ok
+	not opened_before_cleanup
+	msg := "rule=cleanup_section | the PR body has no `## Cleanup` section (ruling R62, founder 2026-08-31) | fix: add a `## Cleanup` section saying what this change removes or leaves behind — worktrees, branches, dead files, state; say `nothing to clean` explicitly if so"
+}
+
 # --- lifecycle_row (crew#618, founder 2026-08-29) -------------------------------------------
 # "no PR covering critical infra like this can have setup going to void: reusable? expiration? we
 # need policy." A pull request that touches a root credential's birth (bin/idp-bootstrap-*, the
diff --git a/tests/test_incident_crew584_optimised_line_gate.py b/tests/test_incident_crew584_optimised_line_gate.py
index 822eb9673..c76b74afa 100644
--- a/tests/test_incident_crew584_optimised_line_gate.py
+++ b/tests/test_incident_crew584_optimised_line_gate.py
@@ -156,9 +156,10 @@ VERIFY = "Verify: `grep -rn imagepolicy platform/ clusters/ --include=*.yaml`"
 
 
 def test_a_body_that_already_carries_both_lines_is_left_alone(tmp_path):
-    """Two backfills now run on every controller push, `Optimised:` and `Verify:` (idp#1046, the
-    same class one gate later), so "left alone" means neither of them found anything to add."""
-    r = _fake_gh(tmp_path, "Written by image-automation-controller.\n\nOptimised: 1 -> 1 steps, 1 -> 1 round trips; cut: nothing\n" + VERIFY + "\n")
+    """Three backfills now run on every controller push, `Optimised:`, `Verify:` and `Cleanup:`
+    (idp#1046 and ruling R62, the same class one gate later each time), so "left alone" means
+    none of them found anything to add."""
+    r = _fake_gh(tmp_path, "Written by image-automation-controller.\n\nOptimised: 1 -> 1 steps, 1 -> 1 round trips; cut: nothing\nCleanup: nothing to clean\n" + VERIFY + "\n")
     assert r.returncode == 0, r.stdout + r.stderr
     assert not (tmp_path / "edits.txt").exists()
 
diff --git a/tests/test_incident_r62_every_pr_names_its_cleanup.py b/tests/test_incident_r62_every_pr_names_its_cleanup.py
new file mode 100644
index 000000000..557593d23
--- /dev/null
+++ b/tests/test_incident_r62_every_pr_names_its_cleanup.py
@@ -0,0 +1,88 @@
+"""Incident, ruling R62 (2026-08-31): the founder found 13 leftover worktrees inside the idp
+folder and 325 registered against the repository, and said "we never cleannup every pr nust
+have cleanup section nandatory ... and i will defie what it says". Work piled up because no
+pull request accounted for what it leaves behind. policy/operating_model.rego rule
+`cleanup_section` refuses a body with no `## Cleanup` heading (or `Cleanup:` line).
+
+Presence only: he defines the section's required content; until that lands the rule says
+nothing about what the section must contain. Grandfathered like `optimised_plan`: a pull
+request opened before the rule existed is not refused.
+Rung 4: conftest on a fixture; opens no socket."""
+
+import json
+import pathlib
+import shutil
+import subprocess
+
+import pytest
+
+ROOT = pathlib.Path(__file__).resolve().parents[1]
+POLICY = ROOT / "policy"
+FIX = POLICY / "fixtures"
+
+pytestmark = pytest.mark.skipif(
+    shutil.which("conftest") is None, reason="conftest not installed"
+)
+
+
+def _rules(path: pathlib.Path) -> set[str]:
+    out = subprocess.run(
+        [
+            "conftest",
+            "test",
+            "--parser",
+            "json",
+            "-p",
+            str(POLICY),
+            "-o",
+            "json",
+            str(path),
+        ],
+        capture_output=True,
+        text=True,
+        check=False,
+    ).stdout
+    return {
+        f["msg"].split(" | ")[0]
+        for r in json.loads(out)
+        for f in (r.get("failures") or [])
+    }
+
+
+def _pr(
+    tmp_path, body_suffix: str = "", created_at: str | None = "2026-09-01T00:00:00Z"
+) -> pathlib.Path:
+    d = json.loads((FIX / "opmodel-no-optimised.json").read_text())
+    d["pr"]["body"] += body_suffix
+    if created_at is None:
+        d["pr"].pop("createdAt", None)
+    else:
+        d["pr"]["createdAt"] = created_at
+    p = tmp_path / "pr.json"
+    p.write_text(json.dumps(d))
+    return p
+
+
+def test_a_body_with_no_cleanup_section_is_refused(tmp_path):
+    assert "rule=cleanup_section" in _rules(_pr(tmp_path))
+
+
+def test_a_cleanup_heading_passes(tmp_path):
+    body = "\n## Cleanup\nRemoves the wt612 worktree; nothing else left behind.\n"
+    assert "rule=cleanup_section" not in _rules(_pr(tmp_path, body))
+
+
+def test_a_cleanup_line_passes(tmp_path):
+    assert "rule=cleanup_section" not in _rules(
+        _pr(tmp_path, "\nCleanup: nothing to clean\n")
+    )
+
+
+def test_a_pull_request_opened_before_the_rule_is_not_refused(tmp_path):
+    assert "rule=cleanup_section" not in _rules(
+        _pr(tmp_path, created_at="2026-08-30T00:00:00Z")
+    )
+
+
+def test_a_report_with_no_opening_time_is_judged(tmp_path):
+    assert "rule=cleanup_section" in _rules(_pr(tmp_path, created_at=None))
