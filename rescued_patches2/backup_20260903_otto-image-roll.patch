diff --git a/docs/explanation/otto-rolls-on-every-build.md b/docs/explanation/otto-rolls-on-every-build.md
new file mode 100644
index 000000000..dea0a1d44
--- /dev/null
+++ b/docs/explanation/otto-rolls-on-every-build.md
@@ -0,0 +1,18 @@
+# Otto rolls on every build
+
+For days, work merged for Otto and the running pod never changed. The cause was one line:
+the Otto pod's version tag was bumped by hand, and the hand never bumped it. Every other
+rolling service in the estate has an automation entry that rewrites its tag on every build;
+Otto's file carried a comment saying to bump it by hand "until this lane earns its own
+policy." Nobody did, so the pod served an old build while the new builds piled up unreleased.
+
+The fix reuses the automation that already exists. Otto runs the same container image as the hermes gateway
+workload, so the tag line in `platform/otto-golden/kustomization.yaml` now carries the same
+marker as `platform/hermes-agent/kustomization.yaml`: the image automation finds the marker,
+writes the newest build's tag, and the update lands as an ordinary pull request that merges
+itself when green. From this change on, every merged build of that shared image rolls the Otto
+pod with no hand involved.
+
+The lesson this page exists to keep: a workload whose version is bumped by hand is a workload
+that does not ship. If a tag line has no automation marker, the work behind it is parked, not
+released.
diff --git a/mkdocs.yml b/mkdocs.yml
index 73e68b410..e9e3e387d 100644
--- a/mkdocs.yml
+++ b/mkdocs.yml
@@ -67,6 +67,7 @@ nav:
       - Founder self-service, the field (crew#66): explanation/founder-self-service-research.md
       - Otto on the founder's Mac, the setup and the friction (crew#561): explanation/otto-on-the-mac.md
       - The otto door is a standing query, not a question to a person (pull request 1188): explanation/otto-door-receipts.md
+      - Otto rolls on every build, never by hand: explanation/otto-rolls-on-every-build.md
       - The dagster scheduler survives a node (pull request 1138): explanation/dagster-availability.md
       - The secret bridge's certificate deadlock (decision 0017): explanation/sdk-server-certificate-deadlock.md
   - Demo:
diff --git a/platform/otto-golden/kustomization.yaml b/platform/otto-golden/kustomization.yaml
index b632a60a6..d8147c4e2 100644
--- a/platform/otto-golden/kustomization.yaml
+++ b/platform/otto-golden/kustomization.yaml
@@ -1,7 +1,8 @@
-# Otto staging (founder order 2026-09-01). Same image as platform/hermes-agent, pinned to the
-# same tag deliberately -- one build, two commands. No Flux image-automation policy here: that
-# is a second thing to keep in sync for a staging lane that is not this branch's job; bump this
-# tag by hand alongside hermes-agent's until this lane earns its own policy.
+# Otto (golden), founder order 2026-09-01. Same image as platform/hermes-agent -- one build,
+# two commands. The tag below is rewritten by image-automation-controller from ImagePolicy
+# flux-system/hermes-agent (the marker is how it finds the line), so every main build of
+# hermes-agent rolls this pod too. It was a hand-bumped tag before 2026-09-03, which is why
+# merged work sat unreleased: the hand never bumped it.
 apiVersion: kustomize.config.k8s.io/v1beta1
 kind: Kustomization
 namespace: otto-golden
@@ -17,4 +18,4 @@ resources:
   - availability.yaml
 images:
   - name: ghcr.io/chidionyema/hermes-agent
-    newTag: main-62-62e3830e633a8ca23e822a4210c1bd0f88d71364
+    newTag: main-63-9fdff657c3ad225f7b7b7e107214103b5fe49157 # {"$imagepolicy": "flux-system:hermes-agent:tag"}
