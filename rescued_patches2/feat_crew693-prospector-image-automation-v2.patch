diff --git a/clusters/oke/edge.yaml b/clusters/oke/edge.yaml
index 047dfe591..db0bbe270 100644
--- a/clusters/oke/edge.yaml
+++ b/clusters/oke/edge.yaml
@@ -139,6 +139,15 @@ spec:
   prune: true
   wait: true
   timeout: 5m
+  # Which build runs is decided here, not in prospector's overlay (crew#693): the markers below are
+  # written by the image-automation-controller from platform/image-automation/prospector.yaml, and
+  # this override is applied after kustomize renders the overlay, so the hand-pinned tags there are
+  # inert. A merge to prospector main becomes a pull request here and a RollingUpdate on merge.
+  images:
+    - name: ghcr.io/chidionyema/prospector-store-web
+      newTag: 99cdc96a341f3365916df59be4fd8f26ebf96a43 # {"$imagepolicy": "flux-system:prospector-store-web:tag"}
+    - name: ghcr.io/chidionyema/prospector-store-api
+      newTag: 99cdc96a341f3365916df59be4fd8f26ebf96a43 # {"$imagepolicy": "flux-system:prospector-store-api:tag"}
   healthChecks:
     - apiVersion: apps/v1
       kind: Deployment
diff --git a/docs/reference/incident-register.yaml b/docs/reference/incident-register.yaml
index ab6477fd5..4a16e7a0a 100644
--- a/docs/reference/incident-register.yaml
+++ b/docs/reference/incident-register.yaml
@@ -1,10 +1,10 @@
 # GENERATED by bin/incident-register; edit the incident test, not this file.
 generated_by: bin/incident-register
 source: tests/test_incident_*.py module docstrings
-incidents: 351
+incidents: 353
 by_class:
   capacity: 14
-  ci-pipeline: 72
+  ci-pipeline: 73
   clock: 12
   network: 25
   pod-crash: 16
@@ -12,7 +12,7 @@ by_class:
   rollout-stall: 43
   scheduler: 23
   secret-rotation: 76
-  unclassified: 29
+  unclassified: 30
 rows:
 - id: 20260829_founder_dm_is_not_an_alert_sink
   test: tests/test_incident_20260829_founder_dm_is_not_an_alert_sink.py
@@ -3134,6 +3134,27 @@ rows:
   summary: 'crew#679 CP1/CP5 (founder 2026-08-30: "take incident as input, use it to generate chaos experiments/drills").
     The incident register is generated from every tests/test_incident_*.py docstring, never hand-typed, and a new incident
     test cannot'
+- id: crew684_the_tools_page_is_every_door_on_one_page
+  test: tests/test_incident_crew684_the_tools_page_is_every_door_on_one_page.py
+  date: '2026-08-30'
+  refs:
+  - crew#401
+  - crew#684
+  fault_class: ci-pipeline
+  injectable: false
+  summary: 'Incident 2026-08-30 (crew#684): the founder saw red, could not tell who was looking, and had no one place with
+    every tool. Verbatim: "i am founder, i am CEO and i am also engineer ... so i need all the tools one place ... another
+    page in ba'
+- id: crew693_prospector_images_reach_the_cluster_without_a_hand
+  test: tests/test_incident_crew693_prospector_images_reach_the_cluster_without_a_hand.py
+  date: '2026-08-26'
+  refs:
+  - crew#693
+  fault_class: unclassified
+  injectable: false
+  summary: 'crew#693: prod ran prospector-store-web f398a66 (built 2026-08-26) while prospector main was a6223ef. CI built
+    every merge; nothing moved the tag: the overlay pinned it by hand and no ImagePolicy existed for prospector. Merged was
+    inventory'
 - id: crew85_definitions_loads_as_a_bare_file
   test: tests/test_incident_crew85_definitions_loads_as_a_bare_file.py
   date: '2026-08-27'
diff --git a/platform/image-automation/backstage.yaml b/platform/image-automation/backstage.yaml
index a0ff3612c..7594bb7e0 100644
--- a/platform/image-automation/backstage.yaml
+++ b/platform/image-automation/backstage.yaml
@@ -73,10 +73,13 @@ spec:
       messageTemplate: |
         platform: image update {{ range $resource, $changes := .Changed.Objects }}{{ range $_, $change := $changes }}{{ $resource.Name }} -> {{ $change.NewValue }} {{ end }}{{ end }}(crew#267)
 
-        Written by image-automation-controller. One automation walks ./platform for every
-        ImagePolicy marker; two automations on one push branch collide (crew#406).
+        Written by image-automation-controller. One automation walks the whole repository for
+        every ImagePolicy marker; two automations on one push branch collide (crew#406).
     push:
       branch: flux/image-updates
   update:
-    path: ./platform
+    # The whole checkout, not ./platform: the prospector images: override lives on its Flux
+    # Kustomization in clusters/oke/edge.yaml (crew#693). Only lines carrying a $imagepolicy
+    # marker are ever rewritten.
+    path: ./
     strategy: Setters
diff --git a/platform/image-automation/kustomization.yaml b/platform/image-automation/kustomization.yaml
index 43ca77902..4112adde3 100644
--- a/platform/image-automation/kustomization.yaml
+++ b/platform/image-automation/kustomization.yaml
@@ -4,4 +4,5 @@ resources:
   - pull-secret.yaml
   - sovereign-worker.yaml
   - hermes-agent.yaml
-  - estate-mcp.yaml
\ No newline at end of file
+  - estate-mcp.yaml
+  - prospector.yaml
diff --git a/platform/image-automation/prospector.yaml b/platform/image-automation/prospector.yaml
new file mode 100644
index 000000000..304f6bbd1
--- /dev/null
+++ b/platform/image-automation/prospector.yaml
@@ -0,0 +1,57 @@
+# The storefront and its API (crew#693): prospector's container-images workflow pushes
+# ghcr.io/chidionyema/prospector-store-web and -store-api as main-<run>-<sha> on every merge to
+# main; the newest run wins and the one automation (backstage.yaml) writes it into the images:
+# override of the prospector Kustomization in clusters/oke/edge.yaml. The prospector repository
+# holds no write credential and no automation of its own: promotion is a commit here.
+# Receipt: prod ran f398a66 (26 Aug) while prospector main was a6223ef (crew#693).
+apiVersion: image.toolkit.fluxcd.io/v1
+kind: ImageRepository
+metadata:
+  name: prospector-store-web
+  namespace: flux-system
+spec:
+  image: ghcr.io/chidionyema/prospector-store-web
+  interval: 5m
+  secretRef:
+    name: ghcr-pull
+---
+apiVersion: image.toolkit.fluxcd.io/v1
+kind: ImagePolicy
+metadata:
+  name: prospector-store-web
+  namespace: flux-system
+spec:
+  imageRepositoryRef:
+    name: prospector-store-web
+  filterTags:
+    pattern: '^main-(?P<run>[0-9]+)-[0-9a-f]{40}$'
+    extract: '$run'
+  policy:
+    numerical:
+      order: asc
+---
+apiVersion: image.toolkit.fluxcd.io/v1
+kind: ImageRepository
+metadata:
+  name: prospector-store-api
+  namespace: flux-system
+spec:
+  image: ghcr.io/chidionyema/prospector-store-api
+  interval: 5m
+  secretRef:
+    name: ghcr-pull
+---
+apiVersion: image.toolkit.fluxcd.io/v1
+kind: ImagePolicy
+metadata:
+  name: prospector-store-api
+  namespace: flux-system
+spec:
+  imageRepositoryRef:
+    name: prospector-store-api
+  filterTags:
+    pattern: '^main-(?P<run>[0-9]+)-[0-9a-f]{40}$'
+    extract: '$run'
+  policy:
+    numerical:
+      order: asc
diff --git a/tests/test_incident_crew693_prospector_images_reach_the_cluster_without_a_hand.py b/tests/test_incident_crew693_prospector_images_reach_the_cluster_without_a_hand.py
new file mode 100644
index 000000000..7375f300e
--- /dev/null
+++ b/tests/test_incident_crew693_prospector_images_reach_the_cluster_without_a_hand.py
@@ -0,0 +1,97 @@
+"""crew#693: prod ran prospector-store-web f398a66 (built 2026-08-26) while prospector main was
+a6223ef. CI built every merge; nothing moved the tag: the overlay pinned it by hand and no
+ImagePolicy existed for prospector. Merged was inventory, not released.
+
+The guard: every image the prospector Kustomization overrides has an ImagePolicy of the estate's
+orderable shape, carries the automation marker, and the one automation walks the path that holds it.
+"""
+
+from __future__ import annotations
+
+import pathlib
+import re
+
+import yaml
+
+IDP = pathlib.Path(__file__).resolve().parents[1]
+EDGE = IDP / "clusters" / "oke" / "edge.yaml"
+AUTOMATION_DIR = IDP / "platform" / "image-automation"
+MARKER = re.compile(
+    r'#\s*\{"\$imagepolicy":\s*"flux-system:(?P<policy>[a-z0-9-]+):tag"\}'
+)
+ORDERABLE = "^main-(?P<run>[0-9]+)-[0-9a-f]{40}$"
+
+
+def _docs(path: pathlib.Path) -> list[dict]:
+    return [d for d in yaml.safe_load_all(path.read_text()) if d]
+
+
+def _prospector_kustomization() -> dict:
+    for d in _docs(EDGE):
+        if d.get("kind") == "Kustomization" and d["metadata"]["name"] == "prospector":
+            return d
+    raise AssertionError(
+        "clusters/oke/edge.yaml has no Flux Kustomization named prospector"
+    )
+
+
+def _policies() -> dict[str, dict]:
+    out: dict[str, dict] = {}
+    for f in AUTOMATION_DIR.glob("*.yaml"):
+        for d in _docs(f):
+            if d.get("kind") == "ImagePolicy":
+                out[d["metadata"]["name"]] = d
+    return out
+
+
+def _markers_in_edge() -> dict[str, str]:
+    found: dict[str, str] = {}
+    for line in EDGE.read_text().splitlines():
+        m = MARKER.search(line)
+        if m:
+            image = line.split("newTag:")[0]
+            found[m.group("policy")] = image
+    return found
+
+
+def test_every_overridden_prospector_image_has_an_orderable_image_policy():
+    images = _prospector_kustomization()["spec"].get("images", [])
+    assert images, (
+        "the prospector Kustomization overrides no image: the tag is back in a hand"
+    )
+    policies = _policies()
+    for entry in images:
+        name = entry["name"].rsplit("/", 1)[-1]
+        assert name in policies, (
+            f"{entry['name']} has no ImagePolicy in platform/image-automation"
+        )
+        assert policies[name]["spec"]["filterTags"]["pattern"] == ORDERABLE, name
+        assert policies[name]["spec"]["policy"] == {"numerical": {"order": "asc"}}, name
+
+
+def test_every_overridden_prospector_image_carries_the_automation_marker():
+    images = {
+        e["name"].rsplit("/", 1)[-1]
+        for e in _prospector_kustomization()["spec"]["images"]
+    }
+    assert images <= set(_markers_in_edge()), (images, _markers_in_edge())
+
+
+def test_the_one_automation_walks_the_path_that_holds_the_markers():
+    automations = [
+        d
+        for f in AUTOMATION_DIR.glob("*.yaml")
+        for d in _docs(f)
+        if d.get("kind") == "ImageUpdateAutomation"
+    ]
+    assert len(automations) == 1, [a["metadata"]["name"] for a in automations]
+    path = automations[0]["spec"]["update"]["path"].rstrip("/") or "."
+    rel = EDGE.relative_to(IDP).as_posix()
+    assert path in (".", "./") or rel.startswith(path.removeprefix("./")), (path, rel)
+
+
+def test_prospector_yaml_is_listed_by_the_automation_kustomization():
+    listed = yaml.safe_load((AUTOMATION_DIR / "kustomization.yaml").read_text())[
+        "resources"
+    ]
+    assert "prospector.yaml" in listed, listed
