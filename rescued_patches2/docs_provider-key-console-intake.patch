diff --git a/docs/how-to/onboarding/litellm.md b/docs/how-to/onboarding/litellm.md
index c275457e6..ace47d9f4 100644
--- a/docs/how-to/onboarding/litellm.md
+++ b/docs/how-to/onboarding/litellm.md
@@ -27,14 +27,44 @@ router serves right now, including anything added in the console.
    `founder_emails` in `platform/oci/identity` (app `estate-router-console`), applied by
    `bin/idp-identity-apply`.
 3. Models → Add Model. Pick the provider, type the public model name, and for the credential
-   reference the key the pod already holds: `os.environ/MINIMAX_API_KEY`,
+   either pick one from the **Existing Credentials** dropdown (see the next section) or
+   reference a key the pod already holds: `os.environ/MINIMAX_API_KEY`,
    `os.environ/DEEPSEEK_API_KEY`, `os.environ/OPENROUTER_API_KEY`, `os.environ/GEMINI_API_KEY`.
-   A provider the vault does not hold a key for needs the key added to the vault entry
-   `litellm-upstream` first (`bin/idp-vault-put --merge`, run by the `oke-check` apply step from
-   whichever `SEED_*` repository secrets are set, never from a laptop; one new key, no re-seed).
 4. Save. `store_model_in_db: true` in `general_settings` means the row is in `litellm-db` and
    survives a pod restart; `platform/llm/config.yaml` is never edited for this.
 
+## Bringing a new provider's key (the console, nothing else)
+
+This is the credential intake an enterprise operator gets, and so it is the one the founder
+gets ([the founder is enterprise client zero](../../reference/policy/enterprise-client-zero.md),
+across the board, not only here): no terminal, no repository secret, no fresh key when one
+exists. Steps read from LiteLLM's own model-management page
+(docs.litellm.ai/docs/proxy/model_management, read 2026-09-03):
+
+1. In the console, open the **LLM Credentials** tab and click **Add Credential**. Pick the
+   provider, paste the key, give the credential a name. The form's fields adapt to the
+   provider picked.
+2. Models → Add Model: pick the model, and choose that credential from the **Existing
+   Credentials** dropdown instead of typing a key.
+3. Done. The model row and the credential persist in `litellm-db` and survive pod restarts.
+   Credentials are encrypted at rest; no `LITELLM_SALT_KEY` is set today, so LiteLLM
+   encrypts with `LITELLM_MASTER_KEY`, which only the vault holds.
+
+Two rules the console enforces that are easy to trip over:
+
+- **A model name that `platform/llm/config.yaml` already defines is owned by git**, and the
+  console refuses to edit it or attach a key to it ("defined in config"). If a lane should
+  be console-owned, its git row is removed first — that is a platform change, not an
+  operator step (the `kimi` lane moved to console-owned this way on 2026-09-03).
+- **A Kimi (Moonshot) key answers only at its home host** (see the section below). The
+  console cannot probe the three hosts, so if the model's test call fails with an
+  incorrect-key answer, set the row's API Base to the next host on the list and test again.
+
+The automated alternative stays available: a `SEED_*` repository secret feeds the vault
+entry `litellm-upstream` through the `oke-check` apply step (never from a laptop; one new
+key, no re-seed), and git-rendered rows reference it as `os.environ/...`. That road suits
+keys minted by code; the console road suits a key a person already holds.
+
 Keys, Teams and Spend are the other tabs of the same console: a product gets its own virtual key
 with a daily budget there (the kernel's is alias `sovereign-kernel`, $5/day).
 
diff --git a/docs/reference/policy/enterprise-client-zero.md b/docs/reference/policy/enterprise-client-zero.md
new file mode 100644
index 000000000..bad8ac287
--- /dev/null
+++ b/docs/reference/policy/enterprise-client-zero.md
@@ -0,0 +1,36 @@
+# The founder is enterprise client zero
+
+Founder ruling, 2026-09-03, and it is across the board — every surface, every vendor,
+every credential, not any one product.
+
+Every capability the founder touches is graded as if a paying enterprise client received
+it. He is not a special operator with special chores; he is the first customer, and the
+road he is given is the road a customer gets.
+
+## What that means in practice
+
+- **No terminal.** A step that asks him to run a command, set a repository secret, or edit
+  a file by hand is a defect, whatever it saves the builder. The fix is redesigning the
+  step, never wording the ask more politely.
+- **The console is the intake.** A credential he already holds is pasted into the
+  product's own signed-in console, stored encrypted by the platform, never sent through
+  chat, never re-minted because a fresh one would be easier to script.
+- **No fresh credential when one exists.** Asking him to create a new key while he holds a
+  working one is the same defect in a second coat.
+- **A capability built for him is built as the customer capability.** If the founder needs
+  it, a buyer's operator will need it; it ships on the product surface with its
+  documentation, not as founder lore.
+- **Repetition is the incident.** If he has to say any of this twice, the process that
+  made him repeat it is the thing to fix.
+
+## Instances on record
+
+- Model router credential intake: [picking and adding models](../../how-to/onboarding/litellm.md)
+  — key brought through the router console's credentials tab, nothing else.
+- Sign-in everywhere: one estate login at the gateway
+  ([auth belongs to the gateway](../../policy/auth-is-infrastructure.md)); no surface
+  ships its own password.
+
+When a new step for the founder is unavoidable, it is one action, named in plain words,
+on a page or a phone — and it is recorded here as an instance with the redesign that
+retires it.
diff --git a/llm/config.yaml b/llm/config.yaml
index 32171da54..35d576ad4 100644
--- a/llm/config.yaml
+++ b/llm/config.yaml
@@ -95,18 +95,6 @@ model_list:
       api_key: os.environ/MINIMAX_API_KEY
       timeout: 300
 
-  # Kimi (Moonshot) -- the founder's primary for both Ottos (2026-09-02). First-class LiteLLM
-  # provider prefix `moonshot/`; kimi-k3 is the flagship (1M-token window, platform.kimi.ai/docs/models
-  # read 2026-09-02). The key arrives as MOONSHOT_API_KEY, the name LiteLLM's moonshot adapter reads.
-  - model_name: kimi
-    litellm_params:
-      model: moonshot/kimi-k3
-      api_key: os.environ/MOONSHOT_API_KEY
-      timeout: 300
-      max_tokens: 8192
-    model_info:
-      max_input_tokens: 1000000
-
   - model_name: gemini
     litellm_params:
       model: gemini/gemini-2.5-flash
diff --git a/mkdocs.yml b/mkdocs.yml
index 42b059cd4..ee07486c7 100644
--- a/mkdocs.yml
+++ b/mkdocs.yml
@@ -9,6 +9,7 @@ hooks:
 nav:
   - Home: index.md
   - Definition of Done: reference/policy/definition-of-done.md
+  - The founder is enterprise client zero: reference/policy/enterprise-client-zero.md
   - Notification channels: reference/policy/notification-channels.md
   - Incident register: reference/incident-register.md
   - Documentation policy: reference/policy/documentation.md
diff --git a/platform/llm/config.yaml b/platform/llm/config.yaml
index 42a34c2b3..0c005ee61 100644
--- a/platform/llm/config.yaml
+++ b/platform/llm/config.yaml
@@ -89,18 +89,6 @@ model_list:
       api_key: os.environ/MINIMAX_API_KEY
       timeout: 300
 
-  # Kimi (Moonshot) -- the founder's primary for both Ottos (2026-09-02). First-class LiteLLM
-  # provider prefix `moonshot/`; kimi-k3 is the flagship (1M-token window, platform.kimi.ai/docs/models
-  # read 2026-09-02). The key arrives as MOONSHOT_API_KEY, the name LiteLLM's moonshot adapter reads.
-  - model_name: kimi
-    litellm_params:
-      model: moonshot/kimi-k3
-      api_key: os.environ/MOONSHOT_API_KEY
-      timeout: 300
-      max_tokens: 8192
-    model_info:
-      max_input_tokens: 1000000
-
   - model_name: gemini
     litellm_params:
       model: gemini/gemini-2.5-flash
diff --git a/platform/llm/external-secret.yaml b/platform/llm/external-secret.yaml
index 90622359c..cb506a3d7 100644
--- a/platform/llm/external-secret.yaml
+++ b/platform/llm/external-secret.yaml
@@ -6,15 +6,15 @@
 #     OPENROUTER_API_KEY=OPENROUTER_API_KEY GEMINI_API_KEY=GEMINI_API_KEY \
 #     LITELLM_MASTER_KEY=LITELLM_MASTER_KEY
 #
-# One more key later (crew#506 CP4): `bin/idp-vault-put --merge litellm-upstream MOONSHOT_API_KEY=MOONSHOT_API_KEY`
-# overlays that key on the entry the vault holds; the oke-check apply step runs exactly that from
+# One more key later (crew#506 CP4): `bin/idp-vault-put --merge litellm-upstream <NAME>=<NAME>`
+# overlays one key on the entry the vault holds; the oke-check apply step runs exactly that from
 # whichever SEED_* repository secrets are set, so a new provider never means re-seeding every key.
 # crew#568 phase 1: `bin/idp-bootstrap-vendors` writes ANTHROPIC_API_KEY=ANTHROPIC_API_KEY here from
 # SEED_ANTHROPIC_API_KEY (platform/vendors/consoles.yaml, target litellm-upstream); the claude lanes read it.
-# Kimi (2026-09-02): the same bootstrapper writes MOONSHOT_API_KEY=MOONSHOT_API_KEY here from
-# SEED_KIMI_API_KEY; the kimi lane reads it. Beside it, MOONSHOT_API_BASE=MOONSHOT_API_BASE names the
-# Kimi host that accepted the key (one of three, see docs/how-to/onboarding/litellm.md); the pod exports
-# it with the key and LiteLLM's moonshot adapter reads it from the environment.
+# Kimi (2026-09-03, R75): the kimi lane is console-owned — its key lives encrypted in litellm-db,
+# brought in through the console's LLM Credentials tab (docs/how-to/onboarding/litellm.md), so no
+# Moonshot key rides this entry today. The seed road (SEED_KIMI_API_KEY, probed across the three
+# Kimi hosts) stays in platform/vendors/consoles.yaml for when a lane is git-owned again.
 #
 # The pod mounts the Secret as files and exports them itself (litellm.yaml command), which is what `os.environ/...` in
 # config.yaml resolves. No key is typed anywhere in this repository (LAW 46).
diff --git a/platform/vendors/consoles.yaml b/platform/vendors/consoles.yaml
index 814db8084..1c92c1c9e 100644
--- a/platform/vendors/consoles.yaml
+++ b/platform/vendors/consoles.yaml
@@ -214,19 +214,15 @@ vendors:
     targets:
       - { entry: litellm-upstream, field: MOONSHOT_API_KEY }
       - { entry: litellm-upstream, field: MOONSHOT_API_BASE, derived: base }
-    router:
-      lanes: |
-        # Kimi (Moonshot) -- the founder's primary for both Ottos (2026-09-02). First-class LiteLLM
-        # provider prefix `moonshot/`; kimi-k3 is the flagship (1M-token window, platform.kimi.ai/docs/models
-        # read 2026-09-02). The key arrives as MOONSHOT_API_KEY, the name LiteLLM's moonshot adapter reads.
-        - model_name: kimi
-          litellm_params:
-            model: moonshot/kimi-k3
-            api_key: os.environ/MOONSHOT_API_KEY
-            timeout: 300
-            max_tokens: 8192
-          model_info:
-            max_input_tokens: 1000000
+    # NO router block, on purpose (R75, founder 2026-09-03). The kimi lane is console-owned:
+    # the key is brought through the router's own console (LLM Credentials -> Add Credential,
+    # then Models -> Add Model, stored encrypted in litellm-db) -- the enterprise intake, no
+    # terminal, no repository secret. A git-rendered row here locks the name `kimi` in the
+    # config file and the console then refuses to attach a key to it ("defined in config",
+    # hit by the founder 2026-09-03), so the row that pointed at env keys the vault never
+    # held was removed. The seed road above (SEED_KIMI_API_KEY -> MOONSHOT_API_KEY/BASE)
+    # remains the automated alternative and re-adding a lane here is one block, but only
+    # WITH the vault key, never before it.
   gemini:
     # Google can mint keys with gcloud, but that road needed the founder's laptop SSO every time the
     # session lapsed (a wrong root, crew#66 comment 5461144560). One key made once is the root (R52).
diff --git a/tests/test_incident_crew506_cp4_one_free_key_never_reseeds_the_router_entry.py b/tests/test_incident_crew506_cp4_one_free_key_never_reseeds_the_router_entry.py
index 989a70bb3..87447be7b 100644
--- a/tests/test_incident_crew506_cp4_one_free_key_never_reseeds_the_router_entry.py
+++ b/tests/test_incident_crew506_cp4_one_free_key_never_reseeds_the_router_entry.py
@@ -107,7 +107,8 @@ def test_every_writer_of_the_router_entry_merges_and_no_seed_secret_remains() ->
 def test_the_groq_lane_is_gone_everywhere_and_the_merge_road_survives() -> None:
     """2026-09-02: the founder removed the vendor ("get rid of Groq"). The lane, its key reference
     and its chains must be out of both rendered configs, and the --merge road this incident proved
-    stays documented on the surviving kimi example."""
+    stays documented (as the one-name example; the kimi lane moved console-owned under R75,
+    2026-09-03, so its key no longer rides this entry)."""
     for cfg in CONFIGS:
         doc = yaml.safe_load(cfg.read_text())
         assert not [m for m in doc["model_list"] if "groq" in str(m).lower()], cfg
@@ -118,5 +119,8 @@ def test_the_groq_lane_is_gone_everywhere_and_the_merge_road_survives() -> None:
         assert not [t for v in chains.values() for t in v if "groq" in t], cfg
         assert "GROQ" not in cfg.read_text(), cfg
     es = (ROOT / "platform" / "llm" / "external-secret.yaml").read_text()
-    assert re.search(r"MOONSHOT_API_KEY=MOONSHOT_API_KEY", es) and "--merge" in es
+    assert (
+        re.search(r"--merge litellm-upstream <NAME>=<NAME>", es)
+        and "never means re-seeding" in es
+    )
     assert "GROQ" not in es
