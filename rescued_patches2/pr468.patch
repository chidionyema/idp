diff --git a/docs/evidence/pr-468/20260827T193955Z-1.txt b/docs/evidence/pr-468/20260827T193955Z-1.txt
new file mode 100644
index 000000000..13b16cf76
--- /dev/null
+++ b/docs/evidence/pr-468/20260827T193955Z-1.txt
@@ -0,0 +1 @@
+2 passed in 0.74s
diff --git a/scheduler/schedule.yml b/scheduler/schedule.yml
index be8aeb87c..5f12ab1d4 100644
--- a/scheduler/schedule.yml
+++ b/scheduler/schedule.yml
@@ -446,7 +446,8 @@ jobs:
   com.estate.board-assign:
     # the board's turn (crew#527 CP3): release claims 24h old with no box ticked, hand every alive
     # session holding nothing the top of the finish-first rank; hourly so an idle session waits
-    # minutes, not a day. Exit 2 = BLIND (no feed, or the board unreadable): a finding, not a crash.
+    # minutes, not a day. Exit 3 = BLIND (no feed, or the board unreadable): a finding, not a crash;
+    # exit 2 is argparse and pages.
     runs_on: mac
     runs_on_ref: crew#516 CP5
     ok_exit: [3]
@@ -462,6 +463,27 @@ jobs:
     cwd: $CODE/crew
     skip_on_battery: false
     timeout_s: 300
+  com.estate.board-close:
+    # the nightly close turn (crew#526 CP2): an issue whose Closes-when row probe exits 0, or whose
+    # every box has been ticked for 24h, is closed with the receipt; counts land in science/closer.jsonl
+    # where velocity reads them. Only the datamap row probe may run from a body line (cg#170).
+    # Exit 3 = BLIND (board unreadable): a finding, not a crash; exit 2 is argparse and pages.
+    runs_on: mac
+    runs_on_ref: crew#516 CP5
+    ok_exit: [3]
+    env:
+      HC_FINDINGS_EXIT: "3"
+      ESTATE_CLOSER_LOG: science/closer.jsonl
+    command:
+    - ~/.claude/scripts/hc-wrap.sh
+    - board-close
+    - python3
+    - ~/.claude/scripts/estate_board.py
+    - close
+    cron: 41 2 * * *
+    cwd: $CODE/crew
+    skip_on_battery: false
+    timeout_s: 1800
   com.estate.velocity:
     # velocity per lane from the board, one row per lane per day into the crew checkout the snapshot commits (crew#527 CP1)
     runs_on: mac
diff --git a/tests/test_incident_crew526_board_close_is_scheduled.py b/tests/test_incident_crew526_board_close_is_scheduled.py
new file mode 100644
index 000000000..31e099e52
--- /dev/null
+++ b/tests/test_incident_crew526_board_close_is_scheduled.py
@@ -0,0 +1,32 @@
+"""crew#526 CP2 (founder 2026-08-27: "close completed issues"): the close turn runs nightly on the one
+scheduler, under the dead-man wrapper, in the crew checkout so its log row lands where velocity reads
+it; BLIND is exit 3 (a finding), exit 2 is argparse's usage error and pages (code-2f, idp#450). A
+close turn nobody schedules is a session closing issues by hand, which is the state this replaces."""
+import os
+import pathlib
+import subprocess
+import sys
+
+import yaml
+
+SCHEDULE = pathlib.Path(__file__).resolve().parents[1] / "scheduler" / "schedule.yml"
+
+
+def test_board_close_is_a_nightly_dead_man_wrapped_turn_in_the_crew_checkout():
+    job = yaml.safe_load(SCHEDULE.read_text())["jobs"]["com.estate.board-close"]
+    assert job["command"][0].endswith("hc-wrap.sh") and job["command"][1] == "board-close"
+    assert job["command"][-2:] == ["~/.claude/scripts/estate_board.py", "close"]
+    assert "--dry-run" not in job["command"]
+    assert job["cron"].split()[2:] == ["*", "*", "*"] and job["cron"].split()[1] != "*"   # once a day
+    assert job["ok_exit"] == [3] and job["env"]["HC_FINDINGS_EXIT"] == "3" and 2 not in job["ok_exit"]
+    assert job["cwd"] == "$CODE/crew" and job["env"]["ESTATE_CLOSER_LOG"] == "science/closer.jsonl"
+    assert job["skip_on_battery"] is False and job["runs_on"] == "mac"
+
+
+def test_the_picker_on_this_machine_has_the_close_verb():
+    picker = pathlib.Path(os.path.expanduser("~/.claude/scripts/estate_board.py"))
+    if not picker.exists():
+        import pytest
+        pytest.skip("no picker installed on this machine")
+    out = subprocess.run([sys.executable, str(picker), "--help"], capture_output=True, text=True, check=False)
+    assert "close" in out.stdout, out.stdout + out.stderr
