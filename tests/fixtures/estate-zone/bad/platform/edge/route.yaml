apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: catalogue
spec:
  hostnames:
    # catalogue.mumchimp.com is what this becomes; a comment may say so
    - catalogue.mumchimp.com
