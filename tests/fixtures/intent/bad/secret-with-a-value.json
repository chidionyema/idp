{
  "capability": "secret",
  "name": "fixture-leaky",
  "workload": "fixture-app",
  "purpose": "an intent that tries to carry the credential itself",
  "keys": [
    { "key": "API_TOKEN", "purpose": "the token the workload reads", "value": "hunter2" }
  ]
}
