{
  "capability": "secret",
  "name": "fixture-signing",
  "workload": "fixture-app",
  "purpose": "signs the session cookies the fixture workload hands out",
  "origin": "generated",
  "rotation_days": 30,
  "keys": [
    { "key": "SESSION_SIGNING_KEY", "purpose": "the key the fixture workload signs cookies with" }
  ]
}
