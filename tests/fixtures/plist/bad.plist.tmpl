<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. --><<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->?<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->x<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->m<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->l<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. --> <!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->v<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->e<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->r<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->s<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies with it. Measured 2026-08-25: the Backstage on 3100 had survived only because
	     a session started it by hand at 21:43 the day before, not because the job did. -->i<!-- idp-up is a one-shot: it nohups the Backstage app and exits. Without this key
	     launchd kills the job's whole process group at exit, so anything the job starts
	     dies wit