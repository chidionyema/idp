apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: edge
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: worker
          image: worker:1
          envFrom:
            - secretRef: { name: worker-creds }
