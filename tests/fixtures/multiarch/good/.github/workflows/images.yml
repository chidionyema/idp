name: images
on: { push: { branches: [main] } }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/build-push-action@v6
        with: { context: ., push: true, tags: ghcr.io/x/y:main, platforms: "linux/amd64,linux/arm64" }
      - run: bin/build-image --load -t x/y:dev .
