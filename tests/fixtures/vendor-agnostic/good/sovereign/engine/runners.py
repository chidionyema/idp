"""The registry -- CONTRACT.md:41's one allowed place for a vendor name.

REGISTRY = {"claude": ..., "llm": ...}. A vendor CLI (claude, anthropic) is named here and
only here; this file is the gate's one exemption, not a finding.
"""
