# must-fail: envFrom.secretRef bulk-injects an entire Secret as env vars.
apiVersion: v1
kind: Pod
metadata:
  name: bad-envfrom-secret
  namespace: default
spec:
  containers:
    - name: app
      image: example/app:latest
      envFrom:
        - secretRef:
            name: db-creds-bulk
